## 2026-06-28-21 - Workspace Grid hardening review fixes

- 用户目标：根据 goal objective 继续完成 Workspace Grid 开发计划收口，并确认文档中的两个决策。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：`buildCodePaneUrl` 保留运行时 workspace URL 的 `#token=` hash；新增 pane 默认 eager 挂载；现有 pane 切换到 Code 时调用 `grid_create_session` 创建真实 server session；嵌入式 Tauri 子 Webview 激活后不再同时渲染 iframe；Grid persisted state 加载/恢复时归一化未知 preset、重复/超限 pane、幽灵 slot、失效 active/maximized pane 和异常 track sizes；补充对应单测/组件测试。
- 修改原因：修复审查中指出的 P1/P2 风险：纯 session 布局可能丢 token bootstrap、新增 pane 不同时可见、切换到 Code 可能没有真实 session、native carrier 与 iframe 并存、坏 localStorage 可能导致渲染崩溃。
- 本次已消除的不确定性：确认 DR-A 已由 `zustand` Grid slice 执行，DR-B 已由 v1 fallback/外部打开基线与后续 WG-7 native carrier follow-up 执行；确认 session URL hash 来自运行时 prop，不进入 persisted state。
- 本次采用的关键假设：不在本轮新增 session picker UI；切换到 Code 的最小正确行为是为当前工作目录创建一个真实 server session。session URL 保留 runtime hash 可接受，因为原始 `codeRemoteUrl` 已在运行时携带该 hash，且不会被持久化。
- 剩余不确定性：真实 Tauri 桌面中的 child Webview z-order/focus/DPI 行为仍需人工点击验证；如果后续要求“只有首个 session pane 带 token hash”，需要在挂载层增加一次性 bootstrap 分配。
- 影响范围：Workspace Grid session pane 加载、pane 新增/切换、外部 native Webview fallback、persisted grid state 恢复和 saved layout 恢复。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过（2 files / 24 tests）；`pnpm --dir apps/kimi-shell exec tsc --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt --manifest-path apps/kimi-shell/src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path apps/kimi-shell/src-tauri/Cargo.toml` 通过；`cargo test --manifest-path apps/kimi-shell/src-tauri/Cargo.toml --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过。
- 是否更新 README：否，本轮不改变用户入口、目录职责或运行方式。
- 是否更新 `.ai/architecture`：是，补充 current state 中 Grid state 归一化与 session URL token bootstrap 事实。
- 是否需要新增或更新 `.ai/decisions`：否，复核并执行既有 Workspace Grid v1 accepted ADR。
- 后续注意事项：后续若继续打磨 WG-7，应优先做真实桌面 child Webview 点击验证；若推进 session 恢复 UX，再考虑接入 `listGridSessions` 选择已有 session，而不是在本轮硬塞。

## 2026-06-28-11 - Bridge token transport hardening

- 用户目标：根据 v3 迁移整合文档推进 kimi-code 迁移与 IM Bridge 体验/质量优化。
- 涉及目录：`apps/kimi-im-bridge`、`apps/kimi-shell/src-tauri`、`.ai/changes`。
- 修改内容：Bridge sidecar 支持通过 `KIMI_IM_BRIDGE_ADMIN_TOKEN` / `KIMI_IM_BRIDGE_ADMIN_TOKEN_FILE` 读取 admin token，通过 `KIMI_IM_BRIDGE_HOST_CONTROL_TOKEN` / `KIMI_IM_BRIDGE_HOST_CONTROL_TOKEN_FILE` 读取 host-control token；Shell 启动 sidecar 时不再把 token 写入进程命令行；Bridge README 同步更新 token transport 契约。
- 修改原因：v3 文档将 `--admin-token` / `--host-control-token` 命令行传 secret 标为高风险安全缺陷，P1B/P4C 门禁要求改为 env/token-file。
- 本次已消除的不确定性：确认现有 Shell 通过 `bridge_manager.rs` 构造 sidecar 命令，现有 Go CLI 强制 `--admin-token`，因此应在这两个边界修复。
- 本次采用的关键假设：保留旧 `--admin-token` / `--host-control-token` 作为兼容 fallback，但 Shell 不再使用它们。
- 剩余不确定性：`.ai/CONSTITUTION.md`、`.ai/architecture/` 在仓库中缺失；DirectServer、`/api/v1` 客户端与 RuntimeAdapter 仍未实施。
- 影响范围：Bridge admin API 鉴权 token 来源改变；Shell 与 sidecar 的本地启动契约改变，但 admin HTTP header 与 token 值本身不变。
- 验证方式：`go test ./...`（`apps/kimi-im-bridge`）通过；`cargo check` 通过；`cargo test configure_bridge_token_env_keeps_tokens_out_of_args --no-run` 通过。Rust 测试二进制运行仍受本机既有 `STATUS_ENTRYPOINT_NOT_FOUND` 影响。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：否，目录原本缺失，本次不补大型架构文档。
- 是否需要新增或更新 `.ai/decisions`：否，本次执行粘贴 v3 文档中已给出的安全门禁，不新增架构决策。
- 后续注意事项：下一步应实现 DirectServer/token resolver 或 Bridge RuntimeAdapter 前，先补齐 `.ai/architecture` 当前事实入口，避免迁移继续依赖过期 README。

## 2026-06-28-11 - DirectServer startup and token locator

- 用户目标：根据 v3 迁移整合文档推进 kimi-code DirectServer MVP。
- 涉及目录：`apps/kimi-shell`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：Shell 后端启动主路径从 `kimi web --no-open` 切换到 `kimi server run --foreground --port <port>`；新增 `token_resolver.rs` 读取 `KIMI_CODE_HOME/server.token` 并生成 `/#token=` workspace URL；`AppStatus`、`DiagnosticsInfo` 与 `FrontendReadyAck` 返回 DirectServer locator 信息；前端优先使用后端返回的 `workspaceUrl`；P1A 暂停默认 workspace proxy 与旧 `/api/sessions` bootstrap。
- 修改原因：v3 P1A 要求默认 DirectServer、读取 server token、WebView 直连官方 Kimi Code Web，并停旧 API 主路径。
- 本次已消除的不确定性：确认旧主路径集中在 `backend_manager/lifecycle.rs`，前端 remote URL 可由后端状态直接驱动，不需要先改 UI 架构。
- 本次采用的关键假设：官方当前 `kimi server run --help` 支持 `--foreground` 与 `--port`；当前官方文档没有稳定 `--host` 参数，因此本轮不传 `--host`，依赖 server loopback 默认行为。
- 剩余不确定性：真实 `kimi-code` installed build 尚未在桌面 UI 中启动验证；新 `/api/v1` workspace/session 客户端还未实现；P2 的 prefill/session deep link 需要重新接到新 API。
- 影响范围：Shell 启动命令、健康探测、workspace URL、诊断状态和 README 架构说明。
- 验证方式：`cargo check` 通过；`cargo test token_resolver --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md`。
- 是否更新 `.ai/architecture`：是，新增 `README.md`、`current-state.md`、`verification-gates.md`。
- 是否需要新增或更新 `.ai/decisions`：否，本轮是执行 v3 已给出的 DirectServer 主路径，不新增取舍。
- 后续注意事项：下一步应补 `api_v1_client`，用 `/api/v1` 替换旧 `workspace_session.rs` 的 `/api/sessions` 调用，并把 Bridge runtime locator 传给 sidecar。

## 2026-06-28-11 - API v1 client and Bridge runtime locator

- 用户目标：根据 v3 迁移整合文档推进 P1B `/api/v1` 客户端与 Bridge runtime 接触面。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src`、`apps/kimi-im-bridge`、`.ai/architecture`、`tasks`。
- 修改内容：新增 Rust `api_v1_client.rs`，统一 Bearer auth、`/api/v1` 路径归一和 `{code,msg,data,request_id}` envelope 解包；新增 `runtime_locator.rs`，Shell 写出不含明文 token 的 `kimi_runtime_locator.json`；Shell 启动 Bridge sidecar 时通过 `--kimi-runtime-locator` 和 `KIMI_APP_RUNTIME_LOCATOR_FILE` 传 locator 路径；Go sidecar 接收 locator 并在 admin status 中报告 configured/readable/health。
- 修改原因：v3 P1B 要求 Rust 侧统一 `/api/v1` 客户端，P4C 前置要求 Bridge 不自己猜 server origin/token。
- 本次已消除的不确定性：确认 locator 文件可以由 Shell 配置目录长期托管，Bridge 只需要路径即可读取 token path/redacted 状态，不需要明文 token。
- 本次采用的关键假设：locator 文件只写 token path 和脱敏 token，后续 `KimiCodeServerAdapter` 运行时再按 token path 读取明文 token。
- 剩余不确定性：真实 kimi-code `/api/v1` workspace/session/prompt schema 尚未接入；Bridge ServerAdapter/ACPAdapter 仍未实现；admin API envelope 仍未升级。
- 影响范围：Shell 后端模块、Bridge CLI/admin status、前端 BridgeStatus 类型、架构事实文档。
- 验证方式：`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 current state 与 verification gates。
- 是否需要新增或更新 `.ai/decisions`：否，本轮仍是执行 v3 已定方案。
- 后续注意事项：下一步应把旧 `workspace_session.rs` 的 `/api/sessions` 逻辑替换为基于 `api_v1_client` 的 `/api/v1` workspace/session 最小映射。

## 2026-06-28-12 - Workspace session API v1 mapping

- 用户目标：根据 v3 迁移整合文档推进 P1B workspace/session 最小映射。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell`、`.ai/architecture`、`tasks`。
- 修改内容：`workspace_session.rs` 不再自建裸 `reqwest` 调旧 `/api/sessions/`；改为通过 `api_v1_client` 读取 runtime origin/token，调用 `GET /api/v1/sessions?page_size=100`、`POST /api/v1/workspaces { root }`、`POST /api/v1/sessions`；恢复 DirectServer ready 后的 session poll/bootstrap；补充官方 `id`、`metadata.cwd`、`updated_at`、`status` shape 的解析测试。
- 修改原因：v3 P1B 要求所有新 REST 调用收束到 `/api/v1` 客户端层，并完成 session/workspace 最小映射。
- 本次已消除的不确定性：通过上游 `MoonshotAI/kimi-code` 当前源码确认 session schema 使用 `id`、`metadata.cwd`、分页 `data.items`，创建 session 支持 `{ metadata: { cwd } }`，workspace registry 支持 `POST /api/v1/workspaces { root }`。
- 本次采用的关键假设：workspace registry 失败不应阻断桌面启动；因此创建 session 时先尝试 `workspace_id`，失败后回退到 `metadata.cwd`。
- 剩余不确定性：真实 installed `kimi-code` runtime 尚未做桌面点击验证；prompt、approval、WS event stream 仍未接入；Bridge RuntimeAdapter 尚未实现。
- 影响范围：Shell 后端 session polling、右键/启动 bootstrap 的 session 创建、Bridge 面板读取 workspace sessions 的后端来源。
- 验证方式：`cargo fmt` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-shell/README.md` 的核心能力描述。
- 是否更新 `.ai/architecture`：是，更新 current state。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定 P1B 路线。
- 后续注意事项：下一步应接 `/api/v1` prompt/approval/WS，随后在 Bridge 侧实现 `KimiCodeServerAdapter` 并升级 admin envelope。

## 2026-06-28-12 - Bridge server runtime adapter foundation

- 用户目标：根据 v3 迁移整合文档推进 IM Bridge RuntimeAdapter 重构。
- 涉及目录：`apps/kimi-im-bridge`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src`、`.ai/architecture`、`tasks`。
- 修改内容：新增 Go `RuntimeAdapter` 契约和 `KimiCodeServerAdapter` REST 客户端；ServerAdapter 从 Shell locator 读取 origin/token path，并按需读取 token 文件；实现 workspace/session ensure、prompt submit、pending approvals、approval resolve 和 prompt abort 的 `/api/v1` 映射；Bridge status 新增 `runtimeAdapter`，Shell Rust/TS 类型同步。
- 修改原因：v3 P4C 要求 Bridge 从 SDK-only runner 迁移到 runtime adapter 层，默认主路径为 `KimiCodeServerAdapter`，并让 degraded/ready 状态可见。
- 本次已消除的不确定性：通过上游 `MoonshotAI/kimi-code` 当前源码确认 prompt、approval、abort、workspace、session REST 路径与请求体字段；确认 locator 文件不含 token 明文，ServerAdapter 应按 token path 读文件。
- 本次采用的关键假设：本轮只建立可测试 REST adapter 地基，不把 Telegram/Feishu/Weixin prompt 主路径立即切过去；在 WS 事件流和 approval reconcile 接好前，保留 SDK-backed provider/runtime 路径。
- 剩余不确定性：真实 IM prompt 尚未通过 ServerAdapter 跑通；`/api/v1/ws` event stream、pending approval 跨 Bridge 重启 reconcile、ACPAdapter 和 SDKAdapter wrapper 尚未实现；Bridge admin API envelope 仍未升级。
- 影响范围：Bridge runtime 包新增长期接口和 server adapter；Bridge admin status schema 增加 `runtimeAdapter`；Shell Rust/TS 类型同步接收新字段。
- 验证方式：`gofmt` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`cargo fmt` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 current state。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定 ServerAdapter 主路径，尚未作新的取舍。
- 后续注意事项：下一步应把 channel prompt 主路径通过 feature/config 切到 ServerAdapter，并接 `/api/v1/ws` 用于 prompt streaming 与 approval projection。

## 2026-06-28-12 - Bridge admin API envelope

- 用户目标：根据 v3 迁移整合文档升级 Bridge admin API 合同与 Shell 解析层。
- 涉及目录：`apps/kimi-im-bridge/internal/admin`、`apps/kimi-shell/src-tauri`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：Go sidecar `/api/v1/*` admin responses 统一返回 `{ ok, data, error, requestId }`；错误响应包含结构化 `code/message/details`；Shell `BridgeHttpClient` 增加 envelope unwrap，并保留旧裸 JSON 兼容；测试覆盖新 envelope 成功/错误与旧响应兼容。
- 修改原因：v3 D5/P4C 要求 Bridge admin API 从裸 `{items}` / `{error}` 升级为稳定 envelope，避免 Rust client 散落 status/body 解析。
- 本次已消除的不确定性：确认 Shell 所有 Bridge admin 调用集中在 `bridge_http_client.rs`，可通过一个兼容解包函数完成滚动升级。
- 本次采用的关键假设：`/healthz` 继续保留裸 `{ok:true}` 作为轻量 liveness；只有 `/api/v1/*` 纳入 admin envelope。
- 剩余不确定性：前端尚未专门展示 envelope `requestId` / error code；真实 installed sidecar 与 Shell 混版升级还未手工验证。
- 影响范围：Bridge admin HTTP response shape；Shell Bridge client decode/error reporting；Go/Rust 单元测试。
- 验证方式：`gofmt` 通过；`go test ./internal/admin` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`cargo fmt` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 current state。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定 admin envelope 方向。
- 后续注意事项：后续 UI 可在 Bridge 错误提示中展示 requestId/error code；ServerAdapter 接管主路径前应复用同样的结构化错误策略。

## 2026-06-28-12 - Bridge log redaction gate

- 用户目标：根据 v3 迁移整合文档补齐 Bridge stdout/stderr 与日志 redaction 安全门禁。
- 涉及目录：`apps/kimi-im-bridge/internal/logging`、`apps/kimi-im-bridge/internal/app`、`apps/kimi-shell/src-tauri`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：Go logger 新增注册 secret 与写入前脱敏能力，Bridge 服务启动时注册 admin token、host-control token 和平台密钥；Shell 启动 sidecar 时改为 pipe stdout/stderr，由 redactor 写入 bridge log；Bridge log tail 和启动失败摘要返回前二次脱敏。
- 修改原因：v3 D6/P1 安全门禁要求 Bridge stdout/stderr、bridge log、诊断摘要覆盖 admin token、server token、平台密钥与 API key redaction。
- 本次已消除的不确定性：确认 sidecar stdout/stderr 原先由 Shell 直接 append 到 bridge log，需要在写盘前拦截；确认平台密钥可从 Bridge secrets store 注册为精确值 redaction。
- 本次采用的关键假设：本轮采用精确 secret value redaction，避免误伤普通诊断文本；server token 不写入 runtime locator，ServerAdapter 也不记录 token，因此本轮不主动读取 server token 只为注册 redaction。
- 剩余不确定性：真实 sidecar 长时间运行时 stdout/stderr redactor 的并发写盘仍需安装版 smoke；Rust 测试二进制在当前 Windows 环境仍不能执行到断言阶段。
- 影响范围：Bridge 日志写入、Shell 托管 sidecar stdio、Bridge UI 日志 tail 和启动失败提示。
- 验证方式：`gofmt` 通过；`go test ./internal/logging ./internal/app` 通过；`cargo fmt` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`cargo test redact_bridge_log_line --lib` 编译完成但运行阶段受既有 `STATUS_ENTRYPOINT_NOT_FOUND` 影响未执行断言。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 current state。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定安全门禁。
- 后续注意事项：后续如引入更多 token 来源，应在 Bridge logger 和 Shell redaction value collector 中同步注册。

## 2026-06-28-12 - Bridge server provider prompt path and WS stream

- 用户目标：根据 v3 迁移整合文档推进 IM Bridge RuntimeAdapter 主路径，让 channel prompt 使用 kimi-code server runtime。
- 涉及目录：`apps/kimi-im-bridge/internal/bridgecore`、`apps/kimi-im-bridge/internal/runtime`、`apps/kimi-im-bridge/internal/providers/runtimeadapter`、`apps/kimi-im-bridge/internal/adapters`、`apps/kimi-im-bridge/internal/app`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：bridgecore binding resolver 支持 rebind，orchestrator 在创建新 binding 前可调用 runtime provider ensure session；新增 server-backed runtimeadapter provider 并在 locator 可用时由 app wiring 优先选择；Telegram/Feishu/Weixin orchestrator 主路径不再预创建 synthetic session binding；`KimiCodeServerAdapter` 接入 `/api/v1/ws` prompt stream，映射 assistant/thinking delta、status、turn/prompt completion 和 approval requested/resolved；测试覆盖 server session ensure、provider event mapping 和 websocket stream。
- 修改原因：v3 P4C 要求 Bridge channel prompt 主路径收敛到 `KimiCodeServerAdapter`，并禁止新增伪 Kimi session id。
- 本次已消除的不确定性：确认 channel adapter 可以把 binding 创建权交给 bridgecore orchestrator；确认 server adapter 可用 `/api/v1/ws` 的 session subscription + cursor 方式提供最小 prompt streaming。
- 本次采用的关键假设：locator 存在时优先 server provider；若 server provider 构造失败或 locator 缺失，仍回退 SDK-backed provider，避免阻断已有 IM 能力。已有 synthetic binding 通过 prompt run 后 rebind 迁移，不在本轮做批量数据库 migration。
- 剩余不确定性：pending approval 跨 Bridge 重启 reconcile 和 durable approval projection 尚未完成；ACPAdapter 与 SDKAdapter wrapper 尚未实现；真实 Telegram/Feishu/Weixin 凭证链路尚未手工验证。
- 影响范围：IM channel prompt runtime 主路径、binding/session id 语义、server prompt stream 事件映射、app runtime provider 选择。
- 验证方式：`go test ./...`（`apps/kimi-im-bridge`）通过；`cargo fmt --manifest-path apps/kimi-shell/src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path apps/kimi-shell/src-tauri/Cargo.toml` 通过；`cargo test --manifest-path apps/kimi-shell/src-tauri/Cargo.toml --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit`（`apps/kimi-shell`）通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定 ServerAdapter 主路径，没有新增取舍。
- 后续注意事项：下一步应实现 server pending approval reconcile 和 durable projection，再补 ACPAdapter 与 SDKAdapter wrapper。

## 2026-06-28-12 - Bridge approval reconcile and adapter fallback implementations

- 用户目标：继续完成 v3 P4C RuntimeAdapter 三实现和 pending approval 重启对账门禁。
- 涉及目录：`apps/kimi-im-bridge/internal/runtime`、`apps/kimi-im-bridge/internal/providers/runtimeadapter`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：server-backed provider 实现 pending approval reconcile，按本地 pending 与已知 server sessions 查询 server pending，保留仍 pending 的审批、将 server 确认不存在的本地 pending 标为 `stale_failed`，并为同一 session 下 server-only pending 创建本地 projection；新增 `SDKAdapter` wrapper 复用现有 SDK driver、session registry、turn runner 和 live approval coordinator；新增实验性 `ACPAdapter`，包含 stdio JSON-RPC transport、initialize、session/new、session/resume、session/prompt、session/cancel 与 prompt update/permission smoke 映射。
- 修改原因：v3 P4C 要求 ServerAdapter 支撑 pending approval 跨 Bridge 重启对账，且 RuntimeAdapter 保留 Server/ACP/SDK 三实现。
- 本次已消除的不确定性：确认 server reconcile 可在不阻断 sidecar 启动的前提下执行；单个 session 查询 server 失败时保留本地 pending，避免误标 stale；SDKAdapter 可复用现有 runtime 层而不复制 SDK provider 逻辑。
- 本次采用的关键假设：ACP 先作为实验性 fallback，manual approval 仍按 live-only auto/cancel 处理，不在本轮实现跨平台异步 approval card；server-only recovered projection 先落库，后续再做 IM 卡片重新投递。
- 剩余不确定性：真实 `kimi acp` prompt 协议字段仍需随官方协议做 installed smoke；server-only approval projection 尚未重新投递到 Telegram/Feishu；真实平台凭证链路尚未验证。
- 影响范围：Bridge runtime adapter contract 实现集合、server provider 启动对账行为、本地 approval status 语义。
- 验证方式：`go test ./internal/providers/runtimeadapter` 通过；`go test ./internal/runtime` 通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 已定 Server/ACP/SDK adapter 方向。
- 后续注意事项：后续应把 server-only recovered approvals 重新投递为 IM approval card，并把 ACPAdapter manual approval 升级为真正异步平台审批或明确保留为实验模式。

## 2026-06-28-12 - Bridge server prompt controls metadata mapping

- 用户目标：推进 v3 P4C prompt controls，覆盖 model、thinking、permission、plan、swarm 和 goal 控制。
- 涉及目录：`apps/kimi-im-bridge/internal/providers/runtimeadapter`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：server-backed provider 从 `TurnRequest.MetadataJSON` 的 `runtime_controls` 或 `controls` 读取 `model`、`thinking`、`permissionMode` / `permission_mode`、`planMode` / `plan_mode`、`swarmMode` / `swarm_mode`、`goalObjective` / `goal_objective`、`goalControl` / `goal_control`，并映射到 `RuntimeControls`；`AutoApprove` 继续作为空 permission mode 的 fallback。
- 修改原因：`RuntimeControls` 和 server adapter 已有字段，但 Bridge provider 此前只映射 `AutoApprove`，未把 v3 prompt controls 传给 `/api/v1` prompt。
- 本次已消除的不确定性：确认 `MetadataJSON` 已由 bridgecore 传入 provider，可作为最小入口，无需新增 channel 配置或前端 UI。
- 本次采用的关键假设：本轮只打通后端控制字段传递，不新增用户配置面；后续配置中心可把默认值写入同一 metadata。
- 剩余不确定性：Shell/Control Center 尚未提供这些 controls 的可视化配置；真实 kimi-code 对每个 control 的最终字段兼容性仍以 installed runtime 为准。
- 影响范围：ServerAdapter prompt request body 的 controls 字段。
- 验证方式：`go test ./internal/providers/runtimeadapter` 通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：后续把 Shell/Bridge 配置中心默认 runtime controls 接到 `MetadataJSON` 即可。

## 2026-06-28-12 - Bridge recovered approval projection context

- 用户目标：推进 pending approval 重启恢复体验，避免 server-only recovered approvals 脱离原 IM chat context。
- 涉及目录：`apps/kimi-im-bridge/internal/providers/runtimeadapter`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：server pending approval reconcile 重建 server-only 本地 projection 时，会按已知 binding 的 `KimiSessionID` 回填 connector、platform、chat 和 thread；没有 binding 时继续使用 server/session 兜底。
- 修改原因：此前 server-only projection 使用 `platform=server` 和 `chat_id=sessionID`，能被 admin UI 看到，但不能自然归入原 Telegram/Feishu chat 的 pending approval 上下文。
- 本次已消除的不确定性：确认 `store.ListBindings` 已存在，可复用，不需要新增 migration 或 delivery 表。
- 本次采用的关键假设：本轮不自动重发 approval card，避免在缺少已投递消息元数据时制造重复平台消息。
- 剩余不确定性：自动重投递 recovered approval card 仍需要稳定 delivery metadata 或幂等策略。
- 影响范围：Bridge 启动时 recovered approval ticket 的 connector/platform/chat/thread 字段。
- 验证方式：`go test ./internal/providers/runtimeadapter` 通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：后续要做自动重投递时，应先记录 approval card 的平台消息 id 或可复用 delivery key。

## 2026-06-28-13 - Shell Kimi Code install chain cleanup

- 用户目标：推进 v3 P3 安装升级链路，移除 uv/Python 作为 Kimi Code 主安装依赖。
- 涉及目录：`apps/kimi-shell/src-tauri/src/install_manager.rs`、`apps/kimi-shell/src-tauri/src/backend_manager.rs`、`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/docs`、`.ai/architecture`、`tasks`。
- 修改内容：Shell quick/core 和 Kimi install 任务改为调用 Kimi Code 官方 Windows installer；upgrade 改为 `kimi upgrade`；core ready 不再依赖 uv/Python；PowerShell/Rust probe 增加 `%USERPROFILE%\.kimi-code\bin` 与 npm 全局 shim 候选路径；删除旧 `backend_manager/install_compat.rs` uv/Python Kimi install compatibility path；uv/Python 任务在新 install catalog 中降级为 legacy repair；控制中心旧 invoke path 文案不再要求 uv/Python；安装文档改为 official installer + npm fallback。
- 修改原因：v3 P3 明确要求安装链路切到 kimi-code，移除 uv/Python 安装流程，并集成 `kimi upgrade`。
- 本次已消除的不确定性：确认官方 Windows installer 默认安装目录为 `%USERPROFILE%\.kimi-code`，当前 npm 包名为 `@moonshot-ai/kimi-code`，本机 `kimi` shim 来自 npm global package。
- 本次采用的关键假设：本轮保留旧 uv/Python probe、mirror schema 和手动 repair task 作为兼容层，不做 enum/settings schema 删除，避免扩大迁移风险。
- 剩余不确定性：Bridge sidecar installed-build smoke 尚未完成；真实干净 Windows 安装仍需 P3 manual gate。
- 影响范围：Shell 安装/升级/卸载任务命令、安装状态判断、控制中心安装提示、安装文档。
- 验证方式：`cargo fmt --manifest-path apps\kimi-shell\src-tauri\Cargo.toml -- --check` 通过；`cargo check --manifest-path apps\kimi-shell\src-tauri\Cargo.toml` 通过；`cargo test --manifest-path apps\kimi-shell\src-tauri\Cargo.toml --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit`（`apps/kimi-shell`）通过。
- 是否更新 README：否，未改变目录职责；删除的是未接入 Tauri command 的旧 compatibility module，并已更新 `apps/kimi-shell/docs/install_kimi.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否，本轮执行 v3 P3 已定方向。
- 后续注意事项：后续删除 uv/Python schema 前，需要先迁移 settings/mirror UI 与旧 command catalog；当前只保证 Kimi Code 主链路不再依赖 uv/Python。

## 2026-06-28-13 - Shell Git Bash KIMI_SHELL_PATH detection

- 用户目标：推进 v3 P3 Git Bash 检测与 `KIMI_SHELL_PATH` 配置。
- 涉及目录：`apps/kimi-shell/src-tauri/src/kimi_locator.rs`、`apps/kimi-shell/src-tauri/src/backend_manager/lifecycle.rs`、`apps/kimi-shell/src-tauri/src/install_manager.rs`、`apps/kimi-shell/src-tauri/src/types.rs`、`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/docs`、`.ai/architecture`、`tasks`。
- 修改内容：新增 Git Bash locator，优先使用已存在的 `KIMI_SHELL_PATH`，再检测 Git for Windows 常见 `bash.exe` 路径，最后回退 PATH `bash`；Shell 启动 `kimi server run` 时向子进程注入检测到的 `KIMI_SHELL_PATH`；安装 probe 增加 `gitBashReady` 和 `kimiShellPath`；安装面板展示 Git Bash 状态并在缺失时给出非阻断提示；安装文档补充 Git Bash / `KIMI_SHELL_PATH` 验证。
- 修改原因：v3 P3 要求安装升级链路明确 Git Bash 依赖，并配置 `KIMI_SHELL_PATH`，避免 shell tool 执行时依赖隐式 PATH。
- 本次已消除的不确定性：确认本机存在 `C:\Program Files\Git\bin\bash.exe` 和 `C:\Program Files\Git\usr\bin\bash.exe`，可作为默认检测路径。
- 本次采用的关键假设：本轮先做自动检测和子进程 env 注入，不新增单独设置 UI；如用户已显式设置 `KIMI_SHELL_PATH` 且路径存在，则尊重用户配置。
- 剩余不确定性：干净 Windows 安装版仍需实际启动 smoke，确认 Git 安装后 PATH 和 `KIMI_SHELL_PATH` 在 Tauri 进程内刷新行为。
- 影响范围：Kimi server 子进程环境、安装探测状态、控制中心安装面板提示、安装文档。
- 验证方式：本机 PowerShell 检测到 Git Bash 并输出 GNU bash 版本；`cargo fmt --manifest-path apps\kimi-shell\src-tauri\Cargo.toml -- --check` 通过；`cargo check --manifest-path apps\kimi-shell\src-tauri\Cargo.toml` 通过；`cargo test --manifest-path apps\kimi-shell\src-tauri\Cargo.toml --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit`（`apps/kimi-shell`）通过。
- 是否更新 README：否，未改变目录职责；更新 `apps/kimi-shell/docs/install_kimi.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果后续需要手动覆盖 Git Bash 路径，再把 `KIMI_SHELL_PATH` 做成 settings 字段和控制中心输入即可。

## 2026-06-28-13 - Bridge bundled sidecar installed-build smoke

- 用户目标：完成 v3 P3 Bridge sidecar installed-build smoke，确保打包二进制与当前 Bridge token/runtime 改动一致。
- 涉及目录：`apps/kimi-shell/src-tauri/binaries`、`apps/kimi-im-bridge`、`.ai/architecture`、`tasks`。
- 修改内容：通过 `apps/kimi-shell/scripts/build_bridge_sidecar.ps1` 直接重建 bundled `kimi-im-bridge.exe`；使用临时 config/secrets/db/log 和 admin/host-control token files 启动 bundled sidecar；验证 `/healthz`、`/api/v1/status` envelope、`/api/v1/runtime/stop`，并检查 stdout/stderr/log 不泄露 smoke token。
- 修改原因：旧 bundled binary 不支持新 token-file/runtime-locator flags，P3 installed-build smoke 需要验证发布包中的 sidecar 与当前源码一致。
- 本次已消除的不确定性：确认旧 bundled binary 是 stale；重建后 token-file 启动成功，admin envelope 和 runtime stop 可用，输出未泄露 admin/host-control token。
- 本次采用的关键假设：本轮 smoke 使用空 connectors 和 SDK fallback，不依赖真实 IM 平台凭证，也不要求 kimi-code runtime locator 在线。
- 剩余不确定性：真实 NSIS/MSI 安装后的资源路径和 sidecar lifecycle 按钮仍需人工安装版回归；真实 Telegram/Feishu/Weixin 凭证链路仍是 P5 manual gate。
- 影响范围：发布包使用的 Bridge sidecar binary、P3 packaging confidence。
- 验证方式：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File apps\kimi-shell\scripts\build_bridge_sidecar.ps1` 通过；bundled `kimi-im-bridge.exe` smoke 返回 `healthOk=true`、`statusOk=true`、`state=running`、`exitCode=0`、`outputRedacted=true`。
- 是否更新 README：否，未改变目录职责。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：`pnpm build:bridge-sidecar` 在当前非 TTY 环境先被 pnpm install 的模块清理确认拦截；CI 或无交互环境应设置 `CI=true` 或直接调用 PowerShell build script。

## 2026-06-28-14 - Shell kimi doctor diagnostics

- 用户目标：推进 v3 P4A login/config/provider/doctor，补齐 Shell 内置 `kimi doctor` 诊断入口。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src`、`apps/kimi-shell/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：新增 Tauri command `run_kimi_doctor`，复用已配置或已检测的 Kimi CLI 路径运行 `kimi doctor`，并注入检测到的 `KIMI_SHELL_PATH`；新增 `KimiDoctorResult` Rust/TS 类型；控制中心运行诊断面板新增“运行 kimi doctor”按钮，展示 exit code、命令路径、Shell 路径和 stdout/stderr；doctor 输出按已知 env/config API key、token、secret 精确值脱敏后返回前端。
- 修改原因：v3 P4A 要求 Shell 提供 `kimi doctor` 诊断能力，让用户不离开控制中心即可验证 Kimi Code 配置。
- 本次已消除的不确定性：本机 `kimi doctor` 可用，输出会校验 `config.toml` 与 `tui.toml`；现有 login/logout CLI command 路径适合承载 doctor，不需要新增后台服务。
- 本次采用的关键假设：`kimi doctor` 是短时本地配置校验命令，可以作为手动 UI 操作同步执行；本轮只做手动入口，不自动在轮询诊断中运行。
- 剩余不确定性：未启动 Tauri 桌面做按钮点击截图验证；真实干净安装环境仍需 P5 manual gate 覆盖。
- 影响范围：Shell 诊断命令集合、控制中心运行诊断 UI、README 核心能力描述。
- 验证方式：本机 `kimi doctor` 通过；`cargo fmt --manifest-path apps\kimi-shell\src-tauri\Cargo.toml -- --check` 通过；`cargo check --manifest-path apps\kimi-shell\src-tauri\Cargo.toml` 通过；`cargo test --manifest-path apps\kimi-shell\src-tauri\Cargo.toml --no-run` 通过；`.\node_modules\.bin\tsc.cmd --noEmit`（`apps/kimi-shell`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-shell/README.md` 的诊断能力描述。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：后续如 doctor 输出新增更多 secret 来源，应同步扩展 redaction value collector；P5 仍需安装版按钮点击验证。

## 2026-06-28-14 - Bridge recovered approval card redelivery

- 用户目标：推进 v3 P4C approval reconcile 后续体验，让 server-only recovered approvals 回到原 IM 审批上下文。
- 涉及目录：`apps/kimi-im-bridge/internal/app`、`apps/kimi-im-bridge/internal/adapters/telegram`、`apps/kimi-im-bridge/internal/adapters/feishu`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：Bridge adapter 启动成功后会调用可选 `RedeliverPendingApprovals` hook；Telegram adapter 扫描 pending approvals，按 `telegram:approval:<approvalID>` delivery key 幂等发送带按钮的 approval message；Feishu adapter 扫描 pending approvals，按 `feishu:approval:<approvalID>` delivery key 幂等回复 interactive approval card，并从 binding 的 last inbound message id 恢复回复/线程上下文；Weixin 保持不变，因为当前没有 in-chat approval UI 或 callback path。
- 修改原因：此前 server pending approval reconcile 已能重建本地 projection 和 chat context，但不会重新把 approval card 投递到 IM，用户需要去 admin/UI 才能处理。
- 本次已消除的不确定性：确认现有 Telegram/Feishu approval card builder 与 sendRecorded delivery key 可以复用，不需要新增 delivery 表或平台消息幂等机制。
- 本次采用的关键假设：重投递只针对现有 pending approvals，且依赖既有 delivery key 跳过已发送卡片；Feishu 没有 last inbound message id 时跳过并记录日志，避免丢失线程上下文。
- 剩余不确定性：真实 Telegram/Feishu 平台凭证链路仍需 P5 manual gate；Weixin 仍只能通过 admin/UI 处理 approval。
- 影响范围：Bridge adapter 启动流程、Telegram/Feishu approval card 恢复体验、Bridge README/current-state。
- 验证方式：`go test ./internal/adapters/telegram ./internal/adapters/feishu ./internal/app` 通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如 Feishu 后续需要无 reply source 的直接投递，应先扩展 sender 支持 create-message 并明确线程退化策略；当前不把缺上下文的 approval 直接发到群里。

## 2026-06-28-14 - ACPAdapter live async manual approvals

- 用户目标：推进 v3 P4C RuntimeAdapter fallback，避免 ACPAdapter manual approval 只能 live auto/cancel。
- 涉及目录：`apps/kimi-im-bridge/internal/runtime`、`apps/kimi-im-bridge/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：ACPAdapter 新增当前进程内 pending approval registry；`session/request_permission` 在 manual/ask/approval mode 下会先登记 pending approval，再发出 `approval_requested` event，并阻塞 JSON-RPC response 直到 `ResolveApproval` 提供 IM 决策；`ListApprovals` 返回当前 live pending approvals；manual approval 会根据 approve/approve_for_session/deny 决策选择 ACP option，找不到匹配 option 时返回 cancelled。
- 修改原因：v3 要求 ACPAdapter manual approval 不再只能自动批准或取消，而应能够通过 Bridge approval flow 异步决策。
- 本次已消除的不确定性：确认当前 ACP transport handler 可以在 live JSON-RPC request 中等待外部 `ResolveApproval`，不需要改 transport 协议或 store schema。
- 本次采用的关键假设：ACP permission request 仍是 live-only 协议事件；没有官方持久 pending approval API 时，本轮不伪造跨重启恢复。
- 剩余不确定性：ACPAdapter 仍未接入默认 app provider selection，真实 `kimi acp` installed runtime 仍需 P5 manual smoke；跨 Bridge 重启恢复仍未实现。
- 影响范围：实验性 ACPAdapter 的 manual approval 行为和测试。
- 验证方式：`go test ./internal/runtime ./internal/providers/runtimeadapter` 通过。
- 是否更新 README：是，更新 `apps/kimi-im-bridge/README.md`。
- 是否更新 `.ai/architecture`：是，更新 `current-state.md`。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若未来官方 ACP 提供 durable approval list/resolve API，再把当前 live pending registry 扩展为跨重启 reconcile；否则不要声称 ACP approval 可跨进程恢复。

## 2026-06-28-14 - v3 local gates closure

- 用户目标：根据 v3 整合目标完整开发本地可闭环代码门禁，并明确不能在当前环境自动完成的发布门禁。
- 涉及目录：`.ai/architecture`、`tasks`、`.ai/changes`。
- 修改内容：将 `current-state.md` 的剩余缺口改写为运行边界与 P5 手工门禁；在 `verification-gates.md` 增加真实 IM 凭证、安装包、诊断脱敏、OpenAPI/AsyncAPI 快照等发布前手工验证项；在 `tasks/todo.md` 补充本地代码门禁收口结论。
- 修改原因：v3 文档把 P5 真实平台验证定义为发布门禁，不能在无真实凭证和安装包环境下伪造完成；同时 Shell prompt/approval 主交互由官方 Web 承载，不应被误读为本轮代码门禁缺失。
- 本次已消除的不确定性：确认 P1A/P1B/P3/P4A/P4C 的本地代码与测试门禁已实现并记录；确认 P5 需要专用环境或平台凭证。
- 本次采用的关键假设：本轮目标交付为代码迁移与本地验证闭环，真实平台凭证验证在发布流程执行。
- 剩余不确定性：真实 Telegram/Feishu/Weixin 凭证链路、NSIS/MSI 安装版点击回归、OpenAPI/AsyncAPI CI 快照尚未在当前环境执行。
- 影响范围：仅文档和任务记录，不改变运行逻辑。
- 验证方式：文档更新后继续运行 `go test ./...`、`cargo fmt --check`、`cargo check`、`cargo test --no-run`、`tsc --noEmit` 与 `git diff --check`。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：是，更新 current state 与 verification gates。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：发布前按 `verification-gates.md` 的 Manual Release Gates 执行，不要把无凭证平台验证标记为已完成。

## 2026-06-28-15 - 配置中心收敛文档

- 用户目标：结合官方 Kimi Code 文档和当前 Kimi App 配置中心，整理配置清单、重复项和未来方向；本轮只写文档。
- 涉及目录：`docs/rebuild`、`.ai/changes`。
- 修改内容：新增 `08-配置中心收敛与配置清单-v1.md`，按官方 Kimi Code、Kimi App 外壳、Skill Center、IM Bridge 四类归属整理配置清单；在 v3 总览文档列表中登记新文档。
- 修改原因：Kimi Code Web 已有官方设置页，当前 `ConfigCenterModal` 继续编辑 `config.toml` 会形成第二套配置中心，需要先以文档明确边界。
- 本次已消除的不确定性：确认官方文档当前路径为 `~/.kimi-code/config.toml` / `$KIMI_CODE_HOME/config.toml`，MCP 归 `mcp.json` 与 `/mcp-config`，Plugins 归 `/plugins`，Skills 有官方扫描目录；同时确认当前 App 配置中心仍有 `~/.kimi` 路径遗留。
- 本次采用的关键假设：本轮不修改代码、类型、API、schema 或 UI；后续实现再删除/降级重复入口。
- 剩余不确定性：官方 Web 设置页未来是否覆盖 Skill 安装/投影；若覆盖，App Skill Center 需要进一步降级。
- 影响范围：仅文档，不改变运行逻辑。
- 验证方式：人工校对官方文档与仓库源码；`rg` 复查关键字段与新增文档索引。
- 是否更新 README：否，未改变目录职责。
- 是否更新 `.ai/architecture`：否，未改变架构事实入口。
- 是否需要新增或更新 `.ai/decisions`：否，本轮是配置清单与方向文档，不新增 accepted ADR。
- 后续注意事项：下一步实现时优先删除/降级 `ConfigCenterModal` 的通用 TOML 编辑能力，不要继续扩展重复配置项。

## 2026-06-28-16 - Workspace Grid v1 foundation

- 用户目标：根据 Workspace Grid 开发计划开始开发，并确认应用文中的两个决策。
- 涉及目录：`apps/kimi-shell`、`.ai/decisions`、`.ai/changes`、`tasks`。
- 修改内容：新增 Vitest 基线与 `pnpm test`；新增 `features/workspace-grid` 的 Pane/Slot 类型、Grid 预设、旧双窗 localStorage 迁移、zustand store、selector、`paneUrl`、`urlSafety` 和单测；新增 ADR 确认 DR-A/DR-B；更新 app README 与任务记录。
- 修改原因：Workspace Grid v1 需要先有可测试的状态模型和迁移逻辑，再替换现有写死双窗 UI。
- 本次已消除的不确定性：确认当前 app 是 React；确认根 `.ai/CONSTITUTION.md` 缺失是 `.ai/architecture/README.md` 已记录的既有缺口；确认当前只需要 WG-0/WG-1 最小基础，不需要立即改 `WorkspaceView`。
- 本次采用的关键假设：Grid v1 先不接入现有 UI，避免一次性改动 `useShellController` 和 iframe 渲染；外部页 v1 只做 fallback，不引入 Tauri 子 Webview。
- 剩余不确定性：WG-2 接入新 Grid renderer 时仍需做真实桌面 UI 回归；`pnpm-lock.yaml` 因新增依赖包含 pnpm 解析出的平台元数据变化。
- 影响范围：前端测试入口、Workspace Grid 后续状态模型、旧双窗状态迁移路径；现有运行 UI 未改变。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过；`.\node_modules\.bin\tsc.cmd --noEmit`（`apps/kimi-shell`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，`apps/kimi-shell/README.md` 增加 `pnpm test`。
- 是否更新 `.ai/architecture`：否，未改变当前运行架构事实。
- 是否需要新增或更新 `.ai/decisions`：是，新增 `2026-06-28-workspace-grid-v1.md`。
- 后续注意事项：WG-2 再把 `WorkspaceView` 替换为 grid renderer；UI 接入前不要删除旧 `WORKSPACE_*` 键，迁移期保持只读兼容。

## 2026-06-28-18 - Workspace Grid renderer and session commands

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中剩余 Workspace Grid v1 开发工作。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/README.md`、根 `README.md`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：`WorkspaceView` 改为委托 `WorkspaceGridView`；新增 `PaneFrame`，支持 1/2/3/4/5/6 预设、空 slot、窗格内容切换、关闭、最大化、外部页 timeout fallback 和 6 窗上限；旧 titlebar 单窗/双窗/换位控制同步到 Grid store；新增 `grid_list_sessions`、`grid_create_session` Tauri command 和前端 service；Code 空 slot 可用当前工作目录创建真实 server session 并渲染 `/sessions/{id}`。
- 修改原因：WG-2 要求替换写死双窗，WG-3 要求复用既有 `/api/v1` session 基础设施，WG-4/WG-5/WG-6 需要最小可用交互、fallback 和资源治理。
- 本次已消除的不确定性：确认现有 `workspace_session.rs` 的私有 fetch/create 路径可通过薄 public wrapper 复用；确认 `WorkspaceView` 可保持 props 兼容，避免大规模改 `App.tsx` 与 shell controller。
- 本次采用的关键假设：Grid v1 先保持 iframe carrier；新增 code pane 依赖首个同源 frame 完成 token bootstrap，Tauri 子 Webview 与 per-pane storage 隔离继续留给 WG-7/WG-8。
- 剩余不确定性：未在真实 Tauri 桌面运行环境手工验证 3 个 Code session 的独立 WebSocket/交互；外部页 XFO/CSP fallback 仍需真实页面点击验证。
- 影响范围：主 workspace 渲染路径、titlebar workspace 控制兼容层、Grid session command、README/架构事实。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，更新 current state 与 verification gates。
- 是否需要新增或更新 `.ai/decisions`：否，沿用已 accepted 的 Workspace Grid v1 ADR。
- 后续注意事项：完成目标前仍需真实桌面手工验证多 Code session、外部页 fallback；WG-7/WG-8 属于 v1b/v2 增强，不应阻塞 v1 代码闭环。

## 2026-06-28-18 - Workspace Grid v1 hardening

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中剩余 Workspace Grid v1 交互、外部页和资源治理工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/package.json`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：新增方向键 active pane 切换；外部网页 pane 支持输入自定义 `http/https` URL 并继续剥离 fragment；mount policy 支持挂起/恢复和非活跃 on-focus 延迟挂载；Grid 状态区展示运行中 Code Session 数；新增 React Testing Library + jsdom 组件测试覆盖键盘切换、自定义外部 URL、挂起/恢复。
- 修改原因：补齐 WG-4/WG-5/WG-6 中上一轮仍缺少直接行为证据的部分，避免只靠编译通过判断 UI 行为。
- 本次已消除的不确定性：确认 `WorkspaceGridView` 可在 jsdom 中验证关键交互；确认最新 jsdom 会引入 Node 20+ engine，已固定到 Node 18 兼容的 `24.1.3`。
- 本次采用的关键假设：Grid v1 使用浏览器 `prompt` 作为最小 custom URL 输入；后续若需要更精细 UX，再替换为设计化弹窗而不改变 state/API。
- 剩余不确定性：真实 Tauri 桌面中 3 个 Code session 的独立交互仍需要人工环境点击验证；当前已安装旧包窗口为 13x13，不能作为新源码视觉证据。
- 影响范围：Workspace Grid 交互、资源挂起策略、前端测试依赖和测试覆盖。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过（2 files / 13 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：否，README 已在上一轮更新 Grid 能力；本轮未改变长期用户入口。
- 是否更新 `.ai/architecture`：是，补充 current state 中的键盘、custom URL 和 mount policy 事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果后续要完成 WG-7/WG-8，需要明确进入 v1b/v2 范围；不要把 Tauri 子 Webview 或 per-pane storage 隔离混进 v1 修补。

## 2026-06-28-18 - Workspace Grid named layouts

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中 WG-8 剩余增强工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/App.css`、`apps/kimi-shell/README.md`、`README.md`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：为 Workspace Grid 增加命名布局保存/恢复；保存内容复用现有 sanitized persisted grid state，恢复时清除 transient 最大化状态；工具栏增加原生保存按钮和布局下拉框；新增单测和组件测试覆盖保存、恢复与 URL fragment 清理。
- 修改原因：推进 WG-8 中“命名布局保存/恢复”要求，先落地可验证的最小版本，不提前引入 modal、拖拽布局编辑器或子 Webview。
- 本次已消除的不确定性：确认命名布局可通过现有 state/storage 模型实现，且不会把外部 URL fragment 写入保存布局。
- 本次采用的关键假设：当前阶段用浏览器 `prompt` 命名布局、`select` 恢复布局足够；更复杂的布局管理 UI 等到确有多个布局管理需求再做。
- 剩余不确定性：WG-7 子 Webview、per-pane localStorage namespace、逐缝拖拽 resize 仍未完成；真实桌面多 Code session 手工验证仍未补齐。
- 影响范围：Workspace Grid 工具栏、grid state 持久化辅助函数、README 能力描述、测试覆盖。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过（2 files / 15 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，补充 current state 中的命名布局事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：继续推进 WG-8 时优先做可验证的 resize/custom template；WG-7 子 Webview 需要单独设计 Tauri geometry/focus/destroy 生命周期，不要和保存布局混在一个改动中。

## 2026-06-28-18 - Workspace Grid resizable tracks

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中 WG-8 的逐缝拖拽 resize 与持久化 custom template 工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/App.css`、`apps/kimi-shell/README.md`、`README.md`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：为 Grid state 增加可选 `trackSizes`；新增 grid track 计数、归一化、CSS 序列化和相邻 track resize 纯函数；Grid canvas 增加列/行拖拽 handle；preset 切换清除不匹配 custom tracks；命名布局保存/恢复自动携带 sanitized track sizes；新增单测和组件测试覆盖 resize 与持久化。
- 修改原因：补齐 WG-8 中“逐缝拖拽 resize + 持久化 custom template”的可运行最小版本。
- 本次已消除的不确定性：确认 custom template 可用当前 preset 的 `fr` track sizes 表达，不需要先引入完整布局编辑器或新依赖。
- 本次采用的关键假设：当前 custom template 只覆盖列/行比例，不改变 slot area 拓扑；拓扑级布局编辑以后有明确产品需求时再做。
- 剩余不确定性：WG-7 子 Webview、per-pane localStorage namespace、真实桌面多 Code session 手工验证仍未完成。
- 影响范围：Workspace Grid 布局状态、Grid canvas UI、命名布局保存内容、README 能力描述与测试覆盖。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过（2 files / 18 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，补充 current state 中的 resize/custom track 事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若继续推进 WG-8，per-pane localStorage namespace 需要先确认是改 `kimi-web` 还是依赖 WG-7 多 Webview；不要用 iframe 里的同源 localStorage hack 模拟隔离。

## 2026-06-28-18 - Workspace Grid external WebviewWindow fallback

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中 WG-7 的 Tauri 承载退路工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/services`、`apps/kimi-shell/src-tauri/capabilities`、`apps/kimi-shell/README.md`、`README.md`、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：新增 `openExternalWebviewWindow` service，复用 `urlSafety` 清理外部 URL 并通过 Tauri `WebviewWindow` 创建独立应用窗口；外部 pane 的挂起和 blocked fallback 增加“在应用窗口打开”；main capability 增加 `core:webview:allow-create-webview-window`；组件测试覆盖外部 iframe 超时后的 WebviewWindow fallback 调用。
- 修改原因：推进 WG-7 中“退而求其次 WebviewWindow 新窗”的可验证实现，让无法 iframe 的外部页不只能跳系统浏览器。
- 本次已消除的不确定性：确认当前安装的 `@tauri-apps/api` 暴露 `new WebviewWindow(label, { url })`，且 Tauri capability 需要显式允许 `create_webview_window`。
- 本次采用的关键假设：独立应用 WebviewWindow 是 WG-7 的最小可交付退路；嵌入式子 Webview 的 pane geometry/focus/destroy 生命周期单独处理。
- 剩余不确定性：嵌入式子 Webview、per-pane localStorage namespace、真实桌面多 Code session 手工验证仍未完成。
- 影响范围：Workspace Grid 外部页 fallback、Tauri main capability、README 能力描述与组件测试。
- 验证方式：`pnpm --dir apps/kimi-shell test` 通过（2 files / 19 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，补充 current state 中的 WebviewWindow fallback 事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：继续 WG-7 时应优先在 Rust/Tauri 层实现 pane geometry 管理，避免让远程外部页面获得主窗口 capability。

## 2026-06-28-19 - Workspace Grid embedded external Webview

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中 WG-7 的窗格内 Tauri 承载工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/services`、`apps/kimi-shell/src-tauri/capabilities`、`apps/kimi-shell/README.md`、根 `README.md`、`.ai/architecture`、`.ai/changes`、`docs/rebuild`、`tasks`。
- 修改内容：扩展外部 Webview service，新增 Tauri v2 子 `Webview` 创建、bounds 归一化、position/size 同步和 close controller；外部页 blocked fallback 增加“在窗格内打开”，由 pane DOM bounds 创建嵌入式 Webview，并在 resize、scroll、source change、挂起和 unmount 时同步或销毁；补充 Tauri webview create/focus/position/size/close capability；组件测试覆盖嵌入调用。
- 修改原因：补齐 WG-7 要求的“被禁 frame 页面可保持窗内显示”，不再只依赖系统浏览器或独立应用窗口。
- 本次已消除的不确定性：确认当前 `@tauri-apps/api` 暴露 `new Webview(getCurrentWindow(), label, { url, x, y, width, height })`、`setPosition`、`setSize` 和 `close`；确认对应 capability identifier 为 `core:webview:allow-create-webview`、`allow-set-webview-position`、`allow-set-webview-size`、`allow-set-webview-focus`、`allow-webview-close`。
- 本次采用的关键假设：pane 的 `getBoundingClientRect()` 逻辑像素可直接作为 Tauri child Webview 的初始 logical bounds；真实 DPI 差异通过 Tauri logical coordinate API 处理。
- 剩余不确定性：尚未在真实 Tauri 桌面窗口中点击验证外部 child Webview 的 z-order/focus/DPI 行为；WG-8 per-pane localStorage namespace 与真实桌面多 Code session 手工验证仍未完成。
- 影响范围：Workspace Grid 外部页 fallback、Tauri main capability、README/架构事实与组件测试。
- 验证方式：`pnpm --dir apps/kimi-shell test -- --run` 通过（2 files / 20 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，补充 current state 中的嵌入式 Tauri 子 Webview 事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：继续完成 WG-8 per-pane localStorage namespace 时，应优先判断是否通过 `kimi-web` storage key 前缀完成，避免让外部页面获得不必要的主窗口 capability。

## 2026-06-28-19 - Workspace Grid native Webview storage namespace

- 用户目标：继续完成 `11-WorkspaceGrid评估与开发计划-基于更新后仓库.md` 中 WG-8 的 per-pane localStorage namespace 工作。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/services`、`apps/kimi-shell/README.md`、`.ai/architecture`、`.ai/decisions`、`.ai/changes`、`docs/rebuild`、`tasks`。
- 修改内容：为每个 Grid pane 增加稳定 `storageNamespace`，旧持久化 pane 缺失时按 pane id 补齐；嵌入式 Tauri 子 `Webview` 和独立 `WebviewWindow` fallback 均把 pane namespace 传入 `dataDirectory`；单测覆盖 legacy pane namespace 补齐，组件测试覆盖 native carrier 调用参数。
- 修改原因：WG-8 要求 per-pane localStorage namespace；当前仓库没有 `apps/kimi-web`，可控落点是多 native Webview carrier 的 Tauri 数据目录隔离。
- 本次已消除的不确定性：确认当前 Tauri JS `WebviewOptions` / `WebviewWindow` options 接受 `dataDirectory`，可为 native carrier 提供稳定本地存储目录。
- 本次采用的关键假设：native Webview 的 localStorage/IndexedDB/cookie/cache 目录由 `dataDirectory` 隔离；iframe carrier 的同源 localStorage 隔离不应通过 shell state 伪造。
- 剩余不确定性：真实 Tauri 桌面中 native Webview `dataDirectory` 隔离仍需人工点击验证；DirectServer iframe code pane 的同源 localStorage 隔离仍需要 hosted web app 支持或迁移为 native Webview carrier。
- 影响范围：Workspace Grid state 持久化、saved layout 内容、native Webview/WebviewWindow carrier storage、README/架构事实与测试覆盖。
- 验证方式：`pnpm --dir apps/kimi-shell test -- --run` 通过（2 files / 21 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 通过；`pnpm --dir apps/kimi-shell build` 通过；`cargo fmt -- --check` 通过；`cargo check` 通过；`cargo test --no-run` 通过；`go test ./...`（`apps/kimi-im-bridge`）通过；`git diff --check` 通过，仅有 CRLF 提示。
- 是否更新 README：是，更新 `apps/kimi-shell/README.md` 的 Workspace Grid 能力描述。
- 是否更新 `.ai/architecture`：是，补充 current state 中的 native Webview `dataDirectory` namespace 事实。
- 是否需要新增或更新 `.ai/decisions`：是，在既有 Workspace Grid ADR 增加 follow-up，说明 v1b/v2 后续实现与 iframe 限制。
- 后续注意事项：不要把 iframe 同源 localStorage 隔离描述为已完成；如要覆盖 code iframe，需拿到 `apps/kimi-web` 源码做 key prefix，或把 code pane carrier 迁移为 native Webview 并补齐 token/session bridge。

## 2026-06-28-19 - Workspace Grid session API smoke

- 用户目标：补齐 Workspace Grid 多 Code session 的真实 server API 验证证据。
- 涉及目录：`.ai/changes`、`tasks`。
- 修改内容：使用临时 `KIMI_CODE_HOME` 启动本机 `kimi server run --foreground --port 58677`，等待 `/api/v1/healthz`，读取临时 token 文件后调用 `/api/v1/workspaces` 和 3 次 `/api/v1/sessions`；验证返回 3 个 distinct server session id，并清理临时 server job 和 token 目录。
- 修改原因：补齐此前只由组件测试覆盖、未由真实 Kimi server API 证明的 WG-3 多会话验收项。
- 本次已消除的不确定性：确认本机 Kimi CLI `0.20.1` 的 `/api/v1` workspace/session API 可按当前 Grid command 语义创建 3 个不同 server session。
- 本次采用的关键假设：直接 API smoke 能证明 Grid command 依赖的 server session contract；UI 点击和 native Webview z-order 仍属于真实桌面验证。
- 剩余不确定性：尚未在真实 Tauri 桌面窗口中点击验证 native child Webview z-order/focus/DPI 与 installed app 视觉行为。
- 影响范围：验证记录和任务状态；无代码变更。
- 验证方式：PowerShell smoke 输出 `healthy=true workspace_created=true sessions_created=3 distinct=3 listed=3`，短 id 前缀为 `session_21,session_3d,session_6d`。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果需要关闭最后的桌面证明缺口，应启动当前源码 Tauri dev/build，在真实窗口中点击外部 blocked pane 的“在窗格内打开”并确认 native Webview geometry/focus。
