## 2026-06-29-00 - Workspace Grid 扩容回填隐藏窗格

- 用户目标：修复多窗格从小布局切回大布局后隐藏 pane 不回填，导致可见 code/chat 数量异常受限的问题。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`
- 修改内容：`setPreset` 在切换布局时保留当前可见 pane 顺序，并追加未分配的隐藏 pane，让扩容布局自动回填已有 pane。
- 修改原因：布局切换应改变可见容量，不应让隐藏 pane 长期占用 6 窗格额度却不可见。
- 本次已消除的不确定性：确认数量限制来自前端 pane 总数上限与当前 preset slot 容量，而非后端 session 限制。
- 本次采用的关键假设：隐藏 pane 是用户已打开的工作上下文，应在扩容时恢复可见。
- 剩余不确定性：未验证安装版 WebView localStorage 中已有异常状态的迁移体验。
- 影响范围：Workspace Grid 布局切换和新增 pane 可用性。
- 验证方式：新增并运行 `vitest` workspace-grid store 回归测试。
- 是否更新 README：否，目录职责和公共运行方式未变化。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如仍出现“可见 pane 少但达到上限”，检查 localStorage 中 `kimi-workspace-grid-state-v1` 是否已有损坏 pane 数据。

## 2026-06-29-10 - 修复 DirectServer Session 导航超时

- 用户目标：排查并修复后端启动后主窗口显示但出现 `Session navigation timed out` 的问题。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/workspace-grid`
- 修改内容：前端收到 `navigate_session` bootstrap/bridge 事件后，优先把 server session id 写入 Workspace Grid 的 Code pane，由现有 `/sessions/<id>#token=...` URL 渲染路径接管；保留旧 postMessage 注入桥作为兜底路径；新增 grid store 回归测试覆盖 pane 配置 session id。
- 修改原因：当前 DirectServer 模式不再默认走 workspace proxy，官方 Kimi Web 页面没有 `workspace_injection.rs` 注入脚本，因此旧 `postMessage -> navigate_session_ack` 链路会等待到超时。
- 本次已消除的不确定性：确认启动日志里的 `frontend_ready`、`report_loading_rendered`、`main shown` 已完成，红条来自前端 session 导航 timer，不是主窗口创建失败。
- 本次采用的关键假设：Workspace Grid 直接构造 `/sessions/<id>` 是当前已存在且受测试保护的 session 打开路径。
- 剩余不确定性：未在真实安装版 WebView 中复现用户截图路径，但已验证类型和相关单元测试。
- 影响范围：启动 bootstrap/open-request session 切换；控制中心和 workspace 层共存时不再依赖 iframe 注入 ack。
- 验证方式：`.\node_modules\.bin\vitest.cmd run src/features/workspace-grid/grid.test.ts`；`.\node_modules\.bin\tsc.cmd --noEmit`
- 是否更新 README：否，未改变目录职责、公共运行方式或长期约定。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如未来恢复 proxy 注入路径，旧 postMessage 兜底仍可工作；DirectServer 主路径应继续优先使用 Grid session URL。

## 2026-06-29-11 - 移除 Kimi 登录 JSON 探测

- 用户目标：移除旧 `kimi login --json` / `logout --json` 探测，改回 Kimi Code Server 认证语义，并先压低后端启动链路中的低风险耗时。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src`
- 修改内容：`probe_kimi_login` 改为读取 Kimi Code Server `/api/v1/auth`；`logout_kimi_login` 改为调用 `/api/v1/oauth/logout`；移除启动前 `kimi server run --help` 契约检查；健康检查只探测选中端口的 `/api/v1/healthz` 和 `/openapi.json`；启动托管 server 时注入 `KIMI_CODE_NO_AUTO_UPDATE=1`；补充 startup timing 日志；更新认证 UI 文案。
- 修改原因：官方 `kimi login` 是交互式登录流程，不是 JSON 状态探测接口；App 不应再执行不存在的 `--json` flag。启动链路也不应每次额外冷启动 CLI help。
- 本次已消除的不确定性：确认坏命令来自 Tauri handler 中的 `.arg("login").arg("--json")` / `.arg("logout").arg("--json")`；确认当前 runtime 已有 `/api/v1` Bearer client 和 server token/origin 状态可复用。
- 本次采用的关键假设：`/api/v1/auth` 的 `ready` 字段足以映射桌面壳当前的认证就绪状态；`/api/v1/oauth/logout` 可接受空 JSON body，且可能返回无 data 的成功 envelope。
- 剩余不确定性：未完整实现 `/api/v1/oauth/login` 登录发起 UI；旧 `KimiCli*` 内部类型和兼容 command 名仍保留以避免大范围破坏前端契约。
- 影响范围：认证刷新/注销按钮、运行诊断文案、Kimi Code Server 启动耗时与健康检查失败提示。
- 验证方式：`cargo check`；`cargo test auth_summary_message_tracks_ready_state --lib` 受本机既有 `STATUS_ENTRYPOINT_NOT_FOUND` 阻塞；`cargo test --no-run`；`.\node_modules\.bin\tsc.cmd --noEmit`。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期模块边界。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若要继续 SPEC-08 清理，应单独做 KimiCli 兼容 API/类型退出和 `/api/v1/oauth/login` 登录入口，不和启动优化混在一个 diff。

## 2026-06-29-12 - 收敛 Kimi Code Access 配置面

- 用户目标：继续 SPEC-08 收尾，删除旧 Kimi CLI 配置中心和默认 API/登录入口残留。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src`
- 修改内容：前端控制中心 task 改为 `kimi_code_access` 并移除“设为默认 API/登录”操作；后端删除旧 `KimiCliApiConfig*`、`KimiCliConfigCenter*`、provider/model/service/loop/MCP 全量配置中心类型与导出；`run_kimi_doctor` 脱敏改为只读取 Kimi Code Access 管理的 provider/search/fetch key；重命名 auth Tauri command、Kimi Code Access 面板和品牌组件；安装管理文案从 Kimi CLI 收敛到 Kimi Code；新增 secret collector 覆盖测试。
- 修改原因：App 边界应是 Kimi Code 桌面壳与 Kimi Code Access 白名单配置，不应继续暴露旧 CLI 配置中心或写全局 `default_model` 的普通入口。
- 本次已消除的不确定性：确认前端不再调用旧 Tauri command；确认源码中 SPEC 点名的旧 `KimiCli*`/`config_center`/默认设置符号已从 `src` 与 `src-tauri/src` 清除；确认剩余 `kimi-cli` 字符串只指真实上游仓库/包名。
- 本次采用的关键假设：doctor 输出只需脱敏 App 管理的 Kimi Code Access key 和常见 API key 环境变量；安装任务内部 id 可继续兼容旧枚举，不作为用户语义暴露。
- 剩余不确定性：尚未实现 `/api/v1/oauth/login` 登录发起 UI；本地 upstream OpenAPI snapshot 不含 `/api/v1/oauth/login`，需要运行时接口证据后再接入。
- 影响范围：控制中心接入配置、Tauri 配置 API 表面积、doctor 输出脱敏。
- 验证方式：`cargo fmt`；`cargo check`；`cargo test --no-run`；`.\node_modules\.bin\tsc.cmd --noEmit`；源码扫描确认旧 `KimiCli*`/`config_center`/`cli_contract`/`login --json`/旧 auth command 符号已清除。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期模块边界。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如继续推进 SPEC-08，可把登录按钮接到 Kimi Code Server OAuth login 流程，并单独整理安装器的 “Kimi CLI” 命名。

## 2026-06-29-13 - 接入 Kimi Code OAuth 登录入口

- 用户目标：完成 SPEC-08 中 Kimi Code Server 认证入口的收尾，避免登录按钮继续只是刷新状态。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src`
- 修改内容：新增 `start_kimi_code_auth` Tauri command，通过 Kimi Code Server `POST /api/v1/oauth/login` 发起 OAuth flow，打开 `verification_uri_complete` 并把验证码与有效期写入认证状态；控制中心认证主按钮改为“打开登录”，刷新状态和退出登录降为次要操作；内部 `LoginProbe*` 语义收口为 `KimiCodeAuth*`；同步 README/安装说明/增强 Web 计划中的 Kimi Code Doctor 与 Kimi Code 命名；补充 OAuth 登录消息单测。
- 修改原因：SPEC-08 要求认证应回到 Kimi Code Server `/api/v1/auth`、`/api/v1/oauth/logout` 和最终 `/api/v1/oauth/login` 语义，不应再依赖旧 `kimi login --json` 思路。
- 本次已消除的不确定性：通过本机已安装 `@moonshot-ai/kimi-code@0.20.1` 运行时代码确认 `/v1/oauth/login` 的 POST/GET/DELETE schema 和 `OAuthFlowStart` 字段，可安全接入登录发起路径；确认当前源码/当前说明文档不再暴露 SPEC 点名的旧 Kimi CLI 配置中心和旧登录探测命名。
- 本次采用的关键假设：默认 provider 可由 server 侧选择，空 JSON body 足够发起默认登录；桌面壳只负责打开官方登录 URL 并提示验证码，后续状态通过 `/api/v1/auth` 刷新。
- 剩余不确定性：未在真实 Kimi Code Server 登录会话中完成端到端 OAuth click-through；SPEC 中 P2 级启动优化（daemon reuse、background preheat、token wait 合并）仍未实现，如继续追启动耗时可单独处理。
- 影响范围：控制中心认证步骤、Tauri auth command 表面积、认证状态提示、当前安装与诊断说明文档。
- 验证方式：`cargo fmt`；`cargo check`；`cargo test --no-run`；`.\node_modules\.bin\tsc.cmd --noEmit`；源码扫描确认旧 `KimiCli*`/`config_center`/`cli_contract`/`login --json`/旧 auth command 符号已清除。
- 是否更新 README：是，仅同步当前诊断命名，不改变目录职责、公共运行方式或长期模块边界。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如用户仍关心启动速度，应把 daemon 复用、后台预热和 token 等待合并拆成独立性能任务，避免和认证/配置收尾混在同一批修改中。

## 2026-06-29-15 - SPEC-08 P0 收口修复

- 用户目标：按 SPEC-08 / Kimi Sidekick 收口计划完成 P0 合并前安全、品牌、iframe 主题、布局不刷新和配置错误显式化要求。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、根 README、`apps/kimi-shell/README.md`、`.ai/changes`
- 修改内容：新增父窗口外链 http/https sanitizer；将 all-frame 注入脚本收敛到 `frame_workspace_bridge.js` 并加入 pane theme head listener；Workspace Grid 切 preset 时 active pane 优先保留且 React key 稳定到 pane id；pane 主题 UI 改为“跟随全局 / 浅色 / 深色”；Kimi Code Access 配置损坏时返回 `configError`、Provider API health 置 Error 且 auth mode 变 Unknown；把当前 app 可见品牌统一为 Kimi 小助手 / Kimi Sidekick，保留 Kimi Code runtime 语义。
- 修改原因：前次 SPEC-08 主体功能已完成，但 P0 review 仍有外链安全、跨 iframe 主题、布局刷新、配置损坏吞错和品牌混用风险。
- 本次已消除的不确定性：确认父窗口外链路径只做 source trust；确认 per-pane theme state 已存在且 `undefined` 可表达跟随全局；确认布局刷新来自 slot id key 和 preset 重排；确认损坏 `config.toml` 会被旧 auth snapshot 吞成默认配置。
- 本次采用的关键假设：Tauri all-frame init script 是跨域 iframe head 同步的最小可行入口；“查看备份”可通过打开同一 Kimi Code 配置目录完成；P1/P2 的 OAuth 自动轮询、capability probe、端口重试和测试连接语义优化不并入本次 P0 diff。
- 剩余不确定性：未做真实 Tauri WebView 多 iframe 视觉验收；未做真实 OAuth click-through；未构建 NSIS/MSI 安装包检查安装器最终展示名。
- 影响范围：Workspace Grid 渲染与持久化顺序、外链打开边界、iframe 主题注入、Kimi Code Access 配置读取、当前品牌文案和 Tauri/package metadata。
- 验证方式：`.\node_modules\.bin\vitest.cmd run`；`.\node_modules\.bin\tsc.cmd --noEmit`；`node scripts/sync_version.mjs`；`.\node_modules\.bin\vite.cmd build`；`cargo fmt --manifest-path apps/kimi-shell/src-tauri/Cargo.toml -- --check`；`cargo check --manifest-path apps/kimi-shell/src-tauri/Cargo.toml`；`cargo test --manifest-path apps/kimi-shell/src-tauri/Cargo.toml --no-run`；`go test ./...` in `apps/kimi-im-bridge`；`git diff --check`。`pnpm --dir apps/kimi-shell test` 被当前非交互 pnpm purge guard 阻止，已按架构文档改用本地 `node_modules` 可执行文件。
- 是否更新 README：是，更新根 README、README_zh 和 `apps/kimi-shell/README.md` 的当前品牌名与安装包示例。
- 是否更新 `.ai/architecture`：否，未改变长期架构事实入口；读取时发现 `.ai/CONSTITUTION.md` 仍缺失，`.ai/architecture` 已记录该缺口。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：P1/P2 仍应拆成独立任务；发布候选前还需要真实 Windows 安装版、Tauri WebView iframe head、Grid no-refresh 和 OAuth click-through 手工验收。

## 2026-06-29-16 - 精简 Workspace 头部控件

- 用户目标：去掉 iframe pane 头部的明暗主题切换；去掉应用窗口头居中的工作区位置与打开目录按钮。
- 涉及目录：`apps/kimi-shell/src/features/window`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src`
- 修改内容：Workspace pane header 不再渲染 pane theme 控件；应用 workspace 标题栏不再显示居中的“工作区 | 路径”，也不再显示右侧“打开当前会话目录”按钮；保留标题栏中部不可见拖拽区，保留底层 pane theme 状态和 iframe theme sync 兼容已保存布局。
- 修改原因：按用户要求减少重复/干扰性的头部入口，同时不破坏已有主题同步和窗口拖拽行为。
- 本次已消除的不确定性：确认目标控件分别位于 `PaneFrame.tsx` 与 `ShellTitlebar.tsx`；确认保留 `pane.theme ?? themeMode` 不会破坏历史状态。
- 本次采用的关键假设：用户只要求移除可见入口，不要求删除全局主题能力、pane theme 持久化字段或 pane “打开此窗格目录”按钮。
- 剩余不确定性：未在真实 Tauri WebView workspace 中做视觉验收；普通浏览器 Vite 态会因缺少 Tauri `invoke` 停在 loading/error 壳。
- 影响范围：Workspace Grid pane header、workspace 窗口标题栏、相关组件测试和 CSS。
- 验证方式：`.\node_modules\.bin\vitest.cmd run src\features\window\ShellTitlebar.test.tsx src\features\workspace-grid\WorkspaceGridView.test.tsx`；`.\node_modules\.bin\tsc.cmd --noEmit`；临时 Vite + Browser 检查确认页面非空但受非 Tauri 运行时限制，不能进入目标 workspace。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期约定。
- 是否更新 `.ai/architecture`：否；读取时确认 `.ai/CONSTITUTION.md` 仍缺失且已在架构 README 中列为已知缺口。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如要彻底废弃 per-pane theme，应单独清理持久化字段、store API 和历史布局迁移策略。

## 2026-06-29-16 - Workspace Grid iframe pane 拖动布局

- 用户目标：调研 iframe pane 拖动布局的具体方法和最佳实践，并在 Workspace Grid 中执行实现。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src`
- 修改内容：将 pane 重排从原生 HTML Drag and Drop 切换为父 grid 管理的 Pointer Events；pane header 按下后由 grid canvas 捕获 pointer，移动时通过 slot DOM rect 命中目标 slot，释放时调用现有 `swapSlots`；拖动期间为 grid 添加 `is-pane-dragging`、源 pane 淡化、目标 slot 高亮，并临时让 iframe `pointer-events: none`。
- 修改原因：iframe 是独立浏览上下文，拖动指针进入 iframe 内容区后父页面不能可靠依赖 iframe 内部事件；父层 pointer 捕获 + iframe 命中禁用是更稳定的布局拖拽方案。
- 本次已消除的不确定性：确认当前 Grid 已有 Pane/Slot 分离和 `swapSlots` store API，可复用；确认拖动应从 pane header 发起，不抢占 iframe 内容区交互。
- 本次采用的关键假设：用户目标是实现 pane 之间的布局重排，而不是在 iframe 内容区域任意点拖动；拖拽交互只需要改变 slot/pane 映射，不需要改变 preset 或 track size。
- 剩余不确定性：未在真实 Tauri WebView 内对跨 iframe 拖动做手工视觉验收；普通 Vite 浏览器态无法进入完整 Tauri workspace。
- 影响范围：Workspace Grid pane header 拖动重排、iframe 命中行为、拖动高亮样式、相关组件测试。
- 验证方式：`.\node_modules\.bin\vitest.cmd run src\features\workspace-grid\WorkspaceGridView.test.tsx src\features\workspace-grid\grid.test.ts`；`.\node_modules\.bin\tsc.cmd --noEmit`；`git diff --check`。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期约定。
- 是否更新 `.ai/architecture`：否；读取时确认 `.ai/CONSTITUTION.md` 仍缺失且已在架构 README 中列为已知缺口。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如未来要支持拖拽过程中浮动预览、键盘重排或拖拽到 preset 外扩容，应单独设计交互和可访问性契约。

## 2026-06-29-16 - 移除控制中心登录阻塞

- 用户目标：去掉控制中心的登录功能和登录验证，并让 API 配置不再作为必选阻塞项。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：移除 workspace 顶部和控制中心内的 Kimi Code Auth 异常横幅、登录/刷新/退出登录按钮与 controller 暴露的前端认证动作；onboarding 第三步改为 `Provider API（可选）`，始终不阻塞完成状态；Overview 和 Runtime issue 列表不再把登录或 Provider API 健康状态作为待办/异常；诊断表移除 Auth Mode 与 Kimi 登录健康检查行，保留 Provider API 作为可选配置事实信息。
- 修改原因：控制中心不应要求用户先完成登录验证或 API 配置才能继续使用核心工作区功能。
- 本次已消除的不确定性：确认登录入口集中在 `App.tsx` 顶层横幅、`ControlCenterView.tsx` onboarding/auth 卡片和 `useShellController.ts` 前端回调；确认 Provider API 配置面板可保留为显式任务入口而不参与阻塞判断。
- 本次采用的关键假设：本次只移除前端登录功能和阻塞 UX，不删除 Rust 后端中可能仍被未来流程复用的 Tauri auth command 定义。
- 剩余不确定性：未在真实 Tauri WebView 中做控制中心视觉验收；普通浏览器 Vite 态仍受缺少 Tauri `invoke` 限制。
- 影响范围：控制中心 onboarding、运行概览、运行诊断、workspace 顶层提示和 shell controller 返回表面积。
- 验证方式：`.\node_modules\.bin\tsc.cmd --noEmit` in `apps/kimi-shell`；`git diff --check -- apps\kimi-shell\src\App.tsx apps\kimi-shell\src\features\control-center\ControlCenterView.tsx apps\kimi-shell\src\app\useShellController.ts`。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期约定。
- 是否更新 `.ai/architecture`：否；读取时确认 `.ai/CONSTITUTION.md` 仍缺失且已在架构 README 中列为已知缺口。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如要彻底删除后端 auth command 和类型，应另开任务同步清理 Rust 命令注册、测试和历史文档。

## 2026-06-29-16 - 移除控制中心 Web 体验入口

- 用户目标：去掉控制中心的 Web 体验，因为官方 Web 已经具备国际化，不再需要暴露本地增强版/i18n 切换。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：删除控制中心运行诊断页中的 `Web 体验` 卡片、官方 Web/本地增强版分段切换、增强版健康/来源/回退信息、自动回退开关和相关操作按钮；运行诊断表不再展示 Workspace Web / Enhanced Web 行；移除对应 props、回调传递和死 CSS；前端读取到历史 `enhanced_local` 设置时自动调用回退命令切回官方 Web。
- 修改原因：官方 Web 已覆盖国际化需求，控制中心继续暴露本地增强版会造成重复选择和不必要的风险提示。
- 本次已消除的不确定性：确认目标 UI 位于 `ControlCenterView.tsx` 的 runtime section；确认控制中心不再需要 `workspaceWebSettings`、`onWorkspaceWebModeChange`、`onWorkspaceWebAutoFallbackChange` 或 `onFallbackWorkspaceWebToOfficial` props。
- 本次采用的关键假设：本次删除的是控制中心的可见体验入口和前端暴露面，不同步删除 Rust 后端 enhanced web 模块、命令和历史设置字段，以保留迁移/兼容空间。
- 剩余不确定性：未在真实 Tauri WebView 中做视觉验收；普通浏览器 Vite 态仍受缺少 Tauri `invoke` 限制。
- 影响范围：控制中心运行诊断页、控制中心 props、shell controller 的 workspace web 控制暴露面和历史 enhanced local 设置回退行为。
- 验证方式：`.\node_modules\.bin\tsc.cmd --noEmit` in `apps/kimi-shell`；`git diff --check -- apps\kimi-shell\src\App.tsx apps\kimi-shell\src\App.css apps\kimi-shell\src\features\control-center\ControlCenterView.tsx apps\kimi-shell\src\components\control-center\control-center.css apps\kimi-shell\src\app\useShellController.ts`。
- 是否更新 README：否，未改变目录职责、公共运行方式或长期约定。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如要彻底废弃 enhanced web 后端，应单独清理 Rust 模块、Tauri command、设置结构和历史迁移策略。

## 2026-06-29-16 - 移除默认打包的 bridge-ops Skill

- 用户目标：去掉 `bridge-ops` 这个默认打包进应用的 skill。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`skills`
- 修改内容：删除仓库内 `skills/bridge-ops` skill 资源和脚本；移除 bridge 启动时自动复制 bundled `bridge-ops` 到用户 `.agents/skills` 的逻辑；bridge 启动命令不再自动追加 `--skills-dir`；保存 bridge 配置不再触发 bundled `bridge-ops` 安装；将 skill center 测试里的临时 bundled skill 名称从 `bridge-ops` 改为中性 fixture。
- 修改原因：`bridge-ops` 不应作为应用默认随包资源或自动安装到用户全局 skill 目录。
- 本次已消除的不确定性：确认默认打包来自 Tauri `skills/` 资源目录；确认自动安装/加载路径位于 `bridge_manager` 和 `save_bridge_settings`；确认删除资源目录后 `skills/` 只剩 docx/pdf/pptx/xlsx。
- 本次采用的关键假设：不主动删除用户机器上既有的 `~/.agents/skills/bridge-ops`，避免误删用户可能修改过的全局 skill；本次只移除应用默认打包和自动安装链路。
- 剩余不确定性：未构建真实安装包检查最终资源清单；已通过源码与文件树确认当前工作区资源目录不再包含 `bridge-ops`。
- 影响范围：IM Bridge 启动参数、bridge 配置保存行为、Tauri bundled `skills/` 资源内容。
- 验证方式：`cargo fmt --manifest-path apps/kimi-shell/src-tauri/Cargo.toml -- --check`；`cargo check --manifest-path apps/kimi-shell/src-tauri/Cargo.toml`；`cargo test --manifest-path apps/kimi-shell/src-tauri/Cargo.toml --no-run`；`git diff --check -- apps\kimi-shell\src-tauri\src\bridge_manager.rs apps\kimi-shell\src-tauri\src\lib.rs apps\kimi-shell\src-tauri\src\skill_center.rs skills\bridge-ops .ai\changes\2026-06-29.md`。
- 是否更新 README：否，未改变目录职责或公共使用说明；历史 release notes 保留为历史事实。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如需要清理已安装用户环境中的旧 `bridge-ops`，应提供显式卸载/确认流程，不能在启动时静默删除。
