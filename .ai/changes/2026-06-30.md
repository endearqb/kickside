## 2026-06-30-10 - 隐藏 Shell 诊断与窗格浮层标题

- 用户目标：移除应用壳右下角 Theme/Port/PID 显示，并让 iframe pane header 默认隐藏、hover 后透明浮现。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：删除 statusbar 右侧诊断渲染；将 workspace grid pane header 改为透明绝对定位浮层，并通过 hover/focus-within 显示。
- 修改原因：减少壳层调试信息干扰，让 iframe 内容区域获得完整高度。
- 本次已消除的不确定性：确认目标 header 是 `.workspace-grid-pane-header`，底部诊断来自 `App.tsx`。
- 本次采用的关键假设：hover 触发范围为整个 pane；键盘聚焦时也应显示 header。
- 剩余不确定性：未在真实 Tauri 窗口中做截图验收。
- 影响范围：仅影响 workspace 壳层展示，不改变后端、API、持久化数据或 pane 业务逻辑。
- 验证方式：运行 workspace grid 定向测试与 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若透明 header 在部分 iframe 内容上对比不足，再按设计系统补充非遮挡式控件可读性处理。

## 2026-06-30-10 - 清零 Workspace Grid 外圈间距

- 用户目标：将 iframe pane 与应用壳之间的间距改成 0，同时保留 iframe pane 之间的间距，并回测 pane header hover 显示行为。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将 workspace grid shell 外层 padding 改为 0，保留 grid canvas gap。
- 修改原因：让 pane 贴合应用壳边界，减少无效留白。
- 本次已消除的不确定性：确认外圈间距来自 `.workspace-stage-grid-shell`，pane 间距来自 `.workspace-grid-canvas`。
- 本次采用的关键假设：pane 之间的现有 4px gap 继续作为保留间距。
- 剩余不确定性：未在真实 Tauri WebView 中截图验收。
- 影响范围：仅影响 workspace grid 外层布局，不改变 pane 内容、拖拽逻辑或持久化状态。
- 验证方式：运行 CSS 回归断言、workspace grid 定向测试与 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如后续需要更强边界感，应优先调整 pane border，而不是恢复外圈 padding。

## 2026-06-30-10 - 简化控制中心内部容器阴影

- 用户目标：去掉控制中心内部组件容器阴影，让 UI 更简洁，并判断是否需要更新设计系统。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将控制中心内部软阴影变量设为 none，并覆盖配置、安装、引导与 Bridge 内部 panel/card 的阴影。
- 修改原因：减少内部容器的浮层感，用边框与留白维持层级。
- 本次已消除的不确定性：确认 DESIGN.md 已支持克制、减少装饰的方向，无需更新。
- 本次采用的关键假设：外层弹窗与菜单 popover 的阴影仍用于表达浮层层级，不属于本次要去掉的内部容器阴影。
- 剩余不确定性：未在真实 Tauri WebView 中截图验收。
- 影响范围：仅影响控制中心视觉样式，不改变交互、数据流或组件结构。
- 验证方式：运行 CSS 回归断言、控制中心相关测试与 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如内部层级不够清晰，优先微调边框色或 section 间距，不恢复阴影。

## 2026-06-30-10 - 恢复 iframe pane header 常驻显示

- 用户目标：将 iframe pane header 改回常驻显示，不再自动隐藏。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：移除 workspace grid pane header 的 hover/focus 显示规则，使 header 始终可见可操作，并保留透明背景。
- 修改原因：用户更需要持续可见的 pane 操作入口。
- 本次已消除的不确定性：确认目标样式是 `.workspace-grid-pane-header` 的 opacity/pointer-events 规则。
- 本次采用的关键假设：仅取消自动隐藏，不恢复 header 占用布局高度。
- 剩余不确定性：未在真实 Tauri WebView 中截图验收。
- 影响范围：仅影响 workspace grid pane header 展示方式，不改变拖拽、按钮逻辑或 iframe 内容。
- 验证方式：运行 CSS 回归断言、workspace grid 定向测试与 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如 header 遮挡内容，再单独评估是否恢复占位式 header。

## 2026-06-30-10 - 恢复 iframe pane header 占位布局

- 用户目标：让常驻 iframe pane header 不遮挡页面内容。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将 workspace grid pane 恢复为 32px header + 内容区两行布局，并移除 header 绝对定位。
- 修改原因：常驻 header 作为浮层会覆盖 iframe 顶部内容，占位布局能从根上避免遮挡。
- 本次已消除的不确定性：确认遮挡来自 `.workspace-grid-pane-header` 的 absolute overlay。
- 本次采用的关键假设：不遮挡页面优先于 iframe 占满 pane 完整高度。
- 剩余不确定性：未在真实 Tauri WebView 中截图验收。
- 影响范围：仅影响 workspace grid pane 布局，不改变拖拽、按钮逻辑或 iframe 内容。
- 验证方式：运行 CSS 回归断言、workspace grid 定向测试与 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如后续需要再压缩空间，可评估降低 header 高度而不是恢复浮层。

## 2026-06-30-11 - 整理 Shell 顶栏与窗格按钮

- 用户目标：压缩窗口顶栏、去掉底栏、移动控制中心入口，并简化 pane header 的 Code/Chat 与挂起按钮。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将工作区顶栏收紧到 26px；移除底栏渲染和样式；把控制中心入口放到顶栏右侧；新增顶栏浏览器打开 Code/Chat 横向菜单；将 pane header 的 Code/Chat 双按钮合并为单按钮；移除 pane header 挂起入口并保留内容区恢复按钮。
- 修改原因：减少重复 chrome 和底部占位，让 pane header 操作更集中。
- 本次已消除的不确定性：确认浏览器打开可复用 `handleOpenExternalUrl`，pane 切换可复用现有 `configurePane`。
- 本次采用的关键假设：去掉技能按钮仅移除顶栏入口，不删除 Skill Center 功能；已挂起 pane 仍需可恢复。
- 剩余不确定性：未在真实 Tauri 窗口中做截图验收。
- 影响范围：仅影响 Shell 顶栏、底栏和 Workspace Grid pane header UI，不改变后端、API 或持久化 schema。
- 验证方式：运行 TypeScript 检查与前端测试。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如需要恢复底栏状态信息，应以新的顶栏/控制中心入口重新设计，不直接还原旧底栏。

## 2026-06-30-14 - 透明化 Workspace Grid 外框与 Header 底线

- 用户目标：让 Workspace Grid 容器外框和应用 header 底部边框透明，不影响 iframe pane 自身边框。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将 `.titlebar` 底部边框改为透明并移除 inset 高光线；为 `.workspace-stage-grid-shell` 增加透明 1px 外框。
- 修改原因：去掉应用壳与内容区之间的可见边线，同时保留布局盒模型和 pane 边界。
- 本次已消除的不确定性：确认目标不是 `.workspace-grid-pane`，而是 `.workspace-stage-grid-shell` 和 `.titlebar`。
- 本次采用的关键假设：透明外框应保留 1px 占位，避免布局尺寸变化。
- 剩余不确定性：未在真实 Tauri 窗口中截图验收。
- 影响范围：仅影响 Shell chrome 与 Workspace Grid 容器视觉边框，不改变交互、数据流或 pane 样式。
- 验证方式：运行 TypeScript 检查与前端测试。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若还看到边线，应优先检查系统窗口阴影或 WebView 背景，而不是 pane 边框。

## 2026-06-30-14 - 按语言收口应用显示名

- 用户目标：先做最简单版本：安装语言决定安装产物名称，应用内名称跟随当前 UI 语言。
- 涉及目录：`apps/kimi-shell`
- 修改内容：将中文/英文打包配置的 `productName` 分别设为 `kimi小助手` / `kimi sidekick`；前端品牌显示名改为按当前浏览器语言选择；补充语言定制安装包命令与产物名说明。
- 修改原因：复用现有 Tauri 分语言打包配置，避免为单个 NSIS 多语言安装器维护自定义模板。
- 本次已消除的不确定性：确认 Tauri NSIS 快捷方式默认使用编译期 `${PRODUCTNAME}`，单纯打开语言选择器不能让快捷方式名随安装器语言动态变化。
- 本次采用的关键假设：当前 UI 语言以浏览器/系统语言为准；中文环境使用 `kimi小助手`，其他环境使用 `kimi sidekick`。
- 剩余不确定性：未实际构建 NSIS/MSI 安装包验证文件名和 Windows 快捷方式。
- 影响范围：影响前端显示名、HTML 兜底标题、Tauri productName 与 README 打包说明；不改变二进制名、安装目录迁移逻辑或完整 UI i18n。
- 验证方式：运行 TypeScript 检查。
- 是否更新 README：是，更新 `apps/kimi-shell/README.md` 的语言定制安装包说明。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果必须做单个安装器内选择语言并动态改快捷方式名，需要维护 NSIS 自定义模板或更深的安装脚本覆盖。

## 2026-06-30-15 - 同步 Kimi Web 品牌标签

- 用户目标：将截图中 Kimi Web 内部标记的 `Kimi Code` 也改成 `kimi小助手` / `kimi sidekick`。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri/src/backend_manager`
- 修改内容：新增前端品牌名 helper；Workspace Grid code pane 默认标题、旧持久化标题归一化、iframe title 与 session bridge 标题均改为当前语言应用名；运行时 HTML 注入脚本会把完整的 `Kimi Code` / `Kimi Code Web` 节点替换为当前语言应用名。
- 修改原因：截图里的品牌来自嵌入式 Kimi Web 页面，外层 pane 标题和内层 iframe 文本需要同时收口。
- 本次已消除的不确定性：确认 upstream 快照不应直接修改；实际运行路径可通过 workspace proxy 注入脚本处理。
- 本次采用的关键假设：只替换完整品牌节点，不替换安装、诊断、Server、Doctor 等技术语境里的 `Kimi Code`。
- 剩余不确定性：未在真实 Tauri WebView 截图验证 iframe 内品牌节点是否被官方前端异步重绘后持续替换。
- 影响范围：影响 workspace pane 标题、旧布局标题加载、iframe title 和 workspace proxy 注入脚本；不改变官方 Kimi Code 安装/升级/诊断语义。
- 验证方式：`tsc --noEmit`；workspace-grid 定向 Vitest；`cargo test theme_bridge_script --lib --no-run`。完整 Rust 测试运行仍受既有 Windows `STATUS_ENTRYPOINT_NOT_FOUND` 影响。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若官方 Web 将品牌渲染进 shadow DOM 或 canvas，需要另行针对真实 DOM 结构处理。

## 2026-06-30-15 - 统一 Workspace Pane 与窗口圆角

- 用户目标：判断内部 iframe pane 圆角是否与应用窗口不一致导致视觉缝隙，并修复。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：新增 shell 窗口圆角变量，将 `.shell-root`、workspace stage 底部裁剪和 `.workspace-grid-pane` 圆角统一为 10px。
- 修改原因：底部边缘 pane 原先使用 6px 圆角，和窗口外角不一致，会在窗口底角附近露出背景/间隙感。
- 本次已消除的不确定性：确认 iframe 本身没有圆角，实际裁剪来自 pane 容器；只改 iframe 不足以解决外角对齐。
- 本次采用的关键假设：10px 符合 DESIGN.md 的 `md` 圆角，并接近当前 Windows/Tauri 窗口外角观感。
- 剩余不确定性：未在真实 Tauri 窗口重新截图做像素级确认。
- 影响范围：影响 shell 根容器、Workspace Grid stage 和 pane 外框圆角；不改变 pane 尺寸、grid gap、拖拽或 iframe URL。
- 验证方式：运行 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果仍有缝隙，下一步应检查 Windows WebView 背景透明度或 `.workspace-stage-grid-shell` 的透明 1px 边框。

## 2026-06-30-15 - 简化 Icon Button 视觉

- 用户目标：去掉 icon 按钮的 border 和背景，只保留 hover 时 icon 加粗，并修复按钮内 icon 不居中。
- 涉及目录：`apps/kimi-shell/src`
- 修改内容：将非窗口控制类 `.icon-btn` 和 `.workspace-grid-icon-btn` 的默认/hover 背景与边框清空；hover 时仅提升 SVG `stroke-width`；让 `.ui-btn-icon` 填满按钮盒子并居中。
- 修改原因：减少工具按钮的边框噪声，并修复图中按钮图标在旧边框盒内的视觉偏移。
- 本次已消除的不确定性：确认红框按钮分别来自通用 `.icon-btn` 和 Workspace Grid `.workspace-grid-icon-btn`。
- 本次采用的关键假设：窗口关闭/最小化等系统窗口控制按钮不属于本次去框范围。
- 剩余不确定性：未在真实 Tauri WebView 截图复验 hover stroke 粗细。
- 影响范围：影响 shell 顶栏、pane header 等 icon-only 工具按钮视觉；不改变点击区域、按钮尺寸或交互逻辑。
- 验证方式：运行 TypeScript 检查。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如果 hover 加粗过重，可将 `stroke-width` 从 2.8 降到 2.5。

## 2026-06-30-15 - 补充 0.1.3 发布概要

- 用户目标：写 0.1.3 update note，概括 0.0.43 到 0.1.3 的主要更新，并完成 git 发布流程。
- 涉及目录：`apps/kimi-shell/docs`、`.ai/changes`
- 修改内容：新增 `release-notes-0.1.3.md`，用概要方式记录品牌收口、Workspace Grid、Kimi Code access、Bridge 收口、UI 简化和版本同步。
- 修改原因：为 0.1.3 发布提供可引用的简短发布说明。
- 本次已消除的不确定性：确认上一发布标签为 `v0.0.43`，当前版本元数据为 `0.1.3`。
- 本次采用的关键假设：用户要求“不需要写细节”，因此不写逐项实现清单和完整验证矩阵。
- 剩余不确定性：安装包产物是否已在本机重新构建，需要发布环境确认。
- 影响范围：仅新增发布说明和变更记录，不改变运行时代码。
- 验证方式：执行发布说明文本检查、git diff 检查和后续最小项目验证。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：如需要 GitHub Release 正文，可直接复用该发布说明。
