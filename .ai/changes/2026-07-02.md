## 2026-07-02-10 - 控制中心三栏基线切片

- 用户目标：以已更新的 `DESIGN.md` 和 `docs/controlcenter_rebuild/` 三份 UI/UX 文档为目标重构控制中心。
- 涉及目录：`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/`。
- 修改内容：将控制中心顶层导航从顶部 tabs 改为左侧 Primary Nav；为 section 增加描述与分组；修复 WorkspaceHub 和调度从导航进入时被错误路由到诊断 core 的问题；追加控制中心灰白三栏视觉基线 CSS，覆盖阴影、badge、workbench rail、switch、card 和响应式布局。
- 修改原因：先落地文档要求的桌面设置面板骨架，保留现有业务逻辑，避免一次性重写 7 个复杂视图。
- 本次已消除的不确定性：确认根 `.ai/CONSTITUTION.md` 缺失；确认可用架构入口为 `.ai/architecture/README.md`、`current-state.md`、`verification-gates.md`；确认控制中心当前入口为 `ControlCenterView.tsx`，共享样式入口为 `App.css` 与 `components/control-center/control-center.css`。
- 本次采用的关键假设：本轮先做最小可用切片，后续再逐页迁移 WorkspaceHub、Schedule、Skill Center、Bridge、Diagnostics 和 Quick Setup 的内部 object rail/detail 结构。
- 剩余不确定性：尚未做浏览器视觉截图对照；各业务页内部仍存在旧卡片结构和部分旧文案，需要后续分阶段迁移。
- 影响范围：控制中心整体导航和视觉基线；未修改 Rust 后端、Tauri command、WorkspaceHub/Schedule API、Skill/Bridge 业务操作。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过；尝试用 Vite + 内置浏览器做渲染验证时，普通浏览器缺少 Tauri `invoke` runtime，页面停在既有 loading error，未完成视觉验收。
- 是否更新 README：否，本次未改变目录职责或长期接口。
- 是否更新 `.ai/architecture`：否，本次未改变长期架构事实；仅记录发现的缺失。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：WorkspaceHub 与 Schedule 已在同日后续记录中继续迁移；后续重点转向 Skill Center、Bridge、Diagnostics 与 Quick Setup。

## 2026-07-02-10 - WorkspaceHub 与调度对象详情迁移

- 用户目标：继续以 `DESIGN.md` 和控制中心 UI/UX 文档为目标，推进 WorkspaceHub 与 Schedule 不再是孤立卡片网格。
- 涉及目录：`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/`。
- 修改内容：将 `WorkspaceHubPanel` 改为 `Harness templates / Registered workspaces` object rail + detail panel；将 `WorkspaceSchedulePanel` 改为 workspace object rail + detail panel；新增 dry run 文件树、workspace 元信息、心跳、任务创建、任务列表、Run history 的统一详情结构；补充 code preview、textarea 和折叠 run record 样式。
- 修改原因：满足需求文档中 WorkspaceHub / Schedule 的验收标准，让两页沿用控制中心三栏骨架。
- 本次已消除的不确定性：确认 WorkspaceHub 与 Schedule 可直接复用 `controlCenterRebuildApi.ts` 数据源，不需要改 Rust 后端。
- 本次采用的关键假设：保留现有 Tauri command 与业务状态流，只迁移 UI 结构；不在本轮新增远端 harness 市场或调度权限语义。
- 剩余不确定性：普通 Vite 浏览器仍缺少 Tauri `invoke` runtime，未完成真实渲染截图验收；Skill Center、Bridge、Diagnostics、Quick Setup 内部仍需继续迁移。
- 影响范围：WorkspaceHub 与 Schedule 控制中心内嵌视图；未修改后端 API。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过。
- 是否更新 README：否，本次未改变目录职责或长期接口。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：下一步应迁移 Skill Center，使中栏呈现 skill/source/target 层级，右侧呈现 `SKILL.md` 元数据和投影状态。

## 2026-07-02-11 - Skill Center 来源分组与详情迁移

- 用户目标：继续以 `DESIGN.md` 和控制中心 UI/UX 文档为目标，让 Skill Center 成为接近参考图的 skill/file-browser 体验。
- 涉及目录：`apps/kimi-shell/src/features/skill-center/`、`apps/kimi-shell/src/`。
- 修改内容：将 `SkillCenterPanel` 的管理 rail 从平铺列表改为 Built-in skills、Git imports、Local imports、Discovered imports、Discoverable locations、Workspace targets 分组；保留搜索、范围切换、扫描、导入、信任、投影、固定、更新、卸载和工作区导入操作；在右侧详情显式展示 tags、triggers、filePatterns、recommendedScopes、update status、投影目录和 relative paths；补充 Skill Center 在控制中心三栏基线下的浅灰选中态、manifest card、token 和窄屏样式。
- 修改原因：满足需求文档中 Skill Center 的来源分组、metadata 详情和投影状态可见性要求，同时避免重写现有复杂业务状态流。
- 本次已消除的不确定性：确认 `SkillDetail` 只提供 `relativePaths` 而没有 `SKILL.md` 正文内容；确认 `InstalledSkill.metadata` 已包含 `triggers`、`tags`、`filePatterns`、`recommendedScopes`，可直接用于右侧详情。
- 本次采用的关键假设：本轮不新增读取 `SKILL.md` 正文的后端能力；文件预览以代码样式 relative path/tree 卡片呈现，避免虚构 markdown 内容。
- 剩余不确定性：普通 Vite 浏览器仍缺少 Tauri `invoke` runtime，未完成真实渲染截图验收；Bridge、Diagnostics、Quick Setup 内部仍需继续迁移为 object rail/detail。
- 影响范围：Skill Center 控制中心内嵌视图与控制中心局部样式；未修改后端 command、安装/导入/投影数据契约或全局主题。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过。
- 是否更新 README：否，本次未改变目录职责、公共接口或运行方式。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：下一步建议迁移 Bridge / Diagnostics / Quick Setup 的内部列表和详情页，并在可用 Tauri runtime 下做截图对照。

## 2026-07-02-11 - Diagnostics 对象详情迁移

- 用户目标：继续以 `DESIGN.md` 和控制中心 UI/UX 文档为目标，让诊断页符合 Primary Nav / Object Rail / Detail Panel 的设置面板体验。
- 涉及目录：`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/`。
- 修改内容：将 `renderRuntimeSection()` 从摘要卡片 + accordion 深潜改为 Diagnostics object rail + detail；rail 包含启动链路 / Doctor、路径与菜单、日志尾部三类对象，并保留风险摘要；右侧详情保留刷新诊断、运行 Kimi Doctor、刷新右键菜单、启用/禁用右键菜单、打开日志目录、关闭窗口行为等原操作；startup trace 改为编号列表；日志与 Doctor 输出改为代码块并提供复制按钮；移除旧 runtime accordion 状态与未使用的 `RuntimePanel` 组件。
- 修改原因：满足需求文档中 Diagnostics 的启动链路、Doctor、LogTail 展示要求，并让诊断页不再是旧式散落卡片和折叠面板。
- 本次已消除的不确定性：确认现有 `RuntimePanelId` 可复用 `core`、`paths`、`logs` 三个稳定对象，不需要新增数据状态或后端 command。
- 本次采用的关键假设：Doctor 和 WebView 信息暂归入核心启动链路详情；本轮不拆新 adapter 文件，先在现有 `ControlCenterView.tsx` 内保守迁移 UI。
- 剩余不确定性：普通 Vite 浏览器仍缺少 Tauri `invoke` runtime，未完成真实渲染截图验收；Quick Setup 与 Workbench 仍有内部卡片式结构可继续收敛。
- 影响范围：Diagnostics 控制中心内嵌视图与控制中心局部样式；未修改诊断数据契约、Rust 后端或日志读取路径。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过。
- 是否更新 README：否，本次未改变目录职责、公共接口或运行方式。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：下一步建议迁移 Quick Setup，让 onboarding step 成为对象 rail，右侧保留安装源、镜像健康、PowerShell 预检和 Kimi Code access 配置。

## 2026-07-02-11 - Quick Setup step rail 迁移

- 用户目标：继续以 `DESIGN.md` 和控制中心 UI/UX 文档为目标，让快速设置页符合统一三栏对象详情结构。
- 涉及目录：`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/`。
- 修改内容：将 `renderOnboardingSection()` 从 editorial/card shell 改为 `ControlCenterWorkbenchLayout`；左侧 rail 显示安装 / 右键菜单 / Provider API / 默认工作目录 step、状态和完成进度；右侧 detail header 显示当前 step、状态与主操作；保留现有安装流、安装源、镜像健康、PowerShell preflight、Kimi Code access、右键菜单和工作目录内容；补充 Quick Setup 在 image-style baseline 下的浅灰 step row、progress、detail header 和响应式样式。
- 修改原因：满足需求文档中 Quick Setup 作为对象 rail 的要求，同时不改安装和配置业务逻辑。
- 本次已消除的不确定性：确认 Quick Setup 的 step 状态已经由现有 `onboardingSteps` 数组集中派生，外壳迁移不需要新增 adapter 或后端接口。
- 本次采用的关键假设：本轮保留四个现有 onboarding step，不额外拆出独立 API 配置 step；Provider API 仍作为可选配置 detail 操作出现。
- 剩余不确定性：普通 Vite 浏览器仍缺少 Tauri `invoke` runtime，未完成真实渲染截图验收；Workbench overview 还可以进一步收敛为对象 rail/detail，但当前顶层三栏已覆盖主要功能区。
- 影响范围：Quick Setup 控制中心内嵌视图与控制中心局部样式；未修改安装、镜像、PowerShell、API 配置或工作目录数据契约。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过。
- 是否更新 README：否，本次未改变目录职责、公共接口或运行方式。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：下一步可做 Workbench overview 细化和 Bridge 视觉清理，并在可用 Tauri runtime 下做全流程视觉验收。

## 2026-07-02-11 - Workbench overview 收敛

- 用户目标：继续以 `DESIGN.md` 和控制中心 UI/UX 文档为目标，让工作台首屏从 hero/task-card 风格收敛为安静的状态工作台。
- 涉及目录：`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/`。
- 修改内容：将 `renderOverviewSection()` 的 hero 与任务卡片改为工作台状态卡；顶部展示 4 个主要指标：Backend state、Workspace、Auth、Enhanced Web / Hotkey；关注事项限制为最多 3 条；快捷操作改为紧凑 object-list 样式，并补充“打开当前工作区”动作；删除旧 hero 专用派生值；补充 overview meta-grid、focus、action list 和响应式样式。
- 修改原因：满足需求文档中 Overview 的 meta-grid、最多 1–3 条异常和真实状态值要求，避免控制中心首屏继续像营销 hero 或大仪表盘。
- 本次已消除的不确定性：确认 `AppStatus` 已提供 active/workspace port、active session、workspace path、auth、provider health、enhanced web 和 hotkey 字段；确认 `onOpenFolder` 可用于打开当前工作区。
- 本次采用的关键假设：当前工作区路径优先使用 `status.activeSessionWorkDir`，为空时回退到控制中心有效工作目录；本轮不新增 workspace URL 打开动作。
- 剩余不确定性：普通 Vite 浏览器仍缺少 Tauri `invoke` runtime，未完成真实渲染截图验收；Bridge 已具备 connector rail/detail，但后续仍可继续做视觉细节清理。
- 影响范围：Overview 控制中心工作台视图与控制中心局部样式；未修改状态数据来源或后端接口。
- 验证方式：`apps/kimi-shell` 下运行 `.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`.\node_modules\.bin\vite.cmd build`，均通过。
- 是否更新 README：否，本次未改变目录职责、公共接口或运行方式。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：建议在真实 Tauri runtime 中做控制中心全页截图验收，并根据截图继续微调 Bridge 详情页内部密度。
