## 2026-07-03-10 - Kimi App Review 高危修复

- 用户目标：
  - 按 code review 修复计划完成 P0/P1/P2 与 DX-15/16/18；DX-17 大文件拆分暂缓。
- 涉及目录：
  - `apps/kimi-im-bridge/`
  - `apps/kimi-shell/`
  - `.github/workflows/`
  - `docs/`
  - `.gitignore`
- 修改内容：
  - Feishu 长连接事件先 ACK 后异步派发，CardAction 保持同步返回；增加 32 个 chat-key 分片锁、recent-event 内存过滤、分片 TTL 和非法 seq warn。
  - 新增 inbound dedupe 迁移 `0013`，`CreateTurn` 将重复入站唯一冲突映射为 `ErrDuplicateInbound`，bridgecore/service 对重复投递不再重复运行和回复。
  - 将新装飞书自动审批默认值改为关闭；旧配置显式开启保留；控制中心飞书配置新增自动审批开关和开启态安全警告。
  - 修复 admin token 常量时间比较、Feishu sender 空操作分支、optional bridge-ops 脚本测试缺席时 skip。
  - SessionPool 增加关闭状态、两阶段关闭和 idle sweep；端口选择改为可失败结果并校验子进程存活；调度器改为本地时间计算 cron、schedule_error 退避、任务 id UUID 形态、tick 并发 spawn。
  - 增强 Web 限制 upstream 为 http/https，并校验 `event.source` 与 upstream origin；同步 i18n 文案。
  - Bridge/Skill JSON 写入改为 temp 文件加 Windows `MoveFileExW(MOVEFILE_REPLACE_EXISTING)`，非 Windows 使用 rename。
  - token fragment 改为最小 percent encoder；pnpm 固定为 `10.34.4`；补 workspace packages；新增 CI workflow；更新手工 runbook。
  - `.gitignore` 增加本地生成物规则，并从索引移除已入库的 `.tmp/`、根目录 tgz、docx pycache。
  - 为新增 clippy gate 清理既有 Rust lint，复杂签名和大枚举仅局部 allow，避免重构扩大范围。
- 修改原因：
  - 消除 Feishu 审批死锁、重复执行、自动审批安全默认值、端口冲突、调度异常处理、JSON 写入不完整和前端消息 relay 风险。
- 本次已消除的不确定性：
  - 确认 Feishu 重复投递可在 DB 层和 service 内存层双重拦截。
  - 确认新 CI 中 Rust clippy `-D warnings` 可在当前代码上通过。
  - 确认增强 Web 新文案通过 i18n key 一致性检查。
- 本次采用的关键假设：
  - 旧配置中显式开启的 `feishu_auto_approve` 应保留，只改变新装和缺省值。
  - `bridge-ops` skill 脚本是可选本地能力；仓库缺少该脚本时测试应 skip，不应阻塞 CI。
  - DX-17 大文件拆分不纳入本轮。
- 剩余不确定性：
  - 本机没有真实 Feishu/Telegram 凭证，手工 IM 回归仍按 runbook 标记 blocked。
  - 本机缺 `gcc`，无法执行 Go race；普通 Go test/vet 已通过，race 需在带 CGO 工具链的环境或 CI 上补跑。
  - `.ai/CONSTITUTION.md` 仍是既有缺口，本轮按计划未补建。
- 影响范围：
  - SQLite 迁移版本提升到 `0013`，新增唯一索引不改既有字段。
  - `bridgecore.HandleResult` 新增内部字段 `Duplicate`，Go 内部 `domain.ErrDuplicateInbound` 新增。
  - Tauri schedule outcome 新增序列化值 `schedule_error`，前端显示为“调度异常”。
  - CI 新增 Go/Rust/TS gate；Rust clippy 从此会约束更多既有代码。
- 验证方式：
  - `go test ./internal/adapters/feishu ./internal/bridgecore ./internal/store ./internal/providers/kimi ./internal/admin`
  - `go test ./...`
  - `go vet ./...`
  - `go test -race ./...`：阻塞，本机 `CGO_ENABLED=0`；开启 CGO 后缺 `gcc`。
  - `cargo fmt -- --check`
  - `cargo check`
  - `cargo clippy -- -D warnings`
  - `cargo test --no-run`
  - `.\node_modules\.bin\tsc.cmd --noEmit`
  - `pnpm test`
  - `node scripts\check_enhanced_web_i18n.mjs`
  - `node scripts\check_enhanced_web_compliance.mjs`
  - `git diff --check`
- 是否更新 README：
  - 是。根 README 与 `apps/kimi-shell/README.md` 更新 pnpm 版本要求。
- 是否更新 `.ai/architecture`：
  - 否。没有改变长期架构事实入口；缺失 `.ai/CONSTITUTION.md` 仅在本记录标注。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。没有新增跨项目长期 ADR。
- 后续注意事项：
  - 在带真实 Feishu 凭证的环境执行新增“turn 阻塞期间 Feishu CardAction 可批准”手工用例。
  - 在带 CGO/gcc 的 Windows 或 CI 环境补跑 `go test -race ./...`。

## 2026-07-03-11 - PR-ready 收口

- 用户目标：
  - 将已实现的 review 修复整理到可提交/可发 PR 状态，补漏复核、验证 CI 稳定性和提交前 hygiene。
- 涉及目录：
  - `.github/workflows/`
  - `apps/kimi-im-bridge/`
  - `apps/kimi-shell/`
  - `.ai/changes/`
- 修改内容：
  - 将 CI 中 Go race 从 Windows job 拆到 `ubuntu-latest` job，Windows job 保留 `go vet` 和普通 `go test`。
  - 修正 sidecar config 层 `feishuAutoApprove` 缺省为 `false`，并补显式 `true` 保留测试。
  - 补 Feishu same-key dispatch 串行单测。
  - 将 enhanced-web 的 http/https upstream 和 relay source/origin 校验纳入 compliance 脚本。
- 修改原因：
  - Windows Go race 依赖本机 C 编译器，放到 Linux CI 更稳定；sidecar config 缺省值需要与 shell 默认值一致。
- 本次已消除的不确定性：
  - 确认 `pnpm install --frozen-lockfile` 不再触发 workspace packages 缺失错误。
  - 确认 sidecar config 默认关闭自动审批，显式开启仍保留。
- 本次采用的关键假设：
  - 不在本机安装 gcc；Go race 由 CI Linux job 补位。
  - 本轮仍不做 DX-17 大文件拆分。
- 剩余不确定性：
  - 本机没有真实 Feishu/Telegram 凭证，真实 IM 手工验证仍 blocked。
  - 本机 `pnpm install` 实际使用 pnpm `11.7.0`，CI 会按 workflow 使用 `10.34.4`。
- 影响范围：
  - CI job 拆分；Bridge 自动审批默认安全性收口；enhanced-web compliance gate 更严格。
- 验证方式：
  - `go test ./internal/config ./internal/adapters/feishu ./internal/bridgecore ./internal/store`
  - `go test ./...`
  - `go vet ./...`
  - `CGO_ENABLED=1 go test -race ./...`：阻塞，本机缺 `gcc`。
  - `cargo fmt -- --check`
  - `cargo check`
  - `cargo clippy -- -D warnings`
  - `cargo test --no-run`
  - `pnpm install --frozen-lockfile`
  - `.\node_modules\.bin\tsc.cmd --noEmit`
  - `pnpm test`
  - `node scripts\check_enhanced_web_i18n.mjs`
  - `node scripts\check_enhanced_web_compliance.mjs`
- 是否更新 README：
  - 否。本次只收口 CI、测试和验证记录。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 等 CI 运行后确认 Linux race job 通过。
  - 有真实 IM 凭证后再执行 runbook 手工用例。

## 2026-07-03-12 - DX-17 大文件拆分

- 用户目标：
  - 将 DX-17 作为 review 修复后的独立收口，降低 `useShellController.ts`、`ControlCenterView.tsx` 和 `install_manager.rs` 的维护风险。
- 涉及目录：
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src-tauri/src/install_manager.rs`
  - `apps/kimi-shell/src-tauri/src/install_manager/`
  - `apps/kimi-shell/README.md`
  - `.ai/changes/`
- 修改内容：
  - 抽出 `src/app/shellControllerDefaults.ts`，承载 shell 默认值、Kimi Code access 转换和 Bridge onboarding 纯 helper。
  - 抽出 `src/app/useInstallController.ts`，承载安装流状态、安装设置、PowerShell preflight、install session channel 和安装任务 handler；`useShellController()` 对外返回 shape 保持不变。
  - 抽出 `src/features/control-center/controlCenterViewModel.tsx`，承载 `ControlCenterView` props 类型、控制中心导航项和纯展示 helper；`ControlCenterView` 对外 export 与 props contract 保持不变。
  - 抽出 `src-tauri/src/install_manager/catalog.rs`，承载安装 catalog、task 和 step 构造；`install_manager.rs` 继续保留 Tauri 调用入口和运行状态管理。
  - `apps/kimi-shell/README.md` 增加一小段代码组织说明，记录新的长期模块边界。
- 修改原因：
  - 将最稳定、最少依赖 UI/运行时状态的代码从大文件中分离，减少后续维护时的认知负担，同时避免引入行为重写。
- 本次已消除的不确定性：
  - 确认 `useShellController()` 仍是唯一 shell 对外入口，`App.tsx` 与控制中心 props 不需要新增必填字段。
  - 确认 Tauri install command 入口仍通过 `install_manager.rs` 暴露，安装 catalog 拆分不改变 command 名称。
- 本次采用的关键假设：
  - DX-17 本轮只拆自然边界；Bridge controller 和 Control Center 大段 JSX section 若需要更细拆分，等当前拆分验证稳定后再单独推进。
  - 不新增依赖，不引入状态库，不改变 UI 文案、CSS class、序列化类型或 Tauri command 名称。
- 剩余不确定性：
  - `.ai/CONSTITUTION.md` 仍是既有缺口，本轮按要求未补建。
- 影响范围：
  - 前端模块边界变化，不改变产品行为。
  - Rust install manager 内部模块边界变化，不改变 Tauri 命令契约。
- 验证方式：
  - `.\node_modules\.bin\tsc.cmd --noEmit`
  - `pnpm test`
  - `cargo fmt -- --check`
  - `cargo check`
  - `cargo clippy -- -D warnings`
  - `cargo test --no-run`
  - `rg -n "get_install_flow_catalog|start_install_task|cancel_install_task|install_kimi_dependencies|install_kimi_code|upgrade_kimi_code|install_nodejs" apps/kimi-shell/src-tauri/src/lib.rs apps/kimi-shell/src-tauri/src/install_manager.rs apps/kimi-shell/src-tauri/src/install_manager/catalog.rs`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 是。`apps/kimi-shell/README.md` 增加代码组织说明。
- 是否更新 `.ai/architecture`：
  - 否。没有改变跨应用架构事实入口。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。仅为内部模块拆分，不新增长期 ADR。
- 后续注意事项：
  - 如果继续拆 Bridge controller 或 Control Center 大段 JSX section，应单独提交，避免和本轮已验证的自然边界混在一起。

## 2026-07-03-13 - 补建项目宪法

- 用户目标：
  - 为仓库补写 `.ai/CONSTITUTION.md`，让已有 README First / Agent 规则引用的项目级治理宪法落地。
- 涉及目录：
  - `.ai/CONSTITUTION.md`
  - `.ai/architecture/README.md`
  - `.ai/changes/`
- 修改内容：
  - 新增 `.ai/CONSTITUTION.md`，规定文档权威、事实权威、项目边界、抽象治理、单向门决策、验证完成状态、安全不变量、重构治理和文档记录规则。
  - 更新 `.ai/architecture/README.md`，移除 `.ai/CONSTITUTION.md` 仍缺失的已知缺口。
  - 在 `.ai/changes/2026-07-03.md` 追加本次文档变更记录。
- 修改原因：
  - `AGENTS.md`、`README_First.md` 和 `.ai/architecture/README.md` 已经把 `.ai/CONSTITUTION.md` 声明为最高治理入口，但仓库中该文件缺失。
- 本次已消除的不确定性：
  - 确认 `.ai/CONSTITUTION.md` 在 main 分支不存在。
  - 确认宪法层应覆盖抽象、单向门决策、完成状态、gate 分层、兼容层退出条件和重构触发标准，而不是重复 `AGENTS.md` 的逐步执行规程。
- 本次采用的关键假设：
  - 本轮为文档-only 治理补齐，不改变代码、构建、运行时行为或发布产物。
  - 既有历史变更记录中“`.ai/CONSTITUTION.md` 缺失”的表述保留为历史事实，不回改旧记录。
- 剩余不确定性：
  - `dependency-boundaries.md`、`module-map.md`、`authorization-ontology.md` 仍未补齐。
  - 未运行代码 gate；本轮不涉及代码路径。
- 影响范围：
  - AI 协作与维护者治理规则变为显式文档化。
  - `.ai/architecture/README.md` 的已知缺口列表更新。
- 验证方式：
  - 读取 `AGENTS.md`、`README_First.md`、根 README、`.ai/architecture/README.md`、`.ai/architecture/current-state.md` 和 `.ai/architecture/verification-gates.md`。
  - 确认 `.ai/CONSTITUTION.md` 创建前为 404，创建后可读取。
  - 文档自检：未写入 secret、本机绝对路径、未验证完成状态或与现有文档冲突的执行细则。
- 是否更新 README：
  - 否。根 README 和应用 README 的目录职责没有变化。
- 是否更新 `.ai/architecture`：
  - 是。移除 `.ai/CONSTITUTION.md` 仍缺失的已知缺口。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。本轮是补齐已由现有文档声明的治理入口，不新增具体技术路线 ADR。
- 后续注意事项：
  - 后续补齐 `dependency-boundaries.md`、`module-map.md`、`authorization-ontology.md` 时，应以本宪法的边界、抽象和完成状态规则为上位约束。
## 2026-07-03-14 - 控制中心说明文案精简

- 用户目标：
  - 去掉控制中心里面向内部理解的说明类文案，让设置项面向更多普通用户时更简洁。
- 涉及目录：
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/bridge/`
  - `apps/kimi-shell/src/components/control-center/`
  - `.ai/changes/`
- 修改内容：
  - 删除控制中心页头副标题、顶部说明型 meta strip、Skill/WorkspaceHub 的 Description 和 tag strip。
  - 删除任务面通用 description 渲染，以及安装流、安装命令弹窗和 Bridge 高级面板里的复述性说明。
  - 保留路径、状态值、错误、警告、危险操作提示、空状态一句话、skill/会话/审批/日志等真实数据内容。
- 修改原因：
  - `DESIGN.md` 明确要求页头副标题删除，复述控件或标题的说明删除；当前截图红框内容会增加普通用户阅读负担。
- 本次已消除的不确定性：
  - 确认说明文案集中在控制中心 React 展示层，不需要改 Tauri/Rust/Go 后端或持久化数据。
- 本次采用的关键假设：
  - “说明类文字”不包含 skill manifest description、日志、路径、审批 prompt、错误和危险操作提示。
  - 本轮只做最小删除，不新增 tooltip 体系。
- 剩余不确定性：
  - `.ai/CONSTITUTION.md` 仍是既有缺口，本轮按现有 README 和 `DESIGN.md` 执行。
- 影响范围：
  - 控制中心视觉与文案密度收敛；不改变业务逻辑、状态计算、接口或存储格式。
- 验证方式：
  - `pnpm -C apps/kimi-shell test`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
- 是否更新 README：
  - 否。目录职责、公共接口和运行方式不变。
- 是否更新 `.ai/architecture`：
  - 否。没有改变长期架构事实。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。没有新增长期架构决策。
- 后续注意事项：
  - 如后续发现某条被删文案确实会改变用户操作决策，再用短内联提示或 tooltip 单独补回。

## 2026-07-03-14 - Skill Directory 文件预览薄切片

- 用户目标：
  - 根据 Control Center Directory UI V2 文档与计划推进目标，让 Skill 中心详情页具备文档优先的 Directory 预览能力。
- 涉及目录：
  - `apps/kimi-shell/src-tauri/src/`
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/services/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - 新增 installed / discovered Skill 的安全文件列表与读取 Tauri 命令：`list_skill_file_entries`、`read_skill_file`、`list_discovered_skill_file_entries`、`read_discovered_skill_file`。
  - 后端统一使用相对路径校验、canonicalize 和 root prefix 检查，拒绝绝对路径与 `..`，读取限制为 512KB，NUL 字节文件标记为二进制。
  - 前端新增 `DirectoryFilePreview` 与 `directory.css`，在 Skill 详情和发现项详情中渲染文件树、Description、License 跳转、preview/source、复制当前文件、二进制/截断占位。
  - 新增受限 Markdown renderer，不使用 `dangerouslySetInnerHTML`，raw HTML 作为文本渲染，`javascript:` 链接禁用。
  - 将控制中心导航和详情页标题中的 `Skill 投影` 改为 V2 要求的 `Skill 中心`。
  - 增加 Markdown 安全最小单测和 Rust 安全 helper 单测。
- 修改原因：
  - V2 Spec 的核心缺口是详情页只能列路径，不能安全审阅 `SKILL.md` / README / LICENSE；先补安全读取与只读预览，避免 UI Directory 化变成静态换皮。
- 本次已消除的不确定性：
  - 确认现有 `SkillCenterPanel` 可在不重写控制中心宿主的情况下接入 Directory 预览。
  - 确认不新增 Markdown 依赖也能满足安全只读预览的首个可运行版本。
- 本次采用的关键假设：
  - 本轮按 Ponytail 收敛为 Skill/Discovered 预览薄切片；WorkspaceHub/Harness 完整 Directory 化、usage stats 和 feature flag 继续后续 PR。
  - 发现项文件读取以 discovery cache 中的 canonical path 为根。
- 剩余不确定性：
  - 根目录 `.ai/CONSTITUTION.md` 与 `docs/README.md` 仍缺失，按 README First 已记录为既有上下文缺口。
  - Rust 单测运行阶段仍受本机已知 `STATUS_ENTRYPOINT_NOT_FOUND` 影响。
- 影响范围：
  - 新增只读文件读取接口与 Skill 中心详情页渲染；不改变 Skill 安装、信任、投影、发现、导入或卸载语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `cargo test read_safe_file --lib`：编译通过，运行阶段因本机已知 `STATUS_ENTRYPOINT_NOT_FOUND` 失败。
  - `cargo check`
  - `pnpm -C apps/kimi-shell build`
- 是否更新 README：
  - 否。本轮新增文件预览组件与命令，未改变目录职责或长期运行方式。
- 是否更新 `.ai/architecture`：
  - 否。安全读取是 V2 计划内能力，没有改变长期架构事实入口。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。未新增长期 ADR；Markdown 依赖策略采用“先受限自研 renderer”的实现假设。
- 后续注意事项：
  - 继续 M3/M4 前，补 Harness file list/read、WorkspaceHub Directory 卡片与详情、usage stats、feature flag 或明确删除旧 fallback。

## 2026-07-03-14 - Harness Directory 文件预览薄切片

- 用户目标：
  - 继续按 Control Center Directory UI V2 推进 WorkspaceHub / Harness 详情，让 Harness 模板也具备文档优先的只读预览。
- 涉及目录：
  - `apps/kimi-shell/src-tauri/src/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - 复用 Skill Directory 安全读取 helper，新增 Harness template 文件列表与读取命令：`list_harness_file_entries`、`read_harness_file`。
  - `controlCenterRebuildApi.ts` 增加 Harness 文件预览 service wrapper。
  - `WorkspaceHubPanel` 的 Harness 详情复用 `DirectoryFilePreview`，在变量表单和 dry-run 之前展示 README/AGENTS/Markdown/source 预览。
  - 增加 Harness template preview 的路径穿越与 README 读取单测。
- 修改原因：
  - V2 Spec 要求 Harness 详情包含模板树、README/AGENTS 渲染、变量表单和 dry-run；上轮只完成 Skill/Discovered，WorkspaceHub 仍缺模板文档预览。
- 本次已消除的不确定性：
  - 确认 WorkspaceHub 可复用 `features/directory/DirectoryFilePreview`，不需要新建一套 Harness 专用 viewer。
  - 确认 Harness 读取可以走同一安全边界，不需要重复实现路径校验。
- 本次采用的关键假设：
  - 本轮只读预览 Harness `template/` 根，不暴露 harness 根目录打开动作。
  - WorkspaceHub 卡片化列表、Workspace 详情 Skill 清单、调度摘要、feature flag 继续后续切片。
- 剩余不确定性：
  - Rust 单测运行阶段仍受本机已知 `STATUS_ENTRYPOINT_NOT_FOUND` 影响；新增 Harness tests 已编译但未能运行完成。
  - Acceptance Checklist 未整体勾选，本轮只推进其中 Harness 文件树/README 预览相关能力。
- 影响范围：
  - 新增 Harness 只读文件预览接口；不改变 dry-run、create、workspace register 或打开工作区语义。
- 验证方式：
  - `cargo fmt -- --check`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `cargo check`
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`
  - `cargo test harness_template_preview --lib`：编译通过，运行阶段因本机已知 `STATUS_ENTRYPOINT_NOT_FOUND` 失败。
  - `pnpm -C apps/kimi-shell build`
- 是否更新 README：
  - 否。未改变目录职责、公共运行方式或长期模块边界。
- 是否更新 `.ai/architecture`：
  - 否。仍是 V2 计划内能力增量。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。未新增跨目录长期 ADR。
- 后续注意事项：
  - 下一步优先把 WorkspaceHub 列表改为 V2 的 Directory 卡片/搜索/filter/sort，或补 `read_harness_file` 在可运行 Rust test 环境中的实际通过证据。

## 2026-07-03-14 - WorkspaceHub Directory 卡片列表

- 用户目标：
  - 继续按 Control Center Directory UI V2 推进 WorkspaceHub 列表页，从行列表转为可搜索、可筛选、可排序的 Directory 卡片入口。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - 新增轻量 `DirectoryCardGrid` 组件，支持卡片主体进入详情、角标动作不冒泡、Enter 键进入详情。
  - `WorkspaceHubPanel` detail-only 表面将 “Harness 与工作区” 从 row list 替换为 Directory card grid。
  - 增加 WorkspaceHub 搜索、类型 pill 筛选和排序：全部、Harness、工作区、Kimi Code、最近打开、Harness 来源、手动；名称/最近打开/runtime/来源排序。
  - `directory.css` 增加 Directory toolbar/card/grid 样式、暗色主题和窄屏单列规则。
  - 新增 `DirectoryCardGrid` 交互测试，覆盖 Enter 进入详情和角标动作不冒泡。
- 修改原因：
  - V2 Spec 要求 WorkspaceHub 列表页是搜索、来源 pill、filter/sort、双列卡片网格；此前仍是简单行列表。
- 本次已消除的不确定性：
  - 确认当前 ControlCenter 中 WorkspaceHub 实际以 `detailOnly` 挂载，因此 V2 列表页改造应落在 detail surface 内。
  - 确认 Directory card primitive 可被 WorkspaceHub 复用，不需要先抽完整 `DirectoryCardModel` adapter。
- 本次采用的关键假设：
  - 本轮先用本地状态保存 WorkspaceHub query/filter/sort；跨 section 持久化和完整 view-state 提升后续再做。
  - Workspace 卡片角标先提供“打开工作区”，完整齿轮菜单和显示目录动作后续补齐。
- 剩余不确定性：
  - Workspace 详情的 Skill 清单、容器分组、调度摘要仍未实现。
  - Skill 中心列表页尚未完全卡片化，Acceptance Checklist 仍未整体通过。
- 影响范围：
  - WorkspaceHub 列表交互和视觉形态变化；不改变 Harness 创建、dry-run、Workspace 注册或打开语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `cargo fmt -- --check`
  - `cargo check`
  - `pnpm -C apps/kimi-shell build`
- 是否更新 README：
  - 否。没有改变长期目录职责或运行入口。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 继续补 Workspace 详情 Skill 清单/调度摘要，或将 Skill 中心列表页迁移到同一 `DirectoryCardGrid`。

## 2026-07-03-14 - Workspace 详情 Skill 与调度摘要

- 用户目标：
  - 继续按 Control Center Directory UI V2 推进 WorkspaceHub 详情页，使已注册工作区能看到 Skill 清单、容器分组和只读调度摘要。
- 涉及目录：
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - `WorkspaceHubPanel` 在选中工作区时读取 `listWorkspaceSkillTargets`、`getWorkspaceSkillInventory` 和 `listSchedule`。
  - Workspace 详情新增只读 `Skill 清单` section，按 `WorkspaceSkillInventory.containers` 展示容器路径、读写状态和 Skill 条目。
  - Workspace 详情新增只读 `调度摘要` section，展示心跳、任务数、下次运行、最近结果和最多 4 个任务摘要。
  - 从 `ControlCenterView` 给 WorkspaceHub 传入 `onOpenSchedule`，`编辑调度` 按钮跳转到 schedule section。
  - 新增 `WorkspaceHubPanel.test.ts`，覆盖 Windows 路径归一化 target 匹配和 schedule 汇总 helper。
- 修改原因：
  - V2 Spec FR-2.8 与 Acceptance Checklist 要求 Workspace 详情展示基础信息、关联 Skill 清单、按容器分组的 inventory 和只读调度摘要；此前只显示路径与 tags。
- 本次已消除的不确定性：
  - 确认 WorkspaceHub 不需要读取整个工作区目录，只需复用 SkillCenter 现有 workspace target/inventory command。
  - 确认调度编辑应跳转既有 schedule section，而不是在 WorkspaceHub 中新增第二套编辑表单。
- 本次采用的关键假设：
  - Workspace record 与 Skill target 以归一化 `cwd` / `rootPath` 匹配；target id 仅作为兼容 fallback。
  - 调度摘要只展示当前 workspace id 下的 heartbeat/tasks，不在本轮修改 schedule 存储或任务编辑能力。
- 剩余不确定性：
  - Skill 中心列表页尚未完全卡片化，Workspace / Skill 互跳仍需后续切片补齐。
  - 当前未启动 Tauri dev server 做视觉截图，遵守本仓库不主动运行 foreground dev server 的规则。
- 影响范围：
  - WorkspaceHub 详情页只读展示增强；不改变 Skill 投影、Workspace 创建、打开工作区或 schedule command 语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。仍是 V2 计划内 UI 能力增量。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续继续补 Skill 中心 Directory 卡片化、Workspace/Skill 互跳和 Workspace 齿轮菜单。

## 2026-07-03-15 - Skill 中心 Directory 卡片列表

- 用户目标：
  - 继续按 Control Center Directory UI V2 推进 Skill 中心列表页，补齐二级导航、搜索、来源 pill、filter/sort 与卡片网格入口。
- 涉及目录：
  - `apps/kimi-shell/src/features/skill-center/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/App.css`
  - `.ai/changes/`
- 修改内容：
  - `SkillCenterPanel` 在 detail-only 模式下新增 Directory list surface：二级导航“技能库 / 工作区发现 / 工作区目标”、搜索、来源 pill、Filter、Sort、清空筛选和 `DirectoryCardGrid`。
  - Installed Skill 卡片标题使用 `/{projectionName}`，显示来源、更新时间、描述和最多 2 个状态 badge。
  - 工作区发现上下文中的 pending discovered Skill 卡片显示 `待导入`，右上角 `+` 调用既有 `onImportDiscoveredSkill`。
  - Workspace target 也映射为 Directory card，用于工作区目标分区入口。
  - 将 `onSkillCenterFilterChange` 从 shell controller 透传到 `ControlCenterView` 和 `SkillCenterPanel`。
  - 新增 `SkillCenterPanel.test.ts`，覆盖来源 pill 对 installed/discovered card 的匹配逻辑。
- 修改原因：
  - V2 Plan M1 要求 Skill 列表页具备卡片字段、二级导航、search/source/filter/sort 和卡片交互；此前 Skill 中心仍主要依赖左侧 rail list。
- 本次已消除的不确定性：
  - 确认可复用 `DirectoryCardGrid`，无需为 Skill 单独再造卡片交互。
  - 确认 Skill filter 状态已在 shell controller 中存在，只需补 prop 透传。
- 本次采用的关键假设：
  - 本轮以 detail-only 控制中心页面为 V2 主入口，保留既有 workbench rail/detail 作为详情和管理操作承载。
  - 工作区发现 tab 默认选择当前工作区 target；没有 workspace target 时禁用该 tab。
- 剩余不确定性：
  - Installed Skill 卡片右上齿轮菜单、完整 Bulk 操作和全部 filter 扩展仍需后续切片。
  - Workspace/Skill 互跳尚未完整打通。
- 影响范围：
  - Skill 中心列表层视觉与交互增强；不改变安装、导入、信任、投影、更新或卸载语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。仍是 V2 计划内 UI 能力增量。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 下一步补 Workspace 详情 Skill 行跳转、Skill 详情工作区目标跳转，或补 Skill 卡片齿轮菜单与全部 filter 项。

## 2026-07-03-15 - Workspace Skill 行跳转

- 用户目标：
  - 继续补齐 Control Center Directory UI V2 的跨对象导航，让 Workspace 详情中的 Skill 行能跳到对应 Skill 详情。
- 涉及目录：
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - `WorkspaceHubPanel` 新增可选 `onOpenSkill` 回调。
  - Workspace Skill 清单中带 `matchedInstalledSkillId` 的行显示“查看 Skill”动作。
  - `ControlCenterView` 将该动作接到 Skill 中心 manage section，并调用既有 `onSelectSkill`。
- 修改原因：
  - V2 Checklist 要求 “Workspace 详情 → Skill 行跳转 Skill 详情”；前一轮只展示只读清单，没有跨对象跳转。
- 本次已消除的不确定性：
  - 确认只需使用 inventory 中已有 `matchedInstalledSkillId`，不需要新增后端查询。
- 本次采用的关键假设：
  - 未匹配到 installed Skill 的目录内 Skill 暂不跳转，保持只读展示。
- 剩余不确定性：
  - 反向 “Skill 详情 → Workspace 详情” 仍需后续切片。
- 影响范围：
  - 仅新增 WorkspaceHub 到 SkillCenter 的前端导航动作；不改变数据或持久化状态。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 下一步补 Skill 详情中的工作区 target/应用目录跳转 WorkspaceHub，或补 Skill 卡片齿轮菜单。

## 2026-07-03-15 - Skill 到 WorkspaceHub 反向跳转

- 用户目标：
  - 继续补齐 Control Center Directory UI V2 的互跳要求，让 Skill 详情里的工作区来源或目标能进入 WorkspaceHub 对应工作区详情。
- 涉及目录：
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `.ai/changes/`
- 修改内容：
  - `WorkspaceHubPanel` 新增 `focusWorkspacePath`，按归一化路径匹配已注册 workspace 并选中详情。
  - `ControlCenterView` 保存待聚焦 workspace path，并从 Skill Center 跳转到 WorkspaceHub。
  - `SkillCenterPanel` 对 workspace-scoped 发现位置和工作区目标详情增加“查看工作区”动作。
  - `WorkspaceHubPanel.test.ts` 增加 path 到 workspace record 的匹配测试。
- 修改原因：
  - V2 Spec/Checklist 要求 Skill 详情中的工作区 target 或应用目录能跳到 Workspace 详情；此前只实现了 Workspace → Skill。
- 本次已消除的不确定性：
  - 确认现有 Skill target/location 已带 root path 信息，不需要新增后端 command。
- 本次采用的关键假设：
  - 只有已注册 workspace 能被 WorkspaceHub 选中；未注册路径仍保留打开目录能力，不自动注册。
- 剩余不确定性：
  - Skill 卡片齿轮菜单、完整 loading/error polish 和视觉截图 QA 仍需后续切片。
- 影响范围：
  - 前端导航状态增强；不改变 Skill、Workspace 或 Schedule 持久化语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续优先补 Skill card gear menu 或对当前 Directory surface 做浏览器视觉 QA。

## 2026-07-03-15 - Directory 卡片操作菜单

- 用户目标：
  - 继续补齐 Control Center Directory UI V2 的卡片齿轮菜单要求，让 Skill 与 Workspace 卡片直接承载常用动作。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - `DirectoryCardGrid` 新增 `cornerSlot`，允许复用现有 `ControlCenterActionMenu`，并阻止角标区域点击/键盘事件冒泡到卡片详情。
  - Workspace 卡片角标从单一打开按钮改为操作菜单：查看详情、打开工作区、显示目录。
  - Installed Skill 卡片新增操作菜单：查看详情、投影当前工作区、移除已有投影、置顶、信任、更新、打开目录、卸载。
  - Skill 卡片在有更新时显示 `有更新` badge。
  - `DirectoryCardGrid.test.tsx` 增加 corner slot 不触发卡片打开的测试。
- 修改原因：
  - V2 Checklist 要求 Skill 卡片齿轮菜单与 Workspace 齿轮菜单；此前卡片只有详情点击和少量角标动作。
- 本次已消除的不确定性：
  - 确认现有 `ControlCenterActionMenu` 足够承载卡片菜单，不需要新增菜单组件或依赖。
- 本次采用的关键假设：
  - Harness 的 `+` 继续保持“查看模板”薄动作；本轮只补最明确的 Skill/Workspace 菜单。
- 剩余不确定性：
  - 菜单视觉位置仍需后续浏览器/Tauri 视觉 QA。
  - 卸载确认、loading skeleton 与失败行内错误仍需后续切片。
- 影响范围：
  - 前端卡片操作入口增强；不改变底层 Skill/Workspace command 语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补卸载确认/失败错误同步，或做当前 Directory surface 的视觉 QA。

## 2026-07-03-15 - Skill 卸载确认文案

- 用户目标：
  - 补齐 Control Center Directory UI V2 中 “卸载弹出确认，文案包含 Skill 名和投影数量” 的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/app/`
  - `.ai/changes/`
- 修改内容：
  - 在共享 `handleUninstallSkill` 中继续统一拦截卸载操作，确认文案改为包含 Skill 名与当前投影数量。
  - 投影数量来自全局投影记录与当前 Session 投影记录；若 Session 只标记已应用但缺少 projection 详情，按 1 个投影计。
  - 新增 `buildSkillUninstallConfirmMessage` 纯 helper 与最小单测。
- 修改原因：
  - 卡片菜单和详情菜单都走同一 uninstall handler；在共享 handler 中补文案可以覆盖所有入口，避免在每个按钮重复确认逻辑。
- 本次已消除的不确定性：
  - 确认既有代码已经有卸载确认，只缺 V2 要求的投影数量。
- 本次采用的关键假设：
  - 当前前端 projection state 足以表达确认文案中的数量；真正阻止仍交给后端 `uninstall_skill`。
- 剩余不确定性：
  - 失败后的行内错误同步仍需后续切片。
- 影响范围：
  - 仅改变卸载前确认文案；不改变卸载 command 或数据状态刷新逻辑。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/app/shellControllerDefaults.test.ts src/features/skill-center/SkillCenterPanel.test.ts src/features/control-center/WorkspaceHubPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx`
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）
  - `pnpm -C apps/kimi-shell build`
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补导入/卸载失败在卡片上的行内错误展示，或做视觉 QA。

## 2026-07-03-16 - Directory UI 完成度审计与 WorkspaceHub 返回

- 用户目标：
  - 根据 Control Center Directory UI V2 文档做真实完成度审计，区分已实现、未实现、需要视觉验证和不做/降级项；优先修复阻塞可交付的大缺口，最后统一验证。
- 涉及目录：
  - `docs/`
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - 新增 `docs/CONTROL_CENTER_DIRECTORY_UI_COMPLETION_AUDIT_V2.md`，按四类记录当前完成度与剩余风险。
  - `WorkspaceHubPanel` 新增 Harness/Workspace 详情 `返回` 按钮。
  - `WorkspaceHubPanel` 新增 Esc 返回判定，清空详情选择后保留搜索、筛选和排序状态。
  - 刷新时若当前已处于无详情选择状态，保持列表状态，不自动选中第一条。
  - 增加 WorkspaceHub 返回判定 helper 测试。
- 修改原因：
  - WorkspaceHub 详情缺少外层返回/Esc 会破坏 Spec v2 的详情页通用验收；审计文档用于避免把未视觉验证或未跑 gate 的项目误标为完成。
- 本次已消除的不确定性：
  - 确认 Skill 详情已有返回/Esc，WorkspaceHub 是本轮阻塞级缺口。
  - 确认 Directory 前端测试已覆盖卡片、viewer、部分安全和返回 helper 行为。
  - 确认 Rust 侧本轮至少通过格式、编译和测试二进制编译门。
- 本次采用的关键假设：
  - 本轮不全量清理 `cc-image-*` / `skill-center-*`，因为这些样式仍被非 Directory section 使用。
  - 视觉 QA、release note、截图和 rollback 方案作为后续发布前 gate，不在本轮静态审计中宣称完成。
- 剩余不确定性：
  - 浏览器/真实 Tauri 视觉 QA 尚未执行。
  - Rust 路径穿越、symlink/junction escape、NUL、UTF-8 异常和 usage stats 的运行期安全测试尚未形成逐项结果。
  - feature flag / fallback 策略尚未实现或决策收口。
- 影响范围：
  - WorkspaceHub 详情导航行为和完成度审计文档；不改变 Harness 创建、Workspace 打开、Skill 投影或后端业务语义。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/control-center/WorkspaceHubPanel.test.ts src/features/skill-center/SkillCenterPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/features/directory/DirectoryFilePreview.test.tsx src/app/shellControllerDefaults.test.ts`：通过，5 files / 23 tests。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `cargo fmt -- --check`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo check`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo test --no-run`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 下一步优先做视觉 QA 和 Rust 安全测试逐项补证，再决定 feature flag/fallback 是否继续保留为发布要求。

## 2026-07-03-16 - WorkspaceHub 视觉 QA 抽查

- 用户目标：
  - 继续补 Control Center Directory UI V2 的视觉 QA 缺口，优先验证 WorkspaceHub、Harness/Workspace 详情是否挡交付。
- 涉及目录：
  - `docs/`
  - `.ai/changes/`
- 修改内容：
  - 用 Vite + Chrome headless CDP 打开 `#/control-center`，注入最小 Tauri mock 数据抽查 WorkspaceHub。
  - 更新 `docs/CONTROL_CENTER_DIRECTORY_UI_COMPLETION_AUDIT_V2.md`，记录桌面/窄屏视觉 QA 已验证和仍未证明的项。
- 修改原因：
  - 普通浏览器无 Tauri invoke，直接打开只能看到空数据和预期 invoke 错误；mock 数据可覆盖卡片、Workspace detail、Harness detail 和文件树布局。
- 本次已消除的不确定性：
  - 桌面 1440×1000 与窄屏 390×844 下，WorkspaceHub 列表、Workspace detail、Harness detail 没有横向溢出。
  - mock 数据下 Harness 文件树、返回按钮、Workspace Skill 清单和调度摘要能渲染，未出现白屏或明显重叠。
- 本次采用的关键假设：
  - 浏览器 mock 只用于视觉布局，不代表真实 Tauri command 行为验证。
- 剩余不确定性：
  - 标题/工具条吸顶与卡片区域独立滚动未证明通过，当前截图更像页面级滚动。
  - 暗色主题、focus/hover/menu keyboard、对比度、200 cards 性能、512 tree entry 真实可感知表现仍未验证。
  - 安装/导入/信任/投影/卸载和真实 Harness dry-run/create 仍需 Tauri 环境手工回归。
- 影响范围：
  - 仅更新审计记录，无产品代码变化。
- 验证方式：
  - `Invoke-WebRequest http://127.0.0.1:5174/`：Vite 页面可访问。
  - Chrome headless CDP：WorkspaceHub mock 数据桌面 1440×1000、窄屏 390×844 截图抽查；`documentElement.scrollWidth === clientWidth`，无横向溢出。
- 是否更新 README：
  - 否。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 若 sticky/独立滚动是发布阻塞，下一轮只改 WorkspaceHub/Skill Directory 的滚动容器；否则保留为 checklist 未完成项。

## 2026-07-03-16 - File Preview Rust 安全测试补证

- 用户目标：
  - 补齐 Control Center Directory UI V2 中后端只读文件预览安全验收的可追溯证据。
- 涉及目录：
  - `apps/kimi-shell/src-tauri/src/`
  - `docs/`
  - `.ai/changes/`
- 修改内容：
  - `skill_center.rs` 增加 junction/symlink escape 测试，覆盖 canonicalize 后逃出 root 的路径。
  - `skill_center.rs` 增加非法 UTF-8 文件 lossy 读取测试，确认不 panic 且不误判二进制。
  - 更新完成度审计，区分“测试源码已补/可编译”和“本机运行期仍被环境阻断”。
- 修改原因：
  - 现有测试已覆盖 `../`、绝对路径、NUL 二进制和 >512KB 截断，但 symlink/junction escape 与 UTF-8 异常缺少直接测试源码。
- 本次已消除的不确定性：
  - 文件预览共用 `resolve_within` / `read_safe_file` 可在测试源码层覆盖 installed/discovered/harness 三类读取路径的核心安全边界。
- 本次采用的关键假设：
  - Windows 上用 junction 测试目录逃逸；Unix 上用 symlink 测试同一语义。
- 剩余不确定性：
  - 当前 Windows 环境执行 Rust test binary 仍失败为既有 `STATUS_ENTRYPOINT_NOT_FOUND`，所以不能宣称 Rust 运行期测试全绿。
  - usage stats apply/uninstall/旧 store 兼容仍未找到实现证据。
- 影响范围：
  - 仅新增 Rust 单元测试和审计记录；不改变运行时代码。
- 验证方式：
  - `cargo fmt -- --check`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo test --no-run`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo test resolve_within_rejects_junction_escape`：测试二进制编译成功，但执行阶段失败为 `STATUS_ENTRYPOINT_NOT_FOUND`。
- 是否更新 README：
  - 否。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 需要在能运行 Rust test binary 的环境补跑新增安全测试和既有 Rust 单测。

## 2026-07-03-15 - Markdown 相对链接按当前目录解析

- 用户目标：
  - 补齐 Control Center Directory UI V2 中相对链接指向树内文件时切换文件的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - Markdown 链接点击时按当前文件所在目录解析 `./`、普通相对路径和 `..`。
  - 只在解析结果匹配文件树内文件时切换文件；外链仍保留确认逻辑，`javascript:` 仍不可点击。
  - 增加嵌套 Markdown 相对链接测试。
- 修改原因：
  - 旧逻辑把 `./next.md` 当作根目录路径，嵌套文件中的相对链接无法切换到同目录文件。
- 本次已消除的不确定性：
  - 确认文件树条目使用 normalized `/` relPath，可直接做前端路径段解析。
- 本次采用的关键假设：
  - 链接 query/hash 不参与本地文件匹配；匹配时只看路径部分。
- 剩余不确定性：
  - 详情加载失败返回按钮和视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响 Markdown preview 内本地链接解析。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败返回按钮，或做视觉 QA。

## 2026-07-03-15 - 文件预览错误态返回

- 用户目标：
  - 补齐 Control Center Directory UI V2 中详情加载失败不白屏、展示错误态和返回按钮的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - 文件列表加载失败时，内容区显示错误态并保留打开所在目录入口。
  - 单文件读取失败时，内容区显示错误态和“返回文件列表”按钮。
  - 增加文件读取失败错误态测试。
- 修改原因：
  - 此前读取失败只在树或内容区显示简单错误文本，缺少明确错误态与返回动作。
- 本次已消除的不确定性：
  - 确认错误状态已经在 `DirectoryFilePreview` 内部维护，可局部补齐，不需要外层导航状态。
- 本次采用的关键假设：
  - 本切片的“返回”限定为返回文件列表；外层详情页整体返回列表仍留给后续页面导航切片。
- 剩余不确定性：
  - 外层 `‹ 返回`、Esc 返回列表和浏览器视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响文件预览加载失败展示。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补外层详情返回/Esc，或做视觉 QA。

## 2026-07-03-15 - Skill 详情返回入口

- 用户目标：
  - 补齐 Control Center Directory UI V2 中详情页 `‹ 返回` 和 Esc 返回列表的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `.ai/changes/`
- 修改内容：
  - `useShellController` 新增 `handleClearSkillSelection`，统一清空已安装 Skill 与 discovered Skill 详情选中状态。
  - Skill 详情头部新增返回按钮，点击后回到空详情/list 状态。
  - Skill 面板在详情打开时捕获 Esc，并走同一个返回逻辑。
  - 增加 Esc 判断 helper 测试。
- 修改原因：
  - V2 Checklist 要求详情页有返回入口，且 Esc 在详情页等效返回；此前只能继续点其他卡片/列表项。
- 本次已消除的不确定性：
  - 确认 selected skill/discovery 状态都在 shell controller 内，可用一个 clear handler 作为共享返回点。
- 本次采用的关键假设：
  - 本切片先覆盖 Skill Directory 详情；WorkspaceHub 详情返回若仍缺，后续单独补。
- 剩余不确定性：
  - WorkspaceHub 外层返回、滚动位置保持和视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响 Skill Center 详情选中状态和返回交互。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补 WorkspaceHub 返回/滚动保持，或做视觉 QA。

## 2026-07-03-15 - 超限文件预览占位

- 用户目标：
  - 补齐 Control Center Directory UI V2 中超限文件显示占位并提供打开目录的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - `DirectoryFilePreview` 对 `truncated` 文件显示占位，不再渲染截断文本。
  - 超限文件提供“打开所在目录”动作，并禁用复制当前文件。
  - 增加超限文件占位测试。
- 修改原因：
  - 截断文本容易被误读为完整内容；V2 Spec 要求二进制或超限文件只显示占位。
- 本次已消除的不确定性：
  - 确认前端内容模型已有 `truncated` 标记，不需要后端改动。
- 本次采用的关键假设：
  - 超限文件即使有后端返回的截断文本，也不在控制中心内展示。
- 剩余不确定性：
  - 详情加载失败返回按钮和视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响超限文件的预览和复制能力。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败返回按钮，或做视觉 QA。

## 2026-07-03-15 - Workspace Grid 贴边修复

- 用户目标：
  - 去掉 Workspace Grid 外层 shell 样式造成的 iframe pane 与应用外框之间的小间距。
- 涉及目录：
  - `apps/kimi-shell/src/features/workspace-grid/`
  - `apps/kimi-shell/src/`
  - `.ai/changes/`
- 修改内容：
  - 移除 `.workspace-stage-grid-shell` 的透明 1px border。
  - 为 single preset 且有窗格、或最大化窗格状态追加 `is-edge-to-edge` class。
  - 在贴边模式下取消 grid gap 与 pane 圆角，保留多窗格布局的 pane border 和视觉分隔。
  - 增加 Workspace Grid DOM 断言，覆盖默认多窗格与单窗贴边状态。
- 修改原因：
  - 截图中的缝隙来自 shell 外层 grid 包裹样式，而不是 iframe、embed host 或 body 默认 margin。
- 本次已消除的不确定性：
  - 确认 iframe、`.workspace-embed` 与全局 body 样式不是本次缝隙根因。
- 本次采用的关键假设：
  - 单窗/最大化需要贴边，多窗格仍应保留 4px gap 与 pane border 作为分隔。
- 剩余不确定性：
  - 实机 WebView2 渲染下是否还存在来自窗口圆角或系统阴影的 1px 视觉边缘，需要后续手动截图确认。
- 影响范围：
  - 仅影响 Workspace Grid 外层 chrome 与贴边状态 class；不改变 store schema、preset、pane 数据或 iframe URL。
- 验证方式：
  - `pnpm -C apps/kimi-shell test -- WorkspaceGridView.test.tsx`：通过（Vitest 实际运行 workspace-grid 相关 9 个测试文件，59 个测试）。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 如实机截图仍看到边缘缝，再检查 Tauri 窗口圆角与外层 window shadow，而不是 iframe 样式。

## 2026-07-03-15 - Skill 操作失败行内错误

- 用户目标：
  - 补齐 Control Center Directory UI V2 中导入/卸载等 Skill 操作失败时应在当前上下文显示错误的验收底线。
- 涉及目录：
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `.ai/changes/`
- 修改内容：
  - 将已有 `actionError` 从 `App` 透传到 `ControlCenterView` 与 `SkillCenterPanel`。
  - 在 Skill Directory surface 中显示内联错误条，失败后保留当前列表、卡片和详情上下文。
  - 错误条增加“重试”按钮，按当前 Skill 分区刷新技能库、发现项或工作区 Skill 管理状态。
  - 增加轻量 destructive 样式，复用现有设计 token。
- 修改原因：
  - V2 Spec/Checklist 要求发现项 `+` 导入失败显示行内错误并保留按钮，详情导入失败不丢失详情页；此前错误只存在全局状态。
- 本次已消除的不确定性：
  - 确认已有命令失败都会写入 `actionError`，不需要新增后端错误字段。
- 本次采用的关键假设：
  - 本切片先提供 Skill 面板级行内错误；per-card 错误状态表可在后续更细验收中再做。
- 剩余不确定性：
  - loading skeleton、详情失败返回按钮和完整视觉 QA 仍需后续切片。
- 影响范围：
  - 仅增加错误展示透传；不改变任何 Skill 操作命令、刷新顺序或失败回滚语义。
- 验证方式：
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补 refresh 失败错误条和重试，或做当前 Directory surface 的视觉 QA。

## 2026-07-03-15 - Directory skeleton loading

- 用户目标：
  - 补齐 Control Center Directory UI V2 中 loading 显示 skeleton card 的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `apps/kimi-shell/src/features/control-center/`
  - `.ai/changes/`
- 修改内容：
  - `DirectoryCardGrid` 新增 `loading` 与 `skeletonCount`，首次无数据且 busy 时显示 6 张 skeleton card。
  - Skill Center Directory surface 与 WorkspaceHub Directory surface 接入 loading 状态。
  - 刷新已有卡片时继续保留旧数据，只在网格上标记 busy。
  - 增加 skeleton 样式和焦点单测。
- 修改原因：
  - V2 Spec 要求首次加载显示 skeleton，刷新不清空旧数据；此前只有空态和禁用按钮。
- 本次已消除的不确定性：
  - 确认 Skill 与 WorkspaceHub 都已复用 `DirectoryCardGrid`，可通过一个通用 props 覆盖两处目录卡片。
- 本次采用的关键假设：
  - skeleton 只用于无旧数据的首次加载；已有数据刷新时保留卡片比覆盖 skeleton 更符合“刷新不清空旧数据”。
- 剩余不确定性：
  - 详情加载失败返回按钮、文件树键盘语义、预览本地图片占位和完整视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响 Directory 卡片加载展示；不改变数据刷新或选择状态。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryCardGrid.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败错误态和返回按钮，或做文件预览安全/键盘验收。

## 2026-07-03-15 - Markdown 图片安全占位

- 用户目标：
  - 补齐 Control Center Directory UI V2 中 Markdown 预览不加载本地图片并显示占位的安全验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - Markdown inline parser 支持识别 `![alt](src)` 图片语法。
  - 图片语法渲染为只读占位 chip，不创建 `<img>`，因此不会加载本地或远程图片。
  - 增加图片占位样式与自动化测试。
- 修改原因：
  - V2 Spec 要求 Markdown 内容按不可信输入处理，图片节点替换为占位文本。
- 本次已消除的不确定性：
  - 确认现有 Markdown viewer 已避免 `dangerouslySetInnerHTML`，裸 `<img onerror>` 已作为文本处理；本轮只补标准图片语法。
- 本次采用的关键假设：
  - 为安全起见，所有 Markdown 图片都不加载，而不只拦截本地相对路径图片。
- 剩余不确定性：
  - 文件树完整键盘语义、详情加载失败返回按钮和浏览器视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响 Markdown preview 的图片语法展示；source 模式仍原样显示文本。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补文件树键盘操作，或做详情失败错误态。

## 2026-07-03-15 - 文件树基础键盘导航

- 用户目标：
  - 推进 Control Center Directory UI V2 中 FileTree 键盘可达性验收。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - 文件树可选文件支持 `ArrowDown` / `ArrowUp` / `Home` / `End` 移动选中项。
  - 键盘移动时同步移动焦点，并保持 `tree` / `treeitem` / `aria-selected` 语义。
  - 增加组件级测试覆盖 ArrowDown 切换文件。
- 修改原因：
  - V2 Spec 要求文件树具备方向键可达性；此前只能 Tab 到按钮或点击文件。
- 本次已消除的不确定性：
  - 确认现有文件树已是 flat entry 列表，基础导航可在当前结构上完成，不需要改后端。
- 本次采用的关键假设：
  - 本切片只补可选文件之间的键盘移动；目录展开/收起需要树模型状态，作为后续独立切片处理。
- 剩余不确定性：
  - `ArrowRight` / `ArrowLeft` 展开收起目录、目录默认收起与展开状态记忆仍需后续切片。
- 影响范围：
  - 仅影响文件树键盘交互；不改变文件读取或 Markdown/source 渲染。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补目录展开/收起，或补详情加载失败错误态。

## 2026-07-03-15 - 文件树目录展开收起

- 用户目标：
  - 补齐 Control Center Directory UI V2 中 FileTree 默认收起、展开状态和左右方向键的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - `DirectoryFilePreview` 在 flat file entries 上维护 `expandedDirs`，目录默认收起。
  - 选中文件会自动展开其父目录，避免默认文件被隐藏。
  - 目录支持点击、`ArrowRight` 展开、`ArrowLeft` 收起，并设置 `aria-expanded`。
  - 增加目录默认收起与方向键展开/收起测试。
- 修改原因：
  - V2 Checklist 要求目录默认收起、展开状态保持，并支持方向键展开/收起；此前目录行禁用且所有文件始终可见。
- 本次已消除的不确定性：
  - 确认后端已提供 flat `relPath/isDir` entry，前端可用父路径推导可见树，无需改后端。
- 本次采用的关键假设：
  - 展开状态按当前详情组件会话保持；切换 Skill/Harness 后重置，符合当前详情生命周期。
- 剩余不确定性：
  - License 根级末尾固定、超 512 条提示和详情加载失败返回按钮仍需后续切片。
- 影响范围：
  - 仅影响文件树展示与键盘行为；不改变文件读取安全边界。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补 License 排序/截断提示，或补详情加载失败返回按钮。

## 2026-07-03-15 - 文件树 License 排序与截断提示

- 用户目标：
  - 补齐 Control Center Directory UI V2 中根级 `LICENSE*` 固定末尾和超过 512 条提示的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - 文件树展示前对 root-level `LICENSE*` 文件做稳定末尾排序。
  - 文件列表达到 512 条时显示截断提示，引导打开目录查看完整内容。
  - 增加 root license 排序测试。
- 修改原因：
  - V2 FileTree 清单明确要求 License 文件末尾固定，并对条目上限给出可见提示。
- 本次已消除的不确定性：
  - 确认现有 `SkillFileEntry` 已带 `relPath/isDir`，前端可直接识别根级 License。
- 本次采用的关键假设：
  - 后端最大返回 512 条；前端以 `entries.length >= 512` 作为截断提示条件。
- 剩余不确定性：
  - 详情加载失败返回按钮和浏览器视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响文件树展示顺序和提示文案；不改变文件读取。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败返回按钮，或做视觉 QA。

## 2026-07-03-15 - Skill Directory 空态区分

- 用户目标：
  - 补齐 Control Center Directory UI V2 中空态区分“无匹配结果”和“本分区为空”的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/skill-center/`
  - `.ai/changes/`
- 修改内容：
  - Skill Directory 按当前二级分区 raw count 判断空态。
  - 分区无对象时显示“本分区为空”；有对象但筛选后为空时显示“没有匹配结果”。
  - 增加纯 helper 测试覆盖两种文案。
- 修改原因：
  - 此前 Skill Directory 空态统一显示“没有匹配结果”，无法区分数据为空和筛选为空。
- 本次已消除的不确定性：
  - 确认 Skill、发现项、工作区目标三类 raw count 都已在面板中可用。
- 本次采用的关键假设：
  - raw count 为 0 时即使用户当前有筛选，也优先提示本分区为空。
- 剩余不确定性：
  - 详情加载失败返回按钮、WorkspaceHub 部分细节和视觉 QA 仍需后续切片。
- 影响范围：
  - 仅改变 Skill Directory 空态文案。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/skill-center/SkillCenterPanel.test.ts`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败返回按钮，或做视觉 QA。

## 2026-07-03-15 - 文件内容滚动复位

- 用户目标：
  - 补齐 Control Center Directory UI V2 中切换文件后内容滚动回顶部的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/directory/`
  - `.ai/changes/`
- 修改内容：
  - `DirectoryFilePreview` 的内容 pane 增加 ref，`selectedPath` 变化时滚动到顶部。
  - 增加组件测试覆盖切换文件触发 `scrollTo({ top: 0 })`。
- 修改原因：
  - V2 Markdown/Source viewer 清单要求切换文件后内容区回到顶部；此前同一滚动容器可能保留旧文件滚动位置。
- 本次已消除的不确定性：
  - 确认内容区已有独立滚动容器，只需在切换文件时复位。
- 本次采用的关键假设：
  - 只在文件切换时复位；同一文件 preview/source 模式切换仍保持当前滚动，避免打断阅读。
- 剩余不确定性：
  - 详情加载失败返回按钮和浏览器视觉 QA 仍需后续切片。
- 影响范围：
  - 仅影响文件预览内容区滚动位置。
- 验证方式：
  - `pnpm -C apps/kimi-shell test src/features/directory/DirectoryFilePreview.test.tsx`：通过。
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补详情加载失败返回按钮，或做视觉 QA。

## 2026-07-03-15 - Skill scripts 风险提示

- 用户目标：
  - 补齐 Control Center Directory UI V2 中 hasScripts 时显示风险提示的验收项。
- 涉及目录：
  - `apps/kimi-shell/src/features/skill-center/`
  - `.ai/changes/`
- 修改内容：
  - 在已安装 Skill 详情中，当 Skill 包含 `scripts/` 时显示内联风险提示。
  - 未信任 Skill 的提示明确说明不能投影且应先检查脚本；已信任 Skill 的提示提醒更新或重新信任前复核脚本。
  - 增加一条轻量 warning 样式，复用现有 warning token。
- 修改原因：
  - V2 Spec/Checklist 要求 hasScripts 风险可见；此前只有 badge 和禁用状态，缺少详情页解释。
- 本次已消除的不确定性：
  - 确认前端已持有 `hasScripts` 与 `trusted` 状态，不需要新增后端字段。
- 本次采用的关键假设：
  - 风险提示只需要在已安装 Skill 详情展示；待导入发现项仍保留现有 `包含 scripts/` 状态 badge。
- 剩余不确定性：
  - 完整浏览器视觉 QA、loading/error polish 和更多 checklist 项仍需后续切片。
- 影响范围：
  - 仅改变 Skill 详情的提示文案和样式；不改变投影、信任或安装语义。
- 验证方式：
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm -C apps/kimi-shell build`：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变长期目录职责、公共 API 或运行方式。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。
- 后续注意事项：
  - 后续补导入/卸载失败在卡片上的行内错误展示，或做视觉 QA。

## 2026-07-03-16 - Directory UI usage stats 与滚动收敛

- 用户目标：
  - 根据 Control Center Directory UI v2 文档完成剩余开发工作，视觉审计由用户后续执行。
- 涉及目录：
  - `apps/kimi-shell/src-tauri/src/`
  - `apps/kimi-shell/src/app/`
  - `apps/kimi-shell/src/services/`
  - `apps/kimi-shell/src/features/control-center/`
  - `apps/kimi-shell/src/features/skill-center/`
  - `apps/kimi-shell/src/App.css`
  - `docs/`
  - `.ai/changes/`
- 修改内容：
  - 为 installed skill 增加 `usageStats` 持久字段，旧 registry 缺字段时默认兼容。
  - `apply_skill` 成功后自增应用次数并记录 `lastAppliedAt`。
  - 增加 `get_skill_usage_stats` Tauri 命令与前端 service/type。
  - Skill 卡片 metric 优先显示 `应用 N 次`，无统计时降级显示更新时间。
  - WorkspaceHub Directory card 与 Skill Directory surface 改为工具区固定、card grid 独立滚动。
  - 更新完成度审计，将 usage stats 移入已实现，并把 feature flag runtime fallback 明确降级为发布版本回滚。
- 修改原因：
  - v2 plan/checklist 要求 usage stats apply 成功自增、旧 store 兼容、uninstall 清理，并要求标题/工具条与卡片滚动行为具备实现基础。
- 本次已消除的不确定性：
  - 确认当前没有既有 usage stats 代码，需要新增后端类型、store 归一化、apply 记录和命令入口。
  - 确认 uninstall 删除 installed skill registry row 后会同步移除附着在该 row 上的 usage stats。
- 本次采用的关键假设：
  - usage stats 从当前版本开始累计，旧数据没有历史次数时显示为 0 并用更新时间兜底。
  - 不恢复旧 UI runtime fallback；若发布需要回退，按版本回滚处理。
  - 视觉正确性由用户后续人工审计，不在本轮用浏览器截图证明。
- 剩余不确定性：
  - 视觉审计仍需覆盖桌面/窄屏滚动、暗色主题、hover/focus、菜单键盘、对比度、真实 Tauri 数据。
  - 当前 Windows 环境仍无法证明 Rust test runtime 全量执行，只能证明测试二进制可编译。
- 影响范围：
  - Skill registry JSON 多出 `usageStats` 字段；旧 registry 可读，保存后会补齐字段。
  - Skill apply 成功路径多一次 registry 写入。
  - Skill/WorkspaceHub Directory 区域滚动行为改变，不影响业务数据。
- 验证方式：
  - `.\node_modules\.bin\tsc.cmd --noEmit`（在 `apps/kimi-shell` 目录执行）：通过。
  - `pnpm test src/features/control-center/WorkspaceHubPanel.test.ts src/features/skill-center/SkillCenterPanel.test.ts src/features/directory/DirectoryCardGrid.test.tsx src/app/shellControllerDefaults.test.ts`（在 `apps/kimi-shell` 目录执行）：4 files / 13 tests 通过。
  - `cargo check`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo test --no-run`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `cargo fmt -- --check`（在 `apps/kimi-shell/src-tauri` 目录执行）：通过。
  - `git diff --check`：通过，仅提示工作区文件下次由 Git 触碰时 LF 会替换为 CRLF。
- 是否更新 README：
  - 否。未改变目录职责或运行方式；开发状态更新记录在完成度审计文档。
- 是否更新 `.ai/architecture`：
  - 否。
- 是否需要新增或更新 `.ai/decisions`：
  - 否。本轮将 feature flag fallback 作为范围降级记录在审计中，未形成新的长期架构决策。
- 后续注意事项：
  - 用户视觉审计通过后，再补 README/docs 截图、release note 和 rollback 说明。
