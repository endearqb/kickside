## 2026-07-07-23 - 控制中心小助手设置精简

- 用户目标：精简小助手设置页，正常状态隐藏启动诊断、路径菜单和日志 Doctor，仅启动失败时暴露诊断信息。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src`。
- 修改内容：小助手设置页仅在启动失败条件下渲染运行诊断；移除运行诊断中的“路径与菜单”分区；去掉“引导步骤”外层卡片容器并保留步骤列表；补充无外框步骤区样式。
- 修改原因：减少正常设置流程里的诊断噪音，符合控制中心设计系统对少层级、少纯展示容器的要求。
- 本次已消除的不确定性：确认运行诊断由现有前端渲染控制，Doctor、日志和诊断 command 不需要后端改动。
- 本次采用的关键假设：启动失败按 `startupPhase === "failed"` 或 `startupFailureDetail` 判定；快速设置中的右键菜单引导步骤继续保留。
- 剩余不确定性：需在真实启动失败状态下人工确认诊断区是否按预期出现。
- 影响范围：仅影响控制中心小助手设置页和失败诊断内容可见性，不改变 Tauri command、路由类型、持久化状态或后端 API。
- 验证方式：运行 `pnpm -C apps/kimi-shell test` 和 `apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit`。
- 是否更新 README：否，目录职责和公共接口未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否，未触碰单向门决策。
- 后续注意事项：若未来需要正常态手动诊断入口，应先明确入口位置，避免恢复常驻诊断块。

## 2026-07-07-23 - 保留单 Pane 贴边圆角

- 用户目标：确认 Windows 应用外壳圆角与内部 pane 圆角不重合的原因，并让单个 pane 贴边时保持 10px 圆角。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：将 edge-to-edge workspace grid pane 的 `border-radius` 从 `0` 改为 `var(--shell-window-radius)`。
- 修改原因：单 pane 贴边时不应取消 pane 自身圆角；当前圆角 token 为 `--shell-window-radius: 10px`。
- 本次已消除的不确定性：确认外壳和 pane 都使用同一个 10px 圆角变量，单 pane 不显示圆角来自 edge-to-edge 覆盖规则。
- 本次采用的关键假设：用户希望保持现有 10px token，不调整全局窗口圆角体系。
- 剩余不确定性：实际 Windows DWM 窗口裁切与 CSS 圆角在不同 DPI 下可能仍有 1px 级视觉差异。
- 影响范围：仅影响 Workspace Grid 单 pane / 最大化贴边模式下的 pane 圆角显示。
- 验证方式：`CI=true pnpm --dir apps/kimi-shell exec vitest run src/features/workspace-grid/WorkspaceGridView.test.tsx`，17 个用例通过。
- 是否更新 README：否，未改变目录职责或公共契约。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若要像原生窗口一样完全无双重圆角，应让外壳或 pane 其中之一成为唯一 clipping owner。

## 2026-07-07-23 - 同步 active session 工作目录到 Grid pane

- 用户目标：session 切换后，Workspace Grid 的“打开此窗格目录”按钮跟随当前 active session 的工作目录。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/workspace-grid`。
- 修改内容：新增 `setPaneWorkDir` store action；收到 `active_session_updated` 时仅在当前 active pane 是 Code pane 且 payload 带非空 `workDir` 时，同步该 pane 的 `workDir`。
- 修改原因：原按钮读取 pane 自身 `workDir` 快照，session 切换只更新 `status.activeSessionWorkDir`，没有回写 pane，导致打开目录仍指向旧目录。
- 本次已消除的不确定性：确认 `configurePane` 会顺带改 `sessionId`/URL，不适合作为只同步目录的入口。
- 本次采用的关键假设：active pane 不是 Code pane 或 payload 没有非空 `workDir` 时，不覆盖任何 pane 目录。
- 剩余不确定性：需在真实 Kimi Web session 切换流里手工确认注入脚本能持续发出 `active_session_updated`。
- 影响范围：仅影响 Grid pane 的内部 `workDir` 状态，不改变 Tauri command、Rust 类型、后端 session、pane URL 或 pane 选中状态。
- 验证方式：`pnpm -C apps/kimi-shell test -- workspace-grid`，9 个测试文件 / 71 个用例通过；`pnpm -C apps/kimi-shell build` 通过。
- 是否更新 README：否，目录职责和公共接口未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否，未触碰单向门决策。
- 后续注意事项：若后续需要跟随非 active pane 的 session，需要先定义 pane 与 Kimi Web 内部 tab/session 的映射契约。

## 2026-07-07-23 - 修复代码审查报告高优先级问题

- 用户目标：根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复。
- 涉及目录：`apps/kimi-shell`、`apps/kimi-im-bridge`、`.ai/changes`。
- 修改内容：补 Tauri CSP 与 skill bundle allowlist；外部 iframe 增加 sandbox/referrerPolicy；link bridge 改为只信任当前 workspace iframe；外部 URL 日志去 query/fragment；diagnostics workspace URL 脱敏；Grid resize 结束时才持久化；`configurePane` 采用传入 storage namespace；安装 commands busy 透传真实状态并删除死 `installAction`；嵌入式 WebView bounds sync 合并到 animation frame；前端隐藏时跳过轮询；frontend ready flag 改为 hook ref；Go Admin API 增加 body 限制与 server timeout；Go websocket 增加 hello/idle read deadline；Bridge log 文件改 0600；动态 SQL prune 入口增加白名单；Shell README 不再手写版本号；登记 install compat commands 退出条件；顺手修复 `cargo clippy -D warnings` 暴露的 `skill_center.rs` 既有 sort lint。
- 修改原因：收紧安全边界、避免 token/URL 泄漏、降低高频同步写入和 IPC 压力，修复确定性状态/namespace bug。
- 本次已消除的不确定性：确认报告中的安装 busy、Grid resize、namespace、link bridge、URL 日志、diagnostics URL、Admin body limit、websocket deadline、日志权限和动态 SQL 问题在当前代码中存在并已落地修复。
- 本次采用的关键假设：`get_app_status.workspaceUrl` 仍是运行时加载 Kimi Code Web 的完整 URL，不能直接改成脱敏值；诊断展示面使用脱敏 URL；外部 pane 仍允许 http/https 输入，但默认 sandbox 隔离，完整 allowlist 作为后续产品约束单独设计。
- 剩余不确定性：F-09 `useShellController` 拆分、F-10 Rust command module/capability 拆分属于大范围治理项，本次未做结构性拆分；F-05 只做 visibility-aware 降载，未改为事件驱动；F-19 经检查已有 `.github/workflows/ci.yml` 覆盖 Go/Rust/前端核心 gate，本次未新增重复 CI；Rust test 运行阶段在本机仍受既有 Windows `STATUS_ENTRYPOINT_NOT_FOUND` 阻断。
- 影响范围：Shell 前端状态与 Workspace Grid 行为、Tauri 安全配置与诊断返回、Bridge Admin API/日志/runtime/store 防御性逻辑；不改变 Tauri command 名称、Bridge admin envelope、数据库 migration 或 Kimi server API 契约。
- 验证方式：`pnpm -C apps/kimi-shell test` 通过；`apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过；`cargo test --no-run` 通过；`go vet ./...` 通过；`go test ./...` 通过；`pnpm -C apps/kimi-shell check:nfr:security` 通过；`git diff --check` 通过；`node` JSON parse 校验 `package.json` 与 `tauri.conf.json` 通过。`cargo test redact_workspace_url_hides_token_fragment` 与 `cargo test external_url_log_display_strips_query_and_fragment` 运行阶段 blocked：Windows 返回既有 `STATUS_ENTRYPOINT_NOT_FOUND`。`pnpm -C apps/kimi-shell check:nfr:reliability` 与 `check:nfr:port-conflict` blocked：本机缺少 `src-tauri/target/debug/appskimi-shell.exe`。
- 是否更新 README：是，`apps/kimi-shell/README.md` 移除漂移版本号和过期 release note 指针。
- 是否更新 `.ai/architecture`：是，`current-state.md` 登记 install compat commands 兼容层与退出条件。
- 是否需要新增或更新 `.ai/decisions`：否，未引入新运行时依赖、migration 或序列化契约变更。
- 后续注意事项：若继续处理报告剩余治理项，应单独拆分 `useShellController`、Rust command modules/capability matrix 和 CI 工作流；外部 pane allowlist 需要先定义产品兼容边界。

## 2026-07-08-00 - 补齐轮询与命令治理检查

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，补齐上一轮仍缺强证据的治理项。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/scripts`、`apps/kimi-shell/package.json`、`.github/workflows`、`apps/kimi-im-bridge/internal/logging`、`.ai/changes`。
- 修改内容：抽出 `useShellPollingController`，将 status/Bridge polling 策略从 `useShellController` 中分离；非控制中心状态轮询降为 5s，后台 Bridge 状态轮询降为 10s，日志 tail 仍只在 Bridge 面板可见时刷新；新增 bundle resources allowlist 检查和 Tauri command registry 检查；`check:nfr:security` 与 CI 接入 capability/resource/command 三类检查；新增 `pnpm verify` 本地一键入口；Bridge logger 增加 5MiB 单文件轮转，保留上一份 `.1`。
- 修改原因：进一步降低 F-05 固定轮询开销；为 F-10/F-13/F-19 提供可执行门禁；补齐 F-17 日志保留/轮转治理。
- 本次已消除的不确定性：确认当前 CI 已有 Go/Rust/前端主 gate，但缺少 resource/command registry 安全检查；确认 `useShellController` 可先拆出 polling 控制器，不触碰 Bridge/Skill 业务状态。
- 本次采用的关键假设：后台 5s/10s 轮询足以维持状态新鲜度；Bridge 日志保留最近当前文件和上一份轮转文件即可，后续如需长期审计再接入多代轮转。
- 剩余不确定性：完整事件驱动 status/log append、完整 command module 拆分和外部 pane 产品级 allowlist 仍需后续独立设计；本轮只补了低风险门禁和职责拆分。
- 影响范围：Shell 前端轮询频率、CI/NFR 检查、Bridge 日志文件生命周期；不改变 Tauri command 名称、Bridge API、数据库 schema 或安装包入口。
- 验证方式：`pnpm -C apps/kimi-shell check:nfr:security` 通过；`pnpm -C apps/kimi-shell verify` 通过；`apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit` 通过；`pnpm -C apps/kimi-shell test` 通过；`go test ./...` 通过；`go vet ./...` 通过。
- 是否更新 README：否，未改变目录职责或用户运行方式；`pnpm verify` 是 package script 聚合现有 gate。
- 是否更新 `.ai/architecture`：否，本轮延续上一条 compat command 登记。
- 是否需要新增或更新 `.ai/decisions`：否，未引入新依赖或单向门决策。
- 后续注意事项：若继续 F-09，应按 Bridge、Skill、Workspace Import 分片拆 controller；若继续 F-10，应迁移 Rust command 到域模块并让 command registry 随迁移更新。

## 2026-07-08-00 - 补齐外链桥接与 iframe 安全边界

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，补齐 F-01/F-02/F-03 的剩余安全闭环。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/features/loading`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：workspace 外链 bridge 增加前端生成 nonce，受信任 iframe 通过 `window.name` 回传，前端同时校验 origin、iframe identity 与 nonce；新增 `get_workspace_embed_url` 专用命令，`get_app_status.workspaceUrl` 改为 redacted 展示值，LoadingView 可见 URL 使用 redactor，并将 embed URL/nonce 状态抽到 `useWorkspaceEmbedUrl`；Tauri capability 将 `main` 与 `prefill`、`workspace-import-picker` 分层；外部 iframe 承载收紧为内置 Kimi origin 与 `VITE_KIMI_EXTERNAL_FRAME_ALLOWLIST` 精确 origin；补 capability/URL/nonce 相关测试与文档。
- 修改原因：消除外部 pane 复用 bridge source 触发系统浏览器、状态对象暴露 `#token=`、非主窗口共享 webview 创建权限和任意 https iframe 承载的安全风险。
- 本次已消除的不确定性：确认 tokenized workspace URL 仍必须用于 iframe 导航，但不需要继续从 status 展示对象传递；确认外部 pane 可以通过显式浏览器或应用窗口 fallback 保留任意 URL 打开能力。
- 本次采用的关键假设：外部 iframe 默认只允许 Kimi origin；需要承载其他站点时由构建环境显式配置 `VITE_KIMI_EXTERNAL_FRAME_ALLOWLIST`，不在运行时静默放开。
- 剩余不确定性：完整 Rust command module 拆分仍未执行；status/log append 的完全事件驱动仍需后续设计；Rust 单元测试运行阶段在本机仍受既有 Windows `STATUS_ENTRYPOINT_NOT_FOUND` 风险约束，因此继续使用 `cargo test --no-run` 作为本轮 Rust gate。
- 影响范围：Shell workspace iframe 导航、外链 bridge 消息契约、LoadingView URL 展示、外部 pane URL 校验、Tauri command registry 与 capability 文件；不改变 Bridge API、数据库 schema、Kimi server API 或安装包入口。
- 验证方式：`pnpm -C apps/kimi-shell verify` 通过；`pnpm -C apps/kimi-shell test` 通过；`apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit` 通过；`pnpm -C apps/kimi-shell check:nfr:security` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过；`cargo test --no-run` 通过；`go test ./...` 通过；`go vet ./...` 通过；`git diff --check` 通过（仅 CRLF 提示）。
- 是否更新 README：是，记录 capability 分层、外部 iframe allowlist 与 workspace URL 展示脱敏约定。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 embed URL/status redaction、capability 分层和外部 iframe allowlist 当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未引入新依赖、migration 或不可逆技术路线。
- 后续注意事项：若后续确需更多默认 iframe origin，应通过产品边界评审后扩展 allowlist，而不是恢复 `https:` 泛化承载。

## 2026-07-08-00 - 拆出 command 注册表与 Workspace Import 控制器

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-09/F-10 的结构治理项。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/src/app`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：将 Tauri `generate_handler!` 注册表从 `lib.rs` 移到 `src-tauri/src/commands.rs` 并按运行域分组；`check_command_registry.mjs` 改读注册表模块，并为每个 domain 校验 owner、窗口 capability 与用途说明；将 Workspace Import picker 的状态、刷新、事件 payload 处理和选择/取消 handler 从 `useShellController.ts` 抽到 `useWorkspaceImportController.ts`。
- 修改原因：降低 `lib.rs` 注册面和 `useShellController.ts` 状态面，补齐报告要求的 command owner/capability/purpose 可执行门禁，并继续拆分巨型 hook。
- 本次已消除的不确定性：确认 Tauri handler 可以在独立模块中以具体 `Wry` runtime 生成；确认 Workspace Import 依赖足够独立，可以从主 controller 移出而不改变 UI props。
- 本次采用的关键假设：command 实现函数暂不搬离 `lib.rs`，先把注册面和门禁拆出；Bridge/Skill 仍在主 controller 编排，后续可继续按域拆分。
- 剩余不确定性：F-09 的 Bridge/Skill 管理仍未拆成独立 controller；F-10 的 command 实现函数仍未迁移到 `commands/runtime.rs` 等实现模块，当前完成的是注册表和门禁层拆分。
- 影响范围：Tauri command 注册入口、command registry NFR 检查、Workspace Import 前端控制器；不改变 command 名称、输入输出类型、Tauri capability JSON、Bridge API、数据库 schema 或 Kimi server API。
- 验证方式：`pnpm -C apps/kimi-shell verify` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过；`cargo test --no-run` 通过；`go test ./...` 通过；`go vet ./...` 通过。
- 是否更新 README：是，记录 command 注册表和前端 controller 拆分入口。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 command 注册表、command metadata gate 和前端 controller 当前拆分事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变序列化契约或引入新依赖。
- 后续注意事项：继续 F-09 时优先拆 Bridge 或 Skill controller；继续 F-10 时再迁移 command 实现函数到域模块，迁移时保持 `commands.rs` 注册名和 registry gate 不变。

## 2026-07-08-00 - 拆出 Bridge 运行态控制器

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-09 巨型前端 controller 拆分。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `useBridgeRuntimeController.ts`，承载 Bridge settings/status/sessions/bindings/approvals/log tail/secrets mask 状态与刷新命令；`useShellController.ts` 改为复用该 hook，复杂的 Bridge 保存、重启与 onboarding handler 暂留主 controller。
- 修改原因：把轮询所依赖的 Bridge 运行态和刷新入口从主壳层编排中移出，降低 `useShellController.ts` 状态面，同时避免一次迁移 Bridge 写操作造成额外行为风险。
- 本次已消除的不确定性：确认 Bridge 轮询只依赖刷新回调和 `bridgeStatus.state`，可由独立 hook 提供；确认主 controller 后续业务 handler 可继续使用同名 state/setter，不需要改 UI props。
- 本次采用的关键假设：Bridge 写操作、connector onboarding 和 Skill 管理后续再按更小分片迁移；本轮只移动无 UI 语义变化的运行态刷新逻辑。
- 剩余不确定性：F-09 仍剩 Bridge 写操作与 Skill 管理拆分；F-10 command 实现函数仍未迁移到按域实现模块。
- 影响范围：Shell 前端 controller 组织；不改变 Tauri command 名称、Bridge API、前端 props、数据库 schema 或 Kimi server API。
- 验证方式：`apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit` 通过。
- 是否更新 README：是，记录 Bridge 运行态 controller 入口。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 Bridge 运行态刷新已拆分的当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期架构约定或不可逆技术路线。
- 后续注意事项：下一步优先拆 Skill controller 或继续把 Bridge 写操作迁移到独立 hook；迁移时保持 control center props 与现有 handler 名称稳定。

## 2026-07-08-00 - 拆出 install command 实现模块

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-10 Rust command module 拆分。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `src-tauri/src/commands/install.rs`，迁移 install 域和 install compat 域 command 实现；`commands.rs` 注册表改为 `install::...` 路径；command registry 检查同步识别模块化 install commands，并继续校验 compat command 退出登记。
- 修改原因：让 `lib.rs` 不再承载 install command 实现，按报告建议推进 command owner/capability/purpose 可审计的域模块结构。
- 本次已消除的不确定性：确认 Tauri `generate_handler!` 可直接注册 `commands::install` 子模块函数；确认命令名保持不变，前端 invoke 调用不需要修改。
- 本次采用的关键假设：install 域转发逻辑独立、风险最低，先作为 Rust command implementation 拆分的第一片；runtime/bridge/skills 等更耦合命令后续按域逐步迁移。
- 剩余不确定性：F-10 仍剩 runtime、bridge、skills、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Tauri command 实现文件组织与 registry gate；不改变 command 名称、输入输出类型、capability JSON、安装任务行为或前端调用。
- 验证方式：`cargo check` 通过；`node apps/kimi-shell/scripts/check_command_registry.mjs` 通过；`cargo clippy -- -D warnings` 通过。
- 是否更新 README：是，记录 install command 实现模块。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 install command 实现模块当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期接口契约或技术路线。
- 后续注意事项：继续 F-10 时优先选择低耦合 diagnostics 或 onboarding/auth command，再处理 runtime/bridge/skills 大域。

## 2026-07-08-00 - 拆出 context menu 与 workspace import command 模块

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，继续推进 F-10 Rust command module 拆分。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `src-tauri/src/commands/context_menu.rs` 和 `src-tauri/src/commands/workspace_import.rs`，迁移对应 command 包装函数；`commands.rs` 与 command registry 检查改为模块路径。
- 修改原因：继续把低耦合 command 实现从 `lib.rs` 移到按域子模块，降低注册与实现混杂的审计成本。
- 本次已消除的不确定性：确认 context menu 与 workspace import command 都只是已有域模块的薄包装，可直接迁移到 `commands/` 子模块而不影响命令名。
- 本次采用的关键假设：先迁移低耦合 command，避免在同一轮改动 runtime/bridge/skills 大域带来额外回归风险。
- 剩余不确定性：F-10 仍剩 runtime、bridge、skills、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Tauri command 实现文件组织与 registry gate；不改变 command 名称、输入输出类型、capability JSON、右键菜单行为或 workspace import 行为。
- 验证方式：`node apps/kimi-shell/scripts/check_command_registry.mjs` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过。
- 是否更新 README：是，记录 context menu 与 workspace import command 实现模块。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充当前已拆出的 command 域模块。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期接口契约或技术路线。
- 后续注意事项：继续 F-10 时优先处理 diagnostics 或 onboarding/auth，再评估 runtime/bridge 的更大迁移。

## 2026-07-08-00 - 拆出 skills command 模块

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-10 中的 Skill command 域拆分。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `src-tauri/src/commands/skills.rs`，迁移 Skill discovery、import、trust、projection、recommendation、workspace skill 管理等 command 包装函数；`commands.rs` 与 command registry 检查改为 `skills::...` 路径。
- 修改原因：把 Skill command 实现从 `lib.rs` 移入独立域模块，进一步降低 `lib.rs` 注册面和审计负担。
- 本次已消除的不确定性：确认 Skill command 都是 `skill_center` 域服务薄包装，可独立迁移且不改变前端 invoke 命令名。
- 本次采用的关键假设：Skill command 模块只做现有转发，不在本轮重构 `skill_center` 内部逻辑或权限模型。
- 剩余不确定性：F-10 仍剩 runtime、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Tauri command 实现文件组织与 registry gate；不改变 command 名称、输入输出类型、capability JSON 或 Skill 行为。
- 验证方式：`node apps/kimi-shell/scripts/check_command_registry.mjs` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过。
- 是否更新 README：是，记录 skills command 实现模块。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 skills command 域模块。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期接口契约或技术路线。
- 后续注意事项：继续 F-10 时优先处理 diagnostics 或 onboarding/auth，再处理 runtime 大域。

## 2026-07-08-00 - 拆出 Bridge command 模块

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-10 中的 Bridge command 域拆分。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `src-tauri/src/commands/bridge.rs`，迁移 Bridge settings/status/runtime control/bindings/sessions/approvals/log/secrets/connector onboarding command 实现和 `sync_idle_bridge_runtime` helper；`commands.rs` 与 command registry 检查改为 `bridge::...` 路径。
- 修改原因：把 Bridge command 实现从 `lib.rs` 移入独立域模块，满足报告要求的 command 域拆分和可审计 owner/capability/purpose 结构。
- 本次已消除的不确定性：确认 Bridge command 可整体迁移，`sync_idle_bridge_runtime` 只服务 Bridge settings 保存路径，可随 Bridge command 模块移动。
- 本次采用的关键假设：Bridge command 层只搬移现有 manager/onboarding/workspace_session 调用，不在本轮改变 Bridge runtime、approval 或 onboarding 行为。
- 剩余不确定性：F-10 仍剩 runtime、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Tauri command 实现文件组织与 registry gate；不改变 command 名称、输入输出类型、capability JSON 或 Bridge 行为。
- 验证方式：`node apps/kimi-shell/scripts/check_command_registry.mjs` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过。
- 是否更新 README：是，记录 Bridge command 实现模块。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 Bridge command 域模块。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期接口契约或技术路线。
- 后续注意事项：继续 F-10 时优先处理 diagnostics 或 onboarding/auth，再评估 runtime command 的更大迁移。

## 2026-07-08-00 - 拆出 workspace grid command 模块

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-10 中的 workspace/session command 域拆分。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `src-tauri/src/commands/workspace_grid.rs`，迁移 `grid_list_sessions` 与 `grid_create_session` command 实现；`commands.rs` 与 command registry 检查改为 `workspace_grid::...` 路径。
- 修改原因：继续把 workspace/session command 实现从 `lib.rs` 移入独立域模块，降低 runtime command 大块之外的噪音。
- 本次已消除的不确定性：确认 Workspace Grid command 只依赖 `workspace_session`，可直接迁移且不改变命令名。
- 本次采用的关键假设：Grid command 仍保持当前最小 session create/list 行为，不在本轮调整 session API 或前端调用。
- 剩余不确定性：F-10 仍剩 runtime、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Tauri command 实现文件组织与 registry gate；不改变 command 名称、输入输出类型、capability JSON 或 Workspace Grid 行为。
- 验证方式：`node apps/kimi-shell/scripts/check_command_registry.mjs` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过。
- 是否更新 README：是，记录 workspace grid command 实现模块。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 workspace grid command 域模块。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期接口契约或技术路线。
- 后续注意事项：继续 F-10 时优先处理 diagnostics 或 onboarding/auth，再评估 runtime command 的更大迁移。

## 2026-07-08-00 - 拆出 Skill Center 状态控制器

- 用户目标：继续根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复，推进 F-09 巨型前端 controller 拆分。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：新增 `useSkillCenterController.ts`，承载 Skill Center state、selection、discovery/workspace skill state 和刷新函数；`useShellController.ts` 改为复用该 hook，Skill apply/import/remove 等用户动作 handler 暂留主 controller。
- 修改原因：把 Skill Center 的状态面与刷新逻辑从主壳层编排中移出，降低 `useShellController.ts` 复杂度，同时避免一次迁移用户动作 handler 带来额外行为风险。
- 本次已消除的不确定性：确认 Skill Center 刷新逻辑主要依赖 `skillCenterService` 和 `setActionError`，可以独立成 hook；确认主 controller 后续 handler 可继续使用 hook 暴露的同名 state/setter。
- 本次采用的关键假设：Skill 动作 handler 后续再按更小分片迁移；本轮只移动无 UI 语义变化的状态和刷新逻辑。
- 剩余不确定性：F-09 仍剩 Bridge 写操作与 Skill 动作 handler 未拆出主 controller；F-10 仍剩 runtime、diagnostics、onboarding/auth 等 command 实现未迁出 `lib.rs`。
- 影响范围：Shell 前端 controller 组织；不改变前端 props、Tauri command、Skill 行为、Bridge API、数据库 schema 或 Kimi server API。
- 验证方式：`apps/kimi-shell/node_modules/.bin/tsc.cmd --noEmit` 通过。
- 是否更新 README：是，记录 Skill Center controller 入口。
- 是否更新 `.ai/architecture`：是，`current-state.md` 补充 Skill Center 状态/刷新已拆分的当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变长期架构约定或不可逆技术路线。
- 后续注意事项：下一步可继续把 Skill 动作 handler 或 Bridge 写操作迁移到独立 hook；迁移时保持 control center props 与现有 handler 名称稳定。

## 2026-07-08-01 - 验证代码审查报告修复集

- 用户目标：根据 `docs/rebuild/kimi_app_code_review_report.md` 的问题清单逐项修复并验证。
- 涉及目录：`apps/kimi-shell`、`apps/kimi-im-bridge`、`.github/workflows`、`.ai/architecture`。
- 修改内容：完成 F-01/F-02/F-03/F-04/F-05/F-06/F-07/F-08/F-11/F-12/F-13/F-14/F-15/F-16/F-17/F-18/F-19 的实现级修复；F-09 通过拆出 install/polling/Bridge runtime/Skill Center/workspace embed/workspace import controllers 降低主 hook 状态面；F-10 通过 command registry、metadata gate、compat command 退出登记，以及 bridge/install/skills/workspace grid/context menu/workspace import command 子模块降低 `lib.rs` 注册与实现面。
- 修改原因：对报告中安全、性能、正确性、运维和治理问题形成可执行修复闭环，并用本地/CI 可复用门禁守住关键边界。
- 本次已消除的不确定性：确认 Shell verify、Rust check/clippy/test compile、Go test/vet、command/capability/resource 安全检查均能通过；确认 command registry gate 继续读取架构文档中的 compat command 退出登记。
- 本次采用的关键假设：F-09/F-10 的本轮验收边界是拆出报告点名的高风险/高变更域和新增可执行审计门禁；runtime、diagnostics、onboarding/auth 仍可继续做更细模块迁移，但不阻塞本轮问题清单修复。
- 剩余不确定性：真实 Windows/Tauri 安装包环境下的 Telegram/Feishu/Weixin 凭证 smoke 与安装包内容审计仍需手工环境验证；runtime/window/prefill 更细 controller/command 拆分可作为后续维护性优化。
- 影响范围：Shell 前端状态组织、Workspace Grid 行为、安全 bridge 与 URL 展示、Tauri capability/resource/command 治理、Rust command 模块组织、Go Bridge Admin/runtime/logger/store 防御性逻辑、CI/local verification gate。
- 验证方式：`pnpm verify` 通过；`cargo check` 通过；`cargo clippy -- -D warnings` 通过；`cargo test --no-run` 通过；`go test ./...` 通过；`go vet ./...` 通过；`pnpm check:nfr:security` 通过；`git diff --check` 通过（仅 CRLF 提示）。
- 是否更新 README：是，记录 controller 与 command 模块当前入口。
- 是否更新 `.ai/architecture`：是，`current-state.md` 更新 security、controller、command registry、compat command 当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未引入新依赖、migration 或不可逆技术路线。
- 后续注意事项：若继续治理，优先做真实安装包/凭证 smoke，再评估 runtime/window/prefill 更细拆分。
