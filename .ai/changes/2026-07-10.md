## 2026-07-10-10 - 极简化 Kimi Code 安装展开区

- 用户目标：按 `DESIGN.md` 收敛安装/升级 Kimi Code 的展开详情，移除详情区重复终端，统一使用侧栏终端展示安装日志。
- 涉及目录：`apps/kimi-shell/src`、`.ai/changes`。
- 修改内容：用紧凑 definition list 替换重复标题、状态卡和主操作卡；按缺失条件显示 Git/Node.js 修复动作；增加检测、任务、失败和重启状态；将来源、镜像、PowerShell、路径与卸载收进默认折叠的高级选项；删除详情终端、复制操作、命令 catalog 传递和废弃 CSS；修正侧栏安装日志对象渲染；增加聚焦组件测试。
- 修改原因：外层安装栏已经承担标题、状态和主操作，展开区重复展示造成层级混乱；侧栏已有唯一安装终端，无需第二套日志界面。
- 本次已消除的不确定性：确认安装日志元素为结构化 `InstallLogChunk`，侧栏需显式映射 stream/text；确认环境探测没有版本字段，因此界面只展示就绪状态、最低版本要求和已探测路径。
- 本次采用的关键假设：安装任务运行中自动展开安装栏；终态允许用户手动折叠；高级选项默认关闭；真实版本展示不扩展 Tauri 契约。
- 剩余不确定性：纯 Web 预览会停在依赖 Tauri invoke 的启动页，真实 Tauri 窗口的桌面与窄屏视觉仍需手工确认。
- 影响范围：仅控制中心安装/升级前端展示和环境探测 busy 状态；不改变安装任务、Tauri command、session 数据结构或 sidebar 布局。
- 验证方式：`pnpm test`（11 files / 82 tests）；`.\node_modules\.bin\tsc.cmd --noEmit`；`.\node_modules\.bin\vite.cmd build`；`git diff --check`；设计稿与 CSS 静态对照。
- 是否更新 README：否，目录职责、运行方式和公共接口未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次是可逆的局部 UI 变更。
- 后续注意事项：在真实 Tauri 控制中心手工检查 820px 以下按钮触控高度、路径换行和侧栏日志滚动。

## 2026-07-10-11 - 合并外部 IM 通道并统一小助手设置

- 用户目标：将外部 IM 通道合并进小助手设置，并以安装/升级 Kimi Code 的极简设计语言统一右键菜单、API、默认工作目录和 Bridge。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/README.md`、`.ai/changes`。
- 修改内容：移除外部 IM 一级导航与重复概览页；新增第 5 个 Bridge 设置条；统一五项的状态、主操作、定义列表、内联反馈和折叠高级选项；将 Bridge 凭据与运行能力保留在专用任务面；瘦身 API 与 Bridge 任务面的嵌套卡片；轮询改为按 Bridge 任务可见性加载重数据。
- 修改原因：减少控制中心重复入口与卡片层级，并让简单设置和复杂任务保持清晰边界。
- 本次已消除的不确定性：确认现有 Bridge task payload、Tauri command 与数据结构可直接复用；确认 bindings、approvals 和日志只需在运行任务面高频轮询。
- 本次采用的关键假设：设置项保持互斥展开；新建机器人先生成本地草稿，再从连接与凭据任务面保存；sidebar 终端继续作为安装日志唯一出口。
- 剩余不确定性：真实凭据扫码、审批和 Bridge 重启需要在 Tauri 环境使用有效账号手工验证。
- 影响范围：控制中心导航、设置布局、Bridge 前端轮询可见性和任务面视觉；不影响后端接口、持久化结构或 Bridge 协议。
- 验证方式：`pnpm test`、`.\node_modules\.bin\tsc.cmd --noEmit`、`.\node_modules\.bin\vite.cmd build`、`git diff --check`，以及桌面/窄屏静态检查。
- 是否更新 README：是，设置项数量与外部 IM 入口发生变化。
- 是否更新 `.ai/architecture`：否，后端模块与依赖边界未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可逆的前端信息架构调整。
- 后续注意事项：使用真实 Tauri 窗口复核扫码、审批、重启以及 820px 以下操作区布局。

## 2026-07-10-14 - 收敛小助手设置并修复 Git Bash 探测

- 用户目标：按截图精简五个设置项，修复已安装 Git Bash 无法识别，内联 API 与扫码配置，并移除 Telegram 产品入口和数据。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/README.md`、`.ai/architecture`、`.ai/decisions`、`.ai/changes`、`tasks`。
- 修改内容：设置栏删除重复状态徽章；安装高级选项增加 uv/Python repair；Git Bash 增加 PATH、Git 根目录和常见目录探测；右键菜单迁移为中文默认；API、工作目录和右键详情扁平化；微信/飞书扫码行内展开；Telegram 默认、入口、connector 与 secrets 删除。
- 修改原因：消除重复信息和历史品牌残留，让常用配置在当前设置项内完成，并修正探测实现与架构文档不一致的根因。
- 本次已消除的不确定性：确认 Git Bash 实现此前只读取 `KIMI_SHELL_PATH`；确认 uv/Python 任务、API 命令和两套扫码 onboarding 可直接复用；确认 Bridge secrets store 可同时清理 connector-scoped 与 legacy Telegram token。
- 本次采用的关键假设：右键菜单仅迁移已知默认值；用户自定义名称保留；Telegram 清理由用户明确授权且不备份密钥；Go Bridge adapter 保留。
- 剩余不确定性：真实 Windows Explorer 注册表显示、本机 Git Bash 路径和有效微信/飞书账号扫码需要桌面环境手工验证。
- 影响范围：控制中心设置 UI、Shell 安装探测、AppSettings schema 9、Bridge 初始化配置清理；不改变 Tauri command、Kimi config 白名单或 Bridge wire protocol。
- 验证方式：`tsc --noEmit`；`pnpm test`（12 files / 86 tests）；Vite build；`cargo fmt -- --check`；`cargo check`；`cargo test --no-run`；`git diff --check`。Rust 单测执行受既有 `STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。
- 是否更新 README：是，API 与扫码入口及右键菜单行为改变。
- 是否更新 `.ai/architecture`：是，修正 Git Bash 探测顺序、schema 9 与 Telegram UI 支持事实。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted ADR 记录菜单默认值迁移与 Telegram 数据清理。
- 后续注意事项：在真实 Tauri 控制中心验证窄屏按钮顺序、二维码展开/取消、菜单自愈和 Git Bash 路径展示。

## 2026-07-10-15 - 修复自定义路径 Git Bash 在升级任务中误报缺失

- 用户目标：从根因修复 Kimi Code 升级时无法识别 `D:\Program Files\Git\bin\bash.exe`。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`.ai/changes`、`tasks`。
- 修改内容：在 managed file、inline retry 和 elevated fallback 三条 PowerShell 启动路径统一注入 `kimi_locator::locate_shell_path()` 探测到的 `KIMI_SHELL_PATH`；增加最小命令环境回归测试。
- 修改原因：安装面板使用完整 locator 探测，而安装/升级脚本只读取进程环境变量和 C 盘默认路径，导致界面显示已配置但任务预检失败。
- 本次已消除的不确定性：确认自定义盘符 Git Bash 可由 PATH 中的 `git.exe` 推导；确认 PowerShell 预检已优先读取 `KIMI_SHELL_PATH`，无需维护第三套路径探测。
- 本次采用的关键假设：提权 `Start-Process` 继承外层 PowerShell 环境。
- 剩余不确定性：需要在真实 Tauri 窗口再次执行升级任务完成 G3 手工验证。
- 影响范围：所有安装任务 PowerShell 子进程的 `KIMI_SHELL_PATH` 环境；不改变任务命令、公共接口或持久化配置。
- 验证方式：`cargo fmt --manifest-path apps\kimi-shell\src-tauri\Cargo.toml -- --check`、`cargo check --manifest-path apps\kimi-shell\src-tauri\Cargo.toml` 通过；目标测试编译通过但执行受本机既有 `0xc0000139 (STATUS_ENTRYPOINT_NOT_FOUND)` 阻塞。
- 是否更新 README：否，目录职责、公共接口和运行方式未变化。
- 是否更新 `.ai/architecture`：否，现有架构事实已经声明安装与运行链路复用探测到的 `KIMI_SHELL_PATH`。
- 是否需要新增或更新 `.ai/decisions`：否，本次是修正实现与既有架构事实不一致的局部 bugfix。
- 后续注意事项：从托盘完全退出并重启小助手后，在未手动设置 `KIMI_SHELL_PATH` 的条件下验证升级成功。

## 2026-07-10-16 - 设置主操作按钮右对齐

- 用户目标：将小助手设置五行中的黑色主操作按钮统一靠右对齐。
- 涉及目录：`apps/kimi-shell/src`、`.ai/changes`、`tasks`。
- 修改内容：设置列表与卡片头显式占满可用宽度；桌面 action 容器同时使用 grid 和 flex 右对齐；`820px` 以下恢复单列拉伸并左对齐按钮。
- 修改原因：避免操作按钮停在卡片中部，统一五行的视觉扫描线，同时防止桌面规则影响窄屏堆叠。
- 本次已消除的不确定性：确认按钮共用 `cc-settings-bar-head > cc-image-row-actions`，无需修改 React 组件或按钮结构。
- 本次采用的关键假设：右对齐仅针对桌面布局，窄屏继续按既有设计上下堆叠。
- 剩余不确定性：纯 Web 预览受 Tauri IPC 限制停在启动页，真实控制中心截图需在 Tauri 窗口复核。
- 影响范围：仅小助手设置卡片头部布局；不改变按钮功能、文案、尺寸、颜色或数据流。
- 验证方式：`tsc --noEmit`、Vite production build、`git diff --check` 通过；生产 CSS 已包含桌面右对齐与 `820px` 响应式覆盖。
- 是否更新 README：否，目录职责和公共行为未变化。
- 是否更新 `.ai/architecture`：否，不涉及架构事实。
- 是否需要新增或更新 `.ai/decisions`：否，本次是局部可逆样式修复。
- 后续注意事项：重启 Tauri 开发窗口或桌面应用后，对照截图确认五个按钮右边缘一致。

## 2026-07-10-17 - 卡片化 Skill Center 与 WorkspaceHub

- 用户目标：按确认的设计稿将 Skill Center 与 WorkspaceHub 收敛为“小助手设置”风格的卡片主页和文件目录二级页。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/scripts`、`apps/kimi-shell/README.md`、`.ai/architecture`、`.ai/changes`。
- 修改内容：Skill 与 WorkspaceHub 主页面改为紧凑筛选和双列卡片目录；详情与目录互斥显示；Skill 添加入口合并为菜单；Harness、Skill 和已注册工作区统一复用文件树预览；新增按已注册 workspace id 读取文件列表和内容的只读 Tauri commands；卡片改为真实按钮语义并补齐局部滚动和窄屏布局。
- 修改原因：消除筛选按钮墙、主页下方重复详情和超长页面滚动，同时让对象浏览与文件审阅保持同一交互模型。
- 本次已消除的不确定性：确认 Skill/Harness 已有通用安全文件读取逻辑可供注册工作区复用；确认无需新增路由、数据模型或依赖。
- 本次采用的关键假设：工作区目标继续保留在 Skill Center 的次级入口；隐藏目录、`.git`、`node_modules` 和符号链接不在控制中心预览；真实文件仍可通过“打开目录”访问。
- 剩余不确定性：真实 Tauri 窗口的字体渲染、系统缩放和本机大目录性能需桌面手工复核。
- 影响范围：控制中心 Skill/WorkspaceHub 信息架构、共享目录卡片语义，以及注册工作区的只读文件预览命令；不改变 Skill、Harness、Workspace 存储结构或写入行为。
- 验证方式：相关 Vitest、TypeScript、Vite build、Rust 测试编译、command registry、安全检查与 `git diff --check`；纯 Web 预览因缺少 Tauri `invoke` 停在启动页，三档真实页面截图留待桌面窗口复核。
- 是否更新 README：是，新增注册工作区只读文件预览公共行为。
- 是否更新 `.ai/architecture`：是，记录新的 workspace 文件读取边界。
- 是否需要新增或更新 `.ai/decisions`：否，复用现有安全读取模式且没有不可逆架构决策。
- 后续注意事项：在真实 Tauri 控制中心验证大目录、二进制文件、缩放、筛选弹层和键盘焦点顺序。

## 2026-07-10-18 - 修复 npm 与 pnpm 双安装导致的伪升级

- 用户目标：让 Kimi Code 升级器更新当前实际命中的安装来源，并在 npm 升级后明确检查 npm 生成的 `kimi.cmd`。
- 涉及目录：`apps/kimi-shell/src-tauri/src/install_manager`、`apps/kimi-shell/README.md`、`.ai/architecture`、`.ai/changes`。
- 修改内容：升级在修改 PATH 前捕获当前 `kimi` shim；使用 `npm prefix -g` 与 `pnpm bin --global` 判断来源；pnpm 来源改用 `pnpm add --global`，npm 来源继续使用 npm，并精确验证对应全局 bin 中的 `kimi.cmd`；无法归属或同时归属 npm/pnpm 的来源直接报错；首次 npm 安装也改为精确验证 npm shim。
- 修改原因：npm 与 pnpm 使用独立的全局 prefix、包存储和 shim 目录，原升级器固定更新 npm，却通过 PATH 复查 pnpm 旧 shim，导致 npm 已更新但界面仍显示旧版本。
- 本次已消除的不确定性：本机 pnpm shim 为 `0.20.3`，npm shim 已是 `0.23.5`；PATH 优先解析 pnpm，确认升级日志前后相同版本不是 npm 安装失败。
- 本次采用的关键假设：首次安装仍使用 npm；升级只支持能够明确归属 npm 或 pnpm 的活动 shim，不自动删除或迁移未知安装。
- 剩余不确定性：需要从桌面控制中心实际执行一次 pnpm 来源升级，确认真实网络安装和重启后的 locator 均命中新版本。
- 影响范围：仅 Kimi Code 升级任务的包管理器选择与升级后验证；不改变首次安装、卸载、持久化结构或公共 Tauri command。
- 验证方式：生成命令回归测试、当前机器只读来源识别、Rust 格式/编译/测试编译与 `git diff --check`。
- 是否更新 README：是，安装与升级的包管理器规则发生变化。
- 是否更新 `.ai/architecture`：是，更新安装主链路当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，这是对升级来源一致性的局部修复。
- 后续注意事项：若未来支持独立二进制来源，应为该来源提供明确升级器，不能回退为安装到 npm 后继续执行旧 shim。
