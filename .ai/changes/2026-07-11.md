## 2026-07-11-10 - 修复小助手设置内容宽度

- 用户目标：让小助手设置 bar 的内部内容横向占满可用宽度。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：为 `.cc-settings-bar` 声明单列满宽 Grid 轨道。
- 修改原因：复用 `.cc-image-row` 后残留的主轴分布使隐式 Grid 列按内容收缩。
- 本次已消除的不确定性：确认 bar 外层宽度正常，问题位于内部 Grid 轨道。
- 本次采用的关键假设：保留设计系统规定的 1120px 详情内容上限。
- 剩余不确定性：无。
- 影响范围：仅控制中心小助手设置条目的头部和展开详情布局。
- 验证方式：`tsc --noEmit` 与 `pnpm build` 已通过；桌面端需确认操作按钮右对齐且窄屏单列布局不变。
- 是否更新 README：否，目录职责和使用方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：无。

## 2026-07-11-10 - 修复 API 折叠异常并增加日志入口

- 用户目标：修复控制中心渲染异常，并在“外部 IM 通道”下方增加日志 bar 和详情。
- 涉及目录：`apps/kimi-shell/src/features/control-center`。
- 修改内容：在读取 React toggle 事件时先同步保存 `open`；新增第 06 个“日志”设置 bar，复用现有诊断与 Bridge 日志尾部展示 `app.log`、`backend.log`、`bridge.log`，提供刷新、复制和打开日志目录操作。
- 修改原因：状态更新回调执行时 React 事件的 `currentTarget` 已被清空；日志能力已有但不在小助手设置主路径中。
- 本次已消除的不确定性：确认异常根因位于 `KimiCodeAccessPanel`，并确认三类日志均已有只读、脱敏或既有诊断读取链路。
- 本次采用的关键假设：沿用应用/后端各 60 行、Bridge 80 行的现有日志尾部数量，不增加实时监听。
- 剩余不确定性：尚未在真实 Tauri 窗口中手工确认三张日志代码卡的最终视觉布局。
- 影响范围：控制中心 API 配置折叠项和小助手设置日志入口；Tauri command、日志格式与持久化结构不变。
- 验证方式：`.\\node_modules\\.bin\\tsc.cmd --noEmit` 通过；定向回归测试 2/2 通过；设置临时 `--localstorage-file` 以兼容本机 Node 26 后，`pnpm test` 12 个文件、90 项测试全部通过。未设置该参数时，既有 Workspace Grid 测试因 `window.localStorage` 未定义失败。
- 是否更新 README：否，目录职责和运行方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实和公共契约未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：发布前在 Tauri 应用中展开 API 配置和日志 bar，确认错误边界不再触发并检查三类日志的滚动显示。

## 2026-07-11-10 - 移除 API 配置详情圆角容器

- 用户目标：移除 API 配置详情中的圆角卡片容器，使其符合小助手设置整体 UI。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：删除 API 配置布局中为配置分区重新添加边框、圆角和背景的高优先级覆盖。
- 修改原因：该覆盖与现有无卡片分区样式及设计系统的层级规则冲突。
- 本次已消除的不确定性：确认圆角来自 CSS 优先级覆盖，不需要修改组件结构。
- 本次采用的关键假设：输入框、按钮和测试结果条目仍保留各自必要边界。
- 剩余不确定性：无。
- 影响范围：仅 API 配置详情内的配置与操作分区容器。
- 验证方式：`KimiCodeAccessPanel.test.tsx` 2 项测试通过，`pnpm build` 通过（含 TypeScript 检查）。
- 是否更新 README：否，目录职责和使用方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：无。

## 2026-07-11-10 - 精简目录页默认视图

- 用户目标：精简 WorkspaceHub 默认目录页，并让 Skill 中心默认停在 Skill 列表。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：移除 WorkspaceHub 默认目录页外层卡片、`Harness 与工作区` 标题和类型分段筛选；Skill 刷新时不再无显式目标地自动选中第一条 Skill；补充默认选择策略测试。
- 修改原因：截图标注的 UI 与设计系统的无额外分组容器规则冲突，且 Skill 中心默认详情页不符合用户期望。
- 本次已消除的不确定性：确认 WorkspaceHub 分段筛选只影响前端目录过滤；确认 Skill 默认详情来自 `refreshInstalledSkills` 自动回退到第一条记录。
- 本次采用的关键假设：保留 WorkspaceHub 的搜索、下拉筛选和排序；“Skill 的列表”指卡片目录页。
- 剩余不确定性：尚未在真实 Tauri 窗口中手工确认截图位置的最终视觉。
- 影响范围：仅控制中心 WorkspaceHub 和 Skill 中心默认目录展示；Tauri command、后端 API 和数据结构不变。
- 验证方式：`pnpm -C apps/kimi-shell test` 首次因本机 Node 26 未设置 `--localstorage-file` 触发既有 Workspace Grid localStorage 失败；设置临时 `NODE_OPTIONS=--localstorage-file=<temp>` 后 `pnpm -C apps/kimi-shell test` 13 个文件、92 项测试全部通过；`.\\node_modules\\.bin\\tsc.cmd --noEmit` 通过。
- 是否更新 README：否，目录职责和运行方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：在桌面控制中心手工确认 WorkspaceHub 无外层白框/标题/分段筛选，Skill 中心默认显示卡片列表。

## 2026-07-11-11 - 精简 Skill 详情为文件预览

- 用户目标：将具体 Skill 详情页统一改成文件树/文件内容预览，并筛掉辅助信息分区。
- 涉及目录：`apps/kimi-shell/src/features/skill-center`、`apps/kimi-shell/src/features/directory`。
- 修改内容：已安装 Skill 与待导入 Skill 详情页只保留紧凑 header、必要操作和 `DirectoryFilePreview`；移除最近使用、推荐理由、恢复结果、已应用目录、发现位置、基础信息、manifest 和描述分区；为文件预览增加可隐藏说明卡的选项。
- 修改原因：当前详情页信息噪声过高，用户希望聚焦 `SKILL.md` 等真实文件内容。
- 本次已消除的不确定性：确认筛掉的内容都只在 `SkillCenterPanel` 详情分支渲染，文件预览复用现有只读读取链路即可。
- 本次采用的关键假设：可执行操作保留在 header 按钮/下拉菜单；非操作性辅助信息直接移除而不是折叠隐藏。
- 剩余不确定性：尚未在真实 Tauri 窗口中手工确认 header 按钮在窄屏下的换行效果。
- 影响范围：控制中心 Skill 详情 UI；文件读取服务、Tauri command、Skill 数据结构不变。
- 验证方式：`SkillCenterPanel.detail.test.tsx` 2 项通过；设置临时 `NODE_OPTIONS=--localstorage-file=<temp>` 后 `pnpm -C apps/kimi-shell test` 14 个文件、94 项测试全部通过；`.\\node_modules\\.bin\\tsc.cmd --noEmit` 通过。未设置 localStorage 参数时，既有 Workspace Grid 测试仍因本机 Node 26 `window.localStorage` 缺失失败。
- 是否更新 README：否，目录职责和运行方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：在桌面控制中心打开已安装 Skill 和待导入 Skill，确认详情页只显示 header 与文件预览。

## 2026-07-11-11 - 调整 Skill 详情 Header 与文件预览布局

- 用户目标：修复进入 Skill 中心仍自动打开第一条 Skill，并把 Skill 详情 header 提升到页面顶部；文件预览占满详情容器，预览/源码/复制按钮进入文件内容区。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/features/skill-center`、`apps/kimi-shell/src/features/directory`。
- 修改内容：`refreshSkillCenterState` 和 discovery 刷新不再无显式 id 时自动选中条目；打开 Skill 中心时清空详情选择；详情态顶部 header 改由 `ControlCenterView` 渲染返回、Skill 名、状态和操作；`SkillCenterPanel` 详情只保留 scripts 提示和文件预览；`DirectoryFilePreview` 在无说明卡模式下把预览/源码/复制按钮放入文件内容 header；补充选择 helper 和文件预览布局测试。
- 修改原因：旧状态回退仍会保留或选中详情；内部二级 header 与用户期望的单一顶部 header 冲突；工具按钮位于文件内容外导致操作归属不清晰。
- 本次已消除的不确定性：确认自动详情来自无参数刷新回退和 discovery 默认第一条；确认文件预览按钮只需移动渲染位置，复制逻辑仍使用当前选中文件。
- 本次采用的关键假设：刷新/重进 Skill 中心默认回到列表页；从 WorkspaceHub、安装、导入等显式 id 路径仍进入对应详情；scripts 风险提示继续保留。
- 剩余不确定性：尚未在真实 Tauri 窗口中手工确认顶部详情 header 在窄屏下的最终换行效果。
- 影响范围：控制中心 Skill 中心前端状态和布局；Tauri command、后端 API、Skill 数据结构不变。
- 验证方式：`.\\node_modules\\.bin\\tsc.cmd --noEmit` 通过；定向 Vitest 3 个文件、17 项测试通过；设置临时 `NODE_OPTIONS=--localstorage-file=<temp>` 后 `pnpm -C apps/kimi-shell test` 14 个文件、97 项测试全部通过；`git diff --check` 无空白错误，仅输出既有 CRLF 提示。
- 是否更新 README：否，目录职责和使用方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实和公共契约未变化。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：在桌面控制中心确认进入 Skill 中心默认是列表，点击已安装和待导入 Skill 后顶部 header 替换为对应 Skill 信息，文件树/文件内容区域占满 header 下方。
