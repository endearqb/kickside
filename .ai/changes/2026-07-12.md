## 2026-07-12-10 - Explorer 打开独立 Session 与 Pane Shelf

- 用户目标：核查并应用 Explorer 右键打开链路审查包。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：持久化右键菜单启用意图；清理重叠 `AllFilesystemObjects` 键并通知 Explorer；把“移动”文案迁移为“复制”；Explorer 打开改为有界串行队列创建独立 session；session payload 增加 pane disposition；Grid 支持六个可见、十二个总 pane 与 Pane Shelf。
- 修改原因：原链路会在热启动时重启共享后端、覆盖当前 pane，禁用菜单也会被启动自愈重新启用。
- 本次已消除的不确定性：补丁包 checksum/manifest 完整，但 `0001`–`0004` 是不可直接应用的人工参考；包内 FIFO、上限回退和通用 reducer 存在缺陷，已按当前 HEAD 改为单消费者和现有 Grid 原子 action。
- 本次采用的关键假设：Explorer 打开默认 `new_pane`；六是可见上限，十二是总上限；达到十二时由用户确认替换或取消并转到 Pane Shelf 管理。
- 剩余不确定性：真实 Windows 10/11 Explorer 菜单刷新、多选合并、冷/热启动 PID 与连续快速打开仍需 G3 手工验证。
- 影响范围：AppSettings schema 10、workspace-session additive payload、Explorer 注册表、启动/单实例打开流、Workspace Grid 持久化接受上限与标题栏 UI。
- 验证方式：`cargo check` 通过；`cargo test --no-run` 通过；Rust test binary 运行被本机既有 `STATUS_ENTRYPOINT_NOT_FOUND` blocked；`tsc --noEmit`、`pnpm test`（16 files / 107 tests）和 `pnpm build` 通过。
- 是否更新 README：是，更新根 README 与 `apps/kimi-shell/README.md`。
- 是否更新 `.ai/architecture`：是，更新 Explorer 菜单、独立 session 与 Pane Shelf 当前事实。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted Explorer open pane routing ADR。
- 后续注意事项：发布前按补丁包 `docs/TEST_MATRIX.md` 执行 Windows Explorer G3；不要恢复 `AllFilesystemObjects` 重叠入口，也不要让 `limit_reached` 回退到当前 iframe 导航。

## 2026-07-12-11 - 收口 Explorer 打开链路 Review

- 用户目标：处理 Review 发现的 worker 世代竞态、失败零反馈及低优先级优化项。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`.ai/architecture`、`tasks`。
- 修改内容：worker 运行标志改为 generation owner；请求确定完成前保留队首，旧 worker 只清理自己的 owner；generation 失效时保留请求等待新 runtime；所有 open-request error 发布前拉起主窗口；增加旧 worker 不得清理新 owner 的单测。
- 修改原因：旧世代 worker 可能覆盖新 worker 标志并并发消费，且托盘态创建 session 失败时用户看不到错误。
- 本次已消除的不确定性：确认竞态不仅会乱序，还可能在 generation 切换时弹出后丢失请求；确认错误事件在窗口隐藏时缺少可见反馈。
- 本次采用的关键假设：只有同 generation 的确定成功或确定失败才消费队首；原生 `window.confirm` 是仓库现有确认模式，本轮不引入通用 modal。
- 剩余不确定性：`Stopping` 恢复与退出、升级共用生命周期，未在缺少停止原因契约时自动重启；真实窄窗口行为加入 G3。mutex poisoned 仍按不可恢复运行时处理，由后续 backend ready 或进程重启恢复。
- 影响范围：Explorer workspace bootstrap worker 生命周期、open-request 错误窗口行为。
- 验证方式：`cargo fmt --check`、`cargo check --message-format short`、`cargo test --no-run --message-format short`、`tsc --noEmit`、Grid 28 项测试通过；新增 Rust 目标测试的二进制执行仍被本机既有 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。
- 是否更新 README：否，目录职责和公共用法未变化。
- 是否更新 `.ai/architecture`：是，明确 generation owner 与队首提交语义。
- 是否需要新增或更新 `.ai/decisions`：否，未改变既有 ADR 决策。
- 后续注意事项：G3 增加后端 `Stopping` 期间 Explorer 打开；只有获得明确 stop reason/deferred-start 契约后才实现自动恢复。应用内 modal 可在产品需要非阻塞三选一时复用现有 overlay 样式。

## 2026-07-12-21 - 建立 Pane 当前会话目录映射

- 用户目标：使 Pane header 的目录按钮始终打开 Web 内当前会话的真实工作目录，而不是全局或 pane 缓存目录。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src-tauri`、`.ai`。
- 修改内容：main Webview all-frames 初始化脚本增加 pane session 路由握手；Pane header 只信任对应 iframe 的当前 session；新增 `grid_get_session` 精确查询 `/api/v1/sessions/{id}`；打开前二次确认 session 未变化；移除所有缓存目录回退，并阻止查询期间重复点击。
- 修改原因：Kimi Web 内切换 session 不会更新 Shell 持久化快照，且 `grid_list_sessions` 的 100 条上限不能可靠解析任意当前会话。
- 本次已消除的不确定性：确认当前 Code pane 均由 main Webview 内 iframe 承载，`initialization_script_for_all_frames` 可覆盖 DirectServer iframe；Windows `open_folder` 校验与 Explorer 调用本身无误。
- 本次采用的关键假设：`/sessions/{id}` 是当前主路由，旧 `?session=id` 仅作兼容；未来新增 native Code carrier 前必须提供等价握手。
- 剩余不确定性：真实 Windows Explorer 打开仍需 G3 手工验证。
- 影响范围：Workspace Grid Pane header 目录动作、main iframe 初始化脚本和 main-only Tauri command 注册表。
- 验证方式：`tsc --noEmit`、`pnpm test`（17 files / 116 tests）、`pnpm build`、`pnpm check:nfr:security`（136 commands）、`cargo fmt --check`、`cargo check`、`cargo test --no-run` 通过；Rust 目标测试执行被本机既有 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND` 阻塞；真实双 pane 切换属于 G3。
- 是否更新 README：否，目录职责和公共运行方式未变。
- 是否更新 `.ai/architecture`：是，登记 iframe 当前路由为目录动作权威来源及 `grid_get_session`。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted pane session folder resolution ADR。
- 后续注意事项：不要恢复 `pane.workDir`、`effectiveWorkDir` 或 session 列表缓存回退。

## 2026-07-12-22 - 修复 Windows Explorer 正斜杠路径

- 用户目标：修复 Pane 已解析出正确 Kimi Code session 目录、但 Explorer 实际回退打开 `Documents` 的问题。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager`。
- 修改内容：Windows 文件管理器启动前把路径分隔符统一为反斜杠，并增加最小单测。
- 修改原因：Kimi Code API 返回 `D:/...`，Rust 路径校验可通过，但 `explorer.exe` 对正斜杠参数解析不可靠并可能回退默认目录。
- 本次已消除的不确定性：安装版包含 session bridge；当前 session 精确 API 返回期望目录，错误位于共享 Explorer 启动边界。
- 本次采用的关键假设：传入 `open_folder` 的路径已通过存在性和目录校验；本轮只修正 Windows CLI 表示。
- 剩余不确定性：安装版真机复测。
- 影响范围：Pane、日志目录和 Kimi 配置目录共用的 Windows 文件管理器入口。
- 验证方式：`cargo fmt --check`、`cargo check`、`cargo test --no-run` 通过；目标单测执行被本机既有 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。
- 是否更新 README：否，公共接口和运行方式未变化。
- 是否更新 `.ai/architecture`：否，未改变架构事实。
- 是否需要新增或更新 `.ai/decisions`：否，属于平台边界 bugfix。
- 后续注意事项：不要在各调用方分别替换路径分隔符。

## 2026-07-12-23 - 统一标题栏菜单收起并整理窗格库

- 用户目标：三个标题栏菜单点击应用内其他位置时收起，并把窗格库改为纵向列表，只显示当前 session 路径的末级目录名。
- 涉及目录：`apps/kimi-shell/src/features/window`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src`。
- 修改内容：为布局和浏览器菜单补齐文档级外部点击与 Escape 收起；窗格库覆盖共享三列布局为纵向单列；列表正文使用 `pane.workDir` 的末级目录名，完整路径保留为 tooltip 和无障碍名称。
- 修改原因：前两个菜单缺少窗格库已有的外部点击监听；窗格库错误继承布局选择器的三列 grid。
- 本次已消除的不确定性：确认 `pane.workDir` 会按当前 session id 同步更新，是窗格库路径的正确来源。
- 本次采用的关键假设：用户所说的“目录名”指末级目录名；无需在正文重复完整路径。
- 剩余不确定性：真实 Tauri WebView 中的视觉密度与 tooltip 需手工复核。
- 影响范围：仅标题栏三个浮层的关闭交互与窗格库展示，不改变 Grid 状态或持久化契约。
- 验证方式：`vitest run src/features/window/ShellTitlebar.test.tsx`（5 tests）与 `tsc --noEmit` 通过；`git diff --check` 通过。
- 是否更新 README：否，目录职责和公共接口未变化。
- 是否更新 `.ai/architecture`：否，未改变架构事实。
- 是否需要新增或更新 `.ai/decisions`：否，属于局部交互 bugfix。
- 后续注意事项：若产品要求显示多级路径，再扩展目录名格式，不在本轮增加配置。
