## 2026-07-13-10 - 同步窗格库工作目录与运行期会话

- 用户目标：窗格内切换 session 后，应用标题栏窗格库显示的末级工作目录名同步更新。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/features/workspace`。
- 修改内容：新增不持久化的 `activeSessionId` 运行期状态；将 iframe 会话变化传入 controller，查询对应 session 工作目录并更新 pane；丢弃快速切换产生的过期异步回写；补充 store 测试。
- 修改原因：原有 `pane_session_changed` 只更新组件本地状态，Pane Shelf 订阅的 grid store 未获得当前 session 及其工作目录。
- 本次已消除的不确定性：确认 `pane.sessionId` 仍只负责启动/深链导航及 frame key，运行期会话变化不会触发 iframe 重挂载；确认 Pane Shelf 会随 store 中 `workDir` 更新。
- 本次采用的关键假设：`pane_session_changed` 是 iframe 当前会话的权威运行期事件；目录查询暂时失败时保留上一次目录。
- 剩余不确定性：未进行 Tauri 应用内手工快速切换验证。
- 影响范围：仅 Workspace Grid 前端运行期状态、会话目录同步路径及相关测试；不新增依赖，不改变持久化格式和 Tauri command 契约。
- 验证方式：`apps/kimi-shell/.\node_modules\.bin\tsc.cmd --noEmit`、`pnpm test`、`git diff --check`。
- 是否更新 README：否，目录职责、公共持久化契约和运行方式未变化。
- 是否更新 `.ai/architecture`：否，既有 Workspace Grid 架构边界未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可逆的局部运行期状态修复。
- 后续注意事项：如需发布级确认，在真实 Tauri 窗格内连续切换多个 session，观察 Pane Shelf 目录名及 iframe 是否保持挂载。

## 2026-07-13-11 - 修复 Skill 中心卡片布局

- 用户目标：缩小 Skill 来源 Tab、减少卡片列表底部留白，并让 Skill 标签完整显示在卡片内部。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：为 Skill 来源分段控件增加紧凑同行样式；将 Skill 页面收敛为实际使用的两行网格并减少底部 padding；修正共享目录卡片的 flex 高度链，使标签稳定留在卡片内容流底部。
- 修改原因：通用带说明分段控件使用两行大尺寸布局；Skill 页面存在空的第三行和重复底部间距；目录卡片的百分比最小高度无法可靠约束底部标签。
- 本次已消除的不确定性：确认标签数据与 JSX 已位于卡片内部，问题来自卡片高度布局；确认 Skill 页面只有页头和列表面两个直接子项。
- 本次采用的关键假设：来源切换继续位于搜索框上方左侧；共享目录卡片的 flex 修复同样适用于 WorkspaceHub。
- 剩余不确定性：真实 Tauri/WebView 下的最终视觉效果仍需手工截图确认。
- 影响范围：仅控制中心目录卡片和 Skill 中心局部布局；不改变公共 API、数据结构或交互语义。
- 验证方式：`pnpm test -- DirectoryCardGrid.test.tsx`（实际运行全部 17 个测试文件，共 122 项通过）、`apps/kimi-shell/.\node_modules\.bin\tsc.cmd --noEmit`、`git diff --check`；真实 Tauri/WebView 截图检查未执行。
- 是否更新 README：否，目录职责和公共接口未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可逆的局部样式修复。
- 后续注意事项：发布前检查长标题、长描述、零至两个标签、右上角动作以及 900px 以下单列布局。

## 2026-07-13-12 - 修复 Skill 工作区目标页面

- 用户目标：收紧工作区目标页底部间距，改正工作区切换入口，统一扫描目录 Tab，并为双栏 Skill 清单增加垂直滚动。
- 涉及目录：`apps/kimi-shell/src/features/skill-center`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src`。
- 修改内容：将查看工作区改为扫描工作区选择菜单；复用紧凑分段按钮规格；补齐工作区详情和双栏列表的高度与滚动链；移除 WorkspaceHub 的空第三网格行并将底部 padding 调整为 12px。
- 修改原因：原按钮错误跳转 WorkspaceHub，容器 Tab 使用旧大卡规格，双栏内容缺少受约束的滚动容器，WorkspaceHub 详情预留了无内容的第三行。
- 本次已消除的不确定性：确认现有 `workspaceSkillTargets` 和选择回调足以原地切换；确认其他固定三行网格均有真实 footer。
- 本次采用的关键假设：工作区菜单只显示 `scope === workspace` 的目标，不显示只读用户主目录；900px 以下使用页面自然滚动。
- 剩余不确定性：真实 Tauri/WebView 下的滚动条和菜单浮层视觉仍需手工截图确认。
- 影响范围：控制中心 Skill 工作区目标页和 WorkspaceHub 详情页局部布局；不改变后端命令、扫描数据或持久化状态。
- 验证方式：SkillCenterPanel 定向测试 3 项通过；`pnpm test` 全部 17 个测试文件、123 项通过；`apps/kimi-shell/.\node_modules\.bin\tsc.cmd --noEmit` 与 `git diff --check` 通过；真实 Tauri/WebView 截图检查待执行。
- 是否更新 README：否，目录职责和公共接口未变化。
- 是否更新 `.ai/architecture`：否，架构事实未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可逆的局部交互和样式修复。
- 后续注意事项：发布前检查长工作区名称、空列表、只读目标、双栏独立滚动和窄屏单列滚动。

## 2026-07-13-13 - 统一工作区 Skill 卡片与菜单滚动

- 用户目标：为查看工作区菜单增加滚动条，并让双栏 Skill 卡片使用控制中心统一的固定高度。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：工作区菜单增加受视口约束的最大高度和内部垂直滚动；双栏 Skill 列表使用 72px 固定行高、12px 圆角和垂直居中布局。
- 修改原因：长工作区菜单会超出可视区域；自动网格行会为容纳全部 Skill 而压缩卡片高度，造成文字裁剪和行高不一致。
- 本次已消除的不确定性：确认两个问题均来自现有局部 CSS，无需修改组件数据或交互逻辑。
- 本次采用的关键假设：72px 足以容纳标题、状态和导入按钮，并与控制中心紧凑 row 语言一致。
- 剩余不确定性：真实 Tauri/WebView 下滚动条宽度仍由系统 WebView 样式决定。
- 影响范围：仅 Skill 工作区目标页的工作区菜单和两列 Skill 清单。
- 验证方式：SkillCenterPanel 定向测试、TypeScript 类型检查、`git diff --check`；真实 Tauri/WebView 截图检查待执行。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：手工确认长菜单、26 项以上列表、导入按钮和窄屏单列下的卡片高度。

## 2026-07-13-13 - 修复文件树层级排序与 Markdown 加粗

- 用户目标：让内容页文件树按常规目录层级排列，并正确渲染 Markdown 加粗。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src/features/directory`。
- 修改内容：共享文件枚举改为按路径分段排序，使目录子树连续显示；共享 Markdown 行内解析器增加 `**...**` 与 `__...__` strong 渲染；补充 Rust 和前端回归测试。
- 修改原因：原排序按全局类型把所有目录放在所有文件前；原最小 Markdown 解析器未识别 strong 标记。
- 本次已消除的不确定性：确认问题分别位于共享后端枚举排序与共享前端预览器，一次修复覆盖 Skill Center、WorkspaceHub 和其他复用入口；确认 Mermaid 当前仅显示代码块。
- 本次采用的关键假设：根 `SKILL.md`、`README` 继续优先，根 `LICENSE` 继续置底；Mermaid 本次不引入渲染依赖。
- 剩余不确定性：Rust 测试程序在当前 Windows 环境无法启动，需在可正常运行测试二进制的环境补跑；未进行真实 Tauri/WebView 手工截图验证。
- 影响范围：只改变文件条目顺序和 Markdown strong 展示，不改变 Tauri 数据结构、公共类型或安全策略，不新增依赖。
- 验证方式：目录预览定向 Vitest 12 项通过；TypeScript `tsc --noEmit`、`cargo check`、`cargo fmt --check` 通过；Rust 定向测试已成功编译，但运行时报既有 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`，因此行为测试为 blocked。
- 是否更新 README：否，目录职责、公共接口和运行方式未变化。
- 是否更新 `.ai/architecture`：否，既有架构边界未变化。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可逆的局部 bugfix。
- 后续注意事项：在 CI 或可运行 Rust 测试程序的 Windows 环境补跑 `cargo test list_safe_file_entries_keeps_each_directory_subtree_together`；发布前确认多层 Skill 文件树展开顺序。

## 2026-07-13-14 - 构建并发布 Kimi Sidekick v0.1.11

- 用户目标：提交并推送 `main`，构建 Windows 安装包并发布 GitHub Release；发布说明采用产品化表达。
- 涉及目录：`apps/kimi-shell/docs`、仓库已有本次功能改动及 `.ai/changes`。
- 修改内容：新增 `release-notes-0.1.11.md`；完成 TypeScript、前端测试、Rust 检查、安全 gate 和 Tauri MSI/NSIS 构建。
- 修改原因：为已完成的窗格会话同步、Skill/WorkspaceHub 布局和文件预览修复提供可发布版本与用户可读说明。
- 本次已消除的不确定性：确认版本 `0.1.11` 在 package、Cargo 和 Tauri 配置中一致，两个安装包均已生成。
- 本次采用的关键假设：当前工作区全部未提交改动均属于本次发布范围。
- 剩余不确定性：未执行真实安装后的完整 UI 手工回归；Rust 行为测试仍受当前 Windows 测试运行环境限制。
- 影响范围：版本发布、Windows 安装包和 GitHub Release 资料；不改变公共 API 或持久化格式。
- 验证方式：TypeScript `tsc --noEmit`、`pnpm test`（17 文件/124 项）、`cargo check`、`pnpm check:nfr:security`、`pnpm tauri build`、`git diff --check`。
- 是否更新 README：否；发布资料已按现有目录约定新增 release notes。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：发布后补做安装版启动、升级和 bundled sidecar 的 G3 手工验证。

## 2026-07-13-23 - 修复启动诊断、配置并发保存与设置页闪烁

- 用户目标：核验评审文档中的三个问题并完成系统修复，避免启动失败信息不足、配置保存阻塞或覆盖外部修改，以及诊断路由下设置项无法展开。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：Kimi 子进程 stdout/stderr 改为脱敏后落盘，诊断读取二次脱敏并增加 `server run --help` 失败态契约探测；API 配置 command 改为阻塞任务异步执行并增加可选 opaque revision、提交前复核和 Windows 有界替换重试；统一三个助手设置 section 的渲染、收起和轮询谓词；补充崩溃错误正文、测试和文档。
- 修改原因：原实现会把后端启动横幅中的 token 直接写入日志，启动错误只显示笼统状态；同步配置 I/O 会占用 command 线程且无法识别面板打开后的外部修改；设置页渲染条件与 effect 收起条件不一致。
- 本次已消除的不确定性：确认官方 npm Kimi Code 的 `--version` 只输出版本号，身份探测必须验证实际命令契约；确认 Windows 当前 `fs::rename` 可替换目标，但仍可能受短暂共享占用影响；确认卡片闪烁由 `runtime_center` 被渲染为设置页却被 effect 当作非设置页造成。
- 本次采用的关键假设：旧调用方可省略 revision 并只获得保存窗口内保护；最终 revision 检查与原子替换之间的极小竞态在 Kimi Code 未提供 CAS API 前可接受；配置只对新建或重新打开的会话生效，不自动重启后端。
- 剩余不确定性：当前 Windows 环境的 Rust 测试二进制仍因 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND` 无法执行；真实 Tauri 内的文件占用、双写冲突和日志脱敏手工回归待发布环境验证。
- 影响范围：Kimi 后端生命周期与日志、诊断返回、API 配置 Tauri 序列化契约、控制中心设置状态和相关测试；不新增依赖，不修改配置 TOML schema，不自动重启后端。
- 验证方式：`cargo check`、`cargo test --no-run`、`cargo fmt --all -- --check`、TypeScript `tsc --noEmit`、Vitest 全量 17 文件/127 项、`pnpm check:nfr:security`、`git diff --check`；Rust 定向测试已编译但运行被既有入口点错误阻塞。
- 是否更新 README：是，记录配置 revision 契约与后端日志落盘前脱敏规则。
- 是否更新 `.ai/architecture`：是，更新运行事实和发布前日志/诊断脱敏门禁。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted ADR `2026-07-13-kimi-config-optimistic-revision.md`。
- 后续注意事项：CI 补跑 Rust 行为测试；发布前使用临时 server token/API key 检查 `backend.log`、`app.log`、诊断复制内容和诊断包，并手工验证外部修改 `config.toml` 后保存会保留磁盘版本与前端草稿。
