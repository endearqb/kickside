## 2026-07-14-12 - 接入小助手本体签名自动更新

- 用户目标：让 Kimi 小助手自动检测新版本，并在用户点击后下载和安装更新。
- 涉及目录：`apps/kimi-shell`、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`tasks`。
- 修改内容：接入 Tauri Updater 检测/下载/安装链路和设置页状态；新增 tag 发布 workflow，生成 NSIS/MSI、签名与 `latest.json`；补齐长期决策与发布门禁。
- 修改原因：用 Tauri 原生签名更新能力替代自定义下载器，并将更新资产纳入可重复的 GitHub Release 流程。
- 本次已消除的不确定性：更新 endpoint、签名边界、Windows 安装模式、NSIS/MSI manifest 选择、tag/version 约束和发布所需工具链均已固定。
- 本次采用的关键假设：`0.1.13` 为首个支持 Updater 的版本；legacy Windows manifest 优先 NSIS；用户必须显式确认安装。
- 剩余不确定性：真实 GitHub Release 资产与旧 NSIS/MSI 安装来源的端到端行为需在首次 tag 发布后验证。
- 影响范围：桌面应用设置页、退出生命周期、Tauri 权限/配置、Windows 发布流程；不改变 Kimi Code 自身升级流程。
- 验证方式：Shell TypeScript、Vitest、安全 gate、Rust `cargo check`/`cargo test --no-run`；workflow diff/语法检查；发布前执行 updater G3 矩阵。
- 是否更新 README：是，更新根 README 与 Shell README 的能力、环境和发布入口。
- 是否更新 `.ai/architecture`：是，登记当前更新链路与 G3 验证门。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted 的签名桌面自更新 ADR。
- 后续注意事项：两个签名 Secrets 已配置；不得把签名私钥或密码写入仓库、命令行或日志；密钥丢失会切断已安装客户端的更新信任链；Tauri 签名不替代 Windows Authenticode。

## 2026-07-14-14 - 复用 Kimi 单实例并收口 Doctor 设置布局

- 用户目标：修复 Kimi 0.23.6 已有 Server 导致小助手后端 code 1，并把 Kimi Doctor 改为不遮挡其他设置项的独立折叠 bar。
- 涉及目录：`apps/kimi-shell`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：启动前与启动竞态失败后读取同一 `KIMI_CODE_HOME` 的 server lock，通过本机地址、healthz、server token 和 `/api/v1/auth` 后复用已有实例；新增 `reused_external` 所有权、外部健康监控、准确错误、启动日志边界和历史日志二次脱敏；Doctor 改为第 07 个互斥设置 bar，日志顺延第 08，旧诊断路由自动展开 Doctor，并删除重复满高诊断页。
- 修改原因：Kimi Server 是全局单实例，随机端口不能绕过其 lock；原 UI 在同一滚动容器连续渲染两个 `min-height: 100%` 页面，造成展开详情时的遮挡感和滚动错位。
- 本次已消除的不确定性：确认 lock 的 PID/host/port 是当前实例入口，`server.token` 可用于复用验证，Bridge 不依赖 locator ownership 的具体值，Doctor 成功只代表配置文件检查有效。
- 本次采用的关键假设：未知 lock 字段向前兼容；无法连接的有效 lock 视为陈旧并回到官方启动路径；复用实例由其原启动方负责终止。
- 剩余不确定性：安装版中复用后退出仍保留原 PID、token rotate 后 Web bootstrap URL 刷新，以及 1200px/760px 实机视觉需在 G3 环境补验。
- 影响范围：Kimi 后端启动/停止/监控、runtime locator 与 AppStatus 增量契约、控制中心设置布局、后端日志安全；不新增依赖，不自动执行 `kimi server kill`。
- 验证方式：`cargo fmt --check`、`cargo check`、`cargo test --no-run` 通过；完整 `cargo test` 的测试程序被本机既有 `STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139)` 阻塞，非断言失败；Shell `pnpm test` 18 files/134 tests 通过，`tsc --noEmit` 通过，`git diff --check` 通过。浏览器视觉连接被本机运行时初始化错误阻塞，需 G3 补验 1200px/760px。
- 是否更新 README：是，更新 Shell README 的 8 个设置项和既有 Server 复用行为。
- 是否更新 `.ai/architecture`：是，登记 `reused_external` 所有权、停止边界、Doctor bar 和日志处理。
- 是否需要新增或更新 `.ai/decisions`：是，新增 accepted 的既有 Kimi Server 复用 ADR。
- 后续注意事项：复用实例的“重新连接后端”只解除并重建 Shell 连接，不终止外部进程；强制终止仍需用户显式执行 `kimi server kill`。

## 2026-07-14-15 - 准备 0.1.14 发布候选

- 用户目标：将开发版本同步升级到 `0.1.14`，补齐中文发布说明和发布前验证准备，但不创建 commit、tag、push 或 GitHub Release。
- 涉及目录：`apps/kimi-shell`、`.ai/changes`。
- 修改内容：通过现有 patch bump 脚本将 `package.json`、`Cargo.toml`、`tauri.conf.json` 同步到 `0.1.14`，仅更新 `Cargo.lock` 中 `appskimi-shell` 自身版本，并新增 `docs/release-notes-0.1.14.md`。
- 修改原因：为既有 Kimi Server 复用、Doctor 独立设置 bar、启动日志与脱敏改进建立一致的发布候选元数据和交付说明。
- 本次已消除的不确定性：四处权威版本均为 `0.1.14`；远端当前没有 `v0.1.14`；现有 patch bump 脚本可正确同步三份源版本文件。
- 本次采用的关键假设：`0.1.14` 是当前改动的下一个补丁版本；发布说明沿用中文格式；历史 ADR 中“`0.1.13` 是首个支持 Updater 的版本”保持不变。
- 剩余不确定性：完整 Rust 测试仍需在不受本机 `0xc0000139` 影响的干净 Windows CI 补跑；安装版复用/自有进程 smoke、1200px/760px 视觉检查以及 NSIS/MSI、签名、`latest.json` 资产矩阵尚待发布环境验证。
- 影响范围：应用与安装包版本元数据、发布说明；本次版本准备不新增业务 API、不引入依赖。
- 验证方式：四处版本一致；`pnpm test` 18 files/134 tests、`tsc --noEmit`、`pnpm check:nfr:security`、`cargo fmt --check`、`cargo check`、`cargo test --no-run` 均通过。
- 是否更新 README：否；本次不改变目录职责或运行方式，功能说明已由同日功能变更更新。
- 是否更新 `.ai/architecture`：否；版本元数据不改变架构事实。
- 是否需要新增或更新 `.ai/decisions`：否；不改变既有发布决策和 `0.1.13` 的历史事实。
- 后续注意事项：补齐剩余 G1/G3 与发布环境门禁后，发布 workflow 必须使用精确 tag `v0.1.14`；当前状态只能称为“0.1.14 发布候选已准备”。

## 2026-07-14-16 - 将连接被拒绝的 Kimi lock 识别为过期

- 用户目标：修复更新到 `0.1.14` 后，旧后端已退出但遗留 lock 导致小助手误报“健康检查失败”并停在 Doctor 的问题。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager`、`.ai/changes`。
- 修改内容：复用探测收到本机端口连接失败且没有 HTTP 状态码时，将 lock 归类为 stale 并继续原有托管启动路径；保留超时及 HTTP 4xx/5xx 为不可安全复用；新增连接被拒绝与 HTTP 503 的分类回归断言。
- 修改原因：更新安装在 16:27:02 结束了旧后端，但 Kimi 0.23.6 的 PID 37348/端口 55333 lock 未清除；PID 已不存在且端口明确拒绝连接，原实现仅依赖 `reqwest::Error::is_connect()`，未覆盖该 Windows 错误形态。
- 本次已消除的不确定性：现场 PID 37348 已不存在，55333 的 healthz/openapi 均连接被拒绝；问题是 stale lock 分类遗漏，不是登录、token 或 API 配置错误。
- 本次采用的关键假设：本机探测未收到 HTTP 响应且并非超时时，lock 不足以证明存在可复用服务，应让 Kimi 官方启动命令处理 stale lock。
- 剩余不确定性：安装版从 `0.1.14` 更新到含此修复的后续版本时，仍需补一次真实更新重启 smoke。
- 影响范围：仅影响既有 Kimi Server 的启动前复用分类；不终止外部进程，不改变健康实例及 Bearer 验证路径。
- 验证方式：`cargo fmt --check`、`cargo check`、`cargo test --no-run` 通过；定向测试程序仍被本机既有 `STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139)` 阻塞，非断言失败。
- 是否更新 README：否；不改变公开运行方式。
- 是否更新 `.ai/architecture`：否；这是既有 stale lock 契约的实现修正。
- 是否需要新增或更新 `.ai/decisions`：否；不改变运行时所有权决策。
- 后续注意事项：发布该修复前在干净 Windows CI 运行完整 Rust 测试，并验证 stale lock 后会生成新端口边界且进入 Running。

## 2026-07-14-17 - 准备 0.1.15 stale-lock 修复发布

- 用户目标：将 stale-lock 启动修复作为 `0.1.15` 补丁提交、推送并发布。
- 涉及目录：`apps/kimi-shell`、`.ai/changes`。
- 修改内容：将 `package.json`、`Cargo.toml`、`tauri.conf.json` 和 `Cargo.lock` 同步到 `0.1.15`，新增中文发布说明，并使用现有 tag workflow 生成 Windows 安装包、签名和 updater manifest。
- 修改原因：`0.1.14` 在更新重启后可能把已退出进程留下的 lock 误判为活动但不健康的 Server，需要及时交付补丁。
- 本次已消除的不确定性：`v0.1.15` 在发布前不存在；发布范围仅包含 stale-lock 修复、回归断言、版本元数据和发布文档。
- 本次采用的关键假设：补丁版本不改变公开 API，继续沿用 `vX.Y.Z` tag 发布流程和现有签名 Secrets。
- 剩余不确定性：真实安装更新重启 smoke 仍需在已安装 `0.1.14` 的 Windows 环境验证。
- 影响范围：Kimi 后端启动前复用分类、版本元数据和发布资产。
- 验证方式：发布前顺序运行 Rust 格式、编译和测试目标编译检查；发布后核对 workflow 与 Release 资产矩阵。
- 是否更新 README：否；不改变公开运行方式。
- 是否更新 `.ai/architecture`：否；不改变既有 stale lock 契约。
- 是否需要新增或更新 `.ai/decisions`：否；不改变运行时所有权与发布决策。
- 后续注意事项：发布后验证 `latest.json` 指向 `0.1.15` NSIS 资产，并执行一次 `0.1.14` 到 `0.1.15` 的真实更新重启 smoke。
