## 2026-07-18-22 - Agent Room Phase 0 capability foundation

- 用户目标：严格按 Agent Room v1.1 计划完成 Observer MVP、Forward MVP 与完整 V1 DoD；本次只推进 Phase 0 Gate。
- 涉及目录：`apps/kimi-im-bridge`、`tasks`、`.ai/plans/agent-room-2026-07-18`、`.ai/architecture`。
- 修改内容：新增只读 `runtime-probe` 与机器可读 `RuntimeCapabilities`；新增 loopback/token-file `fake-runtime`，覆盖 Workspace/Session/Prompt、2/6/12 Session WS、per-Session Cursor、重放/重连、重复/乱序、Approval、Abort、epoch/resync 与 Transcript 开关；在 `tasks/todo.md` 写入任务契约与 Phase/AR 顺序。
- 修改原因：在任何 Runtime 契约、schema 或 UI 变更前关闭可安全探测的不确定性，并为 Observer/Coordinator 建立不依赖真实凭据的可重复夹具。
- 本次已消除的不确定性：基线 commit 与工作树范围；Fake Runtime 的 2/6/12 subscription、Cursor、重复/乱序、重连、Approval、Abort、epoch、Transcript 测试入口；只读 Probe 不修改真实 Session 且输出不含 token/token path；官方 Runtime 0.27.0 的真实 2/6/12 hello/ack、per-Session Cursor、Transcript、Session 状态与 installed contract 已核验。
- 本次采用的关键假设：真实 Runtime 不可用或能力需写操作时，报告 `supported=false` 与明确 degradation，不通过猜测或污染用户 Session 得出结论；PLAN §28 优先于 Phase 章节顺序冲突。
- 剩余不确定性：Abort 完成确认时限、Approval Session scope 跨重启属于未执行的写入型探测，已分别按禁用 abort-and-replace 与 one-shot Approval 降级；SPEC 尚未冻结完整 Run/Member/Session 状态枚举；1/2/6 Pane 手工基线待执行。
- 影响范围：仅开发期 probe/harness 和文档；未修改用户数据 schema、既有 Runtime/Bridge 调用链、React、Grid 或发布配置，未暴露 UI。
- 验证方式：`go test -count=1 ./...` 通过；`go vet ./...` 通过；`pnpm -C apps/kimi-shell test` 18 files/134 tests 通过；`pnpm -C apps/kimi-shell build` 通过；Rust 测试完成编译后被既有 `0xc0000139 / STATUS_ENTRYPOINT_NOT_FOUND` 阻断；`go test -race ./...` 因 `CGO_ENABLED=0` blocked。
- 是否更新 README：是，记录两个开发期命令及其安全边界。
- 是否更新 `.ai/architecture`：是，记录 Phase 0 开发夹具与真实 Runtime blocked 状态。
- 是否需要新增或更新 `.ai/decisions`：否；本次未跨越持久化、序列化、CreateMode 或 Grid V2 单向门。
- 后续注意事项：Phase 1 开始前重读治理/架构/Bridge README；按 §28 先做 per-connector WorkDir，再为 CreateMode 建 accepted ADR；未验证的写入型能力不得因进入后续 Phase 而自动升级为支持。

## 2026-07-18-23 - Agent Room AR-100 Connector WorkDir contract

- 用户目标：按 PLAN §28 在 Capability Spike 后先修复 per-connector WorkDir，再进入 Session Create Mode。
- 涉及目录：`apps/kimi-im-bridge/internal/config`、`apps/kimi-im-bridge/internal/app`、Bridge README、Agent Room 计划与架构事实文档。
- 修改内容：Go `ConnectorConfig` 新增 Rust 同名 JSON 契约 `defaultWorkDir` 与 `resetBindingSessionOnStart`；normalization 保留并 trim override；三类 Adapter 统一取 Connector WorkDir，空值回退 Bridge 全局值；新增 4 Connector、JSON round-trip、legacy 和三平台策略测试。
- 修改原因：此前 Shell 已序列化 Connector override，但 Go 静默丢弃并始终向 Adapter 传全局 WorkDir，会导致多 Connector Workspace 串线。
- 本次已消除的不确定性：Rust/Go 字段名与 optional bool 语义；Connector override 的 normalization；Telegram/Feishu/Weixin 的统一 effective WorkDir；旧 settings 无 override 时的行为。
- 本次采用的关键假设：`resetBindingSessionOnStart` 在 Go 暂无启动重绑消费者，因此只做无损读取/序列化，不提前引入未要求的重绑行为。
- 剩余不确定性：Phase 1 的 exact CreateMode、Session 唯一性、External IM Disabled 启动语义与 Server 附件路径仍未实现。
- 影响范围：Bridge 配置解析与 Adapter WorkDir；无 migration、用户数据重写、React/UI、Runtime 写探测或发布配置变化。
- 验证方式：`go test -count=1 ./internal/config ./internal/app` 通过；`go test -count=1 ./...` 通过；`go vet ./...` 通过；race 仍受本机 `CGO_ENABLED=0` 限制。
- 是否更新 README：是；公共 Connector JSON/回退契约发生变化。
- 是否更新 `.ai/architecture`：是；修正 IM Bridge 当前事实。
- 是否需要新增或更新 `.ai/decisions`：否；该字段已由 Rust 现有持久化契约定义，本次仅补齐丢失的 Go 消费端；下一个 CreateMode 单向门必须先建立 accepted ADR。
- 后续注意事项：AR-100 已验证但 Phase 1 Gate 尚未完成；不得据此开放 Observer 或 Forward Dispatch。

## 2026-07-18-24 - Agent Room AR-101 explicit Session Create Mode

- 用户目标：在 WorkDir 修复后建立并实现 exact Session Policy，禁止 Agent Room 隐式复用 Workspace 第一条 Session。
- 涉及目录：`.ai/decisions`、`apps/kimi-im-bridge/internal/runtime`、`internal/providers/runtimeadapter`、Bridge README、Agent Room 计划与架构事实文档。
- 修改内容：新增 Accepted ADR 与 `SessionCreateMode`/`createMode` 增量契约；Server Adapter 实现 `if_missing`、`always`、`resume_exact`、`reuse_latest`，拒绝未知/矛盾输入，exact 校验显式 Workspace；SessionRef 保留实际或已验证的 Workspace/Source；IM Provider 显式使用 `if_missing`，只有该模式允许旧 ID 兼容重绑。
- 修改原因：原只传 Workspace 的路径会选择 `sessions[0]`，无法证明新 Agent/新任务的 Session 隔离；Provider 的无条件失败重绑会破坏未来 exact 语义。
- 本次已消除的不确定性：四模式边界、零值兼容、camelCase JSON、连续强制创建、准确恢复不存在/Workspace mismatch、显式多 Session 复用、兼容重绑的允许范围。
- 本次采用的关键假设：`if_missing` 保留基线的列表首项兼容行为以支持旧 IM；Agent Room 只会使用 `always`/`resume_exact`。ACP/SDK 不具备严格 Server Session 事实，按 ADR 不作为 Agent Room 静默降级。
- 剩余不确定性：AR-102 历史 Binding 唯一性、AR-103 Sidecar Core/Connector 启动语义、AR-104 附件提交仍未完成；Agent Room SessionPolicy 到 CreateMode 的调用方要在 ExecutionService/Room 实现时验证。
- 影响范围：Go Runtime 增量类型、Server Session 解析与现有 IM Provider；无 migration、React/UI、用户 Session 删除、Runtime Prompt 写探测或发布配置变化。
- 验证方式：`go test -count=1 ./internal/runtime ./internal/providers/runtimeadapter` 通过；`go test -count=1 ./...` 通过；`go vet ./...` 通过；测试覆盖 `always` 两次不同 Session、`resume_exact` 缺失/Workspace mismatch、不回绑、`reuse_latest` 多 Session、`if_missing` 重绑及 `createMode` JSON。
- 是否更新 README：是；Runtime Session 创建公共契约发生增量变化。
- 是否更新 `.ai/architecture`：是；记录 Session 创建/恢复的当前事实与降级边界。
- 是否需要新增或更新 `.ai/decisions`：是；新增 Accepted `2026-07-18-session-create-mode.md`，在修改单向门契约前先冻结模式语义。
- 后续注意事项：Phase 1 仍未通过；Observer/Forward 继续关闭。Agent Room 调用方不得使用缺省/`if_missing`，并必须把 SessionPolicy 显式映射为 `always` 或 `resume_exact`。

## 2026-07-18-25 - Agent Room AR-102 IM Session binding isolation

- 用户目标：审计并保持同一 Kimi Session 的单一 IM Binding 所有权，避免跨 Connector/并发写导致上下文串线。
- 涉及目录：`apps/kimi-im-bridge/internal/store`、`internal/binding`、Bridge README、Agent Room 计划与架构事实文档；只读核对本机 `%APPDATA%/com.kimi.shell/bridge.db`。
- 修改内容：Store Create/Rebind 使用带 `NOT EXISTS` 的单条 SQLite 原子 DML；Router 删除易竞态的 ListBindings 预检并复用 Store 根规则；新 IM Binding 的 Server Provider 改为 `always`，既有 Binding Turn 保留 `if_missing`；新增并发 Create、跨 Connector 冲突 Rebind、失败不改写与新 Binding mode 测试。
- 修改原因：原 Router 仅在 Rebind 前做“先查后写”，CreateBinding 和 Store 直接调用可绕过规则，并发操作也存在检查与更新间竞态。
- 本次已消除的不确定性：本机用户库 `user_version=12`、Binding 数 0、重复 Session group 0、跨 Connector 重复 group 0；现有 IM 语义明确为全 Connector 范围的一 Session 一 Binding。
- 本次采用的关键假设：SQLite 单条条件 DML 是 Session 所有权写入的原子边界。未知其他安装可能存在合法历史数据，因此不新增全局唯一索引。
- 剩余不确定性：Agent Room 独立关系表尚未在 AR-300 创建；其他用户历史库是否存在重复不作猜测，未来如升级为唯一索引必须另做 migration 审计/清理策略。
- 影响范围：IM Binding Create/Rebind 数据完整性；未修改 migration/user_version，未读取消息或 secret，未修改本机用户数据库，未触及 React/UI、Runtime 或发布配置。
- 验证方式：只读 SQLite 聚合审计；`go test -count=1 ./internal/store ./internal/binding` 通过；`go test -count=1 ./...` 通过；`go vet ./...` 通过。
- 是否更新 README：是；IM Binding 公共隔离契约从 Router 约定升级为 Store 强制。
- 是否更新 `.ai/architecture`：是；记录表职责与所有权事实。
- 是否需要新增或更新 `.ai/decisions`：否；未改变 schema 或既有“一 Session 一机器人 Binding”决策，只把规则下沉到共享根因。
- 后续注意事项：严格按 PLAN §28 下一步进入 AR-200 ExecutionService，不提前做 AR-103/104、migration、Observer 或 Forward。

## 2026-07-18-26 - Agent Room AR-200 shared ExecutionService

- 用户目标：按 PLAN §28 在 Session 正确性后提取 Agent Room 与外部 IM 共用的安全执行主链，保持 Connector 行为兼容。
- 涉及目录：`apps/kimi-im-bridge/internal/bridgecore`、`internal/providers/runtimeadapter`、`internal/domain`、Bridge README、Agent Room 计划与架构事实文档。
- 修改内容：新增具体 `ExecutionService` 与 Execution target/request/result/event 类型；迁入 Turn Create/Update、accepted/runtime event、Approval、reply/artifact、Session Upsert 与失败 finalize；Orchestrator 保留 Binding、Duplicate 和 Rebind；新增 strict exact 执行位、PromptID 映射/返回及 Approval 的 Room/Member/Agent/Run 内存关联。
- 修改原因：原 `HandleInbound` 把 IM 去重/Binding 与通用执行混在一起，Room 若复制造成两套事件、审批、Session 和失败状态逻辑；Provider 又会在 exact 运行失败时走 IM 兼容重绑，且 PromptID 未进入 Turn 事件/结果。
- 本次已消除的不确定性：Execution/IM 边界；同 EventID 的持久化与 Room projection 交付；Room strict Session 运行不回退；Runtime PromptID 的显式归属；Runtime 错误后的 Turn/Session finalize；真实 Session Rebind 仍由 IM Orchestrator 执行。
- 本次采用的关键假设：只有一个 ExecutionService 实现，按宪法不新增单实现 interface；Room projection sink 与 turn event store 当前是有序双写而非跨表原子事务，后续依靠 EventID 幂等；Approval Room 关联在 migration 前只作为内存字段携带，不序列化、不冒充已持久化。
- 剩余不确定性：Room Approval 关联的持久化列/映射需随 0014–0016 ADR 冻结；Provider 与 ExecutionService 的重复 Session Upsert 仍可能覆盖 Runtime metadata，需在 Agent Room store 阶段合并；Lease/Queue/Busy 尚未实现。
- 影响范围：Bridge 内部执行模块边界与 Runtime Provider 映射；不改 Admin/Tauri JSON、migration/user_version、React/UI、token 路径或发布配置。
- 验证方式：ExecutionService direct tests 覆盖 Room target/Approval/projection/reply/artifact/PromptID 与 Runtime failure；Orchestrator tests 覆盖 Duplicate 和 real Session Rebind；`go test -count=1 ./internal/bridgecore ./internal/providers/runtimeadapter`、三 Adapter tests、`go test -count=1 ./...`、`go vet ./...` 均通过。
- 是否更新 README：是；新增长期共享执行核心并调整模块职责边界。
- 是否更新 `.ai/architecture`：是；记录 Execution/Orchestrator 职责与未持久化边界。
- 是否需要新增或更新 `.ai/decisions`：否；未新增持久化或 Admin/Tauri 序列化契约，Room 关联显式 `json:"-"`；模块边界由已接受 v1.1 PLAN 直接规定。
- 后续注意事项：按 §28 下一步先为 0014–0016 持久化结构建立 Accepted ADR，再实现 AR-300～304 migrations/store；不得提前推进 Lease/Queue、Observer 或 Forward。

## 2026-07-18-27 - Agent Room AR-300～304 persistence core

- 用户目标：按 PLAN §28 在 ExecutionService 后冻结并实现 Agent Room 0014–0016、Store/Service 与对应迁移/CRUD/Event Gate，且不提前开放 Observer 或 Forward。
- 涉及目录：`.ai/decisions`、`apps/kimi-im-bridge/migrations`、`internal/domain`、`internal/store`、`internal/agentroom`、Bridge README、Agent Room PRD/SPEC/PLAN 与架构事实文档。
- 修改内容：新增 Accepted persistence ADR 与 0014–0016，`userVersion` 13→16；持久化 Profile/Room/Member/Message/Run/Event、Approval Link、event compaction watermark、带 epoch 的 Cursor/Session Observation、Pane Projection/Queue；migration 改为单版本事务并校验版本，SQLite Store 单连接；实现 Profile revision/规范化/Workspace/SessionPolicy/RuntimeControl 白名单、三类 Member、Message+多 Run 部分成功、Run 关联/Retry/Abort、Timeline、Event batch/幂等/分页/Cursor/长轮询；通用 Session partial upsert 不再清空 Runtime state/Lease/metadata。
- 修改原因：草案缺 Profile 乐观锁、Approval 跨重启关联、Runtime epoch 与 Session Observation，既有 migration 非事务且 connection-local PRAGMA 不能在默认连接池保证；ExecutionService partial upsert 还会在未来 Lease 运行中清除所有权。
- 本次已消除的不确定性：0014–0016 最终字段/删除规则；Profile 使用整数 revision 而非秒级 updatedAt；Approval 使用独立 Link 且不被 Connector prune 间接级联；Room Event 删除后保留；Cursor invalid/too-old 由 watermark 区分；Abort 未确认使用 `abort_requested` 并禁止 Retry。
- 本次采用的关键假设：V1 预留 compaction watermark 但不主动压缩 Event；Agent Member 的 per-room Session 可在创建时尚未解析，但进入执行前必须解析明确 Workspace/Session；Agent Room 功能仍不挂 Admin/UI，Feature Flag 行为保持关闭。
- 剩余不确定性：AR-201～203 Lease/FIFO Queue/Runtime Busy 机制、AR-305/306 Admin 契约、Observer/Event projection、Rust/Grid/Pane/Forward/Recovery/Workflow/Release 尚未实现；真实 Abort 完成确认与 Approval scope 跨重启仍按 Phase 0 降级。
- 影响范围：Bridge DB 新增隔离表与内部 Go API；旧 migration 未修改，Room 删除不删除 `bridge_sessions`，Connector prune 白名单不含 Agent Room 表；无 React/token/Tauri/发布配置变化。
- 验证方式：Fresh DB、v13 fixture（Connector/Binding/Approval/Turn/Session）、migration 失败回滚与幂等、FK/integrity、删除/prune、revision/Workspace/Member snapshot、Message/Run/Abort/Retry/Timeline、Event dedupe/batch/Room+Session query/long poll/Cursor/epoch、Session partial upsert 测试；`go test -count=1 ./...` 与 `go vet ./...` 通过。
- 是否更新 README：是；Bridge 增加持久化职责、migration 原子性与尚未开放的边界。
- 是否更新 `.ai/architecture`：是；记录 schema 16、Service 当前事实与未开放阶段。
- 是否需要新增或更新 `.ai/decisions`：是；在 schema 修改前新增 Accepted `.ai/decisions/2026-07-18-agent-room-persistence.md`。
- 后续注意事项：严格按 §28 下一步实现 AR-201 Lease、AR-202 FIFO Queue、AR-203 Runtime Busy；Lease 清理只能 owner-conditional，未确认 Abort 不得提交替代 Run；Observer 和 Forward 继续关闭。

## 2026-07-18-28 - Agent Room AR-201～203 Lease, FIFO Queue and Runtime Busy

- 用户目标：按 PLAN §28 在 persistence core 后实现 Session 单一执行所有权、本地 FIFO 与 Runtime busy 协调，并在 Observer Gate 前保持 Forward 关闭。
- 涉及目录：`apps/kimi-im-bridge/internal/store`、`internal/bridgecore`、`internal/agentroom`、`internal/runtime`、`internal/providers/runtimeadapter`、Bridge README、Agent Room PRD/SPEC/PLAN 与架构事实文档。
- 修改内容：新增 owner-conditional Lease acquire/renew/release/cleanup、30 秒 TTL/10 秒 Heartbeat；共享 ExecutionService Guard 在提交前和 Lease 后检查 Runtime；新增每 Session 50 条上限 FIFO 的 enqueue/claim/finalize/cancel/完成后推进；Coordinator 按 Runtime 状态恢复 claimed/active Run；同 generation 且新鲜的 Observer 状态可优先于 REST；Heartbeat 丢失阻塞关联 Run并写脱敏 warning。
- 修改原因：仅靠本地 lease 无法识别 Pane/外部 Runtime Turn，单调用方 Queue 又无法保护 IM 与未来 Room 共用的执行路径；重启时按 TTL 盲目重排会对仍运行的 Runtime 重复提交。
- 本次已消除的不确定性：跨 Store/并发 writer 的 Lease/Queue 原子性；NULL/非法 expiry 恢复；terminal Run 不复活；claimed 重启恢复顺序；Runtime busy 无 Lease 时的 `runtime_external`；Observer freshness/generation 优先规则；严格未知状态 fail closed。
- 本次采用的关键假设：Runtime 0.27.0 的 REST 精确 Session 状态是 Observer worker 接线前的生产事实源；`follow_up` 统一降级本地 FIFO；`SessionLease.AcquiredAt` 只作为首次 acquire 的瞬时返回值。
- 剩余不确定性：真实 Abort 完成确认仍未做写入型探测，故 `abort_and_replace` 只标 `abort_unconfirmed` blocked 且不调用 Abort；Phase 4 Observer worker 尚未接入 Resolver；Admin API、Rust/Grid/Pane/Forward/Recovery/Workflow/Release 尚未实现。
- 影响范围：Bridge 内部共享执行 chokepoint、Agent Room Queue/Run projection 与 Runtime 状态读取；无 migration/user_version、Admin/Tauri JSON、React/UI、token 或发布配置变化。
- 验证方式：Lease 同 Store/跨 Store 竞争、SQLite busy、非法 expiry、owner match；Queue FIFO/front/idempotent/cancel/limit/terminal/missing Session/并发/restart；Guard Runtime busy/strict unknown/Heartbeat 三次失败；Coordinator follow-up/complete/recovery/abort fail-closed/Observer generation 测试。`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；Admin stop 异步断言首次全量运行抖动一次，定向 `-count=20` 与完整复跑均通过。`go test -race ./...` 仍因本机 `CGO_ENABLED=0` blocked。
- 是否更新 README：是；Bridge 新增长期共享 Lease/Queue/Runtime busy 责任。
- 是否更新 `.ai/architecture`：是；记录当前已实现协调边界与 Observer 未接线事实。
- 是否需要新增或更新 `.ai/decisions`：否；复用 Accepted 0014–0016 schema 与既有 Runtime/ExecutionService 契约，未跨越新的序列化或持久化单向门。
- 后续注意事项：按 §28 下一步先为 `/api/v1/agent-room/*` 建立 Accepted Admin API ADR，再实现 AR-305/306；Observer 与 Forward 继续关闭。

## 2026-07-18-29 - Agent Room AR-305～306 Admin Core and diagnostics

- 用户目标：按 PLAN §28 在 Lease/Queue 后交付无 React 依赖、默认关闭且可用测试 Client 完整操作的 Agent Room Admin Core，并保持 Observer-before-Forward。
- 涉及目录：`.ai/decisions`、`apps/kimi-im-bridge/migrations`、`internal/admin`、`internal/agentroom`、`internal/store`、`internal/app`、`internal/domain`、Bridge CLI/README、Agent Room PRD/SPEC/PLAN 与架构事实文档。
- 修改内容：Accepted Admin API ADR 后新增默认关闭 `KIMI_AGENT_ROOM_ENABLED` 与 `/api/v1/agent-room/*`；实现 Agent/Room/Member CRUD、partial PATCH/revision、Room cursor、Message/Run、Abort/Retry、Timeline before/after、Pane Sync、Observation Pin/List、Event long poll、Capabilities；Bridge Status 增量 Agent Room summary。发现空 Pane snapshot 无法持久 generation 后暂停并建立第二个 Accepted ADR，新增 0017 runtime singleton/hash 与 observation pins，DB 16→17。
- 修改原因：Phase 3 必须在无 UI 时验证 Core，但不能借消息端点提前执行 Runtime；0016 只把 generation 存 Pane 行，空 snapshot 后会丢失单调性，同 generation 矛盾 payload 也无法区分。
- 本次已消除的不确定性：Admin method/path/envelope/status/typed error/body limit/flag；actual Provider 与 locator degradation；persistence-only Dispatch；queued 与 running Abort 边界；Pane active Session precedence、stale/idempotent/conflict/empty/restart；全局 Pin 的准确持久语义。
- 本次采用的关键假设：Phase 3 Message 只创建 Message/Run；`same_session` Retry 只创建新 Run；`new_session` 等 Session resolver；Pin 是 watch intent 而不是已产生 Observation；Admin Core 可在 SDK Provider 下读取历史，但执行/Observer capability false。
- 剩余不确定性：Phase 4 SessionObserver 未接线，故 `observerRunning=false`；Forward/真实 Abort 确认仍关闭；Agent Room flag 尚未由 Rust settings/Tauri command 控制；Rust/Grid/Pane/Reverse Mirror/Approval Recovery/Workflow/Release 尚未实现。
- 影响范围：Bridge DB 新增两张隔离表，Admin JSON 增量 namespace/status 字段与非秘密 env flag；既有 Admin routes/Connector 默认行为不变，flag off 时 Agent Room 路由 404；无 React/token/Runtime Prompt/安装配置变化。
- 验证方式：Handler Core 流覆盖 auth、Agent/Room/Member/Message/Run/Timeline/Event/Abort/Delete、partial PATCH、Room cursor、strict unknown field、1 MiB、typed safe error、Pane effective/stale、Pin、Capabilities；Store 覆盖 fresh/13→17/idempotent migration、empty/same/stale/restart generation、pin missing/idempotent/restart；CLI flag 与 Status actual-provider degradation。`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；race 仍因 `CGO_ENABLED=0` blocked。
- 是否更新 README：是；新增 Admin namespace、flag、schema 17 与长期 Pane/Pin 状态职责。
- 是否更新 `.ai/architecture`：是；记录 DB 17、Admin Core 实际表面与 Observer/Forward 未开放边界。
- 是否需要新增或更新 `.ai/decisions`：是；在修改 API 前新增 Accepted Admin ADR，在新增 0017 前新增 Accepted runtime-state ADR。
- 后续注意事项：严格按 §28 下一步进入 AR-400 SessionObserver；先复用 Runtime 单 generation WS/动态 subscription/cursor 能力，未通过 Observer MVP Gate 不得让 persistence-only Run 进入执行。
## 2026-07-18-30 - Agent Room AR-400～405 Multi Session Observer

- 用户目标：严格按 v1.1 §28 在 Admin Core 后交付并验证 Observer-only Multi Session 纵切，Forward 继续关闭。
- 涉及目录：`apps/kimi-im-bridge/internal/{runtime,agentroom,store,app,domain}`、`apps/kimi-im-bridge/migrations`、Bridge README、Agent Room PLAN/PRD/SPEC、`.ai/architecture`、`tasks`。
- 修改内容：新增一个 generation 一个连接的 `SessionObserver`、动态 watch-set `ObserverCoordinator` 与 `ObserverProjector`；新增 0018 per-Session Runtime generation checkpoint；Event/Run/Approval/Observation/Cursor/checkpoint 单事务投影；接入 default-off Bridge 生命周期和真实 capability；新增按需 Transcript adapter、Fake Runtime ping/connection introspection 与只读 Observer probe。
- 修改原因：在任何 Forward Dispatch 前建立可恢复、幂等、不会错绑 Session/Run 的事实镜像，并关闭旧 Cursor 回退和崩溃窗口。
- 本次已消除的不确定性：1/6 Session 单连接；动态增删/零集合；ping/pong、read deadline、duplicate/乱序、reconnect；epoch resync；numeric Turn ID；metadata/prompt/turn/唯一 active Run 归属；runless external；Approval requested/resolved 乱序；Transcript 与 user Prompt capability；真实 Runtime 0.27.0 新 Observer 1/6 hello/ack。
- 本次采用的关键假设：0.27.0 无 artifact event，因此不伪造 artifact；Prompt metadata 不保证回带，因此只作为最高优先兼容项；无法准确归属时不伪造 Message/Member/Run；真实 Gate 只读，不污染用户 Session。
- 剩余不确定性：Rust Event Pump、Grid V2、Native Pane、Reverse Mirror UI 和产品级 Observer MVP Gate 尚未完成；真实运行中断线事件重放与真实 Approval 写入生命周期留待后续共存/E2E Gate；Abort confirmation 仍禁用。
- 影响范围：仅 flag 开启且实际 Server locator ready 时启动 Observer；flag 默认关闭，Forward 不执行；Room 不保存完整 Transcript；未知 Runtime payload 只保存脱敏 event type。
- 验证方式：`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；Fake Runtime 覆盖 1/6 Session、单连接、动态移除、ping/pong、deadline reconnect、乱序/重复、Approval、epoch restart、pane_manual；真实 0.27.0 临时 loopback server 的只读 probe 返回 Observer 1/6、Transcript、Cursor supported，随后 server 已停止且 locator 已删除。`go test -race ./...` 仍因 `CGO_ENABLED=0` blocked。
- 是否更新 README：是，记录 flag 开启 + ready Server 时 Observer 生命周期，Forward 仍关闭。
- 是否更新 `.ai/architecture`：是，记录 schema 18、Observer worker、原子 checkpoint 与剩余产品 Gate。
- 是否需要新增或更新 `.ai/decisions`：是，新增 Accepted `2026-07-18-agent-room-observer-checkpoint.md`。
- 后续注意事项：下一阶段严格进入 AR-500 Rust Types/Client/Event Pump；不得因 Go Observer Gate 通过而提前打开 Forward。

## 2026-07-18-31 - Agent Room AR-500～504 Shell Client and Event Pump

- 用户目标：严格按 PLAN §28 在 Observer 后建立安全 Sidecar→Tauri→React 通道，保持 Feature Flag 默认关闭且不开放 Forward。
- 涉及目录：`.ai/decisions`、`apps/kimi-shell/{src-tauri,src,README.md,scripts}`、Bridge Agent Room observation Admin route、Agent Room PLAN/PRD/SPEC 与架构事实文档。
- 修改内容：新增 Accepted Shell Contract ADR；Rust/TS 映射 Profile/Room/Member/Message/Run/Event/Observation/Capability/Pane/Status；扩展结构化 Agent Room HTTP Client 与 3 秒/long-poll 分离超时；补齐 Agent/Room/Member CRUD、Run/Retry/Abort、Approval、Pane Sync、Observation pin、Capabilities、Event poll、Open Session 的 163-command main-only registry；新增 React Tauri service 与双事件订阅；新增 generation/Cursor/backoff/30 秒 grace Event Pump、显式 stop/cancel、sidecar ensure/crash recovery；`focus_existing` 精确匹配 active/persisted Session，未命中才新开准确 Pane。Bridge Observation Admin 的 `observerRunning` 改用实时 capability，不再硬编码 false。
- 修改原因：React 不能直接持有 Admin/Runtime token；原 Client 的 3 秒超时会截断 25 秒长轮询，字符串错误会丢 code/details/requestId，Client Debug 还可能暴露 token；原 Shell 在 Agent Room flag 下不会启动或恢复 Sidecar，且缺完整 CRUD 与准确 Session 聚焦。
- 本次已消除的不确定性：Go camelCase 是唯一跨进程权威；列表 page 形状；Raw JSON 映射；main-only ACL；`waitMs+5s`；emit 成功后才推进单调 Cursor；旧 generation 不 emit；`cursor_too_old` 明确 resync_required；显式 Stop 不被 Pump 立即重启；Agent Room core 启动不修改 Connector enabled/settings；Admin token 对 error code/message/requestId/details 键和值递归脱敏。
- 本次采用的关键假设：Feature Flag 当前继续由 `KIMI_AGENT_ROOM_ENABLED` 显式控制，尚未增加用户可见设置；Shell 重启从 Cursor 0 重放并依赖稳定 seq/Event ID 去重；Pump Cursor 不落盘；Forward command 即使注册也由 Observer capability fail-closed 且没有产品 UI 入口。
- 剩余不确定性：Rust test executable 在本机仍以 `STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139)` 退出，因此 Pump 连续运行与 sidecar stop/restart 的真实 Tauri UI Gate blocked；Native Pane/React store 尚未消费 Pump status；Approval Room 归属投影、Grid V2、Reverse Mirror UI、Forward、Recovery/Workflow/Release 尚未实现。
- 影响范围：Feature Flag 开启时 Shell 可启动同一 Bridge core 并在 demand 下运行 Pump；外部 Connector 配置与 enable 状态不变；prefill/workspace-import-picker 无新增权限；不新增依赖、不复制 Session transcript、不持久 token。
- 验证方式：`cargo check`、`cargo test --no-run`、前端 `tsc --noEmit`、Agent Room service 3 tests、全量 `pnpm test` 137 tests、`pnpm build`、`pnpm verify`、command registry/capability/bundle checks、Bridge 相关 Go packages、`git diff --check`；Rust 实际 `cargo test` blocked 于既有 Windows entrypoint 错误。
- 是否更新 README：是；记录新的 Agent Room command/service/Event Pump 长期职责与 token 边界。
- 是否更新 `.ai/architecture`：是；记录 Shell 当前事实、验证证据和产品 Gate 未通过。
- 是否需要新增或更新 `.ai/decisions`：是；在修改 Tauri command/type/event 单向门前新增 Accepted `.ai/decisions/2026-07-18-agent-room-shell-contract.md`。
- 后续注意事项：Phase 5 产品 Gate 保持未完成；按 §28 下一步进入 AR-600 Grid V2 ADR/迁移，Observer MVP Gate 通过前不得开放 Forward。

## 2026-07-18-32 - Agent Room AR-600～604 Grid V2 and Native Pane

- 用户目标：严格按 PLAN §28 在 Shell Event Pump 后交付可回滚的 Grid V2、Agent Room 本地 Pane shell 与泛化 Session 打开动作，不提前开放 Forward。
- 涉及目录：`.ai/decisions`、`apps/kimi-shell/src/features/workspace-grid`、`src/app/useShellController.ts`、`src/App.css`、Shell README、Agent Room PRD/SPEC/PLAN 与架构事实文档。
- 修改内容：新增 Accepted Grid V2 ADR；state/saved layouts 使用独立 V2 keys 并从 V1 只读迁移；新增 `agent_room/local/roomId` 与 sanitizer/唯一 Room 规则；新增无 iframe 的 AgentRoomPane、Room selector、Member rail、Timeline/Pane Session/health shell；提取 `openSessionInWorkspaceGrid` 并按 effective session 聚焦或准确创建。
- 修改原因：同 key 升级会让旧 V1 loader 重置布局；把本地 UI 塞进 iframe 路径会触发 URL、存储与 Session 观察副作用；Explorer 专用动作无法安全复用 Room 的明确 Session 引用。
- 本次已消除的不确定性：V1 回滚键保留策略、saved layout 损坏回退、local Pane 不持有 storage namespace、同 Room 去重、运行期 Session 匹配优先级、Room 失效修复入口与 Forward UI 边界。
- 本次采用的关键假设：Phase 6 只需要 Observer 前的 UI shell；Timeline 与 Pane Sessions 保持明确占位，Phase 7/8 再接 Registry/Event Store。正式 Member 的 `effectiveSessionId` 是打开 Session 的唯一输入。
- 剩余不确定性：Pane Session Registry、Reverse Mirror、连续 Pump/重启桌面行为、产品 Timeline、Forward/Approval/Recovery/Workflow/Connector/Release 尚未验证；Rust test binary 仍受本机 `STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。
- 影响范围：Workspace Grid 持久化与 React 本地 Pane；V1 keys 未修改或删除，Code/Chat/External carrier 和 6/12 上限不变，Feature Flag 仍默认关闭，无 Go/Rust/Admin/token/发布配置变化。
- 验证方式：Grid migration/store/local rendering/effective Session 定向 64 tests；完整 `pnpm test` 20 files/146 tests、`pnpm build`、`pnpm verify`、capabilities/bundle/command registry（163 commands）均通过。
- 是否更新 README：是；新增核心 Native Pane 文件并改变 Grid 持久化版本/加载规则。
- 是否更新 `.ai/architecture`：是；记录 Grid V2/Native Pane 当前事实与仍未通过的 Observer MVP 边界。
- 是否需要新增或更新 `.ai/decisions`：是；修改持久化前先新增 Accepted `.ai/decisions/2026-07-18-workspace-grid-v2-agent-room.md`。
- 后续注意事项：严格进入 Phase 7 AR-700 PaneSessionRegistry；Observer MVP Gate 通过前不得开放 Forward composer 或主动调度。

## 2026-07-18-33 - Agent Room AR-700～704 Reverse Mirror

- 用户目标：在 Native Pane 后自动发现、去重、观察并恢复 1–6 个 Code Pane 的实际 Session，完成 Observer-only Reverse Mirror，Observer Gate 前不开放 Forward。
- 涉及目录：`apps/kimi-shell/src/features/workspace-grid`、`src/services/agentRoomService.ts`、Rust Agent Room command、Go Admin/Store、Shell README、Agent Room PRD/SPEC/PLAN 与架构事实。
- 修改内容：新增 250ms PaneSessionRegistry 与隔离 Observation Store；active Session 优先、visible/active/maximized/shelved、同 Session 多 Pane/primary 与 closed pin；UI 展示 Workspace/状态/回复/审批/来源并提供聚焦、新 Pane、pin、加入/跟随 Room、保存 Agent；Go 返回 generation 恢复 detail 并限制 12 Pane/枚举；Rust Pump demand 改为实际 Session 数，打开前验证真实 Session。
- 修改原因：失败的 snapshot generation 无恢复游标会在 Shell 重启后永久 stale；按 Pane 数维持 Pump 会让无 Session Pane产生虚假需求；本地 Grid 打开无法判定 Session 已被 Runtime 删除。
- 本次已消除的不确定性：Pane generation 与 Runtime generation 分离；同 generation 幂等/冲突恢复；多 Pane Session 选择；关闭 Pane 后 pin 保留；`pane_manual` Prompt 不可得的降级文案；正式 Member 只从明确 Session/Workspace 创建。
- 本次采用的关键假设：Go Session Observation 是 Reply/Status/Approval 的可靠投影，React Event 只做 seq 去重、近期摘要与 debounce refetch；Room Pane 不存在时不启动前端 Registry，下一次打开 Room 会用全量快照清理异常退出遗留 Pane rows。
- 剩余不确定性：真实 Tauri UI 下 Pump/Sidecar/Runtime 重连仍 blocked 于 Rust test binary `STATUS_ENTRYPOINT_NOT_FOUND`；缺少 runless 历史回填 API，AR-703“完成后加入 Room 记录”未实现；Forward/Approval Inbox/Recovery/Workflow/Connector/Release 尚未执行。
- 影响范围：默认关闭的 Agent Room 观察链与本地 UI；不复制完整 Session、不持久 token、不调用 `postAgentRoomMessage`，外部 IM/Connector 配置不变。
- 验证方式：Go `go test -count=1 ./...`、`go vet ./...`；React/TS 22 files/156 tests、build、verify、163-command security Gate；Rust `cargo check` 与 `cargo test --no-run` 通过，实际 `cargo test` blocked `0xc0000139`；`go test -race ./...` blocked 于 `CGO_ENABLED=0`。
- 是否更新 README：是；新增长期 Registry/Observation Store 文件职责。
- 是否更新 `.ai/architecture`：是；记录 Reverse Mirror 当前事实、generation 恢复、Observer Gate blocked 边界。
- 是否需要新增或更新 `.ai/decisions`：否；未新增持久化 schema/command 名/type，沿用 Accepted Admin/Shell/Grid 契约，仅在既有结构化 error details 增加恢复值。
- 后续注意事项：Observer MVP Gate 最后一项未验证，不得开放 Forward。可继续实现不触发调度的 Agent/Room CRUD UI；真实桌面重连需修复 Rust 测试运行环境后补 Gate。
