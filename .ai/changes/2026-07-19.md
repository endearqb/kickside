## 2026-07-19-12 - Agent Room Agent Profile 管理

- 用户目标：按 Agent Room v1.1 Phase 8 顺序交付 AR-800，并在 Observer Gate 未完整通过时继续保持 Forward 关闭。
- 涉及目录：`apps/kimi-im-bridge/internal/agentroom`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/App.css`、Agent Room 计划/架构文档与 Shell README。
- 修改内容：Go Profile 信任边界新增非空 Role Prompt；Native Pane 新增 Agent 列表、新建/编辑/复制/删除、Workspace/Session Picker、四种 Session Policy、白名单 Runtime Controls、AutoApprove 风险提示、revision conflict 重载与 Connector 未绑定占位；Observer Session 保存动作改为打开准确预填的完整表单。
- 修改原因：旧快捷保存会创建空 Role Profile，违反 PRD 的名称/角色/默认 Workspace 必填规则；Phase 8 需要在开放 Forward 前先具备可验证的 Agent 配置入口。
- 本次已消除的不确定性：确认复用 `workspace_list`、已安装的 Tauri dialog plugin、Agent CRUD 与 Observation Store 即可完成 AR-800，无需新 command、依赖或前端持久化状态。
- 本次采用的关键假设：`enabled` 继续沿用既有 Profile 开关语义；Connector Binding 在 Phase 10 前只显示明确占位，不写 IM Binding。
- 剩余不确定性：纯 Vite 浏览器缺少 Tauri `invoke`，无法作为原生 Pane 视觉运行证据；真实 Tauri test binary 仍受既有 `STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。Room/Member/Timeline/Forward UI 尚未完成。
- 影响范围：Agent Profile 创建/更新现在拒绝 trim 后空 Role Prompt；既有空 Role Profile 仍可读取，但下次更新必须补齐 Role。
- 验证方式：`go test ./...`；`pnpm test`（23 files / 158 tests）；`pnpm build`；定向 `tsc --noEmit` 与 Agent Pane/Profile 组件测试均通过。纯 Vite 浏览器准确显示缺少 Tauri `invoke` 的启动错误，未计作视觉通过。
- 是否更新 README：是；新增核心 UI 文件并补充其职责。
- 是否更新 `.ai/architecture`：是；记录 AR-800 当前事实与剩余缺口。
- 是否需要新增或更新 `.ai/decisions`：否；复用已接受的持久化/Admin/Tauri 契约，没有新增单向门。
- 后续注意事项：AR-801 需先补 Shared Brief 64 KiB 服务端限制与 archived Room fail-closed；AR-802 的 follow/rebind 必须先建立原子契约，不能用 delete+create 冒充修复。

## 2026-07-19-12 - Agent Room Room CRUD 与归档只读

- 用户目标：继续按 Phase 8 顺序交付 AR-801，不提前开放 Composer/Forward。
- 涉及目录：`apps/kimi-im-bridge/internal/agentroom`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/App.css`、Agent Room 计划/架构文档与 Shell README。
- 修改内容：Native Pane 新增房间管理，覆盖创建、编辑、Direct/Parallel mode、Shared Brief、归档只读、恢复、删除与 Pane roomId 恢复；Go 新增 Shared Brief 64 KiB 限制和 archived Room 写入拒绝。
- 修改原因：AR-801 需要完整 Room 元数据管理；仅在 UI 禁写无法阻止 Admin 调用继续修改归档 Room，因此必须在共享 Service 信任边界 fail closed。
- 本次已消除的不确定性：确认最近 Room 无需新增 preference，Grid V2 已持久化 Pane `roomId`；归档后允许 Abort/Approval 收口，但禁止新增协作写入与 Retry。
- 本次采用的关键假设：Workflow 在 Phase 10 前只显示禁用选项；归档不强制中止已经运行的 Run。
- 剩余不确定性：Room Timeline 的统一游标/历史分页仍不成立；Member follow/rebind 原子修复尚无契约；真实 Tauri visual QA 仍受运行环境阻塞。
- 影响范围：归档 Room 现在拒绝 Member 新增/修改/删除、Message/Run 创建、Retry 与配置编辑；恢复和 Room 删除仍可用，Kimi Session 不受 Room 删除影响。
- 验证方式：`go test ./...`；`pnpm test`（24 files / 160 tests）；`pnpm build`（TypeScript + Vite production）均通过。
- 是否更新 README：是；新增核心 Room 管理文件并记录职责。
- 是否更新 `.ai/architecture`：是；更新 Phase 8 实际边界。
- 是否需要新增或更新 `.ai/decisions`：否；未新增 Schema、Admin/Tauri 序列化或持久前端状态。
- 后续注意事项：AR-802 前先接受 Member 原子 rebind/repair 契约，并让 followed Pane 在 Pane Session 变化时同步更新 Member；不可用 delete+create 冒充原子修复。

## 2026-07-19-13 - Agent Room Member 显式绑定与 Pane 跟随

- 用户目标：继续按 Phase 8 顺序交付 AR-802，同时保持 Observer 未过最终 Gate 时 Forward Dispatch 关闭。
- 涉及目录：`apps/kimi-im-bridge/internal/{admin,agentroom,store}`、`apps/kimi-shell/src{,-tauri}`、Agent Room 计划/架构文档、Shell README 与 Member binding ADR。
- 修改内容：Member PATCH 增加可选原子 `binding`；Store 同事务校验 pin/follow 的精确 Room/Member/Pane/Session/Workspace，并在完整 Pane snapshot 中同步 followed Member。Native Pane 新增成员管理，覆盖 enabled Agent、显式 Session、显式 Pane 加入，pin/follow 修复、实时状态、归档只读与安全移除。
- 修改原因：followed Member 必须随 Pane 实际 Session 变化，Workspace mismatch 修复必须原子失败并保留旧绑定；UI 不得隐式选择第一条 Session 或把持久化状态冒充 Runtime 实时状态。
- 本次已消除的不确定性：确认无需 migration、新 endpoint、新依赖或前端 storage；既有 Member PATCH 可加法扩展，Pane snapshot transaction 可同时维护 followed Member projection。
- 本次采用的关键假设：显式 rebind 保留 Member kind、Agent/Role snapshot，并将 Session Policy 设为 `resume_selected`；Pane 缺失/无 Session/Session 不存在/Workspace mismatch 均 fail closed 为待配置状态。
- 剩余不确定性：真实 Tauri UI 视觉与 Pump 重连仍受既有 Rust 运行环境阻塞；本轮 `cargo check` 在获授权后长时间无输出并终止。Timeline 统一 Cursor/分页和 Forward Composer 尚未实现。
- 影响范围：旧 Member PATCH payload 兼容；绑定校验失败不写任何同 PATCH 字段；Pane Session 切换会更新 followed Member，Pane 消失会清空 effective Session。移除 Member 不删除 Kimi Session。
- 验证方式：`go test ./...` 通过；`pnpm test`（25 files / 163 tests）通过；`pnpm build`（TypeScript + Vite production）通过。Rust `cargo check` 本轮 blocked 于无输出挂起并已终止，既有实际 test binary blocker 仍为 `STATUS_ENTRYPOINT_NOT_FOUND`。
- 是否更新 README：是；新增核心 Member 管理文件并记录其职责。
- 是否更新 `.ai/architecture`：是；记录 AR-802 实际事务与降级边界。
- 是否需要新增或更新 `.ai/decisions`：是；新增 accepted Member binding ADR，固定加法 PATCH 与同事务 Pane projection。
- 后续注意事项：AR-803 主动 Composer 不得在 Observer 最终 Gate 通过前开放；AR-804 只能先推进不触发 Forward 的只读 Timeline 纵切。

## 2026-07-19-14 - Agent Room 只读 Timeline 纵切

- 用户目标：在 Observer 最终 Gate blocked 且 Forward 必须关闭的约束下，继续完成安全可做的 Phase 8 工作。
- 涉及目录：`apps/kimi-shell/src/{app,services,features/workspace-grid}`、`App.css`、Agent Room 计划/架构文档与 Shell README。
- 修改内容：TypeScript 增加现有 Timeline command 的类型化薄封装；Native Pane 新增只读 Timeline，展示 User Message、Run Card、seq 合并 reply delta、状态、只读 Approval、真实 Artifact、Error 与精确 Session 打开，并随 Pump Cursor 前进刷新。
- 修改原因：Observer 已有可靠投影可先提供可验证的读取体验，同时不越过 Forward Gate 或提前暴露半成品写操作。
- 本次已消除的不确定性：确认 Rust/Tauri 已有 Timeline command 和等价类型，React 只缺薄封装与视图；现有 Go Timeline 不是统一历史分页契约。
- 本次采用的关键假设：最新 100 条投影只作为当前只读快照，不宣称完整历史；Artifact 必须来自非空 event artifact，Reply 只合并 `run.reply_delta`。
- 剩余不确定性：Messages/Runs/Events 独立 limit 无法安全实现 load older、Virtualization 或统一 Cursor；Auto-scroll、完整 accessibility QA、Retry/Abort 与 Approval 写入未完成。
- 影响范围：无 Schema、Admin、Rust command、token 或 Forward 行为变化；新增 React 只读调用和本地展示。
- 验证方式：`pnpm test`（26 files / 165 tests）通过；`pnpm build`（TypeScript + Vite production）通过。组件测试覆盖 delta 顺序合并、Approval/Artifact/Error、状态、Cursor 刷新与精确 Session open。
- 是否更新 README：是；新增核心 Timeline 文件并声明只读边界。
- 是否更新 `.ai/architecture`：是；记录 Timeline 当前事实与统一 Cursor 缺口。
- 是否需要新增或更新 `.ai/decisions`：否；复用已接受的 Shell/Admin 契约，不新增单向门。
- 后续注意事项：不得勾选 AR-804 整体完成；统一历史 Cursor 需要先设计并接受契约，Retry/Abort 必须等 Forward/Takeover Gate。

## 2026-07-19-15 - Rust Gate 恢复与既有失败修复

- 用户目标：解除 Observer Gate 的 Rust 运行阻塞，并完成可安全执行的本地验证。
- 涉及目录：`apps/kimi-shell/src-tauri`、Agent Room 计划与架构事实文档。
- 修改内容：为 Windows crate 产物声明 Common Controls v6 manifest dependency，解除 `comctl32!TaskDialogIndirect` 导致的 test harness 启动失败；修复缺失 TOML 根表访问、空 2xx Admin 响应、Connector 删除后 legacy secret 回灌与严格 `HH:MM` 校验；同步两条已过期默认值测试。
- 修改原因：Rust 二进制此前在进入测试代码前即以 `STATUS_ENTRYPOINT_NOT_FOUND` 退出；入口恢复后暴露 8 个可稳定复现的既有断言失败，完整 Gate 要求修复共享根因。
- 本次已消除的不确定性：PE 导入核对确认唯一缺失入口为 `comctl32.dll!TaskDialogIndirect`；`/MANIFESTDEPENDENCY` 可同时支持 unit-test harness、main test harness 和正式 dev binary，不会注入第二份冲突 manifest。
- 本次采用的关键假设：Bridge stop/approval 的 2xx 空响应是合法成功契约；Connector secrets 的 legacy 字段只作兼容镜像，删除前清空后由剩余 Connector 重新生成；计划时间格式保持文档要求的两位小时。
- 剩余不确定性：真实 Tauri UI + Sidecar/Runtime 端到端重连仍未演练；因此 Observer MVP Gate 仍未标记通过，Forward 继续关闭。
- 影响范围：无 Schema、Admin endpoint、React token 或 Feature Flag 变化；生产 dialog/download 路径未做条件裁剪。
- 验证方式：`cargo test --manifest-path apps/kimi-shell/src-tauri/Cargo.toml` 实际通过（232 passed，main/doc tests 通过）；`cargo build --manifest-path apps/kimi-shell/src-tauri/Cargo.toml` 通过；Agent Room 定向 Rust tests 7 passed。
- 是否更新 README：否；未改变目录职责或运行方式。
- 是否更新 `.ai/architecture`：是；移除已解除的 Rust test binary blocker，并保留真实 Tauri 重连缺口。
- 是否需要新增或更新 `.ai/decisions`：否；Windows manifest dependency 是既有原生依赖的链接修复，不改变 Agent Room 单向门契约。
- 后续注意事项：下一步必须在真实 Tauri 主窗口内执行 Sidecar/Runtime 重连演练，通过前不得开放 AR-803 Composer。

## 2026-07-19-15 - Observer MVP Tauri 重连 Gate

- 用户目标：在不影响正在运行的正式版与用户 Runtime 的前提下，完成真实 Tauri UI + Sidecar 重连验证。
- 涉及目录：`apps/kimi-shell/src-tauri`、Agent Room 计划/PRD/架构事实文档。
- 修改内容：全局热键占用时改为记录并继续启动；修复 Sidecar crash 检测在持有 `bridge_runtime` mutex 时调用脱敏诊断、再次获取同一锁造成的死锁，并防止延迟 exit 诊断覆盖已重启进程。
- 修改原因：独立 identifier/app-data 的 Tauri dev 实例首次被正式版热键占用阻止；允许降级后，强杀测试 Sidecar 暴露 refresh crash 路径自锁，主窗口 Not Responding 且无法恢复。
- 本次已消除的不确定性：确认 Agent Room Pane 在真实 WebView2/Tauri 主窗口运行；Sidecar crash 后共享 manager 会选择新 Admin port、拉起新 PID，Pump/UI 从 degraded 恢复 ready；优雅退出同时停止测试 Sidecar。
- 本次采用的关键假设：全局热键是便利能力而非应用启动的硬前置；同一正式版 Runtime 正在被用户使用，因此不终止它，Runtime reconnect 继续采用已通过的 Go 真实 Runtime 集成证据。
- 剩余不确定性：Observer DoD 的安装包 Smoke 仍属于 Release Gate；AR-703 历史回填仍未实现。
- 影响范围：热键注册冲突不再使 setup 失败；Sidecar 异常退出诊断在 mutex 外构造，重启竞态 fail-safe 保留新进程状态。正式版进程和用户 Runtime 未被终止。
- 验证方式：独立 `com.kimi.shell.agent-room-test` Tauri 实例实际显示本地 Pane ready；强杀测试 Sidecar PID 12216 后新 PID 29144/新端口启动，Pane degraded -> ready；CDP 始终可响应；`quit_app_gracefully` 后测试 App/Sidecar 均退出。新增 race guard 单测通过。
- 是否更新 README：否；不改变目录职责或公开运行方式。
- 是否更新 `.ai/architecture`：是；记录 Observer Gate 实际通过证据。
- 是否需要新增或更新 `.ai/decisions`：否；属于既有生命周期/错误降级行为的 bugfix。
- 后续注意事项：可开始 AR-803，但 Forward Feature Flag 必须继续缺省关闭，且每个 Member 必须先解析明确 Workspace/Session。

## 2026-07-19-16 - Forward Dispatcher、Composer 与真实 Runtime 降级

- 用户目标：Observer Gate 通过后按 AR-803 最小纵切实现 Forward MVP，并保留精确 Session、Queue/Lease、脱敏和默认关闭约束。
- 涉及目录：`apps/kimi-im-bridge/internal/{admin,agentroom,app,providers/runtimeadapter,runtime,store}`、`apps/kimi-shell/src/features/workspace-grid`、计划/架构文档与两端 README。
- 修改内容：Message Admin route 接入 Dispatcher；实现四类 Session Policy 的精确解析、record-only、Lease/FIFO、Prompt Assembly、shared completed Run、附件校验和每队列项独立重建；新增 Native Composer；Runtime 0.27.0 状态改按 `busy/main_turn_active/pending_interaction` 解析；Observer/Dispatcher 投影稳定失败码并阻止旧 replay 事件串入新 Run。
- 修改原因：Observer 已通过允许打开 Forward 最小纵切；真实 Runtime 验证同时暴露旧 `status` 假设和 replay 事件按 Session fallback 误关联的可靠性缺口。
- 本次已消除的不确定性：Fake Runtime 已证明 4 Agent/4 Workspace、`@all` 4 个独立 Session、Reply 实时投影、同 Session continuation 和无串线；真实 Runtime 已证明精确创建和 `/prompts` 接收。真实执行失败根因明确为 `model.not_configured`，不是 token、Session 或 Queue。
- 本次采用的关键假设：未验证附件 wire schema 前显式 `attachments_unsupported`；未知 Runtime 错误只暴露通用稳定文案；事件时间早于 Run 创建时间时不使用“同 Session 唯一活动 Run”fallback。
- 剩余不确定性：真实 Forward Gate blocked，解除条件是在当前 Kimi Runtime 配置 model 后重跑 opt-in `real_forward_integration_test.go`；Windows `go test -race ./...` blocked 于 `CGO_ENABLED=0` 且 PATH 无 gcc/clang/zig，解除条件是安装可用 Windows CGO toolchain。busy conflict、Retry/Abort UI、Approval/Recovery/Doctor、Workflow/Connector 和 Release Gate 尚未完成。
- 影响范围：Feature Flag 仍缺省关闭；React 不访问 Runtime/Admin token；附件不再静默丢失；真实 Runtime 中新增的测试 Session 为加法数据，未删除用户 Session。
- 验证方式：`go test ./...` 通过；Fake Forward Gate 通过；真实 Runtime opt-in Gate返回可追溯 `model.not_configured`；`pnpm -C apps/kimi-shell test` 通过（27 files / 167 tests）；`pnpm -C apps/kimi-shell build` 通过。`go test -race ./...` 按上述环境证据标记 blocked。
- 是否更新 README：是；新增核心 Composer 并改变 Admin Message route 的实际职责。
- 是否更新 `.ai/architecture`：是；记录 Forward 当前事实、真实阻塞和剩余边界。
- 是否需要新增或更新 `.ai/decisions`：否；复用已接受的 persistence/Admin/CreateMode/Grid 契约，无新增单向门。
- 后续注意事项：不得因 Fake Gate 将 Forward MVP 整体标记完成；配置真实 Runtime model 后补 Gate，并继续 Phase 9 的 Abort/Approval/Recovery。

## 2026-07-19-17 - Agent Room Queue、Approval、Recovery 与 Doctor

- 用户目标：按 Phase 9 完成双向控制、Approval、恢复 UI 和本地 Gate，同时保持未验证 Runtime 能力明确降级。
- 涉及目录：`apps/kimi-im-bridge/internal/{agentroom,app,domain,store}`、`apps/kimi-shell/src{,-tauri}`、Agent Room 计划/架构文档与两端 README。
- 修改内容：Dispatcher 启动恢复持久 Queue 并在 Observer terminal 后推进 FIFO；修复 Observer stale snapshot 擦除 Prompt/Turn ID；新增 Agent Room Approval Inbox、Timeline 控制、busy 结果 dialog、Prompt Assembly 诊断、Recovery/Doctor；Status 加法返回 DB/Lease/Approval/Pane 指标；queued Abort 以状态 CAS 同事务删除 Queue、更新 Run、写审计 Event。
- 修改原因：重启后的 queued Run 必须恢复执行，取消排队不能只改 Run 而留下可被 worker 领取的 Queue 记录；Agent Room approval 必须与 Connector approval 隔离；诊断必须可复制但不泄漏 token/raw log。
- 本次已消除的不确定性：确认 Agent Room 可复用既有 Approval Admin/Tauri 链，但 resolve 前必须按 platform 预检；Session-scope Approval 跨重启仍无证据；当前 Timeline 可安全做最近 100 条内的有界渲染，但没有统一历史 Cursor。
- 本次采用的关键假设：V1 Approval 仅 one-shot；新 Session Retry 和真实 active Abort replacement 在能力验证前显示为禁用/blocked；`follow_up` 继续降级 FIFO。
- 剩余不确定性：真实 Runtime Forward 仍 blocked 于 `model.not_configured`；`go test -race` blocked 于本机无 Windows CGO compiler；Phase 10 Workflow/Connector、真实 Connector 共存和 Release/NSIS/MSI 尚未执行。
- 影响范围：Feature Flag 仍缺省关闭；Connector platform enum 与 Agent Room approval enum 分离；Room 不复制 Session transcript；React 不获得 Runtime/Admin token。
- 验证方式：`go test ./...`；恢复/Approval/Queue 定向 Gate；Rust 234 tests；前端 29 files/173 tests；`pnpm build`；`pnpm verify`；163-command/Capability/Bundle Gate；Agent Room source + built dist 敏感标识扫描均通过。真实 Sidecar crash recovery 沿用 Observer Gate 的独立 Tauri 实机证据。
- 是否更新 README：是；新增 Approval/Doctor 核心文件职责和 Queue recovery/abort 事务边界。
- 是否更新 `.ai/architecture`：是；更新 Phase 9 实际能力与降级。
- 是否需要新增或更新 `.ai/decisions`：是；仅对已接受 Admin/Shell ADR 做加法 status/approval contract 说明，没有新增 endpoint、migration 或命令。
- 后续注意事项：Phase 10 必须先读取 Connector/Workflow 相关事实并保持 Agent Secret 与 Connector Secret 分离；真实 Connector 测试不得使用假凭据冒充。

## 2026-07-19-18 - Workflow、Agent Connector 绑定与飞书精确 Mention

- 用户目标：按 Phase 10 完成显式 Workflow、Agent/Connector 解耦和真实 Connector 共存前的安全本地 Gate。
- 涉及目录：`apps/kimi-im-bridge/{migrations,internal}`、`apps/kimi-shell/src{,-tauri}`、Agent Room 计划/ADR/架构文档与两端 README。
- 修改内容：新增 migration 0019；实现有界 DAG、固定 Stage/Run、Reply 前置、失败策略、人工 resolve、恢复与四类 UI 模板；新增独立 Connector Binding、WorkDir/Session 策略、不可变 Turn 来源；飞书启动获取本 Bot Open ID 并严格 mention-self、防 bot/app/self 回环；补齐 Rust command 和 Native Pane UI。
- 修改原因：Workflow 必须复用既有执行所有权与恢复机制，Connector 身份/凭据不得成为 Agent Profile，且多机器人群聊不能按显示名或任意 at-tag 误触发。
- 本次已消除的不确定性：本地 Fake Runtime 已验证 Parallel Review、结果引用、continue/stop/require_user、重复终态和重启恢复；Store 已验证双方删除/prune 不级联；双 Bot 身份矩阵已证明同一事件只命中被精确 mention 的机器人。
- 本次采用的关键假设：Workflow V1 只支持 `aggregation=all` 与显式 Prompt；一个 Agent 可属于多个 Room，因此没有 Room mapping 时不把外部 Turn 猜成 Agent Room Run；Bot Open ID 只在内存缓存。
- 剩余不确定性：真实 Feishu 多机器人与 Weixin 共存 blocked 于 Connector 关闭且无凭据，解除条件是两个隔离测试 Bot、测试群与 Weixin 测试账号；真实 Forward 仍 blocked 于 Runtime `model.not_configured`；Windows race 仍 blocked 于无 CGO compiler。AR-1004 外部 Room mirror 保持非阻塞 Spike。
- 影响范围：Feature Flag 仍缺省关闭；migration 只增不改；React 不接触 Connector Secret、Runtime/Admin token；Connector prune 与 Agent 删除均不破坏另一侧实体。
- 验证方式：`go test ./...`；Workflow/Binding/Feishu/Orchestrator 定向 Gate；Rust 235 tests；前端 30 files/175 tests；`pnpm build`；`pnpm verify`；167-command、Capability、Bundle Resource Gate；frontend dist 内部 token 标识扫描 0 命中。`go test -race ./...` 按上述环境证据标记 blocked。
- 是否更新 README：是；schema/API、核心 UI 文件与 Connector 行为属于长期入口。
- 是否更新 `.ai/architecture`：是；记录 Phase 10 当前事实、阻塞与非 Room mapping 边界。
- 是否需要新增或更新 `.ai/decisions`：是；新增 accepted Workflow Contract 与 Agent/Connector Binding ADR。
- 后续注意事项：Release Gate 不得把本地双身份矩阵写成真实 Connector 共存；签名、安装与升级验证必须保留明确证据。

## 2026-07-19-19 - Agent Room V1 本地发布 Gate

- 用户目标：验证 Agent Room V1 的正式构建、安装器、升级兼容、安全与发布边界，不影响现有正式安装和用户数据。
- 涉及目录：`apps/kimi-shell/{src-tauri,docs}`、bundled Go sidecar、Agent Room 计划/PRD/架构事实文档。
- 修改内容：重建 release Go sidecar/Rust binary，生成 `0.1.16` NSIS/MSI，补充 Agent Room release notes；同步最终 Checklist 和发布 blocked 证据。
- 修改原因：源码测试通过不足以证明 beforeBuild 使用最新 sidecar、安装器含正确版本/资源或 updater 可签名；真实 installer lifecycle 又不能在当前正式安装机器上冒险覆盖 AppData。
- 本次已消除的不确定性：正式 beforeBuild 确实重建 sidecar；MSI/NSIS 均含 bundled sidecar；打包 sidecar token-file/Admin/DB/log redaction smoke 通过；旧 0.1.12 与新 0.1.16 MSI UpgradeCode 相同且 ProductCode 变化，NSIS 保持稳定卸载键与 semver 判断。
- 本次采用的关键假设：本地产物只用于 Gate，不发布、不安装；已有旧安装器只读核对，不删除；签名密钥只应存在于 CI Secrets，不能为本地 Gate索取或写入仓库。
- 剩余不确定性：Updater manifest/签名 blocked 于缺少 `TAURI_SIGNING_PRIVATE_KEY`；NSIS/MSI 安装、旧版升级、断网/坏签名回滚 blocked 于缺少隔离 Windows VM；真实 Feishu/Weixin matrix 与真实 Runtime Forward 分别 blocked 于凭据和 `model.not_configured`；Go race blocked 于 `CGO_ENABLED=0`。`cargo clippy --all-targets -- -D warnings` 仍被 HEAD 已存在的 `workspace_session.rs` needless borrow、`lib.rs` test initializer 和 `bridge_http_client.rs` test-module ordering 阻塞，本次新增 needless borrow 已修复。
- 影响范围：Feature Flag 继续缺省关闭；未启动/停止用户 Runtime 或 Connector，未安装/卸载应用，未修改用户 AppData，未 commit/push/release。
- 验证方式：`go test ./...`、`go vet ./...`；Rust 235 tests、`cargo fmt --check`；前端 30 files/175 tests、`pnpm verify`；正式 `pnpm tauri build` 生成 MSI/NSIS 后在缺私钥签名阶段按预期 blocked；capability/resource/167-command ACL、公开产物/Markdown 绝对路径扫描；打包 sidecar smoke；MSI Property 表与 NSIS installer script 只读升级核对。`go test -race ./...` 与 clippy 按上述证据标记 blocked。
- 是否更新 README：否；打包入口与长期职责未变化，release note 记录本版本交付范围。
- 是否更新 `.ai/architecture`：是；记录实际构建产物、升级证据与 G3 blocked 边界。
- 是否需要新增或更新 `.ai/decisions`：否；沿用既有安装/发布表面，没有改变单向门。
- 后续注意事项：在隔离 VM 分别从旧 NSIS/MSI 升级，核对 Grid V1→V2、Bridge DB v13→v19、Connector/Binding/Approval/Turn/Session 数据、服务停止/恢复与 rollback；配置两只 Feishu Bot、Weixin 测试账号、Runtime model 和 CI signing Secrets 后再解除 blocked。

## 2026-07-19-20 - 飞书 Prompt 继承 Runtime 默认模型

- 用户目标：修复飞书消息已进入 Kimi Code Session、但会话以 `model.not_configured / Model not set` 中断的问题。
- 涉及目录：`apps/kimi-im-bridge/internal/runtime`、Shell bundled Bridge sidecar 与 IM Bridge 架构事实。
- 修改内容：Server Adapter 在调用方未显式指定模型时，从 Runtime `/api/v1/config` 读取 `default_model` 并写入 Prompt；显式 Agent/Run 模型继续优先；新增默认继承与显式优先回归覆盖，并重建 bundled sidecar。
- 修改原因：Kimi Code Web 显示的默认模型不会自动补入 Bridge 的 `/prompts` 请求；飞书普通 Connector 又没有 Agent Profile runtime controls，导致 Runtime 接收用户消息后以空模型结束 Turn。
- 本次已消除的不确定性：本机 Runtime 配置和只读 API 均确认存在非空 `default_model`；错误发生在模型选择层，不是飞书连接、Session、token 或消息投递层。
- 本次采用的关键假设：Runtime 配置端点是默认模型的唯一动态事实源；不缓存以允许用户修改配置后立即生效；旧 Runtime 若不支持 `/config`，保留原有服务端 canonical error，不伪造模型。
- 剩余不确定性：当前正在运行的已安装应用需使用重建后的 sidecar 并重启 Bridge/应用后才能获得修复；Windows race 仍 blocked 于 `CGO_ENABLED=0`。
- 影响范围：仅影响未显式携带模型的 Server Prompt；不修改用户 Session、Connector 凭据、Runtime 配置或 Agent Profile；Feature Flag 与发布流程不变。
- 验证方式：两项 Server Adapter 定向回归通过；`go test ./...` 全量通过；`pnpm build:bridge-sidecar` 成功。`go test -race ./...` 因本机 `CGO_ENABLED=0` 未执行。
- 是否更新 README：否；目录职责、公共入口和运行方式未变化。
- 是否更新 `.ai/architecture`：是；记录默认模型解析的实际契约。
- 是否需要新增或更新 `.ai/decisions`：否；复用 Runtime 既有只读配置 API，没有引入单向门。
- 后续注意事项：重启应用后从飞书发送新消息验证；历史已中断 Turn 不会自动重放。
