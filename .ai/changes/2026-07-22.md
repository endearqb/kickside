## 2026-07-22-11 - Agent Room Redesign Phase 0–2

- 用户目标：按 `agent-room-redesign-prd-spec-plan.md` 完成 Agent Room 开发计划，并以静态预览为 UI/UX 规范性基线。
- 涉及目录：`apps/kimi-shell`、`apps/kimi-im-bridge/internal/admin`、`.ai/decisions`、`.ai/architecture`、`.ai/plans`、`tasks`。
- 修改内容：新增独立 lazy `agent-room` 窗口、dedicated capability、show/hide/toggle 生命周期、main/agent-room 定向 Event Pump、capability-driven Pane Session Publisher、Observation `panes` 投影、标题栏入口，以及房间/执行成员/任务动态/Compact Composer/内嵌审批/精确 Session 打开的窄用途 UI。
- 修改原因：旧 V1 作为 Workspace Grid Pane 且使用 10 个平级 Tab，发送、Timeline、审批与 Session 观察被拆散，无法形成低切换的多 Session 调度闭环。
- 本次已消除的不确定性：核对 `main=f7a89d8082486913d3dd77b0dfc1d03d6416502f` 与计划基线一致；确认现有 Go/Bridge 业务链路可复用；确认独立窗口需要加法 Observation Pane Projection，而无需数据库 migration 或新依赖。
- 本次采用的关键假设：Phase 0–2 是 Product Gate 前唯一允许完成的开发范围；应用重启后选择最近更新的活动 Room，不新增 Window Preference 持久化 key。
- 剩余不确定性：真实双窗口焦点、窗口置顶、2–6 个 Runtime Session、实际审批、Sidecar/Runtime 重启、暗色与最小尺寸尚需 G3；7 天 Dogfood 尚未开始。
- 影响范围：Feature Flag 关闭时标题栏不显示入口且不持续发布 Pane Snapshot；旧 Agent Room Pane 与 Grid V2 keys 保留。Feature Flag 开启后主窗口发布 Code Pane 投影，独立窗口可管理 Room/Member/Run/Approval，但无 Agent/Connector/sync/Secrets/文件/Webview 权限。
- 验证方式：`pnpm verify` 通过（35 files / 186 tests、TypeScript、安全 capability/bundle/170-command ACL）；`pnpm build` 通过；Rust `cargo fmt --check` 与 `cargo test --lib` 237 tests 通过；Go `go test ./...` 全量通过；Workflow 竞态目标测试 `-count=10` 通过。
- 是否更新 README：是；新增 `features/agent-room/README.md` 并更新 Shell README 的窗口、数据流与 capability 边界。
- 是否更新 `.ai/architecture`：是；记录独立窗口、定向 Pump、Pane Projection 与 Product Gate 后续限制。
- 是否需要新增或更新 `.ai/decisions`：是；新增 Accepted Window Contract 与 blocked-by-Product-Gate 的 Grid V3 Draft。
- 后续注意事项：按 `agent-room-redesign-dogfood.md` 连续记录 7 天真实使用；只有 Gate 通过后才能接受 Grid V3 ADR、完成能力等价、删除旧 Pane 或决定默认开放 Feature Flag。

## 2026-07-22-11 - Agent Room MVP 验收补强与 Workflow 竞态修复

- 用户目标：继续按 Redesign PRD/SPEC/Plan 收敛 Phase 0–2 验收缺口，并解除 Go G1 失败。
- 涉及目录：`apps/kimi-shell/src/features/agent-room`、`apps/kimi-shell/src/services`、`apps/kimi-im-bridge/internal/agentroom`、`tasks`。
- 修改内容：归档 Room 禁止审批、Abort、Retry；Run 增加 `new_pane` 次级打开；Session 打开按错误码给出不同文案；Event Batch 按 generation 重置序列去重；成员状态只由最新 Run 的终态错误决定；部分派发显示逐目标失败；Composer 支持 Arrow/Enter/Escape 键盘选择；补齐内部 Run 状态中文映射；增加无成员、无有效 Session、归档 Room 空态与 Observer 降级重试；补齐 Room Switcher combobox/listbox 关系、键盘选中可见态和焦点返回测试。修复本地 Run 仍在 `activeRuns` 时 Reverse Mirror terminal 通知被丢弃造成下游 Workflow 永久等待的竞态，并增加确定性回归覆盖。
- 修改原因：Phase 2 审计发现归档只读、精确 Session 次级动作、恢复和键盘契约存在缺口；Go 失败进一步暴露事件投影与本地执行清理之间的 lost wake-up。
- 本次已消除的不确定性：原 Go 失败不只是测试读取过早；确有 `HandleTerminalRun` 在本地执行清理窗口直接返回的产品竞态。Workflow transition 和稳定 Event ID 允许安全幂等推进，无需重复 Lease 或 Session 调度。
- 本次采用的关键假设：归档 Room 仍允许只读打开准确 Session；Event Pump generation 变化或 `resync_required` 后以 Snapshot 恢复为准；Phase 3–5 仍由 Product Gate 阻断。
- 剩余不确定性：真实 Tauri G3 与连续 7 天 Dogfood 未执行，不能据自动化测试宣称 MVP/Product Gate 完成。
- 影响范围：仅 Agent Room 窗口交互、事件恢复去重和 Workflow terminal 推进；未修改 Grid V2 持久化、旧 Pane、数据库 schema 或公开 HTTP wire 字段。
- 验证方式：Workflow 目标测试连续 10 次通过；Go `go test ./...` 通过；`pnpm verify` 35 files / 186 tests 通过；`pnpm build` 通过；Rust `cargo fmt --check` 与 `cargo test --lib` 237 tests 通过；`git diff --check` 无内容错误。
- 是否更新 README：否；目录职责、公共入口和运行方式未改变。
- 是否更新 `.ai/architecture`：否；窗口与数据流事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否；竞态修复和局部验收补强未跨越新的单向门。
- 后续注意事项：用真实双窗口/Session/审批完成 G3，并连续记录 7 天 Dogfood；Gate 通过前不得执行 Grid V3 或旧 Pane 退场。
## 2026-07-22-14 - Agent Room 应用内开关

- 用户目标：把 `KIMI_AGENT_ROOM_ENABLED` 外部 Feature Flag 改为小助手设置中的应用内开关，并仅在存在 Kimi Code Pane 时显示标题栏入口。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`.ai/plans`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：新增默认关闭的 `AppSettings.agentRoomEnabled`、main-only `set_agent_room_enabled` 命令、Control Center Agent Room 设置项；Shell 显式向 Bridge 子进程传递 `KIMI_AGENT_ROOM_ENABLED=1/0`，运行中切换自动重启 Bridge，关闭时停止 Event Pump 并隐藏窗口；标题栏增加 Kimi Code Pane 存在性条件。
- 修改原因：安装版用户不应依赖进程环境变量启用产品能力，且空白 Workspace 或只有 Chat/External Pane 时没有可调度的 Kimi Code Session。
- 本次已消除的不确定性：确认应用设置是偏好唯一真值，环境变量仅保留为 Shell→Go sidecar 内部传输契约；确认设置项位于默认工作目录与外部 IM 通道之间。
- 本次采用的关键假设：开关默认关闭；已停止的 Bridge 不因切换被隐式启动，正在运行或降级的 Bridge 自动重启。
- 剩余不确定性：真实安装版的 Bridge 重启提示、双窗口隐藏和入口显隐仍需 G3 手工验证。
- 影响范围：Settings schema 10→11；新增一个 main-only Tauri command，不扩大 Agent Room window capability，不修改 Go HTTP/DB schema。
- 验证方式：TypeScript `tsc --noEmit` 通过；Vitest 35 files / 186 tests 通过；`pnpm build` 通过；Rust `cargo fmt --check` 与 `cargo test --lib` 238 tests 通过；`pnpm check:nfr:security` 通过（171-command registry/window ACL）。
- 是否更新 README：是；小助手设置由 8 项更新为 9 项，并记录开关与标题栏条件。
- 是否更新 `.ai/architecture`：是；记录应用设置唯一真值与 sidecar 映射。
- 是否需要新增或更新 `.ai/decisions`：是；新增 Accepted Agent Room App Toggle ADR。
- 后续注意事项：Product Gate 与旧 Pane 退出条件不变；`KIMI_AGENT_ROOM_ENABLED` 不再作为用户配置入口。

## 2026-07-22-14 - 飞书复用 Session 回复丢失修复

- 用户目标：修复飞书机器人首条消息能回复、同一 Kimi Code Session 后续消息只在 Web 中完成但不回传飞书的问题。
- 涉及目录：`apps/kimi-im-bridge/internal/runtime`、`.ai/changes`、`tasks`。
- 修改内容：Server Runtime Adapter 在已知当前 `prompt_id` 时，不再让缺少匹配 ID 的 `turn.ended` 结束当前事件流；回归测试覆盖从错误的零游标回放旧 Prompt 后仍能收到当前 Prompt 的完整回复。
- 修改原因：Runtime Session REST 对该真实 Session 返回 `last_seq=0`，Bridge 每轮从头回放；历史 `turn.ended` 不带 `prompt_id`，原 fail-open 匹配会把它误认成当前 Prompt 完成并提前关闭 WS。
- 本次已消除的不确定性：Bridge DB 证明后两条消息均成功入站但无内容增量和 outbound；Runtime journal 证明对应 Turn 实际分别晚约 44 秒和 80 秒完成；真实只读 WS 证明 `turn.ended` 无 Prompt ID，而 `prompt.completed` 携带精确 ID。
- 本次采用的关键假设：当前受支持 Runtime 保持已验证的 `prompt.completed.promptId` 契约；内容增量继续允许不带 Prompt ID，避免丢失正常流式文本。
- 剩余不确定性：真实飞书凭证回归属于 G3，需重建并重启 bundled sidecar 后发送至少两条同 Session 消息确认。
- 影响范围：共享 Server Runtime Adapter，因而同时修复所有使用该路径的 IM Connector；未改飞书 API、数据库 schema、公共类型或 UI。
- 验证方式：Runtime 三条定向测试通过；`go test ./...` 全量通过；`git diff --check` 通过。
- 是否更新 README：否；目录职责、公共接口和运行方式未改变。
- 是否更新 `.ai/architecture`：否；这是既有 Runtime 事件关联契约的 bugfix。
- 是否需要新增或更新 `.ai/decisions`：否；未引入长期架构决策或单向门。
- 后续注意事项：历史已完成但未回传的两条回复不会自动补发；安装版生效需要随 Shell 重新构建 bundled Bridge sidecar。
