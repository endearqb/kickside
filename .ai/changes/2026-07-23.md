## 2026-07-23-10 - Agent Room 独立窗口交互修复

- 用户目标：核实 Agent Room 窗口审查结论，并修复标题栏入口不见独立窗口、无边框窗口难以拖动和关闭按钮无反馈的问题。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri/src`、`tasks`。
- 修改内容：主窗口 launcher 从 toggle 改为幂等 show，失败进入现有可见错误栏；Agent Room 标题栏统一使用 `startDragging()` 并排除交互控件；X 改走原生 `close()`，复用 Rust `CloseRequested -> prevent_close -> hide`；Rust show 恢复最小化窗口且 focus 失败只记录，hide 的 topmost 清理改为 best-effort；新增标题、状态、空白拖动、交互控件阻断、close 与错误反馈测试。
- 修改原因：窗口配置、command 与 capability 均已存在；实际缺口是 launcher 语义和错误处理、复杂标题栏拖动命中区、重复关闭链，以及原生窗口操作部分成功后的错误语义叠加。
- 本次已消除的不确定性：确认 `agent-room` 为 lazy 单例窗口且权限完整；确认首次点击时 toggle 会进入 show，因此不能把首次无窗单独归因于 toggle；确认原 hide 先隐藏再清 topmost，topmost 失败不能单独解释窗口不消失。
- 本次采用的关键假设：launcher 文案“打开”要求幂等 show；focus 和 topmost 清理是增强操作，不应否定已完成的显示或隐藏。
- 剩余不确定性：真实 Windows 前台焦点、双显示器、最小化恢复、X 与 Alt+F4 仍需 G3 手工验证；自动化测试不能证明操作系统窗口层级。
- 影响范围：仅 Agent Room 窗口生命周期与标题栏交互；未修改 Tauri command 名称、序列化类型、capability、Bridge API、数据库或持久化状态。
- 验证方式：定向 Vitest 3 files / 13 tests；前端全量 Vitest 36 files / 188 tests；`tsc --noEmit`；`cargo fmt -- --check`；`cargo check`；`cargo test --no-run`；`cargo test --lib` 238 tests。
- 是否更新 README：否；目录职责、公共接口和运行方式未改变。
- 是否更新 `.ai/architecture`：否；独立窗口架构事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否；未跨越新的单向门。
- 后续注意事项：在 Windows 安装版完成首次打开、重复打开、隐藏后重开、最小化恢复、标题/状态/空白拖动、交互控件不拖动、X、Alt+F4 和双显示器 G3。

## 2026-07-23-11 - 桌面 Chrome 原生指针语义

- 用户目标：审查现有 Windows 桌面壳的 native-feel，并实现最小高影响改进。
- 涉及目录：`apps/kimi-shell/src`、`.ai/changes`、`tasks`。
- 修改内容：在共享基础样式中统一按钮、disclosure、label 和 ARIA 交互项使用 Windows 原生默认箭头，并禁止选中 Chrome 文案；真实内容链接、输入框、可编辑内容和 resize/drag handle 不受影响。
- 修改原因：首方样式中大量按钮、列表行、tab 和标题栏控件显式使用 Web hand cursor，是 native-feel 审查中最集中且最便宜的可验证缺口。
- 本次已消除的不确定性：确认应用当前只发布 Windows，且已具备 Tauri 原生窗口、托盘、单实例、原生保存和更新主链路；本次不需要引入四层跨平台架构。
- 本次采用的关键假设：用户要求是在现有架构内实现审查收益，而不是把 Windows-only Tauri 应用重写成 macOS + Windows 双原生壳。
- 剩余不确定性：启动闪烁、IME、Narrator、窗口位置恢复、后台 CPU 与真实光标行为仍需 Windows G3 手工审查。
- 影响范围：仅共享桌面 Chrome 的 cursor 与 text-selection；未修改链接、业务逻辑、IPC、窗口生命周期、依赖或持久化状态。
- 验证方式：`tsc --noEmit`；`pnpm test`；`pnpm build`；真实光标与系统辅助功能仍属于 G3。
- 是否更新 README：否；目录职责、公共接口与运行方式未改变。
- 是否更新 `.ai/architecture`：否；架构事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否；未跨越单向门。
- 后续注意事项：若产品确定 macOS 目标，再按 native-feel decision tree 重新评估双原生 shell；不要为尚未存在的平台提前搭层。

## 2026-07-23-12 - Agent Room 完整下线与冻结

- 用户目标：完整下线并冻结 Agent Room 功能。
- 涉及目录：`apps/kimi-shell`、`apps/kimi-im-bridge`、`.ai/architecture`、`.ai/decisions`、`.ai/changes`、`tasks`。
- 修改内容：移除设置、标题栏、hash 路由、空 Pane 入口、独立窗口配置/capability；Shell feature gate 恒为 false；Bridge 忽略环境变量并把启用 Options 归一为 false；旧 V2 Room Pane 加载时剔除并修复布局引用；冻结长期文档与 ADR。
- 修改原因：仅默认关闭仍可被历史设置、旧 Shell 或手工环境变量重新启用，不能满足完整下线。
- 本次已消除的不确定性：确认 migration 0014–0019 不可回写或删除；确认 ExecutionService、lease、approval link、turn origin 和 connector binding 存在普通 IM 共享调用方；确认产品入口与执行门必须双端独立 fail closed。
- 本次采用的关键假设：“冻结”表示历史 schema/data 和短期兼容墓碑惰性保留，禁止继续写入和新增功能，不包含破坏性清库。
- 剩余不确定性：旧 Agent Room Run/Queue 可能保留最后一次非 terminal 状态；兼容墓碑需在一个发布周期后依据 release gate 单独删除。
- 影响范围：Agent Room 产品表面与生产执行路径不可达；普通 Kimi Workspace、IM Connector 和既有数据库迁移链保留。
- 验证方式：`tsc --noEmit`；Shell Vitest 36 files / 188 tests；`pnpm build`；capability 与 command registry checks；Go `go test ./...`；Rust `cargo check` 与 `cargo test --lib` 238 tests。
- 是否更新 README：是；Shell、Bridge 与冻结目录 README 已同步。
- 是否更新 `.ai/architecture`：是；当前事实改为 retired/inert legacy。
- 是否需要新增或更新 `.ai/decisions`：是；新增 Accepted decommission ADR，Grid V3 Draft 改为 Rejected。
- 后续注意事项：如需恢复功能或清除历史数据，必须新建 ADR 并取得显式授权；不得重新打开旧 flag。
