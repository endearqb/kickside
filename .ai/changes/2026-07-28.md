## 2026-07-28-16 - 修复首次引导入口与 Windows 原生 Kimi 升级

- 用户目标：修复同事电脑安装后后端已运行但无法进入工作区，以及 `%USERPROFILE%\.kimi-code\bin\kimi.exe` 无法应用内升级的问题。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri/src/install_manager`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：后端运行且首次引导未完成时恢复“进入工作区”操作；升级前增加中断连接确认；Windows 原生安装在升级/卸载时兼容停止旧 Server，升级继续走官方安装脚本和原路径版本验证，npm/pnpm 分支保持不变。
- 修改原因：控制中心重构遗漏已有引导完成回调；升级脚本只接受 npm/pnpm，与官方 Windows 原生安装来源不兼容。
- 本次已消除的不确定性：日志已证明健康检查、Bearer 验证、会话恢复/创建均成功；截图确认活动命令为 Windows 原生路径。
- 本次采用的关键假设：`server/lock` 存在时使用旧版兼容 `kimi server kill`；无旧 lock 时允许官方脚本继续。
- 剩余不确定性：真实同事电脑上的 Windows 文件锁、网络下载和安装脚本行为仍需安装包 G3 验证。
- 影响范围：首次引导导航、Kimi Code 升级确认和安装来源分派；不改变 Tauri command、序列化契约或普通后端停止语义。
- 验证方式：`pnpm test`（37 files / 190 tests）、`tsc --noEmit`、Rust 安装管理测试（27 tests）、`cargo check` 与 `cargo fmt -- --check` 已通过；Windows 原生 0.26.0 安装包手工回归仍为 G3。
- 是否更新 README：是，更新 Shell 运行环境中的升级来源说明。
- 是否更新 `.ai/architecture`：是，记录原生升级和显式确认的外部实例停止例外。
- 是否需要新增或更新 `.ai/decisions`：是，更新 accepted 的复用外部 Kimi Server ADR。
- 后续注意事项：普通退出、停止和重新连接仍不得终止 `reused_external`；只有用户明确确认的升级/卸载任务可执行兼容停止。

## 2026-07-28-17 - 发布 0.1.19

- 用户目标：版本号增加 `0.0.1`，提交并推送 `main`，发布新的 GitHub Release。
- 涉及目录：`apps/kimi-shell`、`.ai/changes`。
- 修改内容：复用现有 `version:bump:patch` 脚本将版本从 `0.1.18` 同步升级到 `0.1.19`，并以 `v0.1.19` 标签触发现有 Release 工作流。
- 修改原因：发布本轮 Agent Room 收口、首次引导入口和 Windows 原生 Kimi 升级修复。
- 本次已消除的不确定性：本地与 `origin/main` 同步，远端不存在 `v0.1.19` 标签；Release 工作流要求 tag 与 `package.json` 版本精确一致。
- 本次采用的关键假设：当前工作树全部属于用户明确要求发布的范围。
- 剩余不确定性：Windows 安装包中的真实原生 Kimi `0.26.0` 升级仍需 G3 手工回归。
- 影响范围：应用版本、GitHub Release 产物与 updater manifest。
- 验证方式：`pnpm test`（37 files / 190 tests）、`tsc --noEmit`、Rust 安装管理测试（27 tests）、`cargo check`、`go test ./...`、`pnpm check:nfr:security`、`pnpm build`、`cargo fmt -- --check` 与 `git diff --check` 均通过；推送标签后确认 Release 工作流结果。
- 是否更新 README：否，发布机制与运行方式未变化。
- 是否更新 `.ai/architecture`：否，架构事实已在同日修复记录中更新。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：Release 成功后执行 Windows 安装包 G3 回归。
