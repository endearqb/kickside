## 2026-07-29-14 - 控制中心 API 配置 canonical 化

- 用户目标：修复 API 重复配置、404 误报成功和手工配置才能使用的问题。
- 涉及目录：`apps/kimi-shell`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：统一 `managed:kimi-code` / `kimi-code/*`，使用 `/models` 验证并同步模型，增加默认模型选择、旧配置安全迁移、OAuth 阻断和 Kimi Doctor 保存门禁。
- 修改原因：旧实现固定创建小助手私有 Provider/Model，并把任何 HTTP 响应视为连接成功。
- 本次已消除的不确定性：以本机 Kimi Code 0.29.1 的模型刷新逻辑、服务请求实现和 Doctor command 为事实入口。
- 本次采用的关键假设：`/models` 是 API Key 的无副作用认证入口；Search/Fetch 测试不应发起真实业务请求。
- 剩余不确定性：真实用户 API Key、代理网络和安装版 Kimi 路径仍需 G3 手工验证。
- 影响范围：控制中心 API 配置的 Tauri view/input/result 增量字段、TOML managed 条目和相关 UI。
- 验证方式：Rust 配置测试、`cargo check`、`cargo test --no-run`、前端测试、`tsc --noEmit`、脱敏候选文件 `kimi doctor config`。
- 是否更新 README：是，更新 Shell API 配置契约。
- 是否更新 `.ai/architecture`：是。
- 是否需要新增或更新 `.ai/decisions`：是，新增 canonical Provider Accepted ADR。
- 后续注意事项：不得把 API Key、Doctor 候选内容或 HTTP 错误中的秘密写入日志和文档。
