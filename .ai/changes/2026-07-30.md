## 2026-07-30-10 - 简化新建窗格交互

- 用户目标：用标题栏 `+` 直接新建 Code/Chat 窗格，并让布局随窗格新增和删除自动调整。
- 涉及目录：`apps/kimi-shell/src/features/window`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src`。
- 修改内容：移除标题栏手动布局菜单；新增窗格头插并自动扩展，六格时收纳原末格；删除可见窗格后压紧并缩容；空工作区只保留新建提示。
- 修改原因：减少先选择布局、再进入空格创建窗格的重复操作。
- 本次已消除的不确定性：六格继续新建时将原末格收入窗格库；最后一个窗格允许删除并保留单格空工作区。
- 本次采用的关键假设：可见上限维持 6，总窗格上限维持 12，删除可见窗格时不自动拉取窗格库内容。
- 剩余不确定性：无。
- 影响范围：Workspace Grid 前端状态、标题栏菜单、空状态、相关测试与文档。
- 验证方式：`pnpm test` 已通过（37 files / 194 tests）；`.\node_modules\.bin\tsc.cmd --noEmit` 已通过。
- 是否更新 README：是，更新 Shell Workspace Grid 能力说明。
- 是否更新 `.ai/architecture`：是，更新 Workspace Grid 当前交互事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变持久化结构或跨技术栈契约。
- 后续注意事项：旧 preset 与持久化字段继续作为兼容和内部布局几何使用。

## 2026-07-30-11 - Kimi Code 启动静默检测

- 用户目标：启动后无需手动“重新检测”即可升级 Kimi Code，并在设置 bar 提示可用新版本。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src-tauri`。
- 修改内容：启动时自动探测本地安装环境；新增只读版本检测 command；已安装时查询官方最新版本并复用小助手更新的状态提示；升级成功后自动复检。
- 修改原因：消除每次启动后必须手动复检的无效步骤，同时让用户在控制中心直接看到 Kimi Code 更新状态。
- 本次已消除的不确定性：最新版来源使用官方 `https://code.kimi.com/kimi-code/latest`；提示仅位于安装/升级设置 bar；不静默安装。
- 本次采用的关键假设：每个应用进程只自动检测一次；网络检测失败时保留手动升级入口。
- 剩余不确定性：无。
- 影响范围：安装环境初始化、Kimi Code 版本检测、控制中心安装/升级状态与 Tauri command 注册。
- 验证方式：前端测试、TypeScript 类型检查、Rust 单元测试与 `cargo check`。
- 是否更新 README：是，补充启动静默检测能力。
- 是否更新 `.ai/architecture`：是，记录版本检测入口与失败回退行为。
- 是否需要新增或更新 `.ai/decisions`：否，未引入长期依赖或新的持久化策略。
- 后续注意事项：无定时轮询或缓存；需要重新获取状态时使用设置项内“重新检测”。
