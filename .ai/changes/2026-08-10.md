## 2026-08-10-13:03 - Apple Silicon macOS V1

- 用户目标：依据 2026-08-05 macOS 文档审查仓库、调研 Kimi Code 与 Tauri 社区/官方实践，并开发适合当前 Apple Silicon 主机的 Kimi Sidekick macOS 版本。
- 涉及目录：`apps/kimi-shell`、`apps/kimi-im-bridge`、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`tasks` 与根 README。
- 修改内容：新增 additive `PlatformCapabilities`、macOS 原生 decorations/App Menu/close-to-hide/Dock reopen/Cmd+Q、平台分层 Tauri config、macOS guided install、Finder-safe Kimi locator、新版 instance registry、Unix process-group lifecycle、target-triple Bridge external binary、本机 unsigned `.app` 构建入口，以及 Windows/macOS draft release + 单一 updater manifest workflow。
- 修改内容：将 Kimi Code 实测基线刷新为 0.34.0；registry 作为主发现协议，旧 `server/lock` 只作兼容 fallback；locator 通过新版 `web --help` family markers 排除旧 Python `kimi-cli`，不按较大 semver 误判。
- 修改内容：修复 macOS canonical temp path 导致的 Go Agent Room legacy test 失败，并让 Runtime SessionObserver 等待 server ack 后才报告 connected；这两项使既有 Go G1 gate 恢复绿色，不恢复已下线的 Agent Room 产品面。
- 修改原因：原实现把 Windows decorations、Explorer、PowerShell install、WebView `dataDirectory`、resource sidecar 与 lock discovery 当作共享事实，无法形成可靠的 macOS 生命周期、Kimi 0.34 runtime ownership 或发布供应链。
- 本次已消除的不确定性：官方 0.34.0 darwin-arm64 SHA-256 与 Mach-O/Developer ID 签名已核对；registry schema、Bearer contract、token rotation、实际端口、TERM cleanup、默认安装路径与 hosted Web UI 边界已用临时隔离环境验证。
- 本次采用的关键假设：macOS V1 最低系统为 13，接受默认共享 WKWebsiteDataStore；首个实现只支持 Apple Silicon；Windows 既有产品行为保持；Kimi Code 外置安装，不复制私有 Web UI 或 SDK。
- 剩余不确定性：最终 Developer ID/notarization/stapling 状态、DMG 安装、OAuth/download/file chooser/IME、App Menu 点击、关闭隐藏与 Dock reopen 仍需具备 Apple 凭据、完整发布环境和辅助功能权限的 G3 手工验收。
- 影响范围：新增序列化字段均为 additive；Windows updater/install/titlebar/context-menu 仍走原分支；macOS 不执行 Windows managed install，不传 `dataDirectory`，不展示 Explorer 设置。
- 验证方式：`go vet ./...`、`go test ./...`、`go test -race ./...`；`cargo fmt --all -- --check`、`cargo check --locked`、`cargo clippy --locked --all-targets -- -D warnings`、`cargo test --locked`；`pnpm exec tsc --noEmit`、`pnpm test`、`pnpm check:nfr:security`、updater manifest Node tests；`pnpm tauri:build:macos:local`；本机 `.app` + Kimi Code 0.34.0 registry/Bearer OpenAPI process smoke。
- 是否更新 README：是，更新根 README、中文 README 与 Shell README 的平台说明边界。
- 是否更新 `.ai/architecture`：是，更新 current state 与 verification gates。
- 是否需要新增或更新 `.ai/decisions`：是，新增 macOS V1 平台边界与 Kimi instance registry 两份 Accepted ADR。
- 后续注意事项：公开 tag 前必须配置 Tauri updater 和 Apple signing/notarization Secrets；任一平台失败都只保留 draft，不得手工发布不完整的 `latest.json`。

## 2026-08-10-15:44 - macOS 单行标题栏操作区

- 用户目标：把 workspace 标题栏的五个应用按钮移动到 macOS 窗口右上角，并让 header 保持单行。
- 涉及目录：`apps/kimi-shell/src/features/window`、`apps/kimi-shell/src` 与 `apps/kimi-shell/src-tauri`。
- 修改内容：macOS 主窗口从透明标题栏切换为 Overlay；五个 workspace 操作合并到右侧单一 action group；traffic lights 仍由系统绘制；右侧 popover 改为向左展开；Windows 继续保留左侧应用操作和右侧窗口控制。
- 修改原因：Transparent 标题栏会为系统 chrome 单独保留高度，导致 React 工具栏出现在 traffic lights 下方，无法形成单行 header。
- 本次已消除的不确定性：Tauri Overlay 会把 WebView 延伸到系统标题栏下方，并保留 decorations/traffic lights；主窗口最小宽度足以容纳 78px 系统安全区与五个 24px 操作按钮。
- 本次采用的关键假设：用户标记的五个按钮指新建窗格、窗格库、浏览器打开、主题切换和控制中心。
- 剩余不确定性：不同 macOS 版本的系统标题栏高度仍需通过本机视觉复核；Tauri 官方也将此列为 Overlay 模式的系统差异。
- 影响范围：仅 macOS 主 workspace 标题栏布局变化；prefill 与 workspace import picker 继续使用 Transparent；Windows 布局与窗口控制行为不变。
- 验证方式：`pnpm exec tsc --noEmit`、标题栏定向测试 8/8、前端全量测试 205/205、173 项命令/窗口 ACL 检查、`pnpm build`、`pnpm tauri:build:macos:local`。
- 是否更新 README：否，目录职责、公共接口与运行方式未变化。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否，延续已接受的 macOS 原生窗口平台 ADR。
- 后续注意事项：若实体机观察到 traffic lights 垂直位置在特定系统版本偏移，应使用 Tauri `trafficLightPosition` 做平台配置校准，不要重绘系统按钮。

## 2026-08-10-15:49 - 修复 macOS 标题栏拖动 ACL

- 用户目标：解释并修复 header 下方 `Command plugin:window|start_dragging not allowed by ACL` 红色错误提示。
- 涉及目录：`apps/kimi-shell/src-tauri/capabilities` 与 `apps/kimi-shell/scripts`。
- 修改内容：新增仅适用于 `macOS`、仅匹配 `main` 窗口、且只授权 `core:window:allow-start-dragging` 的 `macos-overlay-titlebar` capability；安全检查固定验证其平台、窗口和权限精确集合，并拒绝未知 capability 持有拖动权限。
- 修改原因：主窗口切换为 Overlay 后使用前端自定义拖拽区，但原有 `start-dragging` 权限只存在于 Windows-only 的 frameless capability，Tauri 因此按 ACL 正确拒绝 macOS 调用。
- 本次已消除的不确定性：错误来自 Tauri core window command 的权限归属，不是 Kimi Code、网络、登录或模型服务故障。
- 本次采用的关键假设：仅 main Overlay 标题栏需要 macOS 自定义拖动；prefill 与 workspace import picker 保持系统 Transparent 标题栏，不扩大其权限。
- 剩余不确定性：需在用户正常退出旧进程、重新打开新包后进行一次实体拖动确认。
- 影响范围：仅增加 main 本地窗口在 macOS 上发起系统拖动的能力；未增加关闭、最小化、文件、网络、shell 或远程页面权限。
- 验证方式：capability JSON 解析、`cargo check --locked`、`pnpm check:nfr:security`、前端 205/205、TypeScript 检查与 `pnpm tauri:build:macos:local`。
- 是否更新 README：否，目录职责、公共接口和运行方式未变化。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否，属于既有 macOS Overlay ADR 的最小权限修复。
- 后续注意事项：不要把该权限并入 `core:default` 或扩展到 `*` 窗口；新增可拖动窗口时必须单独做能力审查。

## 2026-08-10-15:55 - macOS 原生 Split View pane 样式

- 用户目标：优化内部 pane 的圆角和边框，使其与 macOS 原生应用窗口及单行标题栏更协调。
- 涉及目录：`apps/kimi-shell/src`。
- 修改内容：仅在 macOS 将常驻 pane 的圆角与完整外框移除；多 pane 之间改用 1px 中性分隔线；单 pane/maximized 状态贴合内容区；header 增加 hairline，激活态改为轻微中性色 header 底色；拖放目标继续保留瞬态圆角强调。
- 修改原因：常驻 pane 使用与应用窗口相同的 10px 圆角和完整边框会形成“窗口套窗口”的卡片感，不符合 macOS Split View 的层级表达。
- 本次已消除的不确定性：调整可完全限制在 `.platform-macos` 样式作用域内，不需要修改 pane 拖拽、缩放、WebView 生命周期或 Windows 布局。
- 本次采用的关键假设：常驻工作区使用系统式平面分隔，只有临时拖放反馈需要浮层式圆角；激活状态不使用品牌色，避免与状态语义冲突。
- 剩余不确定性：不同显示缩放下的 1px hairline 和 4% header tint 仍需实体机视觉复核。
- 影响范围：仅 macOS workspace grid 视觉；Windows pane 卡片样式、交互与状态逻辑不变。
- 验证方式：`pnpm exec tsc --noEmit`、前端测试 205/205、`pnpm check:nfr:security`、`pnpm build`、`git diff --check`、`pnpm tauri:build:macos:local`。
- 是否更新 README：否，目录职责、公共接口和运行方式未变化。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否，延续已接受的 macOS 原生窗口平台 ADR。
- 后续注意事项：实体机若觉得激活 header 对比度过强或过弱，应只微调中性色混合比例，不恢复 pane 完整描边或品牌色焦点框。

## 2026-08-10-16:03 - 修复 pane 明暗主题同步

- 用户目标：修复外壳切换明暗主题后，Kimi Code pane 内部仍保持浅色的问题。
- 涉及目录：`apps/kimi-shell/src-tauri/src` 与 `apps/kimi-shell/src/app`。
- 修改内容：全 frame 主题桥接改为写入 Kimi Code 0.34 的 `kimi-web.color-scheme`，同步设置 `html[data-color-scheme]`、原生 `color-scheme`、theme-color 与兼容 `.dark`/`kimi-theme`；旧 workspace proxy 注入路径同步更新；新增深色到浅色双向回归断言与 Rust 契约测试。
- 修改原因：Kimi Code 当前 Web UI 已从旧 `kimi-theme`/`.dark` 契约迁移到 `kimi-web.color-scheme`/`data-color-scheme`，旧桥接只能改变 frame 背景提示，无法驱动其组件 token。
- 本次已消除的不确定性：本机 Kimi Code 0.34.0 的 `/boot.js`、生产 JS 与 CSS 均确认新主题键、允许值和根节点选择器；pane 主题消息本身已正确发送到目标 origin；本机持久化 Grid state 中该 pane 不含独立 `theme` 覆盖，排除历史浅色状态干扰。
- 本次采用的关键假设：宿主只发送显式 `light`/`dark`；保留旧键和 `.dark` 作为旧 Kimi Web 兼容，不覆盖上游组件 CSS。
- 剩余不确定性：未来 Kimi Code 若再次修改外观存储契约，需要更新该薄桥接并刷新契约测试基线。
- 影响范围：Code/Chat iframe 的主题消息接收层；不改变主题按钮、pane 布局、WebView 生命周期或 token 导航；Windows 与 macOS 共用修复。
- 验证方式：主题定向前端测试 30/30、前端全量 205/205、Rust 258/258、`cargo fmt --check`、`cargo check --locked`、`cargo clippy --locked --all-targets -- -D warnings`、TypeScript、安全门禁、生产构建、`git diff --check` 与 `pnpm tauri:build:macos:local`。
- 是否更新 README：否，公共能力和运行方式未变化。
- 是否更新 `.ai/architecture`：否，修复后实现重新符合其“未设置独立主题的 pane 跟随全局主题”事实。
- 是否需要新增或更新 `.ai/decisions`：否，属于既有 iframe/WebView 桥接的上游兼容修复。
- 后续注意事项：不要通过大段 CSS 覆盖模拟 Kimi 暗色主题；应继续驱动上游原生外观契约，避免组件 token 漂移。
