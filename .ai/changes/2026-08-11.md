## 2026-08-11-00:13 - macOS V1 审查修复与合并门禁

- 用户目标：依据 macOS 分支审查报告制定并执行修复/优化 goal，使分支达到可验证的合并候选状态，并明确仍需外部环境完成的门禁。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src`、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`apps/kimi-shell/README.md` 与 `tasks/todo.md`。
- 修改内容：owned Kimi 进程在 spawn 后立即登记 child 与 process-group identity；新增 backend lifecycle operation 串行 stop/restart/start-failure/monitor 收口；TERM/KILL 必须确认 leader 与整组消失，未确认时保留 owned ledger、拒绝 external 重分类和 App exit；runtime locator 写入增加 generation/state 串行校验。
- 修改内容：owned registry 归属改为本次 child PID 精确匹配并受 launch-time 容差约束，删除新增 server-id 与端口扫描启发式；external runtime 继续 never-kill；补充 startup/stop/restart/quit/process-group/PID-reuse 回归。
- 修改内容：macOS guided install 增加本地中文步骤、精确安装/升级命令复制、成功/失败内联反馈与系统 Terminal.app 入口；系统入口只执行 `open -a Terminal`，不粘贴或执行命令，并隐藏 macOS 不适用的“内置终端”输出区。
- 修改内容：CI Rust matrix 从 test compile 改为实际 `cargo test --locked`；新增 PR macOS arm64 `.app` 构建、Info.plist/架构校验与 artifact；release 发布前精确校验 Windows NSIS/MSI、macOS DMG/app updater、最终 app/sidecar 架构与 codesign。
- 修改原因：原实现存在 Starting 阶段 child 句柄不可见、leader 退出后 descendants 遗留、所有权启发式误分类、stale locator side effect、复制静默失败以及 CI 可在 Rust tests/`.app` 打包损坏时仍变绿的问题。
- 本次已消除的不确定性：明确安装阶段使用用户系统 Terminal、运行阶段使用 App-owned 后台进程；明确 ownership 只能由本次 spawn/OS 进程关系证明；明确未确认 owned tree 消亡时 fail closed，而不是继续退出或洗成 external。
- 本次采用的关键假设：当前支持的 Kimi 0.34 registry PID 与 `Command::spawn` PID 一致；如真实 wrapper 不满足，只允许以后增加 OS-verifiable descendant 兼容。macOS Terminal 入口遵循 native-feel T3，采用平台能力而不模拟终端。
- 剩余不确定性：macOS Kimi 0.34.0 已以隔离 home 证明 spawn PID 与 registry PID 一致且 health/Bearer 均为 200；最终 Windows 安装基线仍需确认 exact PID 契约。GitHub hosted runner 的新增 `.app` job、Developer ID/notarization/stapling、最终 DMG 和真实 updater E2E 必须在外部 G3 环境完成。
- 影响范围：Kimi runtime lifecycle/locator/ownership，macOS 安装引导，Tauri command ACL，PR/release workflow；不改变 external runtime 的 never-kill 语义，不在 macOS 自动执行安装脚本，不修改 Kimi Server API、Bridge DB 或 Workspace 数据。
- 验证方式：Rust fmt/check/clippy、registry/lifecycle 定向 tests、完整 Rust tests；TypeScript、Vitest、安全 command/capability gate、生产构建；Go vet/test/race；workflow YAML/actionlint；本地 Apple Silicon `.app` build 与 `plutil`/`lipo` 校验。外部 G3 项保留为未完成。
- 是否更新 README：是，补充 macOS Terminal 打开但不执行命令的长期安装边界。
- 是否更新 `.ai/architecture`：是，刷新 owned lifecycle/registry、CI artifact 与安装引导当前事实和验证门。
- 是否需要新增或更新 `.ai/decisions`：是，更新 instance registry ownership 证据规则与 macOS 外部安装边界；未新增新的架构路线。
- 后续注意事项：合并前要求 GitHub Actions 两平台 Rust/前端/Go 与 macOS `.app` artifact job 绿色；公开发布前仍必须完成签名、公证、stapling、DMG 和 updater G3。

## 2026-08-11-00:31 - macOS 标题栏双击与光标修复

- 用户目标：修复 macOS 主窗口双击 header 无法最大化的问题，并让鼠标位于 header 时保持系统箭头而不是手形光标。
- 涉及目录：`apps/kimi-shell/src/features/window`、`apps/kimi-shell/src/App.css`、`apps/kimi-shell/src-tauri/capabilities` 与安全 capability gate。
- 修改内容：macOS `main` Overlay 标题栏 capability 在既有 `start-dragging` 外增加最小范围的 `toggle-maximize` 与 `is-maximized`；安全脚本固定三项权限及唯一 owner；标题栏拖拽面、按下态和工具栏按钮统一使用默认箭头；增加 drag zone 双击回归测试。
- 修改原因：前端已经调用 `toggleMaximize()` 并随后读取 `isMaximized()`，但 macOS capability 未授权这两个 window commands；CSS 又显式将 drag zone 设为 `grab/grabbing`、按钮设为 `pointer`。
- 本次已消除的不确定性：截图中的红色错误是 Tauri ACL 拒绝，不是 macOS zoom、窗口尺寸或事件命中失败；workspace drag zone 已有主键双击事件且不会覆盖交互控件。
- 本次采用的关键假设：用户所说的“最大化”沿用当前产品的 Tauri window zoom/toggle 行为；Windows frameless 权限和窗口按钮保持不变。
- 剩余不确定性：不同 macOS 的系统“双击标题栏”用户偏好不由当前自定义 Overlay 区域读取；本轮按用户明确要求固定为窗口 zoom。
- 影响范围：仅 macOS `main` Overlay 标题栏增加两项窗口级权限；不授权 prefill、picker、远程页面或文件/网络/进程能力。
- 验证方式：ShellTitlebar 定向测试、TypeScript、capability/security gate、生产构建与本机 `.app` smoke。
- 是否更新 README：否，修复后重新符合既有 macOS 原生标题栏行为描述。
- 是否更新 `.ai/architecture`：否，未改变窗口平台边界。
- 是否需要新增或更新 `.ai/decisions`：否，延续既有 macOS V1 ADR。
- 后续注意事项：标题栏工具栏是桌面 chrome，继续使用系统箭头；不要恢复 Web 风格 `pointer` 或拖拽手形。

## 2026-08-11-00:38 - 恢复 pane 分隔线 resize 光标

- 用户目标：鼠标悬停在两个 pane 之间可调整尺寸的分隔位置时，显示对应的横向或纵向 resize 光标。
- 涉及目录：`apps/kimi-shell/src/index.css` 与既有 Workspace Grid resize handle。
- 修改内容：全局桌面 chrome 的默认箭头规则排除 `.workspace-grid-resize-handle`，让已有列分隔线 `col-resize` 和行分隔线 `row-resize` 样式重新生效。
- 修改原因：resize handle 使用原生 `button` 承载键盘与可访问性语义，但全局 `button { cursor: default !important; }` 覆盖了组件中更具体但非 important 的 resize cursor。
- 本次已消除的不确定性：分隔线拖动逻辑、8px 命中区域、pointer capture 和列/行方向样式均已存在；问题仅是全局 CSS 优先级覆盖。
- 本次采用的关键假设：只为真正可调整尺寸的 handle 使用系统 resize 光标；普通 toolbar、列表和标题栏继续使用默认箭头。
- 剩余不确定性：无代码级剩余项；最终体感仍需在新构建 `.app` 中悬停横向与纵向分隔线确认。
- 影响范围：仅 Workspace Grid resize handles；不改变 pane 尺寸算法、持久化、拖拽交换、iframe/WebView 或其他按钮光标。
- 验证方式：TypeScript、WorkspaceGridView/全量前端测试、生产构建、安全门禁和本机 macOS `.app` 构建。
- 是否更新 README：否，属于既有 resize 能力的 CSS 回归修复。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：全局默认箭头规则新增控件类型时，应为 resize/text 等系统语义光标保留窄范围例外。

## 2026-08-11-01:05 - macOS 应用内升级 Kimi Code

- 用户目标：在 macOS 上由用户确认后停止小助手管理的 `kimi web`，执行已定位的 `kimi upgrade`，实时显示日志，完成后重新检测并重启服务。
- 涉及目录：`apps/kimi-shell/src-tauri/src/install_manager.rs`、控制中心安装 UI、Tauri dialog capability、Shell/架构 README、macOS 平台 ADR 与任务记录。
- 修改内容：macOS 首次安装继续 external guided；已安装后的 `upgrade_kimi` 改走独立 managed path，先验证 locator 返回的绝对 executable，再收口 Shell-owned 后端，以无 shell 字符串的单参数 `upgrade` 启动独立进程组，实时流式展示 stdout/stderr，支持 TERM→KILL 取消和整组退出确认，成功后执行版本复检并自动请求重启后端。
- 修改内容：升级入口使用 Tauri 原生 warning 对话框；Mac 控制中心新增升级状态、精确 executable、取消动作和实时日志面板；升级流 stdout/stderr 在进入 snapshot/channel 前统一经过 secret redactor。
- 修改原因：macOS 技术上支持受控本地升级，但不应为了统一 Windows 实现而复用 PowerShell，也不应把首次安装的远程 pipe 静默化；本次按 native-feel T3 将系统确认和进程生命周期放在原生/Rust 边界。
- 本次已消除的不确定性：Mac managed upgrade 只运行 locator 已验证的 executable，不经过用户 PATH 二次解析；安装仍不自动执行远程脚本；external runtime 保持 never-kill，只有 Shell-owned runtime 进入停止路径。
- 本次采用的关键假设：已安装的受支持 Kimi Code 提供稳定的 `upgrade` 子命令，且命令成功退出表示 binary 更新已完成；真实网络下载与官方服务响应由 Kimi CLI 自身负责。
- 剩余不确定性：真实生产安装的 Kimi Code 升级下载、签名/公证 `.app` 中的原生对话框、升级后版本变化和后端恢复仍需 G3 实机用例；自动化只验证命令边界、平台 catalog、日志脱敏、UI 状态和 Rust/TS 行为。
- 影响范围：新增 macOS managed upgrade，不改变 Windows install catalog，不开放 macOS 应用内首次安装，不修改 Kimi Server/Bridge 数据契约；新增 `main` 窗口最小 `dialog:allow-ask` capability。
- 验证方式：Rust fmt/check/clippy 与 267 项全量 test 通过，install_manager 31 项定向 test 通过；TypeScript、43 files / 211 tests 全量 Vitest、security capability/command/resource gate 与生产 build 通过；本机 Apple Silicon unsigned `.app` 构建、Info.plist 和主程序/Bridge arm64 校验通过。真实 Kimi upgrade/restart 仍作为 G3。
- 是否更新 README：是，更新根 README 与 Shell README 的长期平台安装/升级边界。
- 是否更新 `.ai/architecture`：是，记录 macOS guided install + managed upgrade 的当前事实、安全边界与失败语义。
- 是否需要新增或更新 `.ai/decisions`：是，更新既有 macOS V1 Accepted ADR，明确允许直接调用已验证 executable 的受控升级，同时继续禁止远程安装 pipe 和 external kill。
- 后续注意事项：不得把 managed upgrade 扩展为通用交互终端；任何新的子进程日志出口必须先脱敏，任何 wrapper/descendant 兼容必须以 OS 可证明的进程组/进程树为依据。

## 2026-08-11-08:28 - WorkspaceHub 目标目录选择与打开

- 用户目标：确认 Harness 的目标目录是否只能输入，并改成与默认工作目录一致的“输入、浏览、打开目录”交互。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/App.css`、控制中心公共字段样式与 `tasks/todo.md`。
- 修改内容：保留目标路径文本输入；所有 `type: "path"` 的 Harness 变量自动显示“浏览”按钮和文件夹图标按钮；浏览调用 Tauri 原生目录选择器并回填路径，文件夹按钮复用既有受控 `open_folder` 调用链；空路径和 busy 状态禁用打开动作。
- 修改原因：目标目录原先只是普通 Input，容易输错且与默认工作目录的既有交互不一致。
- 本次已消除的不确定性：Harness manifest 已用 `type: "path"` 明确标注目录变量，因此不需要依赖 `workDir` 字段名或中文 label 判断；主窗口已有目录选择 capability 和受控打开目录 command，无需新增权限。
- 本次采用的关键假设：用户仍可能需要粘贴或编辑尚未最终选择的路径，因此保留可编辑输入，而不是替换成只读选择结果。
- 剩余不确定性：原生 picker 和 Finder/Explorer 的最终视觉与行为仍随系统版本变化，自动化只验证调用参数、回填和打开动作。
- 影响范围：仅 WorkspaceHub Harness path 变量的输入体验；不改变 Harness schema、目录创建/覆盖校验、已注册 Workspace 数据或 backend 生命周期。
- 验证方式：WorkspaceHub 定向交互测试通过；TypeScript、43 个测试文件 / 212 项全量 Vitest、安全 capability/command/resource gate与 Vite production build 通过；`git diff --check` 通过。
- 是否更新 README：否，属于既有 Harness path 字段的局部交互增强。
- 是否更新 `.ai/architecture`：否，未改变模块职责、数据流或权限边界。
- 是否需要新增或更新 `.ai/decisions`：否，沿用原生系统目录选择器的既有平台边界。
- 后续注意事项：新增 Harness 目录变量应继续声明 `type: "path"`，不要通过 key/label 特判；不要把系统 picker 替换为 WebView 自绘文件浏览器。
