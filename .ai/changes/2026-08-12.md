## 2026-08-12-10 - 技能库滚动与 WorkspaceHub 详情扁平化

- 用户目标：恢复技能库的独立滚动，并优化 WorkspaceHub Harness 详情中的冗余卡片层级和返回按钮位置。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/App.css`、WorkspaceHub 组件测试。
- 修改内容：修正紧凑 Skill 列表覆盖纵向滚动的问题，禁止统一 Rail 横向溢出；将 Harness 返回入口、标题和主操作提升到页面标题栏；移除 Harness 详情外层卡片及变量字段嵌套卡片，改为无框概览和分隔线分区。
- 修改原因：后置紧凑样式覆盖了列表已有的 `overflow-y: auto`；WorkspaceHub 详情同时存在页面、外层卡片、分区卡片和字段卡片，层级超过设计系统限制。
- 本次已消除的不确定性：确认问题来自 CSS 覆盖而不是数据未加载；确认 Harness 与已注册工作区可以共用同一详情标题栏模式。
- 本次采用的关键假设：技能库列表在内容区内独立滚动，统一 Rail 仅纵向滚动；Harness 详情保留原操作和数据，不改变创建流程。
- 剩余不确定性：系统滚动条在不同 macOS“始终显示滚动条”偏好下的视觉宽度仍需真机检查。
- 影响范围：控制中心前端布局、样式和测试；不改变 WorkspaceHub manifest、文件系统权限或 Tauri command。
- 验证方式：TypeScript、WorkspaceHub/Skill 定向测试、全量 Vitest、安全 gate、生产构建、macOS `.app` 构建与 arm64/Info.plist 校验。
- 是否更新 README：否，模块职责和公共接口未改变。
- 是否更新 `.ai/architecture`：否，运行架构与长期事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否，本次为局部可回退 UI 修复。
- 后续注意事项：在真实 macOS 窗口中检查技能列表滚轮、Rail 长名称省略和 Harness 窄窗口布局。

## 2026-08-12-09 - 控制中心信息架构与密度优化

- 用户目标：将已确认的控制中心原型落实到正式应用，统一页面层级、收紧列表密度，并把 WorkspaceHub 创建所需变量前置。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/features/skill-center`、`apps/kimi-shell/src/App.css`、前端组件测试。
- 修改内容：小助手设置由八张独立卡收拢为“更新与运行 / 工作目录与通道 / 诊断与日志”三个分区；统一 Rail 接入 Skill、Harness、已注册工作区和调度工作区上下文；Skill 与 WorkspaceHub 目录改为紧凑列表；Harness 变量和目标目录选择前置，模板文件改为按需展开；全局后端重启移入诊断分区，Rail footer 只显示运行状态；控制中心取消页面切换动画、平滑滚动和按钮手型鼠标。
- 修改原因：原界面操作优先级不清、卡片层级过多、对象列表与详情上下文分离，且部分 Web 风格交互不符合桌面原生习惯。
- 本次已消除的不确定性：确认 WorkspaceHub 和调度面板已有统一 Rail 回调，正式实现无需修改 Tauri command 或后端数据契约；确认目录选择继续使用系统原生 picker。
- 本次采用的关键假设：用户确认的原型代表本轮信息架构基线；所有既有动作必须保留，但可移动到更贴近任务的位置。
- 剩余不确定性：真实 macOS / Windows WebView 中的字体渲染、系统缩放和窄窗口手感仍需 G3 人工走查。
- 影响范围：仅控制中心 React 组件、局部 CSS 与测试；不改变安装升级、Bridge、Skill、WorkspaceHub 或调度的序列化契约。
- 验证方式：`pnpm exec tsc --noEmit`；`pnpm test`（43 files / 214 tests）；`pnpm check:nfr:security`；`pnpm build`；`git diff --check`。
- 是否更新 README：否，目录职责与公共接口未改变。
- 是否更新 `.ai/architecture`：否，运行架构与长期事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否，本次为可回退的 UI 信息架构调整。
- 后续注意事项：发布前分别在 macOS 与 Windows 检查 Rail 滚动、系统字体缩放、键盘焦点和原生文件选择器。
