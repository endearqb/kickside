## 2026-08-13-09 - macOS V1 合并与 0.1.23 发布准备

- 用户目标：将 `codex/macos-v1` 合并到 `main`，提升版本号，并发布 Windows/macOS Release；同时确认 Windows 影响与测试责任。
- 涉及目录：`apps/kimi-shell`、`.github/workflows`、`.ai/changes`、`tasks`。
- 修改内容：将应用版本从 `0.1.22` 同步提升到 `0.1.23`；为 macOS-only Rust 项补充最小条件编译边界，避免 Windows `clippy -D warnings` 失败；将 Unix login-shell parser 测试限制在 Unix；准备通过 PR CI 验证 Windows/macOS 后合并。
- 修改原因：macOS 分支修改了共享 UI、Rust 生命周期、Bridge sidecar 与两端发布流程，Windows 同样受影响；版本 tag 必须与 package/Cargo/Tauri 版本一致。
- 本次已消除的不确定性：`origin/codex/macos-v1` 基于最新 `origin/main` 且可快进；下一可用补丁版本是 `0.1.23`；仓库现有 Release 工作流会同时构建 Windows x86_64 与 macOS arm64。
- 本次采用的关键假设：本次按补丁版本发布 `0.1.23`；Windows 与 macOS 必须共享同一 tag 和唯一跨平台 `latest.json`。
- 剩余不确定性：Apple Developer ID 签名、公证、stapling、最终 DMG 与两端真实 updater/安装生命周期仍需 G3；仓库尚未配置工作流要求的 Apple 发布 Secrets。
- 影响范围：Windows 会获得共享控制中心/UI、Kimi 定位与生命周期、Bridge sidecar、Updater manifest 和发布配置变更；macOS 新增 arm64 `.app`/DMG、原生窗口菜单与平台安装/升级路径。
- 验证方式：Windows 本地 `cargo fmt --all -- --check`、`cargo check --locked`、`cargo clippy --locked --all-targets -- -D warnings`、`cargo test --locked`（262 项）、TypeScript、Vitest（49 files / 230 tests，Node 25 关闭实验性 Web Storage）、Updater manifest tests、安全 gate、`go vet ./...`、`go test ./...`；Windows/macOS GitHub PR CI 待执行。
- 是否更新 README：否；目标分支已包含 Windows/macOS 职责、构建和发布说明更新。
- 是否更新 `.ai/architecture`：否；目标分支已包含双平台当前事实和验证门更新。
- 是否需要新增或更新 `.ai/decisions`：否；目标分支已包含 Accepted macOS V1 平台 ADR。
- 后续注意事项：合并前要求 PR CI 全绿；发布前配置 `APPLE_CERTIFICATE`、`APPLE_CERTIFICATE_PASSWORD`、`APPLE_SIGNING_IDENTITY`、`APPLE_ID`、`APPLE_PASSWORD`、`APPLE_TEAM_ID`，并完成 Windows/macOS G3 回归。

## 2026-08-13-12 - Windows CMD shim 后端归属修复

- 用户目标：修复 0.1.23 Windows 安装版在 Kimi Code 已安装且服务已启动时仍显示“后端异常”和“缺失”。
- 涉及目录：`apps/kimi-shell/src-tauri`、`.ai/changes`、`tasks`。
- 修改内容：Windows registry owned candidate 从只接受启动器 PID，扩展为接受该 PID 的当前进程树；增加 ToolHelp 进程快照 feature 和真实 CMD 子进程测试；安装探针统一复用 `kimi_locator`。
- 修改原因：pnpm/npm 的 `kimi.CMD` 由 `cmd.exe` 派生 Node 服务，registry 记录的是 Node PID；合并后的严格 PID 等值校验误拒绝了真实服务。安装页另用 canonical `\\?\` 路径直接执行 CMD，会返回失败并误报缺失。
- 本次已消除的不确定性：本机 `kimi.CMD --version` 返回 0.34.0；backend.log 显示 56560 已 ready；app.log 显示启动 PID 与 registry PID 不同，随后因 15 秒内找不到同 PID registry 而失败。
- 本次采用的关键假设：CMD 派生进程属于应用本次创建的 Windows 进程树；仍需 registry 时间窗口、loopback、health 和 bearer auth 全部通过才能接管。
- 剩余不确定性：本地安装包的真实升级安装与应用启动需用户完成 G3；macOS 签名/公证仍未配置。
- 影响范围：Windows npm/pnpm Kimi Code 启动与安装检测；Unix/macOS PID 等值规则和安全验证不变。
- 验证方式：`cargo fmt --all -- --check`、instance registry 专项 7 项、Rust 全量 264 项、`cargo clippy --locked --all-targets -- -D warnings`、前端生产构建、Bridge sidecar 构建、本地关闭 updater artifact 后的 NSIS/MSI 构建。
- 是否更新 README：否；没有改变已记录的平台职责或运行方式，而是恢复 README 声明的 npm/pnpm shim 支持。
- 是否更新 `.ai/architecture`：否；不改变架构方向或验证入口。
- 是否需要新增或更新 `.ai/decisions`：否；属于 Windows 兼容性 bugfix。
- 后续注意事项：用户安装新包后应确认启动直接进入工作区、Kimi 显示已安装、退出后不残留本次启动的 Node 服务树。

## 2026-08-13-12 - macOS Kimi Code native 升级修复

- 用户目标：修复 macOS 不显示 Kimi Code 0.35.0 可升级状态，以及执行 native `kimi upgrade` 后仍停留 0.34.0 却报告成功。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/README.md`、`.ai/decisions`、`tasks`。
- 修改内容：安装状态复用跨平台 `kimi_locator`；抽取 latest version 查询；macOS 升级改为读取官方 manifest、下载架构 binary、校验 SHA-256/版本、备份并原子替换；复检必须等于目标版本，失败自动回滚；手动升级命令同步为官方 install.sh。
- 修改原因：旧探针只覆盖 Windows 候选，阻断 macOS 版本检测；Kimi Code 0.35.0 的 native `upgrade` 子命令只提示手动安装脚本且返回成功码，旧逻辑据此虚报升级成功。
- 本次已消除的不确定性：官方 latest 当前返回 0.35.0；manifest 提供 darwin-arm64/x64 filename 和 SHA-256；官方 install.sh 同样按 manifest 下载并校验 checksum。
- 本次采用的关键假设：官方 binary manifest 是 native 安装的可执行发布契约；V1 支持 Apple Silicon，仍保留 x64 分支以避免 Rosetta/开发机误装架构。
- 剩余不确定性：需在用户 Apple Silicon Mac 上完成真实 0.34.0 → 0.35.0、取消、断网/checksum 失败和后端重启 G3；当前 Windows 主机不能生成或运行 macOS app。
- 影响范围：macOS Kimi Code 安装状态、版本检测、受管升级与 Terminal 指引；Windows 继续使用既有任务。
- 验证方式：官方 endpoint/manifest 只读核对、bash `-n`、Rust 265 项全量测试、clippy `-D warnings`、TypeScript、Vitest 49 files/230 tests、安全 gate、生产构建。
- 是否更新 README：是；受管升级方式由直接 `kimi upgrade` 改为官方 manifest/checksum binary replacement。
- 是否更新 `.ai/architecture`：否；外部 Kimi、owned-only 停止和验证边界不变。
- 是否需要新增或更新 `.ai/decisions`：是；更新 accepted macOS V1 ADR 中已经失效的上游升级契约。
- 后续注意事项：发布前必须在 macOS 验证 binary 替换权限、`.bak` 回滚、取消进程组、目标版本展示和 owned backend 自动重启。
## 2026-08-13-14 - 0.1.24 过渡发布准备

- 用户目标：将 Windows/macOS Kimi 修复合并到 `main`，补丁版本提升到 `0.1.24`，构建 Windows 安装包，并先发布带警告的未签名 macOS 版本。
- 涉及目录：`apps/kimi-shell`、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`tasks`。
- 修改内容：快进合并 `codex/fix-windows-kimi-shim`；同步 package/Cargo/Tauri 版本为 `0.1.24`；Release workflow 仅允许 `v0.1.24` 使用不含 Developer ID 身份的 macOS ad-hoc 签名并跳过公证，验证 `.app` 无 Apple signing authority，并在 Release 顶部加入警告；Tauri updater 私钥和两端 `.sig` 仍为必需项。
- 修改原因：当前没有 Apple Developer ID/公证 Secrets，用户明确要求先发布未签名 macOS 过渡版；需要在不削弱 updater 完整性校验的前提下放宽 Apple 发布门禁。
- 本次已消除的不确定性：修复分支包含当前 `origin/main` 且可快进；下一补丁版本为 `0.1.24`；Tauri Apple 代码签名与 updater minisign 为独立边界。
- 本次采用的关键假设：`0.1.24` 是一次性过渡例外；Windows 与 macOS 继续共用单一 tag 和 `latest.json`。
- 剩余不确定性：macOS GitHub runner 的实际 app/DMG 产物、未签名校验和安装行为需等 tag workflow/G3；Apple Developer ID 受信发布仍 blocked。
- 影响范围：`0.1.24` 版本元数据、Windows NSIS/MSI、macOS arm64 app/DMG、GitHub Release 描述和 updater manifest。
- 验证方式：版本四处一致性检查；Release YAML 解析和一次性 tag/警告/ad-hoc 配置检查；`cargo fmt --all -- --check`、`cargo clippy --locked --all-targets -- -D warnings`、`cargo test --locked`（265 项）；TypeScript、Vitest（49 files / 230 tests）、Updater manifest 2 项、安全 gate；本地 `tauri build --no-sign --bundles nsis,msi` 成功，NSIS/MSI 和主程序版本均为 `0.1.24`。GitHub Actions 与 macOS 产物仍待 tag 后验证。
- 是否更新 README：是；发布方式和签名要求发生临时变化。
- 是否更新 `.ai/architecture`：是；同步当前 Release workflow 事实和验证门禁。
- 是否需要新增或更新 `.ai/decisions`：是；在 macOS V1 ADR 中登记 `0.1.24` 一次性未签名例外及退出条件。
- 后续注意事项：后续版本在未恢复 Developer ID 工作流前必须 fail closed；不得将未签名 macOS 包宣称为已公证或 Gatekeeper 受信。

## 2026-08-13-14 - macOS CI stop endpoint 测试同步

- 用户目标：完成 `0.1.24` 合并与发布；main CI 必须先通过。
- 涉及目录：`apps/kimi-im-bridge/internal/admin`、`.ai/changes`、`tasks`。
- 修改内容：stop endpoint 测试在收到 HTTP 202 后有界等待异步 `RequestStop` 回调，而不是立即读取计数器。
- 修改原因：生产 handler 明确用 goroutine 在响应后请求停止；macOS runner 在 goroutine 调度前执行了断言，产生 timing-dependent CI 失败。
- 本次已消除的不确定性：GitHub Actions 完整日志确认唯一失败为 `TestApprovalsAndRuntimeStopEndpoints` 的 `expected RequestStop to be called`；实现与测试之间存在明确异步契约。
- 本次采用的关键假设：一秒上限远高于无 I/O fake callback 的合理调度时间，同时保持测试失败有界。
- 剩余不确定性：后续 Release workflow、ad-hoc app/DMG 和 updater manifest 仍待 tag 后验证。
- 影响范围：仅 Go admin endpoint 测试同步；生产代码和 API 契约不变。
- 验证方式：admin 定向测试连续 100 次通过、Go 全量测试通过；main CI 八个 Windows/macOS jobs 全部通过。
- 是否更新 README：否；运行方式和接口职责未改变。
- 是否更新 `.ai/architecture`：否；验证入口未改变。
- 是否需要新增或更新 `.ai/decisions`：否；测试竞态修复不属于长期架构决策。
- 后续注意事项：异步 HTTP handler 测试不得用响应返回作为后台副作用已经完成的证明。

## 2026-08-13-15 - v0.1.24 已发布

- 用户目标：合并到 `main`、版本号增加 `0.0.1`、构建 Windows 安装包，并发布带未签名警告的 macOS 版本。
- 涉及目录：`main`、GitHub Actions/Release、本地 `apps/kimi-shell/src-tauri/target/release/bundle`。
- 修改内容：推送 annotated `v0.1.24` tag；Release workflow 构建并发布 Windows x86_64 NSIS/MSI、macOS arm64 ad-hoc app updater/DMG、两端 updater `.sig` 与唯一 `latest.json`。
- 修改原因：完成用户要求的 `0.1.24` 双平台过渡发布。
- 本次已消除的不确定性：main CI 八个 jobs 全绿；Release 四个 jobs 全绿；Release 已非 draft/prerelease；macOS job 验证 ad-hoc identity 且无 Apple signing authority；线上 manifest 版本和双平台键正确。
- 本次采用的关键假设：用户会将当前 macOS 包视为明确告警的测试版，不视为 Developer ID 签名、公证或 Gatekeeper 受信版本。
- 剩余不确定性：Windows 旧版升级与 macOS 首次打开/安装、Kimi 0.34.0 → 0.35.0 仍需用户完成真实 G3。
- 影响范围：GitHub `v0.1.24` 最新 Release 与桌面 updater endpoint。
- 验证方式：main CI run `31676495432` 成功；Release run `31677959370` 成功；线上 Release 资产清单为 8 项；`latest.json` 为 `0.1.24` 且包含 `windows-x86_64` / `darwin-aarch64`。
- 是否更新 README：否；发布策略已在发布提交中更新。
- 是否更新 `.ai/architecture`：否；当前事实和临时例外已在发布提交中更新。
- 是否需要新增或更新 `.ai/decisions`：否；`0.1.24` 例外已在发布提交中登记。
- 后续注意事项：下一版本必须先恢复 Developer ID 签名/公证 workflow；当前 workflow 对非 `v0.1.24` tag 会 fail closed。
## 2026-08-13-16 - README 截图与仓库 About 更新

- 用户目标：使用提供的四张最新应用截图替换 README 中的旧图片，并更新 About。
- 涉及目录：根 README、中文 README、`apps/kimi-shell/README.md`、`apps/kimi-shell/public`、GitHub repository metadata。
- 修改内容：新增小助手设置、Skill 中心、会话设置/并行工作和六窗格 Workspace Grid 四张语义化截图；三个 README 的预览统一改用新截图；GitHub About 更新为当前 Windows/macOS 双平台产品描述并指向最新 Release。
- 修改原因：原截图仍展示早期首页、Quick Setup、安装页与旧控制中心，不能代表当前 Workspace Grid、Skill 与设置能力。
- 本次已消除的不确定性：仓库不存在独立 About 文档；“about”按 GitHub 仓库右侧 About 元数据处理；四张源图均为有效 PNG。
- 本次采用的关键假设：四图按内容分别命名，不覆盖应用运行时可能引用的旧 public 图片。
- 剩余不确定性：无。
- 影响范围：仅文档展示和 GitHub 仓库元数据，不影响应用构建或运行。
- 验证方式：校验所有 README 图片路径存在、PNG 尺寸可读、Markdown diff 无空白错误，并读取 GitHub repository metadata 回查。
- 是否更新 README：是；本次交付物即 README 更新。
- 是否更新 `.ai/architecture`：否；架构事实未改变。
- 是否需要新增或更新 `.ai/decisions`：否；无长期架构决策。
- 后续注意事项：产品截图更新时继续使用语义化文件名，避免 README 引用临时剪贴板路径。
