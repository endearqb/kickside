## 2026-08-14-00 - DSH P0 Web pane 与计划对齐

- 用户目标：依据 DSH research、PRD、spec 和 plan 审查代码库，优化计划并实现开发目标。
- 涉及目录：`.ai/plans`、`.ai/decisions`、`.ai/architecture`、`apps/kimi-shell`、`tasks`。
- 修改内容：新增代码库对齐审查与 Accepted ADR；修订 headless/安装/生命周期计划；新增 schema 12 `agentBackends.dsh`、main-only DSH commands、固定 pin 私有安装、preflight、单实例生命周期、精确 loopback health、脱敏日志、Grid `dsh` pane、控制中心实验开关、目录 picker、首次引导，以及退出/更新停止协调。
- 修改原因：原草案错误预测通用 registry、持久化 PID 和 Bridge RuntimeAdapter/三 connector 自然复用；这些与宪法、真实代码与安全边界不符。
- 本次已消除的不确定性：P0 命名/schema/ownership/URL/安装入口/状态源；上游 pin；headless 默认 approval fail-closed；Bridge 的 one-shot 边界。
- 本次采用的关键假设：P0 只支持一个 DSH 实例；Node 采用应用开发基线并由 preflight 给出路径/版本，不伪造 DSH 未声明的 engines；本地开关不是远程 flag。
- 剩余不确定性：Windows/macOS 私有安装耗时、WebView2/WKWebView 完整交互、Windows 软停与双平台真机 descendant-kill；headless 凭据/connector 契约。
- 影响范围：新增 additive 设置字段和 pane kind；现有 Kimi/Bridge 行为不迁移，external allowlist 不变。
- 验证方式：TypeScript；Vitest；Rust lib tests；command registry/ACL；fmt/diff；Go Bridge `go test`/`go vet` 审计基线。2026-08-14 本机固定 npm 安装超过两分钟未完成后人工终止，无残留进程。
- 是否更新 README：是，记录 DSH P0 职责与安全边界。
- 是否更新 `.ai/architecture`：是，记录当前事实、Known Gap 与发布 G3。
- 是否需要新增或更新 `.ai/decisions`：是，新增 Accepted DSH P0 contract ADR。
- 后续注意事项：完成双平台 G3 前不得宣称 P0 双平台可发布；headless 硬前置不满足时保持 No-Go。

## 2026-08-14-10 - 修复 macOS 冷环境 Node/npm 探测

- 用户目标：修复本机已通过 NVM 安装 Node.js，但 Finder 启动的 macOS App 无法检测 Node/npm、导致 DSH 安装按钮禁用的问题。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/README.md`。
- 修改内容：新增具体的 Node.js 工具链定位模块，统一供原安装探测与 DSH preflight 使用；支持 PATH、NVM、Volta、asdf、nodenv、mise、fnm、Homebrew/系统常见路径，并在启动 npm/DSH 时把已解析 Node 目录显式加入受控 PATH。
- 修改原因：Finder 启动的 GUI App 不读取 shell 初始化文件，原 DSH 仅使用 `which`，无法发现 `~/.nvm/versions/node/*/bin` 中的 Node/npm。
- 本次已消除的不确定性：用户 Node 位于 `~/.nvm/versions/node/v24.19.0/bin`，版本满足 22.19+；按钮禁用由 Node 与 npm 路径均未解析导致，与 DSH pin 或网络无关。
- 本次采用的关键假设：版本管理器目录中的最高语义版本作为冷环境 fallback；显式 PATH 中可用运行时仍优先。
- 剩余不确定性：Windows 的 NVM/Volta 真机组合仍需发布 G3 覆盖；DSH npm 下载耗时与上游可用性需真机验证。
- 影响范围：只改变 Node/npm 发现与 DSH 子进程的受控 PATH；不加载或执行用户 shell rc，不改变凭据、端口和进程所有权边界。
- 验证方式：Node locator NVM/PATH 单元测试、DSH 定向测试、Rust 全量测试与 macOS arm64 本地 bundle 校验。
- 是否更新 README：是，记录共享 Node/npm 冷环境定位职责。
- 是否更新 `.ai/architecture`：否，模块边界与既有 DSH P0 架构不变。
- 是否需要新增或更新 `.ai/decisions`：否，不涉及新的单向门决策。
- 后续注意事项：正式发布前继续验证 Windows PATH/NVM 差异；不要通过执行 login shell 加载用户 rc 来弥补 GUI PATH。

## 2026-08-14-10 - DSH 默认启动与品牌化窗格入口

- 用户目标：把 DeepSeek Harness 合并到“小助手设置”的“更新与运行”，随应用启动并加载默认工作区；优化新建窗格菜单与窗格库命名、排列和官方图标。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/public`、`apps/kimi-shell/README.md`。
- 修改内容：已启用且安装就绪的 DSH 在壳启动、启用设置或安装完成后自动使用全局默认工作目录启动；新建 DSH pane 不再弹目录选择器；DSH 设置变为“更新与运行”中的可展开列表行；新建窗格和浏览器菜单改为纵向并统一为 `KimiCode`、`KimiChat`、`DeepSeek Harness`；窗格标题与窗格库接入 Kimi Code/DeepSeek Harness 官方 SVG 图标；历史 Kimi pane 标题加载时规范化为新名称。
- 修改原因：消除 Finder 启动后还需手动启 DSH、重复选工作区及横向菜单换行问题，并让同名工作区在窗格库中可通过后端品牌快速区分。
- 本次已消除的不确定性：DSH 自动启动使用与 Kimi 后端相同的全局默认工作目录解析；现有 `enabled=false` 安全默认保持不变，用户显式启用后即成为自动启动开关；图标来自官方仓库固定提交。
- 本次采用的关键假设：KimiCode 与 KimiChat 共用 Kimi Code 官方品牌图标；单实例 DSH 对相同 canonical 工作区的重复启动请求保持幂等。
- 剩余不确定性：首次安装的 npm 网络耗时及 WKWebView 中 DSH 完整交互仍需 macOS 真机验收；Windows 图标/菜单与 DSH 进程树仍需发布 G3。
- 影响范围：只调整 DSH 壳生命周期、Grid 显示与设置组织；不改变 DSH 凭据边界、loopback URL 权威或默认关闭的实验开关。
- 验证方式：TypeScript noEmit、Vitest 全量、Rust DSH 定向测试、Rust 全量测试、安全 registry 检查、macOS arm64 app bundle 构建与本机启动验证。
- 是否更新 README：是，更新 Grid、设置分组和 DSH 自动启动事实。
- 是否更新 `.ai/architecture`：否，继续沿用已接受的 DSH P0 ownership 与安全边界。
- 是否需要新增或更新 `.ai/decisions`：否，本次未新增单向门；保留原 ADR 的显式启用与默认关闭约束。
- 后续注意事项：不要把“随应用启动”误写成“新安装默认启用”；DSH 未安装或 preflight 失败时只记录诊断，不阻塞 Kimi 后端启动。

## 2026-08-14-10 - DSH 主题同步与显式浏览器打开

- 用户目标：让 DSH Web 跟随壳应用切换明暗主题，并在标题栏“在浏览器打开”菜单中加入 DSH。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/README.md`、`.ai/architecture`、`tasks`。
- 修改内容：主题 frame bridge 同步切换 DSH `body[data-ds-dark-theme]` 契约；DSH pane 始终使用壳全局主题信号；新增单一精确 loopback 运行 URL 校验 helper，并供 pane 与标题栏浏览器菜单复用；DSH 启用但未运行时菜单项保持可见禁用。
- 修改原因：既有 bridge 已收到主题消息，但只应用 Kimi 的 `html.dark`/localStorage 契约，未触发 DSH rc.6 的 token selector；浏览器菜单则静态只渲染 KimiCode/KimiChat。
- 本次已消除的不确定性：DSH rc.6 暗色权威 selector 为 `body[data-ds-dark-theme]`；标题栏显式打开可复用既有系统 opener，且无需扩大 generic external allowlist 或新增 Rust command。
- 本次采用的关键假设：DSH 在壳内应始终跟随全局主题，不采用历史 pane 独立主题；只有 `running` 状态且精确匹配 `http://127.0.0.1:<valid-port>` 的 URL 可交给系统浏览器。
- 剩余不确定性：真实 WKWebView/WebView2 的视觉切换与外部浏览器启动仍需双平台 G3 手工确认。
- 影响范围：仅扩展现有 frame 主题桥和标题栏显式动作；不修改 DSH settings.yaml、凭据、进程生命周期、URL 持久化或 external iframe allowlist。
- 验证方式：TypeScript noEmit；Vitest 51 个文件 / 235 项；安全 capability/command registry；生产构建；Rust fmt；Rust 277 项测试；`git diff --check`。
- 是否更新 README：是，区分显式浏览器打开与加载失败自动 fallback。
- 是否更新 `.ai/architecture`：是，修正 DSH 外部打开当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，沿用 Accepted DSH P0 精确 loopback 活状态 URL 契约。
- 后续注意事项：不得把 DSH URL 放入 persisted Grid state；DSH 未运行或 URL 不可信时浏览器菜单必须 fail closed。

## 2026-08-14-11 - 统一 KickSide / 启伴品牌并改名仓库

- 用户目标：先将 Kimi App / Kimi 小助手统一为 `KickSide / 启伴` 并改名 GitHub 仓库，同时保留 `com.kimi.shell`、兼容性内部 key 和当前图标。
- 涉及目录：根 README/设计系统、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`apps/kimi-shell`，以及 GitHub 仓库设置。
- 修改内容：英文与系统产品名统一为 `KickSide`，中文 UI 统一为 `KickSide 启伴`；Tauri 窗口、macOS 菜单/托盘、控制中心、启动页、增强 Web 注入、安装包、Release/updater、包元数据与文档同步改名；设置 schema 升至 13，仅迁移旧内置 Explorer 文案；GitHub 仓库改为 `endearqb/kickside`，本地 origin、仓库描述和 homepage 同步更新；生成 `KickSide.app` 与本机测试 zip。
- 修改原因：旧 `Kimi App`、`Kimi Sidekick`、`kimi小助手` 和仓库名不一致，而且壳已承载多个后端；展示品牌应与后端产品身份解耦。
- 本次已消除的不确定性：品牌规范为英文/系统级 `KickSide`、中文 `KickSide 启伴`；旧仓库 Web 与 updater URL 均可重定向到新仓库/Release 资产；当前仓库未启用 GitHub Pages，未发现依赖旧仓库名的托管 Action。
- 本次采用的关键假设：Go module/import path、Rust crate/binary、sidecar 名、目录名与现有数据 key 属兼容身份而非展示品牌；历史 Release/ADR/更新说明保持历史真实性，不回写品牌。
- 剩余不确定性：Windows NSIS/MSI 新产物名与 Explorer 展示仍需 Windows CI/G3；本机 macOS 包仅为 ad-hoc 签名，未经过 Developer ID、notarization 或 Gatekeeper 信任验证；正式发布前需另行选择新版本号并恢复发布签名门禁。
- 影响范围：用户可见身份、安装包/更新地址和 schema 13 文案迁移；不迁移 bundle identifier、数据目录、登录态、localStorage/IPC/event key、Go module 或图标。
- 验证方式：TypeScript noEmit；Vitest 52 文件/237 项；updater manifest Node tests；Rust fmt 与 278 项 lib tests；Go vet/test；capability/resource/command registry 检查；production Web build；macOS arm64 `KickSide.app` 构建、Info.plist、双二进制架构、ad-hoc codesign、图标哈希与 zip 校验；新旧 GitHub/Updater URL HTTP 200/301 重定向检查。
- 是否更新 README：是，更新根英文/中文 README 与 Shell README 的当前品牌、仓库、打包产物和兼容边界。
- 是否更新 `.ai/architecture`：是，记录当前展示品牌、仓库与保留的兼容身份。
- 是否需要新增或更新 `.ai/decisions`：是，新增 Accepted `2026-08-14-kickside-brand-identity.md`。
- 后续注意事项：不要重新创建 `endearqb/kimi-app`，否则会破坏 GitHub rename redirect；若将来修改 `com.kimi.shell` 或旧内部 key，必须单独设计安装、设置、WebView 数据与登录态迁移。

## 2026-08-14-11 - 修复 DSH 新建窗格复用旧窗格

- 用户目标：点击标题栏 DeepSeek Harness“新建窗格”时必须新增 DSH 窗格；关闭任意窗格和最后一个窗格都不得停止 DSH 后端，后端只在退出应用或控制中心手动关闭时停止。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/README.md`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：移除 `handleCreateDshPane` 的既有 DSH pane 去重/聚焦分支，每次动作都创建唯一 pane/storage namespace；已运行/启动中的单后端不重复调用 start；Grid 删除窗格不再调用 `dsh_stop`；移除 pane header 的停止按钮，只保留状态刷新；删除 UI controller 暴露的通用 stop 回调，控制中心仍保留显式停止与关闭开关。
- 修改原因：单后端实例不等于单视图窗格；“新建窗格”复用旧窗格违背动作语义，而且自动运行后端不应被视图生命周期意外终止。
- 本次已消除的不确定性：多个 DSH pane 共享同一个 Rust 权威 loopback URL/owned process；窗格库删除与 Grid header 删除语义一致，均只删除 view state。
- 本次采用的关键假设：多个 iframe 共享同一 DSH 后端与工作区是当前 P0 所需行为，不实现 FR-8 的不同目录多后端实例。
- 剩余不确定性：同一 DSH Web 应用在两个 WKWebView iframe 中同时交互的上游会话体验仍需真机手工确认；Windows WebView2 仍属 G3。
- 影响范围：只改变 DSH pane 创建/删除和 pane header 动作；不改变进程所有权、端口、凭据、持久化 URL、控制中心 stop 或应用退出收口。
- 验证方式：新增重复创建两个 DSH pane、唯一 namespace、两 pane 逐个关闭且无 pane stop 动作测试；TypeScript noEmit；Vitest 52 文件/239 项。
- 是否更新 README：是，记录“单后端、多 pane、关闭 view 不停服”。
- 是否更新 `.ai/architecture`：是，更新 DSH 当前生命周期事实。
- 是否需要新增或更新 `.ai/decisions`：是，修订 Accepted DSH P0 第 7 条以反映用户确认的生命周期契约。
- 后续注意事项：不要在 PaneShelf、Grid 或 pane header 重新接入 `dsh_stop`；新增停服入口必须属于应用退出协调或控制中心显式操作。

## 2026-08-14-12 - 对齐控制中心标题并升级到 0.2.0

- 用户目标：统一设置页与 Skill 中心页标题的垂直位置，将“KickSide 启伴设置”精简为“KickSide 设置”，并把应用版本升级到 `0.2.0`。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/package.json`。
- 修改内容：移除 Skill 中心页面重复的顶端内边距，使其标题与设置页及控制中心栏标题共用同一首屏基线；更新设置页标题；同步 package、Cargo、Tauri 与 lockfile 中的应用版本；生成 `KickSide_0.2.0_macos_arm64.zip` 本机测试包。
- 修改原因：Skill 中心同时承受主内容区和页面自身的顶端内边距，比设置页多下沉一层；品牌标题已无需重复中文副品牌；版本需要进入新的 `0.2.0` 开发线。
- 本次已消除的不确定性：偏移来自 Skill 中心页面自身重复的 `18px` 顶端内边距，而不是字体或单个标题的 transform；应用运行时版本由 `package.json` 同步到 Cargo/Tauri。
- 本次采用的关键假设：设置页当前与左侧“控制中心”一致的标题位置是统一基准；`0.2.0` 先作为本机测试版本，不修改现有正式发布签名门禁。
- 剩余不确定性：不同 macOS 缩放比例下的视觉效果仍需用户在新测试包中确认；`0.2.0` 正式发布仍需恢复 Developer ID 签名、公证与 Gatekeeper 验证。
- 影响范围：控制中心 Skill 页面顶部布局、设置页展示文案和应用版本元数据；不改变 `com.kimi.shell`、兼容性内部 key、数据目录或当前图标。
- 验证方式：TypeScript noEmit；Vitest 52 文件/239 项；Rust 278 项；安全 capability/resource/command registry 门禁；生产 Web/Tauri 构建；Info.plist 版本与 bundle identifier；主程序/sidecar arm64；完整 ad-hoc codesign；zip SHA-256；`git diff --check`。
- 是否更新 README：否，页面局部布局与普通版本推进不改变长期运行方式；README 中 `0.1.24` 仍是历史过渡 Release 的准确说明。
- 是否更新 `.ai/architecture`：否，未改变模块职责、依赖边界或发布门禁。
- 是否需要新增或更新 `.ai/decisions`：否，不涉及新的单向门决策。
- 后续注意事项：不得因版本已到 `0.2.0` 而绕过 release workflow 对 Developer ID/notarization 的 fail-closed 约束。

## 2026-08-14-12 - 跳过重复 Kimi Code 欢迎页

- 用户目标：KimiCode 每次打开都直接进入工作区，不再重复显示语言与外观欢迎页。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：Shell 生成 Kimi workspace URL 时统一附加 Kimi Code 0.36 官方支持的 `kimi_onboarded=1` query，并继续把 token 放在 fragment；Code session URL 构造同时保留 query 与 fragment；新增有/无 token、session 路由和脱敏显示回归测试。
- 修改原因：Kimi Code 以 `localStorage["kimi-web.onboarded"] === "1"` 判断欢迎流程完成状态；不同 WebView carrier/存储域首次加载时可能没有该标记。官方 query 会在应用初始化前幂等补齐标记，避免依赖 DOM 结构或自动点击。
- 本次已消除的不确定性：本机 Kimi Code 0.36.0 bundle 的完成 key、期望值和启动 query 均已确认；语言 `kimi-locale`、主题、Provider 与登录态不需要也不会被覆盖。
- 本次采用的关键假设：当前受支持 Kimi Code 使用该官方 query 契约；更旧版本若不识别该参数会按普通无害 query 忽略。
- 剩余不确定性：未来 Kimi Code 若删除或改名该 query，需在升级契约测试中同步调整；Windows WebView2 的首次存储域行为仍需 G3 实机确认。
- 影响范围：所有 KimiCode 根页面、恢复的历史 session 页面和独立 Webview 导航 URL；不改变 server token、认证、工作区、session 或 Grid 持久化 schema。
- 验证方式：Kimi Code 0.36.0 实际 bundle 契约检查；前端定向 41 项与全量 52 文件/239 项；Rust 定向 5 项与全量 279 项；Rust fmt；TypeScript noEmit；capability/resource/182-command 安全门禁；macOS arm64 本地 bundle 与签名/版本检查。
- 是否更新 README：是，记录官方跳过参数及 session URL 必须保留 query/fragment 的运行契约。
- 是否更新 `.ai/architecture`：是，更新 workspace URL 当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，未新增持久化 schema、公共 IPC 或不可逆数据决策。
- 后续注意事项：不要改为选择器点击“跳过”或直接覆写语言/主题；后续 Kimi Code 升级应先验证 `kimi_onboarded=1` 仍能幂等写入完成标记。

## 2026-08-14-12 - 同步 DSH 当前会话目录到窗格壳层

- 用户目标：为 DeepSeek Harness pane 增加“打开当前会话目录”按钮，并让窗格库标题像 KimiCode 一样跟随当前会话目录变化。
- 涉及目录：`apps/kimi-shell/src`、`apps/kimi-shell/src-tauri`、`apps/kimi-shell/README.md`、`.ai/architecture`。
- 修改内容：在所有 frame 初始化桥中增加 DSH 专属工作区观测；每个 DSH iframe 只截获自身写入的 `dsh.sessions.current`，再调用固定版本官方 `session.list` 读取当前 session 的绝对 `cwd`；Shell 按精确 iframe window、运行时 loopback origin、session id 长度与绝对路径格式校验消息。DSH pane header 新增目录按钮，观测到的新目录同步到 pane `workDir`，Pane Shelf 因而实时显示目录 basename 与 DSH 图标。
- 修改原因：原实现只把创建 DSH pane 时的默认目录写入 Grid，DSH 页面内切换到其他 workspace/session 后，壳层既没有当前目录动作，也继续显示旧标题。
- 本次已消除的不确定性：DSH 0.1.0-rc.6 的稳定状态源为 `dsh.sessions.current` 与 `POST /api/session.list`，响应 `SessionSummary.cwd` 是当前会话目录；同 origin 多 iframe 的 DSH 客户端不会订阅 storage event，因此必须按 iframe realm 捕获本窗格写入，不能用全局 storage change 同步所有 pane。
- 本次采用的关键假设：DSH session 的 `cwd` 在 session 生命周期内代表应打开的当前会话目录；当窗格尚无当前 session 或接口暂不可用时按钮 fail closed，并保留上一次已确认目录标题，与 KimiCode 的历史目录展示行为一致。
- 剩余不确定性：macOS WKWebView 与 Windows WebView2 中真实多 DSH pane 独立选择仍需发布 G3 手工覆盖；未来升级 DSH pin 时必须复验 `dsh.sessions.current` 和 `session.list` 契约。
- 影响范围：仅新增 DSH iframe 到壳层的只读会话目录投影；不修改 DSH 源码、凭据、iframe URL、运行端口、进程所有权、关闭策略或 generic external allowlist。
- 验证方式：TypeScript noEmit；Vitest frame bridge/parser/query、DSH pane 目录按钮、Pane Shelf 动态标题及全量 244 项回归；安全 registry；Rust 279 项全量测试；macOS arm64 本地 bundle、ad-hoc 深签、ZIP 完整性、版本与双二进制架构校验。
- 是否更新 README：是，记录 DSH pane 目录观测职责与安全边界。
- 是否更新 `.ai/architecture`：是，记录 per-pane 观测事实与不跨 iframe 串联的约束。
- 是否需要新增或更新 `.ai/decisions`：否，沿用 Accepted DSH P0 固定 pin、精确 loopback URL 与最小桥接边界，未新增单向门。
- 后续注意事项：不要改成监听 `storage` 事件或全局轮询，否则一个 DSH pane 的选择会错误覆盖其他 pane 标题；升级 DSH 固定版本时把该契约纳入 pin 验收。

## 2026-08-14-13 - 统一控制中心运行时设置行与暗色主题

- 用户目标：优化控制中心暗色模式，并统一 KimiCode 与 DeepSeek Harness 在“更新与运行”中的组件和交互。
- 涉及目录：`apps/kimi-shell/src/components/control-center`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/App.css`、`apps/kimi-shell/README.md`。
- 修改内容：新增共享 `ControlCenterSettingsRow`，把 KimiCode、DSH 及同页设置项统一为原生 button disclosure、状态点、摘要、chevron、独立 trailing action 与详情区域；DSH 纳入页面级互斥折叠状态，更新按钮/启停开关不会误触发展开；KimiCode 增加官方图标并统一名称。移除 rail、footer 和 workbench rail 的浅色硬编码，补齐暗色 primary 前景、原生 `color-scheme`、文本与操作列 token。
- 修改原因：旧控制中心只给详情面应用暗色 token，左侧栏仍固定为浅色；DSH 自己管理展开状态并使用独立 JSX，导致与 Kimi 更新行的键盘、展开和布局语义漂移。
- 本次已消除的不确定性：暗色错位来源是三个 `rgba(248, 248, 246, …)` 背景和 rail item 固定文字色；Kimi 与 DSH 可共享展示/展开组件，但更新与启停属于不同业务动作，应保留为独立 trailing control。
- 本次采用的关键假设：控制中心所有折叠设置行采用互斥展开；KimiCode 的更新动作与 DSH 的启用开关只统一位置、尺寸和事件隔离，不统一为同一种控件。
- 剩余不确定性：macOS WKWebView 与 Windows WebView2 的系统强调色、不同显示缩放和高对比度仍需 G3 手工视觉验收。
- 影响范围：仅控制中心 React/CSS 与展示名称；不改变 Kimi/DSH 生命周期、安装命令、设置 schema、bundle identifier 或兼容 key。
- 验证方式：TypeScript noEmit；Vitest 53 文件 / 249 项；182-command 安全门禁；production build；Rust fmt、check、clippy 与 279 项 lib tests；updater manifest 2 项；`git diff --check`；macOS arm64 `KickSide.app` 构建、Info.plist、双二进制架构、ad-hoc 深签与 ZIP 完整性校验。
- 是否更新 README：是，记录共享设置行职责和亮/暗主题行为。
- 是否更新 `.ai/architecture`：否，未改变进程、IPC、数据或依赖边界。
- 是否需要新增或更新 `.ai/decisions`：否，属于可逆 UI 组件收敛，不是单向门。
- 后续注意事项：不要把 Kimi 更新按钮改造成运行时开关，或把 DSH 开关放入 disclosure button 内；新增设置行继续复用共享组件并保持 trailing action 事件隔离。

## 2026-08-14-13 - 收口 DSH P0 运行期诊断并回填完成度审计

- 用户目标：基于 DSH research/PRD/spec/plan 持续对齐代码库，完善计划并以可验证证据判断开发目标完成度。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src`、`apps/kimi-shell/README.md`、`.ai/plans/dsh-integration-2026-08-13`、`.ai/architecture`、`.ai/decisions`。
- 修改内容：DSH readiness 成功后继续每 5 秒探测，连续 3 次失败进入 `degraded` 并在恢复后回到 `running`；运行时错误同步到窗格与控制中心；DSH 日志新增独立锁、projected-size 10MiB 轮转与 64KiB UTF-8 单行上限；Node/npm 安装故障补齐 E-DSH-001/002，端口耗尽测试 E-DSH-003；PRD/spec/plan 更新为 KickSide 0.2.0 当前事实并新增完成度证据矩阵。
- 修改原因：原实现只做启动期 readiness，日志只在下次启动前检查大小，且安装失败未进入约定错误码；旧计划也把代码完成、macOS G3、Windows G3 和被安全阻断的 headless 混成同一种“未完成”。
- 本次已消除的不确定性：本机私有安装确为 `@deepseek-ai/dsh@0.1.0-rc.6`；当前 App 实例返回 HTTP 200；隔离 macOS 探针 805ms ready，SIGTERM 60ms exit 0 且 process-group 残留为 0；GitHub 仓库已是 `endearqb/kickside`。
- 本次采用的关键假设：当前固定 pin 继续以根路径 2xx/3xx 为 health；连续 15 秒不可用足以提示降级但不擅自重启；OS 级 macOS 拖拽卸载不会执行自定义清理，因此 NFR-4 明确归 M3 受管组件清理，不能作为 P0 虚假承诺。
- 剩余不确定性：Windows 11 npm/WebView2/taskkill G3、双平台 2h 长稳和完整故障注入仍需测试机；headless 继续受 A-8/A-10、凭据库与双平台 descendant-kill 硬阻塞。
- 影响范围：DSH 状态枚举新增 `degraded`，AppState 新增 DSH 日志互斥；不改变 pin、bundle id、设置 key、pane 关闭策略、generic URL allowlist 或凭据所有权。
- 验证方式：Rust DSH 7 项定向与 282 项全量测试、fmt/check/clippy；TypeScript noEmit；前端 DSH/Workspace 28 项定向及 53 文件/250 项全量测试；182-command capability/resource/ACL 门禁；updater 2 项；Go vet/test/race；生产 Web 与 macOS arm64 `KickSide.app` 构建；macOS 固定入口隔离启动/readiness/SIGTERM/残留探针；当前 App-owned DSH HTTP 200。
- 是否更新 README：是，记录持续 health/degraded 与运行中有界日志。
- 是否更新 `.ai/architecture`：是，记录当前状态机、探测和日志事实。
- 是否需要新增或更新 `.ai/decisions`：是，补充 Accepted P0 HTTP health 的运行期降级语义；未新增新的单向门。
- 后续注意事项：不得因 macOS 证据通过而宣称双平台发布完成；headless 在安全前置完成前保持 No-Go。

## 2026-08-14-14 - 前移 DSH 双平台 runtime 证据并提供 Windows G3 清单

- 用户目标：继续完成 DSH 集成的跨平台验证；Windows WebView2 手测由用户执行。
- 涉及目录：`.github/workflows`、`apps/kimi-shell/scripts`、`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/package.json`、`.ai/plans/dsh-integration-2026-08-13`、`.ai/architecture`、`apps/kimi-shell/README.md`。
- 修改内容：新增真实 DSH runtime smoke，隔离安装 npm pin/latest 后验证 package/固定入口、精确 loopback HTTP readiness、有界输出、软停/强停兜底与端口释放；新增每周/手动 Windows/macOS canary，固定 pin 覆盖 Node 18/20/22，latest 只用于 breaking 告警；新增 Windows 专属 Rust 父子进程树 `taskkill /T` 测试；整理可直接勾选的 Windows 11 G3 手测清单与只读残留检查命令。
- 修改原因：原双平台 CI 只编译和单测，从未下载/启动真实 DSH，无法证明 npm 包、Node 版本、CLI 与操作系统进程语义仍兼容；Windows 人工验证也缺少统一回填格式。
- 本次已消除的不确定性：macOS 全新临时前缀安装固定 pin 用时约 202 秒，Node 24.19 下 1.011 秒 HTTP ready，SIGTERM 252ms 正常停止且端口释放，无需强杀；smoke 不读取或写入用户 `$DSH_HOME`。
- 本次采用的关键假设：定时 canary 失败用于告警而不改变应用固定 pin；Node 18/20/22 的兼容结论必须以 Actions 真跑结果为准，当前代码不提前伪造最低版本。
- 剩余不确定性：Windows workflow 首跑结果、WebView2/UI/更新退出与 2h 长稳仍需用户 G3；若 Node 18/20 任一失败，应据结果确定最低版本并让 preflight fail closed。
- 影响范围：新增验证脚本、定时 workflow 和 Windows-only 测试；不改变生产 DSH 生命周期、网络 authority、设置或凭据。
- 验证方式：本机 `pnpm check:dsh:runtime` 真实固定 pin 通过；`cargo fmt --check`、`cargo clippy --locked --all-targets -- -D warnings`、Rust 282 项测试、TypeScript noEmit、前端 53 文件/250 项测试、182-command security gate、updater 2 项、smoke 脚本语法、workflow YAML 与 `git diff --check` 均通过。Mac 宿主上的 `x86_64-pc-windows-msvc` 交叉检查已尝试，但第三方 C 依赖因缺少 Windows SDK 的 `assert.h`/`stdlib.h` 在进入本项目代码前停止；Windows-only 测试仍须由原生 Windows Actions 执行。
- 是否更新 README：是，记录 runtime smoke/canary 的职责与不替代 WebView 人工验收的边界。
- 是否更新 `.ai/architecture`：是，加入新的验证入口。
- 是否需要新增或更新 `.ai/decisions`：否，属于 Accepted P0 契约的验证实现，没有新单向门。
- 后续注意事项：不要把 latest canary 自动写回 `DSH_PINNED_VERSION`；Windows 残留检查不得使用按进程名批量终止 Node。

## 2026-08-14-15 - 收口 DSH 运行期状态投影与 WebView 存活

- 用户目标：继续按 completion audit 补齐不依赖 Windows 手测的 DSH P0 缺口。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/services`、`.ai/architecture`、`.ai/plans/dsh-integration-2026-08-13`。
- 修改内容：控制中心在 DSH 开关启用期间每秒刷新运行态；`degraded` 继续接受 Rust 当前持有且端口一致的精确 `127.0.0.1` URL，并保持同一 iframe 挂载，不再把瞬态健康失败当作 URL authority 丢失；新增状态轮询和 iframe identity 回归测试。
- 修改原因：原 Grid controller 会刷新状态，但设置面板自己的快照不会自动更新；同时 degraded 会卸载 iframe，导致短暂健康抖动丢失用户正在查看的 DSH 页面并在恢复后重载，不符合 FR-3 的 2 秒投影与桌面 WebView 存活预期。
- 本次已消除的不确定性：degraded 仍表示当前进程内持有的 Child/port/URL，只是 HTTP 探针失败，不等同于 stopped/crashed 或未知 URL；因此保留经过 host、端口与状态校验的原 URL 不扩大信任边界。
- 本次采用的关键假设：运行期告警由控制中心呈现；pane 在服务恢复前保留最后一帧与交互状态，比阻断式覆盖层更符合用户感知。
- 剩余不确定性：Windows WebView2 在真实网络抖动、休眠唤醒和进程 crash 下的视觉行为仍需用户 G3；crashed/stopped 继续移除 URL 并显示恢复入口。
- 影响范围：仅改变启用后的控制中心刷新频率和 degraded 时的前端承载；不改变 Rust ownership、端口、PID、停止路径、持久化 schema 或 generic external allowlist。
- 验证方式：DSH service、控制中心与 Workspace Grid 定向测试 30 项通过；TypeScript noEmit、前端全量 53 文件/252 项、182-command capability/resource/ACL、updater 2 项与 `git diff --check` 通过；重新构建 macOS arm64 `KickSide.app`，对主程序与 Bridge sidecar deep ad-hoc 重签，并从最终 zip 解包复验签名、双 arm64 架构、`com.kimi.shell` 与 `0.2.0`。
- 是否更新 README：否，应用 README 已描述持续 health/degraded；补充 `.ai/architecture/current-state.md` 的精确承载事实。
- 是否更新 `.ai/architecture`：是，记录控制中心刷新和 degraded iframe 存活事实。
- 是否需要新增或更新 `.ai/decisions`：否，属于既有 Accepted ownership/readiness 契约内的状态投影修复。
- 后续注意事项：不得让 starting、stopped、crashed 状态获得 iframe URL；只有 running/degraded 且 URL host、port 与 Rust status 完全一致时可继续承载。

## 2026-08-14-16 - 复验 KickSide 发布身份并补齐 macOS DSH 启动基线

- 用户目标：Windows 真机由用户手测；继续收口不依赖 Windows 的 KickSide 0.2.0 与 DSH 发布证据。
- 涉及目录：`.ai/plans/dsh-integration-2026-08-13`、`tasks/todo.md`；应用与仓库配置只读复验。
- 修改内容：在线确认公开仓库为 `endearqb/kickside` 且默认分支为 `main`；复验 package/Tauri/Cargo 版本、产品名、兼容 bundle id、repository 与 updater endpoint；对固定 DSH pin 完成 macOS S-10 冷/热启动各 5 次真实基线并回填证据；把关闭实验开关的验收从“隐藏全部 UI”纠正为“不启动进程、不自动建 pane、不提供运行 URL”，保留控制中心检测/安装入口，标题栏 DSH 入口仍只在启用后出现。
- 修改原因：品牌改名后的更新链路与启动阈值都需要运行/配置证据，不能只依赖历史修改意图；Windows 人工责任边界也不能被 macOS 或 CI 结果替代。
- 本次已消除的不确定性：当前发布身份统一为 KickSide 0.2.0 / `endearqb/kickside`，`com.kimi.shell` 仍保留；Node 24.19.0 / arm64 下冷启动中位数 361ms、热启动中位数 320ms，10 次均通过 SIGTERM 软停并释放端口。
- 本次采用的关键假设：隔离新 `DSH_HOME` 代表冷状态，完成一次预热后复用同一 `DSH_HOME` 代表热状态；M4 Pro 数据只建立 macOS 当前 pin 基线，不外推 Windows 或低配机器。
- 剩余不确定性：用户负责的 Windows WebView2/npm/进程树 G3、原生 canary 首跑、双平台 0.2.0 RC 两小时长稳与完整故障注入仍未完成；本机没有 Node 18/20/22，不伪填其结果。
- 影响范围：仅证据与计划回填；未改变应用运行逻辑、超时、版本、bundle id、兼容 key 或发布签名策略。
- 验证方式：`gh repo view endearqb/kickside`；品牌 Vitest 2 项；updater manifest Node tests 2 项；package/Tauri/Cargo 契约断言；真实 DSH 10 次启动/readiness/SIGTERM/端口释放；`git diff --check`。
- 是否更新 README：否，现有 README 已描述品牌、仓库与 DSH runtime 验证入口。
- 是否更新 `.ai/architecture`：否，没有改变系统事实或依赖边界。
- 是否需要新增或更新 `.ai/decisions`：否，复验既有 Accepted 品牌与 P0 契约。
- 后续注意事项：Windows 数据取得前保留 spec/生产设置的 60 秒默认启动超时；runtime smoke 的 90 秒仅是 CI 容错上限。不得以 canary 或 macOS 数据替代用户的 Windows 应用级手测。

## 2026-08-14-17 - 将 DSH 原生 canary 升级为多轮生命周期采样

- 用户目标：Windows UI 由用户手测；继续强化原生自动化能证明的 DSH 跨平台生命周期证据。
- 涉及目录：`apps/kimi-shell/scripts`、`.github/workflows`、Shell README、`.ai/architecture` 与 DSH 计划证据。
- 修改内容：为真实 runtime smoke 增加严格范围为 1–10 的 `--samples`；一次隔离 npm 安装后复用同一 workspace/`DSH_HOME` 连续执行 HTTP readiness、整树停止和端口释放，输出逐次数据及 ready/stop 中位数；固定 pin 的 Windows/macOS × Node 18/20/22 canary 改为每个 job 采样 5 次，latest 告警仍保持单次。
- 修改原因：一次启动成功不足以证明进程生命周期稳定，也不能给 S-10 提供可比较基线；复用安装能在不重复下载的前提下增加 Windows `taskkill /T` 与 macOS process-group 覆盖。
- 本次已消除的不确定性：新参数对 0 和越界值 fail closed；本机真实固定 pin 在 165926ms 私有安装后连续 3 次 ready 为 1012/507/505ms、stop 为 252/251/252ms，中位数 507/252ms，全部软停且端口关闭。
- 本次采用的关键假设：固定 pin 的首轮包含新 `DSH_HOME` 冷状态，后续轮次代表同一用户数据目录的热状态；canary 只验证 runtime，不代表 WebView UI 或应用退出链路。
- 剩余不确定性：原生 Windows/macOS Actions 尚未首跑；Windows WebView2/npm/应用退出 G3 与双端 2 小时 pane 长稳仍需真机。
- 影响范围：仅验证脚本与定时/手动 canary；不改变生产安装、启动、停止、设置或 release workflow。
- 验证方式：Node 语法检查；非法 `--samples 0` 返回非零及明确错误；真实 `--samples 3` 安装/启动/停止；workflow YAML 解析；`git diff --check`。
- 是否更新 README：是，记录参数、5 次采样矩阵和人工门禁边界。
- 是否更新 `.ai/architecture`：是，更新 runtime 验证入口事实。
- 是否需要新增或更新 `.ai/decisions`：否，未改变 Accepted P0 运行契约。
- 后续注意事项：canary 失败不得自动更新 pin；多轮采样仍不能替代用户的 Windows WebView2、窗格交互和应用级退出测试。

## 2026-08-14-18 - 收紧 DSH 控制中心停止交互

- 用户目标：统一 KimiCode 与 DSH 更新/运行设置的组件和交互，并保证 DSH 生命周期操作可控。
- 涉及目录：`apps/kimi-shell/src/features/control-center`、DSH 完成审计与任务记录。
- 修改内容：把“停止实例”纳入与安装/开关共享的互斥动作状态；停止进行时禁用所有冲突控件并阻止重复 IPC；用显式 action 区分 `安装中` 与 `停止中`，避免全局 busy 让多个按钮同时显示同一错误文案；新增延迟 stop Promise 回归测试。
- 修改原因：原停止按钮没有设置 busy，快速点击可重复提交；初步统一为布尔 busy 后，安装与停止按钮又会同时显示“处理中”，无法表达真实动作。
- 本次已消除的不确定性：停止请求在 UI 层单飞；完成状态来自 `dsh_stop` 返回值并立即投影；安装按钮在停止期间保持自身语义而只被禁用。
- 本次采用的关键假设：安装、开关和停止属于同一 DSH 生命周期互斥域，日志读取仍可独立执行，不需要阻塞诊断。
- 剩余不确定性：Windows 上停止按钮到 `taskkill /T` 的真实耗时与视觉反馈仍由用户 G3 验证。
- 影响范围：仅控制中心动作状态和文案；不改变 Rust 停止策略、8 秒上限、开关持久化或 pane 生命周期。
- 验证方式：DSH 设置面板 5 项定向测试；TypeScript noEmit；前端全量 53 文件/253 项；182-command capability/resource/ACL；updater 2 项；workflow YAML 与 `git diff --check`；production Web/Tauri macOS arm64 构建；主程序和 Bridge deep ad-hoc 重签；最终 zip 完整性及解包后 strict deep codesign、双 arm64、`com.kimi.shell`/0.2.0 复验。测试包 `KickSide_0.2.0_macos_arm64_dsh-p0-canary-ui.zip`，SHA-256 `6b4b8c8a4a102b591da8647c77b74314bc512465a4ea10f4a2a8a39b2e1607e9`。
- 是否更新 README：否，组件内部交互没有改变运行方式。
- 是否更新 `.ai/architecture`：否，没有改变跨边界契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：后续新增重启/卸载动作时应扩展显式 action union，不退回含义不明的共享布尔 loading 文案。

## 2026-08-14-19 - 修复 DSH 手动停止与崩溃后的显式恢复

- 用户目标：关闭最后一个 pane 不停止 DSH；只有退出应用或控制中心显式停止才结束，同时显式停止或 crash 后可直接恢复。
- 涉及目录：`apps/kimi-shell/src/App.tsx`、`features/control-center`、`features/workspace*`、DSH 计划审计与任务记录。
- 修改内容：控制中心在 enabled 且 stopped/crashed 时显示“启动实例/重试启动”，以实际默认工作区调用 `dsh_start`；pane 的失败恢复从只读 refresh 改为独立 recover 回调，优先使用 pane 最后观测的会话目录、再回退默认目录；加入 DSH 专属 busy 投影与启动单飞测试。
- 修改原因：旧“重试后端启动”只刷新状态，完全不会启动进程；控制中心停止后也没有 start 入口，形成只能切开关或新建 pane 的恢复死角。
- 本次已消除的不确定性：toolbar 刷新保持只读；只有空白/失败恢复动作调用 start；关闭 pane 仍只移除 UI，不触发后端 stop。
- 本次采用的关键假设：恢复现有 pane 应优先延续该 pane 已观测的当前 DSH 会话目录；控制中心恢复使用 Shell 已生效默认工作区。
- 剩余不确定性：Windows WebView2 下的实际按钮反馈、`taskkill /T` 和应用退出链路由用户 G3 手测；2h coexist soak 仍在运行，未提前宣称通过。
- 影响范围：新增前端回调与 UI 动作，不新增 IPC，不修改 Rust 生命周期、持久化 schema、URL trust 或自动重启策略。
- 验证方式：恢复交互定向 32 项；最终加入 controller 失败边界后前端全量 54 文件/256 项、TypeScript noEmit、182-command 安全 gate、`git diff --check`、production Vite/Tauri macOS arm64 build；最终 zip 解包后 strict deep ad-hoc codesign、主程序/Bridge arm64、`com.kimi.shell` 与 0.2.0 复验。
- 是否更新 README：否，外部运行方式不变。
- 是否更新 `.ai/architecture`：否，未改变跨边界运行契约，只修正现有 start 与 refresh 的 UI wiring。
- 是否需要新增或更新 `.ai/decisions`：否，属于 Accepted P0 生命周期内的可逆修复。
- 后续注意事项：任何标为“启动/重试”的按钮必须调用 start path，不得退化成仅刷新状态；不得把 pane close 重新绑定为 stop。

## 2026-08-14-20 - 对齐 DSH pane 与 Windows 关停规格到当前实现

- 用户目标：继续按 research/PRD/spec/plan 审查并对齐 KickSide 当前 DSH 实现，Windows 真机由用户手测。
- 涉及目录：`.ai/plans/dsh-integration-2026-08-13/03-spec.md`、`04-plan.md`、`07-windows-g3-checklist.md`。
- 修改内容：把 pane kind、每 pane storage namespace、degraded iframe 存活、默认工作区启动与会话目录切换写回 spec；将 Windows 关停从“方案待选”更新为当前 `taskkill /T → 8s → /F /T` 实现，并明确优雅 drain/退出码/落盘仍待 G3；M1 验收去掉已经被产品要求替代的“新建时系统目录选择器”；Windows 清单补充停止后开关保持启用、显式启动/重试入口和默认工作区恢复断言。
- 修改原因：旧规格混合了 research 时点的待定状态与当前代码事实，会让 Windows 手测人员按不存在的目录 picker 或未采用的 CTRL_BREAK 路径验收。
- 本次已消除的不确定性：当前多个 DSH pane 共享一个后端、各自拥有 Grid storage namespace；首次运行使用默认工作区，之后由 DSH UI 切换并通过桥更新 pane workDir。
- 本次采用的关键假设：Windows 非 `/F` taskkill 只称“第一阶段终止请求”，在真机证据前不声称等价于 POSIX SIGTERM drain。
- 剩余不确定性：Windows taskkill 的实际 DSH drain、exit code、会话落盘和 WebView2 行为由用户 G3 回填。
- 影响范围：仅权威规格/计划对齐，不改变运行代码。
- 验证方式：对照 `gridTypes.ts`、`gridStore.ts`、`PaneFrame.tsx` 与 `dsh_manager.rs` 当前实现逐项核验；`git diff --check`。
- 是否更新 README：否，README 已描述用户级行为。
- 是否更新 `.ai/architecture`：否，当前事实文档已包含 singleton、degraded 与生命周期边界。
- 是否需要新增或更新 `.ai/decisions`：否，仍属于 Accepted P0 契约。
- 后续注意事项：Windows G3 回填后再决定是否保留 taskkill 第一阶段或改用可证明的控制台事件，不先把未证行为升级为 Accepted 事实。

## 2026-08-14-21 - 完成 macOS App 级 Kimi + DSH 退出复验

- 用户目标：Windows 由用户手测；继续补齐本机可自动证明的 DSH 发布级生命周期证据。
- 涉及目录：临时 `KickSide Soak.app`、隔离 `com.kimi.shell.soak` 配置/日志、DSH 计划证据。
- 修改内容：以临时 bundle id 构建 production App，复制已验证私有 DSH pin 到隔离前缀，使用独立 Kimi/DSH home 和临时工作区同时启动 Kimi 0.36.0 与 DSH rc.6；发送标准 macOS Quit 事件并核验 App、两个 owned child 和两个监听端口全部消失；随后将隔离配置/日志移动到 `/tmp` 证据目录。
- 修改原因：独立 CLI SIGTERM 探针不能证明 Tauri `ExitRequested` 实际串联 DSH 与 Kimi shutdown；需要应用级证据覆盖用户“只有关闭应用才关闭后端”的关键路径。
- 本次已消除的不确定性：macOS production App 启动时确实同时拉起默认工作区 Kimi/DSH；标准 Quit 触发 DSH stop 与 Kimi graceful stop，未遗留 PID 或端口。
- 本次采用的关键假设：临时 bundle id 只隔离持久配置，不改变同一 release Rust/React 代码与进程生命周期路径；该结果可证明 macOS Quit 路径，但不外推 Windows taskkill。
- 剩余不确定性：真实版本差的 updater exit、2 小时真实 pane 交互和 Windows G3 仍待完成。
- 影响范围：无生产源码改动；临时测试 App/配置均与正式 `com.kimi.shell` 隔离。
- 验证方式：App PID 42090；Kimi PID/port 42201/58235；DSH PID/port 42118/33080；两个 HTTP 服务 ready；Quit 后第三次 250ms probe 前 App 已退出，`ps` 和 `lsof` 对三个 PID/两个端口均无输出；日志记录 `DSH 已停止` 与 Kimi graceful stop 254ms。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否，验证既有生命周期事实。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：该隔离证据不得被描述为 Windows 或 updater G3；正式用户配置未修改，隔离证据保存在 `/tmp/kickside-app-exit-*-evidence`。

## 2026-08-14-22 - 收口 DSH pane 恢复失败的 Promise 边界

- 用户目标：继续完善 DSH 停止/崩溃后的可恢复体验，避免 UI 重试制造新的不可见异常。
- 涉及目录：`apps/kimi-shell/src/app/useDshController.ts` 及其回归测试。
- 修改内容：DSH UI controller 的 `start()` 在 IPC 失败时继续写入 error/busy 状态，但返回 `null` 而不是向 click handler 重新抛出；新增 hook 测试验证 E-DSH 错误可见、Promise 正常完成且 busy 复位。
- 修改原因：pane 使用 `void onRecoverDsh(...)` 调用恢复动作；controller 先记录错误后再 throw 会形成未处理 Promise rejection，与 UI 事件边界不匹配。
- 本次已消除的不确定性：底层 `dshService.startDsh` 仍以 reject 表达 IPC 失败；只有面向 UI 的 controller 将失败投影为状态和 nullable result。
- 本次采用的关键假设：pane 与标题栏创建动作都依赖 controller 可见 error，不需要通过异常控制流程；控制中心直接调用 service 并自行 catch，不受改变。
- 剩余不确定性：Windows 可见错误文案与 WebView2 控制台仍由 G3 复核。
- 影响范围：只改变 controller Promise 失败返回值，不改变 Rust、IPC、重试策略或错误码。
- 验证方式：新增 hook 回归；定向 3 文件/33 项、TypeScript noEmit、前端全量 54 文件/256 项、182-command 安全 gate、production Vite/Tauri macOS arm64 build；包含后续 readiness/入口/崩溃日志收紧的最终 recovery zip SHA-256 `988bebd335bcaf11348cdb80c126ff564dcf809a27a2611c365a85015734781d`，20379346 bytes，并通过解包后 strict deep codesign/架构/身份/版本复验。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：React `void` 事件调用的异步 controller 不应泄漏未处理 rejection；底层 service 仍可保留 reject 供显式 catch 使用。

## 2026-08-14-23 - 收紧真实 DSH canary 的 HTTP 页面身份

- 用户目标：继续强化 DSH 集成验证，使自动门禁证明实际 Web runtime 而不只是任意 HTTP listener。
- 涉及目录：`apps/kimi-shell/scripts/dsh_runtime_smoke.mjs`、Shell README、架构验证入口。
- 修改内容：runtime readiness 从仅看 2xx/3xx 收紧为同时要求响应体出现当前固定 DSH Web 启动标记 `__DSH_BOOT__`；响应体最多读取 512KiB，命中后立即结束，不无界缓存。
- 修改原因：独占动态端口和 owned child 已降低串台概率，但只校验状态码仍可能让错误入口或意外 HTTP 页面通过真实 npm canary，无法证明 pane 所需 Web 资产存在。
- 本次已消除的不确定性：当前 rc.6 隔离/共存实例的根页面确实包含该标记；长稳探针也以同一标记验证 DSH identity。
- 本次采用的关键假设：`__DSH_BOOT__` 是固定 pin 当前页面契约；latest 若移除它应触发 breaking 告警并先审查新页面身份，再决定是否更新 pin/探针。
- 剩余不确定性：Node 18/20/22 与 Windows npm 包仍待原生 canary 首跑。
- 影响范围：只收紧验证脚本，不改变 production readiness、URL authority 或运行时行为。
- 验证方式：Node 语法检查、当前真实 DSH 根页 marker 探针、长稳双 runtime identity 探针；后续 canary 首跑提供全安装证据。
- 是否更新 README：是，记录页面身份与有界响应验证。
- 是否更新 `.ai/architecture`：是，更新 G1/G2 runtime gate 的证明范围。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：不得因为 latest canary 仅缺 marker 就自动改 pin；先确认上游页面/启动契约及壳内桥仍兼容。

## 2026-08-14-24 - 将 DSH 页面身份纳入 production 首次 readiness

- 用户目标：继续完善 DSH 集成的启动正确性，使 running 明确代表可承载 DSH Web UI。
- 涉及目录：`src-tauri/src/dsh_manager.rs`、DSH spec/ADR、当前架构事实。
- 修改内容：启动期从仅校验 2xx/3xx 改为同时在响应前 512KiB 按原始字节查找固定 pin 的 `__DSH_BOOT__`，不因读取上限恰好截断多字节 UTF-8 而误判；任意其他 HTTP 200 页面不再获得 ready；进入 running 后仍复用轻量 status health，不持续下载页面；增加 marker 存在、缺失和超出上限的单测。
- 修改原因：exact port 与 owned child 证明进程归属，但不能证明错误入口返回的 200 页面就是 pane 所需 DSH Web 资产。
- 本次已消除的不确定性：当前 rc.6 页面身份在独立探针、长稳实例和应用级启动中均存在；512KiB 足以覆盖标记且使内存有界。
- 本次采用的关键假设：页面身份属于固定 pin 的受审契约；升级 pin 前由 latest canary 提前暴露变化。
- 剩余不确定性：Windows Node 版本矩阵和 WebView2 行为仍待 canary/G3；上游未来移除 marker 时需要重新审查而非静默放宽。
- 影响范围：收紧首次 running 判定；不改变 URL origin、进程 ownership、运行期探测频率、pane 持久化或凭据。
- 验证方式：Rust marker 定向测试、283 项全量 Rust、fmt、严格 clippy、真实 rc.6 marker 探针；隔离 production App 在 767ms 通过新 readiness，Quit 后 App/Kimi/DSH 与端口清零；正式 macOS arm64 构建及最终 zip 解包后 strict deep codesign、双 arm64、`com.kimi.shell`/0.2.0 复验。
- 是否更新 README：否，用户级行为仍是等待 DSH ready。
- 是否更新 `.ai/architecture`：是，记录首次页面身份与后续轻量 health 的区别。
- 是否需要新增或更新 `.ai/decisions`：是，在 Accepted P0 URL/readiness 条款中加入有界页面身份，未扩大 authority。
- 后续注意事项：不要把页面 identity 检查复用于每 5 秒 health；避免持续下载大 HTML 和把 UI 资产微调变成运行期抖动。

## 2026-08-14-25 - 对齐 DSH readiness 失败诊断与主流程文档

- 用户目标：继续审查 DSH 代码与 research/PRD/spec/plan 的一致性，确保安全收紧后仍可诊断。
- 涉及目录：`dsh_manager.rs`、DSH spec/plan/review alignment 与变更记录。
- 修改内容：E-DSH-004 从含糊的“HTTP readiness 未通过”改为“HTTP 状态或 DSH 页面身份未通过”，继续附脱敏日志尾部；用稳定常量和单测锁定页面身份提示；同步把 review/plan 中旧“选择目录 + 只看 HTTP”流程改为默认工作区 + 状态/身份 readiness + DSH UI 内切换。
- 修改原因：页面身份成为 running 前置后，旧错误无法告诉用户是监听失败还是错误页面；旧 review 主流程也仍描述已被用户要求替代的系统目录 picker。
- 本次已消除的不确定性：E-DSH-004 同时覆盖无监听、非成功状态和错误页面三类首次就绪失败，日志仍用于进一步区分。
- 本次采用的关键假设：不把响应正文或 URL 写入错误，避免泄露或扩大 authority；面向用户只暴露失败类别和既有脱敏日志。
- 剩余不确定性：Windows 上实际错误呈现与日志内容由 G3 故障注入复核。
- 影响范围：错误文案和权威文档，不改变超时、重试频率、状态机或停止路径。
- 验证方式：Rust 定向/全量测试、fmt/clippy、文档搜索和 diff gate。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否，上一条已记录页面身份事实。
- 是否需要新增或更新 `.ai/decisions`：否，错误文案不改变 Accepted 契约。
- 后续注意事项：若未来进一步拆分 E-DSH-004 子原因，应保持外部响应内容有界且脱敏，不直接显示页面正文。

## 2026-08-14-26 - 将 PRD P0 验收文字对齐到已接受实现

- 用户目标：继续按 PRD/spec/plan 逐项审计当前 DSH P0，而不是让历史方案文字与代码并存。
- 涉及目录：`.ai/plans/dsh-integration-2026-08-13/02-prd.md`。
- 修改内容：FR-1 从历史 npx 检测改为 Node/npm/固定入口与 GUI 冷环境发现；FR-2 固化默认工作区、独立 dsh pane 与页面身份 readiness；FR-4 写明 singleton 日志、串行脱敏与运行中有界；FR-5 区分“隐藏新建入口/运行 URL”与“保留历史 pane 空态”。
- 修改原因：生产已明确禁止 npx fallback，且用户要求默认工作区自动启动、关闭 flag 不抹除 pane；旧 PRD 验收会要求不存在或不安全的行为。
- 本次已消除的不确定性：P0 安装工具是 npm、运行入口是受控 Node + 私有 `lib/bin.js`；flag 控制进程与入口，不破坏用户已保存的 Grid 布局。
- 本次采用的关键假设：历史 pane 保留为空态属于可恢复 UX，不等同于 flag 关闭时仍暴露新建能力或运行 authority。
- 剩余不确定性：Windows GUI 冷环境候选路径和 flag 关闭视觉由用户 G3 复核。
- 影响范围：PRD 验收表文字；无代码行为变化。
- 验证方式：对照 `nodejs_locator.rs`、`dsh_manager.rs`、`ShellTitlebar.tsx`、Grid store/pane 行为及现有测试。
- 是否更新 README：否，README 已记录现行用户行为。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：research 中 npx 仍作为历史调研事实保留，但 PRD/spec/production 不得重新把它作为启动 fallback。

## 2026-08-14-27 - 封闭 DSH 私有入口的符号链接越界

- 用户目标：继续审查并收紧 DSH 固定入口，使“私有前缀”成为可执行的安全边界而不只是文本路径约定。
- 涉及目录：`src-tauri/src/dsh_manager.rs`、DSH spec/ADR、当前架构事实与完成审计。
- 修改内容：读取 `package.json` 与启动 `lib/bin.js` 前均 canonicalize，并要求实体 regular file 仍位于 canonical 私有安装根内；preflight 复用完整安装校验，启动再次取得并直接执行已验证 canonical entry；增加 Unix 符号链接越界回归测试。
- 修改原因：`Path::is_file()` 会跟随符号链接，仅校验私有前缀下的词法路径不能阻止入口实体逃逸到该前缀外。
- 本次已消除的不确定性：版本读取和生产启动不再接受越出当前安装根的 package/entry；缺失、损坏与越界统一 fail closed；当前固定 pin 的正常实体入口仍能由 production App 启动并通过页面身份 readiness。
- 本次采用的关键假设：Windows junction/symlink 由同一 canonical + `starts_with` 边界拒绝；其原生行为仍由用户执行的 Windows G3 复核。
- 剩余不确定性：Windows junction 权限与路径大小写的真实表现尚未取得原生证据。
- 影响范围：只收紧已安装 DSH 的验证与启动入口，不改变 pin、安装位置、argv、工作区或生命周期。
- 验证方式：Rust 定向及全量 283 项通过，`cargo fmt` 与严格 clippy 通过；production Vite/Tauri macOS arm64 重建成功；最终 zip SHA-256 `988bebd335bcaf11348cdb80c126ff564dcf809a27a2611c365a85015734781d`、20379346 bytes，从 zip 解包后 strict deep ad-hoc 签名、双 arm64、`com.kimi.shell` 与 0.2.0 复验通过；另用编译期隔离 `com.kimi.shell.soak` production App 从隔离私有根启动 canonical DSH entry，1787ms ready，根页 marker 命中，标准 Quit 后 App/DSH/Kimi PID 与两个端口全部清零。
- 是否更新 README：否，用户行为仍是固定 pin 安装/启动。
- 是否更新 `.ai/architecture`：是，记录 canonical 私有根边界。
- 是否需要新增或更新 `.ai/decisions`：是，收紧 Accepted P0 的安装/执行 authority。
- 后续注意事项：不得在生产启动处退回词法路径或 `npx`；若未来升级器支持多个 pin，仍需对本次 current 根重新验证。

## 2026-08-14-28 - 将 DSH 异常退出写入受管日志

- 用户目标：按发布候选故障注入验证 DSH 崩溃后的状态、错误码与诊断入口，不让 pane 只剩不可追溯的空态。
- 涉及目录：`src-tauri/src/dsh_manager.rs`、DSH 完成审计与变更记录。
- 修改内容：`refresh_exit_state` 在非 stopping 的 owned child 退出时，除设置 `crashed / E-DSH-005` 内存状态外，将同一脱敏错误写入有界 `dsh.log`；主动停止仍不记录为异常。
- 修改原因：production 故障注入确认内存状态路径会被前端轮询触发，但原实现只更新 `last_error`，控制中心“查看日志尾部”无法看到导致崩溃的结构化错误。
- 本次已消除的不确定性：状态面和日志面使用同一 E-DSH-005 文案；日志仍经过既有单行上限、轮转和 redaction。
- 本次采用的关键假设：单次 child exit 只被 `try_wait` 收割一次，因此不会重复追加同一崩溃行。
- 剩余不确定性：Windows Task Manager 结束进程后的 exit code 呈现由用户 G3 复核。
- 影响范围：只增加非主动退出日志；不改变崩溃重启策略、pane 生命周期、停止语义或进程 ownership。
- 验证方式：Rust 全量 283 项与严格 clippy 通过；编译期隔离 `com.kimi.shell.soak` production App 启动固定 pin 后 SIGKILL owned DSH，前端轮询在 1250ms 内触发状态收割并将 `E-DSH-005（exit=None）` 写入 `dsh.log`；DSH PID/端口消失且未自动重启。正式 macOS arm64 包已重建，SHA-256 `988bebd335bcaf11348cdb80c126ff564dcf809a27a2611c365a85015734781d`。
- 是否更新 README：否，既有 README 已承诺诊断日志。
- 是否更新 `.ai/architecture`：否，状态机事实未变。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：不得在持有 runtime mutex 时调用可能反向获取该锁的诊断路径；当前实现先 drop runtime 再写日志。

## 2026-08-14-29 - 消除进程树测试读取半写 PID 的竞态

- 用户目标：让 DSH 收尾所依赖的完整 Rust 门禁可重复运行，避免把偶发红灯误判为产品回归。
- 涉及目录：`src-tauri/src/backend_manager/lifecycle.rs` 测试与变更记录。
- 修改内容：`quit_during_starting_leaves_no_child` 不再以 `descendant.pid` 仅“存在”为就绪条件；改为在同一 2 秒上限内等待文件内容成为可解析 PID。
- 修改原因：shell 创建重定向文件与写入 `$!` 之间存在短窗口；并行全量测试命中该窗口时读取空字符串并产生 `ParseIntError`。
- 本次已消除的不确定性：等待条件现在对应测试真正需要的事实“descendant PID 已完整写入”，而不是较弱的目录项存在。
- 本次采用的关键假设：2 秒仍是原测试允许的进程启动上限，不放宽生产停止时间。
- 剩余不确定性：无；该修改只作用于 Unix 测试同步。
- 影响范围：测试代码，不改变 Kimi/DSH 生产生命周期。
- 验证方式：原失败用例独立连续 5 次通过；修改后全量 Rust 283 项与严格 clippy 通过。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：进程测试等待文件时应等待可消费内容，不能只等待文件名出现。

## 2026-08-14-30 - 补齐 macOS 发布候选故障注入矩阵

- 用户目标：按 DSH M1 验收验证端口冲突、崩溃和 flag 关闭行为，并为用户接手 Windows 手测提供可比对的预期结果。
- 涉及目录：隔离 `com.kimi.shell.soak` production App、DSH 完成审计与变更记录。
- 修改内容：无新增生产行为；分别运行首端口占用、完整端口范围耗尽、owned DSH SIGKILL、DSH flag 关闭四组隔离测试并保存 `/tmp` 证据。
- 修改原因：单元测试能证明分支逻辑，但不能证明 production Tauri App、真实 Node/DSH pin 与前端轮询共同触发预期状态。
- 本次已消除的不确定性：首端口冲突会选择 33081；100 个端口全满会 fail closed 为 E-DSH-003；SIGKILL 会收割为 E-DSH-005 且不自启；flag false 不产生 DSH child/日志。
- 本次采用的关键假设：临时 bundle id 只隔离持久状态，不改变相同 release Rust/React 生命周期代码；macOS 结果不外推 Windows taskkill/WebView2。
- 剩余不确定性：Windows 四组对应行为由用户 G3；macOS 2h runtime coexistence 探针仍在运行。
- 影响范围：验证与文档；正式 `com.kimi.shell` 配置未修改。
- 验证方式：真实 `@deepseek-ai/dsh@0.1.0-rc.6`、Node 24.19.0、精确端口与页面 marker；证据目录 `/tmp/kickside-port-shift-evidence.ifRSKw`、`/tmp/kickside-port-exhaustion-evidence.ZBrRIG`、`/tmp/kickside-crash-log-evidence.bJFmrr`、`/tmp/kickside-flag-off-evidence.wHEjPK`。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否，验证既有契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：Windows checklist 不得根据 macOS 证据代填；用户手测需记录原生 PID/descendant 与 WebView2 UI。

## 2026-08-14-31 - 收口 DSH 的最低 Node 运行时能力

- 用户目标：完成 M0 A-1，不让控制中心把无法运行固定 DSH pin 的旧 Node 误报为可安装/可启动。
- 涉及目录：`dsh_manager.rs`、DSH runtime canary、Shell README、DSH spec/plan/review/completion 文档。
- 修改内容：preflight 与安装选择 Node 时增加固定 `node:util.parseEnv` 能力探针；缺失能力返回 E-DSH-001 与 Node 20.12.0+ 升级提示；canary 矩阵从已证不兼容的 Node 18 改为最低边界 20.12、22、24。
- 修改原因：真实 Node 18.20.8 安装 pin 成功但启动依赖时因 `parseEnv` 未导出而退出；仅执行 `node --version` 会把该环境误判为 ready。
- 本次已消除的不确定性：Node 18 对 rc.6 明确 No-Go；Node 20.20.2 已完成真实安装、422ms 页面身份 ready、754ms 软停与端口释放；Node 官方文档确认 `util.parseEnv` 于 20.12.0/21.7.0 引入。
- 本次采用的关键假设：能力探针比单纯 semver 更能覆盖奇数版本或定制发行版；用户文案仍给出最早官方边界 20.12.0，并推荐 LTS。
- 剩余不确定性：Node 22 smoke 正在运行；Windows 对应矩阵待 Actions/用户 G3。
- 影响范围：过旧 Node 从“检测到即可用”改为 fail closed；不会安装或替换系统 Node。
- 验证方式：Node 18.20.8 真实失败栈指向 `@deepseek-ai/dsh-app-boot/lib/index.js` 的 `parseEnv` import；Node 20.20.2 真实 smoke 通过；待补 Rust/前端/安全门禁和最终重建。
- 是否更新 README：是，记录运行时最低能力与 canary 矩阵。
- 是否更新 `.ai/architecture`：待 Node 22 结果后更新当前事实。
- 是否需要新增或更新 `.ai/decisions`：否，属于 Accepted 安装契约的实测参数收口。
- 后续注意事项：上游未来补 `engines` 后应与能力探针交叉验证，不能只信声明或只信版本字符串。
