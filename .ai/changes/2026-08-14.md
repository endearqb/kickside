## 2026-08-14-00 - DSH P0 Web pane 与计划对齐

- 用户目标：依据 DSH research、PRD、spec 和 plan 审查代码库，优化计划并实现开发目标。
- 涉及目录：`.ai/plans`、`.ai/decisions`、`.ai/architecture`、`apps/kimi-shell`、`tasks`。
- 修改内容：新增代码库对齐审查与 Accepted ADR；修订 headless/安装/生命周期计划；新增 schema 12 `agentBackends.dsh`、main-only DSH commands、固定 pin 私有安装、preflight、单实例生命周期、精确 loopback health、脱敏日志、Grid `dsh` pane、控制中心实验开关、目录 picker、首次引导，以及退出/更新停止协调。
- 修改原因：原草案错误预测通用 registry、持久化 PID 和 Bridge RuntimeAdapter/三 connector 自然复用；这些与宪法、真实代码与安全边界不符。
- 本次已消除的不确定性：P0 命名/schema/ownership/URL/安装入口/状态源；上游 pin；headless 默认 approval fail-closed；Bridge 的 one-shot 边界。
- 本次采用的关键假设：P0 只支持一个 DSH 实例；Node 采用应用开发基线并由 preflight 给出路径/版本，不伪造 DSH 未声明的 engines；本地开关不是远程 flag。
- 剩余不确定性：Windows/macOS 私有安装耗时、WebView2/WKWebView 完整交互、Windows 软停与双平台真机 descendant-kill；headless 凭据/connector 契约。
- 影响范围：新增 additive 设置字段和 pane kind；现有 Kimi/Bridge 行为不迁移，external allowlist 不变。
- 验证方式：TypeScript；Vitest；Rust lib tests；command registry/ACL；fmt/diff；Go Bridge `go test`/`go vet` 审计基线。2026-08-14 本机固定 npm 安装超过两分钟未完成后人工终止，无残留进程。
- 是否更新 README：是，记录 DSH P0 职责与安全边界。
- 是否更新 `.ai/architecture`：是，记录当前事实、Known Gap 与发布 G3。
- 是否需要新增或更新 `.ai/decisions`：是，新增 Accepted DSH P0 contract ADR。
- 后续注意事项：完成双平台 G3 前不得宣称 P0 双平台可发布；headless 硬前置不满足时保持 No-Go。
