## 2026-08-15-20 - DSH Windows 合并前运行时与安装体验收口

- 用户目标：根据粘贴的 Windows 安装、安装反馈、Node 预检和 Explorer 右键菜单核查结论完成优化与开发。
- 涉及目录：`.github/workflows`、`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src/features/control-center`、`apps/kimi-shell/src/services`、根/应用 README、`.ai/architecture`、`.ai/decisions`、`tasks`。
- 修改内容：DSH 支持矩阵收紧为 Node 22.19+ 的 22.x 或 24+ 并继续要求 `parseEnv`；runtime 与 install toolchain 独立选择；preflight additive 增加运行/安装就绪、Node/npm、安装有效性字段并保留 `ready == runtimeReady`；Node locator 不再跨来源配对 PATH npm；Windows npm shim 优先 `node.exe + npm-cli.js`，缺失时由 canonical System32 `cmd.exe` 执行固定命令；安装阶段与脱敏 stdout/stderr 经 typed Tauri Channel 实时进入控制中心；PR canary 增加双平台 Node 22.19/24 矩阵与稳定汇总检查，Node 20/latest 留作非 PR 观察。
- 修改原因：原实现会接受未纳入产品支持矩阵的 Node 版本、把无 npm 的首个 Node 用作安装候选、跨工具链配对 npm，且 shim 布局会误报 npm 缺失；安装期间只有“安装中”且无实时输出；真实 runtime smoke 未进入 PR。
- 本次已消除的不确定性：npm registry 的固定 rc.6 元数据确实没有 `engines`；Node 22.19/24 是 KickSide 自有支持策略而非上游声明。rc.6 官方包确实提供 `POST /api/workspace.create`，但没有稳定的外部 UI workspace/session 选中 seam；因此 Explorer→DSH 不能仅靠 adoption 达成“打开目标工作区”。
- 本次采用的关键假设：支持矩阵主动与 Kimi/上游开发基线统一，既有 Node 20 成功证据保留为观察性事实；系统 `cmd.exe` fallback 的兼容收益以同工具链约束、固定命令串、专用环境变量、关闭 AutoRun/延迟展开和 Windows 特殊字符测试为安全前提。
- 剩余不确定性：当前 macOS 环境不能执行 Windows-only shim 真测试和安装包 G3；远端仓库尚未把 `DSH runtime gate` 配成 required；Explorer DSH 打开仍需官方 UI selection seam 或单独接受并测试的固定 pin bridge 契约。
- 影响范围：已安装 DSH 在受支持 Node 存在但 npm 缺失时仍可启动；安装/重装只接受完整同工具链；Node 20.12、22.18 与 23 将被产品 preflight 拒绝；安装 UI 新增阶段和可选择文本的实时日志区。
- 验证方式：`cargo fmt --all -- --check`；`cargo check --locked`；`cargo clippy --locked --all-targets -- -D warnings`；`cargo test --locked`；`pnpm exec tsc --noEmit`；`pnpm test`；`pnpm check:nfr:security`；workflow 通过 YAML parser、`actionlint v1.7.7` 和 `git diff --check`。Windows 安装包矩阵记录为 G3 待补，不由上述结果替代。
- 是否更新 README：是，更新根开发 Node 基线与应用 DSH 工具链、实时安装反馈和 canary 契约。
- 是否更新 `.ai/architecture`：是，更新当前 DSH runtime 事实与验证 gate。
- 是否需要新增或更新 `.ai/decisions`：是，更新 Accepted DSH P0 ADR 的支持矩阵、preflight additive 契约和受控 shim 回退安全边界。
- 后续注意事项：在 GitHub ruleset 将稳定汇总检查设为 required；用正式 Windows 安装包覆盖官方 Node/nvm-windows/Volta、中文/空格目录、代理 registry、WebView2、PowerShell 中文输出和 descendant taskkill。工作区中同期出现的 Kimi frame layout 相关改动与未跟踪 `.ai/plans/kimi-code-web-ui-development-plan.md` 不属于本次修改，未覆盖或纳入本记录。

## 2026-08-15-21 - Kimi Code Web 响应式布局增强

> 2026-08-16 纠错：本节关于“0.36.1 不存在右侧 conversation outline”及“会话目录对应左侧 Sessions sidebar”的判断已被当前生产二进制内嵌 bundle 反证。纠正实现与验证记录见 `.ai/changes/2026-08-16.md`；本节保留为昨日改动的历史记录，不再作为当前事实依据。

- 用户目标：完成 `.ai/plans/kimi-code-web-ui-development-plan.md`。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/app`、`apps/kimi-shell/tests`、`apps/kimi-shell/scripts`、`apps/kimi-shell/third_party/kimi-cli-web`、应用 README/docs、`.ai/architecture`、`tasks`。
- 修改内容：扩展现有精确 origin 主题消息，仅为 Kimi Code pane 增加 surface/layout/accent；在生产 all-frame bridge 中实现 1180/960 断点状态机、52px Header、12px + safe-area composer 底距、Kimi 原生蓝色图标、compact/narrow 左抽屉、可访问焦点管理、rAF 合并观察器、Shell/frame 双层功能关闭与 fail-open 诊断；新增脱敏 DOM fixture、8 张 Chrome 视觉基线和定向测试。
- 修改原因：原计划引用的 workspace proxy/injection 已是 dead code，当前生产直接加载 runtime URL；当前 Kimi 0.36.1 已有左侧 Sessions sidebar 与原生移动 topbar，不存在计划假设的右侧 conversation outline。
- 本次已消除的不确定性：真实 0.36.1 页面在 1280px 的 sidebar/header/composer 计算值、959/960/1179/1180 边界、640px 原生移动形态以及 `--logo`/`--blue` 原生蓝色已完成脱敏审计；宿主现有消息 envelope 和 all-frame 注入路径已由源码与运行时交叉确认。
- 本次采用的关键假设：用户所称会话目录对应当前左侧 Sessions sidebar；上游已原生处理移动模式时不重复注入；宿主 accent 只作 Kimi 变量后的 fallback。
- 剩余不确定性：macOS WKWebView、Windows WebView2、3:2 显示器、125%/150% 缩放、中文 IME、触控和屏幕阅读器仍需 G3。
- 影响范围：只在 parent 发出的 `surface=kimi-code` / `layoutEnhancement=v2` 消息后增强 Kimi iframe；DSH、generic external、retired EnhancedLocal 与旧 proxy 行为不变。
- 验证方式：定向 Vitest 49 项；完整 Vitest 54 文件/268 项；TypeScript 与生产 Vite build；JS 语法；8 张视觉基线；182-command security gate；Rust fmt/check/clippy 与 288 项测试；updater manifest 2 项；`git diff --check`。
- 是否更新 README：是，补充 all-frame bridge 责任、视觉 fixture 命令与 patch 目录边界；同时校正 Kimi Web style map 的 shell/Kimi accent 所有权。
- 是否更新 `.ai/architecture`：是，新增 Kimi Web DOM contract 并更新 current state/verification gates。
- 是否需要新增或更新 `.ai/decisions`：否；这是既有 direct-runtime/all-frame 边界内的局部产品适配，没有改变长期架构方向。
- 后续注意事项：升级 Kimi Code 后按 DOM contract 复核；上游提供等价行为时删除对应兼容层；发布前完成两端 G3，不从 Chromium 截图外推。
