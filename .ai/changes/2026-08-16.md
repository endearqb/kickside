## 2026-08-16-16 - 恢复 Sessions sidebar 并实现真实消息 TOC

- 用户目标：把带 Sessions sidebar 的 Kimi Code UI 恢复为原始状态，并实现截图红框消息 TOC 的 wide 左置与窄 pane 抽屉需求。
- 涉及目录：`apps/kimi-shell/src-tauri/src`、`apps/kimi-shell/src/app`、`apps/kimi-shell/tests/kimi-web-layout`、`apps/kimi-shell/scripts`、应用 README/docs、`.ai/architecture`、`.ai/changes`、`tasks`。
- 修改内容：将 `aside.side` 从 TOC 识别、样式、toggle、backdrop、inert 和焦点路径中完全移除；按真实生产 bundle 契约识别 `nav.conversation-toc`，wide 使用 container 镜像公式左置并按 gutter 限制 label 宽度，compact/narrow 无-sidebar pane 复用原生 TOC 做可访问抽屉；原生 mobile 不渲染 TOC 时，仅在至少两个唯一 `.turn-anchor[data-turn-id]` 存在时生成 body-level 有界 projection。Header、composer 和 `.wsq` 增强也收窄到无-sidebar 的 compact/narrow pane。
- 修改原因：昨日审计错误地以 vendored React 源码快照代替当前安装二进制的 Vue 生产 bundle，误判 0.36.1 不存在右侧 `ConversationToc`，导致实现修改了左 Sessions sidebar，真实 TOC 完全未执行。
- 本次已消除的不确定性：当前 0.36.1 生产 bundle 明确包含 `section.con > nav.conversation-toc`、中英文 aria、`.toc-row/.toc-bar/.toc-label`、`.turn-anchor[data-turn-id]` 点击契约、渲染条件和 `.toc-clipped` 空间规则；`section.con` 的 760px 正文 container 与精确左右定位公式也已核对。
- 本次采用的关键假设：用户要求的“恢复原始状态”指 Sessions sidebar 及带-sidebar 窄 pane 不再接受先前 Header/composer/icon/drawer 覆盖；wide 消息 TOC 的左置仍是明确目标。mobile projection 只存在于当前 frame DOM，不跨边界保存或记录用户文字。
- 剩余不确定性：真实 macOS WKWebView、Windows WebView2、3:2 显示器、125%/150% 缩放、长对话、中文 IME、触控和屏幕阅读器仍需 G3；当前自动化不能替代用户对实际会话 TOC 点击/滚动的人工确认。
- 影响范围：只有来自当前 parent、`surface=kimi-code`、`layoutEnhancement=v2` 且未关闭 kill switch 的 frame 会初始化；DSH、generic external、Kimi Chat、旧 proxy 和 postMessage 精确 origin 均不变。Sessions sidebar 不再被注入层修改。
- 验证方式：`node --check src-tauri/src/frame_workspace_bridge.js`；定向 Vitest 2 文件/60 项；完整 Vitest 54 文件/279 项；`pnpm exec tsc --noEmit`；10 张 Chrome 视觉基线生成后再次字节级验证；182-command security gate；Vite production build；Rust fmt/check、严格 clippy 与 288 项测试；`git diff --check`。本机 Apple Silicon `KickSide.app` 已重建，主程序/sidecar 均为 arm64，内嵌新 TOC bridge 标记，ad-hoc 深签验证通过；两端真实 WebView G3 仍待用户验证。
- 是否更新 README：是，纠正 all-frame bridge 职责和 10 张视觉基线说明。
- 是否更新 `.ai/architecture`：是，重写 Kimi Web DOM contract 并更新 current state/verification gates。
- 是否需要新增或更新 `.ai/decisions`：否；仍位于既有 direct-runtime/all-frame 适配边界内，没有新增持久化、公共 IPC 或外部依赖。
- 后续注意事项：升级 Kimi Code 时必须直接核对当前生产 bundle/live DOM，不得从 vendored snapshot 推断运行时组件；若上游公开与生产 bundle一致的可构建源码，应优先把 mobile projection 迁回源码组件并删除注入兼容层。
## 2026-08-16-17 - 收敛 Kimi TOC 为全宽度原生短条交互

- 用户目标：让窄 pane Header 与正常高度一致，删除顶部注入的目录按钮，并让 TOC 在所有宽度常驻左侧短竖条、仅 hover / focus 时向右展开。
- 涉及目录：`apps/kimi-shell/src-tauri/src/`、`apps/kimi-shell/src/app/`、`apps/kimi-shell/tests/kimi-web-layout/`、`apps/kimi-shell/scripts/`、`.ai/architecture/`、`.ai/plans/`。
- 修改内容：删除 52px Header 覆盖及 TOC toggle/backdrop/drawer/inert/focus-trap 状态机；统一 native TOC 与 mobile projection 的左侧短条定位、默认折叠 label 和 hover/focus 向右展开；保留原生 TOC 按钮、active 与滚动语义；重写单元与视觉基线覆盖。
- 修改原因：前一版把 TOC 做成了窄屏抽屉，并在 wide 强制文字常显且反向展开，与实测截图和用户交互目标不符。
- 本次已消除的不确定性：顶部红框组件确定是 KickSide 注入的 TOC toggle；Header 高度差确定来自 KickSide 的 52px 强制覆盖；真实 TOC 的 DOM 顺序本身就是 `bar → label`，可直接复用向右展开。
- 本次采用的关键假设：真正 mobile 下上游不渲染 TOC 时，继续用仅来自 `.u-bub.turn-anchor[data-turn-id]` 的临时 projection，以满足“所有宽度”常驻短条；展开可覆盖正文，因为窄宽度没有容纳 220px 独立 rail 的固定 gutter。
- 剩余不确定性：真实 WKWebView/WebView2 的 scoped CSS、触控 focus 保留和极长会话滚动仍需 G3 人工验收。
- 影响范围：仅 `surface=kimi-code` 且 `layoutEnhancement=v2` 的 frame；其他 pane、主题消息安全边界和 Sessions sidebar 不变。
- 验证方式：桥接/Workspace Grid 定向 2 文件 60 项；完整 Vitest 54 文件 279 项；TypeScript；10 张 Chrome 视觉基线；Vite production build；182-command security gate；updater manifest 2 项；Rust fmt/check/严格 clippy 与 288 项测试；`git diff --check`。本机 Apple Silicon `KickSide.app` 已重建并 ad-hoc 深签，主程序与 sidecar 均为 arm64，二进制包含新 rail CSS 标记。
- 是否更新 README：是，更新视觉夹具说明。
- 是否更新 `.ai/architecture`：是，更新 Kimi Web DOM/响应式契约。
- 是否需要新增或更新 `.ai/decisions`：否；属于既有 v2 增强的交互纠偏。
- 后续注意事项：上游若开始在 mobile 原生渲染 `ConversationToc`，应自动优先原生节点并删除 projection 兼容路径。

## 2026-08-16-17 - 将主窗口文件拖放交还 Kimi Code Web

- 用户目标：让拖入 Tauri App 的文件直接交给 Kimi Code Web，使聊天框能够接收附件。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/scripts`、`.ai/changes`、`tasks`。
- 修改内容：为 `main` 窗口显式设置 `dragDropEnabled=false`，关闭 Tauri/Wry 原生文件拖放 handler；在 capability/config gate 中要求该值保持为 false。
- 修改原因：主窗口此前沿用默认原生拖放，系统文件事件被宿主消费，iframe 内 Kimi Code 已有的 HTML5 `dragover/drop` 监听无法收到文件。
- 本次已消除的不确定性：Kimi Code iframe 自身具备附件 drop handler；主窗口没有覆盖 Tauri 默认值；独立嵌入式 WebView 已采用同一配置。
- 本次采用的关键假设：用户需要的是 Kimi Code 聊天框附件拖放，不需要主窗口同时处理 Tauri 原生 drag/drop event。
- 剩余不确定性：真实 macOS WKWebView 与 Windows WebView2 的 Finder/Explorer 文件拖放仍需 G3 实机确认。
- 影响范围：只改变 `main` WebView 的系统文件拖放归属；prefill、workspace import picker、窗口内 pane 排序拖拽和 Kimi Code 附件实现均不变。
- 验证方式：Tauri 配置 JSON 解析；`pnpm check:nfr:security`（capability、bundle resource、182-command registry/ACL）；完整 Vitest 54 文件/279 项；`pnpm build`（TypeScript + Vite production build）；`cargo check --locked`；`git diff --check`。真实 Finder/Explorer 拖入属于 G3，尚未在本轮自动化中证明。
- 是否更新 README：否；目录职责、公共接口和运行方式未变。
- 是否更新 `.ai/architecture`：否；属于现有 WebView 边界的局部行为修复。
- 是否需要新增或更新 `.ai/decisions`：否；未改变持久化、序列化契约、依赖或长期架构路线。
- 后续注意事项：若未来恢复 Shell 级文件拖入导入，不应重新开启会吞掉 HTML5 drop 的原生 handler；应设计独立入口或经验证的事件路由。

## 2026-08-16-18 - 同步 DSH 控制中心与标题栏菜单状态

- 用户目标：修复控制中心从关闭状态开启 DSH 后，新建窗格与浏览器打开菜单仍看不到 DSH，并评估 KimiCode 后端开关。
- 涉及目录：`apps/kimi-shell/src`、`.ai/changes`、`tasks`。
- 修改内容：控制中心 DSH 面板在启用、安装、启动、停止成功后调用 App 级 `dsh.refresh`；经 `ControlCenterViewProps` 显式透传该刷新入口；回归测试验证关闭→开启会通知外层且不误触发展开。
- 修改原因：控制中心 Panel 与 App `useDshController` 分别维护 settings/status；前者开启成功后没有通知后者，而标题栏两个菜单只读后者的 `settings.enabled`。
- 本次已消除的不确定性：Rust `dsh_save_settings` 已正确保存并启动默认工作区；菜单条件渲染和 URL 信任逻辑正确；故障仅是 React 状态投影陈旧。
- 本次采用的关键假设：本轮用有界变更同步既有两个投影，不扩大为 DSH 状态架构重构；Kimi 后端开关不属于该 Bug 的必要修复。
- 剩余不确定性：真实 App 中关闭→开启后的菜单时序与 DSH URL ready 仍需 G3；若 DSH 控制面继续扩展，应将 settings/status 收口为单一受控 owner。
- 影响范围：DSH 控制中心动作完成后的前端刷新；Kimi、Bridge、pane 持久化、Rust command 与序列化契约不变。
- 验证方式：定向 DSH Panel + ShellTitlebar 2 文件/18 项；完整 Vitest 54 文件/279 项；`pnpm exec tsc --noEmit`；Vite production build；capability、bundle resource 与 182-command registry/ACL security gate；`git diff --check`。真实 App 菜单时序仍待 G3。
- 是否更新 README：否；目录职责、公共 API 和长期运行方式未变。
- 是否更新 `.ai/architecture`：否；未改变当前生命周期事实。
- 是否需要新增或更新 `.ai/decisions`：否；未跨越持久化、序列化或依赖单向门。
- 后续注意事项：Kimi 当前是 Shell 主启动门且可能复用 external 实例；若增加运行控制，必须区分 owned“停止”与 external“断开”，并先让控制中心在 Kimi stopped 状态保持可访问。
## 2026-08-16-17:55 - 为展开 TOC 增加半透明毛玻璃浮层

- 用户目标：为 hover/focus 展开的消息 TOC 增加毛玻璃半透明背景。
- 涉及目录：`apps/kimi-shell/src-tauri/src/`、`apps/kimi-shell/src/app/`、`apps/kimi-shell/tests/kimi-web-layout/`、`apps/kimi-shell/scripts/`、`.ai/architecture/`。
- 修改内容：在 TOC `::after` 展开层增加亮暗主题自适应的 72% 半透明背景、18px blur、140% saturation、1px 轻边框、12px 圆角和浮层阴影；折叠态透明且不接收指针；`prefers-reduced-motion` 下关闭过渡。
- 修改原因：展开文字直接覆盖消息内容，可读性不足；该背景只属于临时浮层，不改变静态页面层级。
- 本次已消除的不确定性：背景作用域限定为 TOC hover/focus-within，折叠 rail 与 Header/Sidebar 均不受影响。
- 本次采用的关键假设：WKWebView/WebView2 均可使用标准或 `-webkit-` backdrop filter；不支持时以半透明背景安全降级。
- 剩余不确定性：真实 Windows 高对比度模式和低端 GPU 下的材质观感仍需 G3。
- 影响范围：仅启用 Kimi Code layout v2 的展开 TOC。
- 验证方式：单元测试、亮暗主题视觉基线、完整前端测试、生产构建与本机 App 构建。
- 是否更新 README：是，更新视觉夹具覆盖。
- 是否更新 `.ai/architecture`：是，补充展开材质契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：若真实 WebView 模糊性能不稳，可仅降低 blur 半径，不应恢复不透明白底。
## 2026-08-16-18:10 - 统一 TOC 展开宽度并提高透明度

- 用户目标：让毛玻璃背景更透明，并让非窄屏 TOC 使用与窄屏一致的展开宽度。
- 涉及目录：`apps/kimi-shell/src-tauri/src/`、`apps/kimi-shell/src/app/`、`apps/kimi-shell/tests/kimi-web-layout/`、`.ai/architecture/`。
- 修改内容：亮暗背景不透明度从 72% 降至 58%；新增所有断点共用的 `--kimi-outline-expanded-panel-width`，面板最大宽度固定为 220px，并为 label 预留 bar、gap 与背景内边距后的统一可用宽度。
- 修改原因：wide pane 的长标题会把浮层撑得过宽，且上一版背景遮挡正文偏多。
- 本次已消除的不确定性：宽度约束不再由当前标题长度或 pane 模式决定；窄屏、compact、wide 使用同一上限。
- 本次采用的关键假设：220px 是当前窄屏浮层的目标视觉规格；极窄 viewport 仍由可用空间公式自动缩小。
- 剩余不确定性：真实业务中的中英文极长标题需要用户最终确认截断观感。
- 影响范围：仅 TOC hover/focus 展开态。
- 验证方式：单元测试、亮暗主题视觉基线、完整前端测试、生产构建与本机 App 构建。
- 是否更新 README：否；验证入口不变。
- 是否更新 `.ai/architecture`：是，固定当前材质和宽度契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：后续若调整面板宽度，只修改统一 token，不再按断点分叉。

## 2026-08-16-18:35 - 重写中英文 README 与产品展示素材

- 用户目标：根据本机演示视频和四张实测截图重写仓库 README，以中文作为主文档、提供英文版跳转，并将当前完整改动发布到 GitHub 后合并至 `main`。
- 涉及目录：根目录 README、`apps/kimi-shell/public/readme/`、`.ai/changes/`、`tasks/`。
- 修改内容：将 `README.md` 重写为中文产品主页，新增完整英文镜像 `README_EN.md`，将旧 `README_zh.md` 收敛为兼容跳转；整理多窗格总览、DeepSeek Harness、Kimi Code TOC、控制中心四张截图与 30 秒演示视频，补充产品定位、能力矩阵、安装要求、本地开发和许可说明。
- 修改原因：旧主 README 为英文且截图和能力描述已落后于当前 Workspace Grid、DSH 与 Kimi Code TOC 实现，无法准确展示当前产品形态。
- 本次已消除的不确定性：四张截图分别对应多智能体并排、DSH 工作区、Kimi Code 会话/TOC 和控制中心；视频展示 Pane Shelf、窗格编排和跨运行时工作流。README 中的版本、平台、Node 支持矩阵和开发命令已与当前源码及文档核对。
- 本次采用的关键假设：用户提供的媒体可作为仓库公开展示素材；中文主文档与英文版保持同一信息结构，`README_zh.md` 仅保留历史链接兼容，不再维护第三份正文。
- 剩余不确定性：GitHub 对 MP4 的内联预览行为取决于客户端，故 README 使用普通链接并以 PNG 作为主要视觉内容；发布包签名状态继续以具体 Release 页面为准。
- 影响范围：仅仓库入口文档与展示资源，不改变应用运行时、构建接口或持久化数据。
- 验证方式：检查 Markdown 相对链接和媒体类型；扫描绝对本机路径与常见凭据模式；运行仓库完整 TypeScript、Vitest、视觉基线、安全、生产构建和 Rust 门禁，并在提交后等待 GitHub PR checks。
- 是否更新 README：是，中文主文档、英文镜像和中文兼容入口均已更新。
- 是否更新 `.ai/architecture`：否；README 只呈现已有架构事实，没有新增运行边界。
- 是否需要新增或更新 `.ai/decisions`：否；文档语言入口与媒体组织不构成长周期架构决策。
- 后续注意事项：产品界面或平台支持矩阵变化时，应同时更新 `README.md` 与 `README_EN.md`；截图和视频只放置脱敏的公开素材。

## 2026-08-16-18:45 - 修复 Windows DSH canary 的 npm 启动方式

- 用户目标：在 README 与功能改动合并前，让 GitHub Windows/macOS 检查全部通过。
- 涉及目录：`apps/kimi-shell/scripts/`、`.ai/changes/`、`tasks/`。
- 修改内容：DSH runtime smoke test 不再直接 spawn Windows `npm.cmd`，改为使用当前 `process.execPath` 直接执行同一 Node 安装目录的 `node_modules/npm/bin/npm-cli.js`。
- 修改原因：GitHub Windows runner 的 Node 22.19 与 24 均在 `spawn npm.cmd` 时返回 `EINVAL`；`.cmd` 不能作为无 shell 的可执行文件直接启动。
- 本次已消除的不确定性：两个 Windows matrix job 的失败堆栈都指向 smoke script 的 npm spawn；安装尚未开始，与 DSH 包版本或运行 readiness 无关。
- 本次采用的关键假设：官方 `actions/setup-node` 的 npm CLI 位于当前 Node executable 同目录的标准 `node_modules/npm/bin/npm-cli.js`；缺失时通过读取检查直接 fail closed。
- 剩余不确定性：无；最终由 Windows 22.19/24 与 macOS 22.19/24 四个远端 matrix job 共同验证。
- 影响范围：仅 CI/runtime canary 的 DSH 安装启动方式，不改变应用运行时或用户安装流程。
- 验证方式：Node 语法检查、本地 smoke（平台可用时）以及 GitHub 四平台/版本 matrix 重跑。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否；修复遵循既有同工具链 npm 契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：Windows Node 脚本若要求无 shell 执行 npm，应直调受验证的 npm CLI，不得直接 spawn `.cmd` shim。

## 2026-08-16-19:05 - 收口 Windows npm fallback 与进程树测试

- 用户目标：修复 PR 合并门禁中唯一失败的 Windows Rust job。
- 涉及目录：`apps/kimi-shell/src-tauri/src/`、`.ai/architecture/`、`.ai/decisions/`、`.ai/changes/`。
- 修改内容：canonical `cmd.exe` fallback 改用原生 working directory 定位固定名 `npm.cmd`，命令串不再包含任何动态路径，安装前缀只由 `npm_config_prefix` 环境变量传入；特殊字符真测试同步验证 native cwd、固定 argv 与原始 prefix 环境值。Windows 进程树测试将高负载启动等待从 5 秒扩至 15 秒，并在 PID 文件缺失时先清理已启动进程树再失败。
- 修改原因：Windows Server 2025 runner 证明 `%VAR%` 展开的值若自身含 `%`，仍会被 `cmd.exe` 重新解释并破坏命令；并行 284 项测试时 PowerShell descendant 在 5 秒边界后才完成启动，旧断言既误报又遗留进程。
- 本次已消除的不确定性：失败日志明确显示 `100%` 被拆成第二条命令；runner cleanup 同时发现 PowerShell/cmd/PING 孤儿，证明进程树启动测试属于等待/清理边界而非终止实现失败。
- 本次采用的关键假设：Node 同目录 fallback shim 的文件名必须为标准 `npm.cmd`；其他脚本名不扩大 shell 信任面并直接 fail closed。
- 剩余不确定性：Windows 专属两项只能由 GitHub Windows runner 最终复验，本机 macOS 无法执行相同 cfg 测试。
- 影响范围：DSH 安装工具链的低优先级 Windows cmd fallback与 Windows-only 测试；优先 `node.exe + npm-cli.js` 路径、macOS direct exec 和 DSH runtime 均不变。
- 验证方式：本机 Rust fmt/check/严格 clippy/288 项测试；GitHub Windows Rust job 重跑，并继续要求 Windows/macOS DSH matrix 与汇总 gate 通过。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：是，记录 shim native cwd 与 `npm_config_prefix` 契约。
- 是否需要新增或更新 `.ai/decisions`：是，同步 accepted DSH P0 ADR 的 shell fallback 约束。
- 后续注意事项：任何经 `cmd.exe` 的动态值都不得进入命令串，即使先放入 `%VAR%`；优先使用 native process 字段或目标程序直接读取的环境配置。

## 2026-08-16-21:07 - 为 README 增加内联 GIF 产品演示

- 用户目标：将现有产品演示视频转换为可在 GitHub README 中像图片一样直接播放的动图。
- 涉及目录：根目录 README、`apps/kimi-shell/public/readme/`、`.ai/changes/`。
- 修改内容：从 30 秒 MP4 中截取最集中展示 DeepSeek Harness、Kimi Code、多窗格编排和窗格菜单的 5–17 秒片段，生成 960×563、10 FPS、循环播放的 GIF；中英文 README 改用 GIF 作为首屏主视觉，并保留原始 30 秒 MP4 的高清链接。
- 修改原因：GitHub Markdown 对 GIF 可直接内联播放，而 MP4 通常只能作为链接打开；同时保留 MP4 可兼顾清晰度和完整演示。
- 本次已消除的不确定性：通过时间轴预览确认截取范围避开开头的重叠窗口和后段的浏览器切换；生成文件为标准 GIF87a，120 帧且体积约 514KB。
- 本次采用的关键假设：自动循环且无音频的 12 秒片段适合作为 README 首屏预览；需要完整流程或更高清画质的用户会使用相邻 MP4 链接。
- 剩余不确定性：不同浏览器可能对 GIF 动画节能策略不同，但静态首帧仍可作为安全降级。
- 影响范围：仅仓库展示文档与公开媒体素材，不改变应用运行时或构建流程。
- 验证方式：检查 GIF 文件类型、尺寸、帧数和体积；人工检查动画画面；校验中英文 README 的所有本地链接与本机绝对路径。
- 是否更新 README：是，中英文主文档同步更新。
- 是否更新 `.ai/architecture`：否；不涉及架构事实。
- 是否需要新增或更新 `.ai/decisions`：否；属于展示素材优化。
- 后续注意事项：更新产品演示时应同步替换 GIF 与 MP4，GIF 优先保持在适合 GitHub 加载的体积范围内。

## 2026-08-16-21:28 - 准备 v0.2.0 未签名 macOS 预览发布

- 用户目标：按推荐发布流程发布 `v0.2.0` 和中文更新说明，并明确继续使用未签名、未公证的 macOS 安装包。
- 涉及目录：`.github/workflows/`、`apps/kimi-shell/docs/`、`apps/kimi-shell/README.md`、`.ai/architecture/`、`.ai/decisions/`、`.ai/changes/`、`tasks/`。
- 修改内容：将未签名 macOS Release 例外从已完成的 `v0.1.24` 精确迁移为仅允许 `v0.2.0`；新增 accepted 例外决策和中文发布说明；Release draft 改为读取版本化 notes 文件并验证首行警告，重跑时同步更新 draft；继续强制两端 updater Secrets、ad-hoc/no-Authority 校验、完整资产矩阵和唯一 `latest.json`。
- 修改原因：现有工作流会拒绝任何非 `v0.1.24` tag，而用户明确选择 `v0.2.0` 继续以未 Developer ID 签名、未公证方式作为预览版分发；该单向门必须以精确 tag、显式警告和可回收决策记录，不能永久放宽。
- 本次已消除的不确定性：应用四处版本均为 `0.2.0`；远端不存在 `v0.2.0` tag/Release；Tauri updater 私钥 Secrets 已配置，但它们不等同于 Apple Developer ID 或 Windows Authenticode；发布产物由 tag workflow 构建而非本机上传。
- 本次采用的关键假设：`v0.2.0` 是明确标注的预览发布；用户接受 macOS 手动 Gatekeeper 放行与 Windows SmartScreen 信誉边界；后续版本恢复正式 Apple 签名，或重新取得 accepted 例外。
- 剩余不确定性：最终 Windows NSIS/MSI、macOS app/DMG 和公开 updater 的真实安装生命周期需在 Actions 产物生成后继续 G3；真实 WKWebView/WebView2、Windows Node 管理器和辅助功能矩阵不从自动化外推。
- 影响范围：GitHub Release 供应链与发布文档；不改变应用运行时、持久化数据、Tauri updater 信任密钥或安装器身份。
- 验证方式：版本/notes/tag 静态断言、YAML 解析、Updater manifest 测试、安全与完整代码门禁、main CI、tag Release workflow，以及发布后资产与 `latest.json` 核验。
- 是否更新 README：是，更新 Shell 发布契约和精确例外范围。
- 是否更新 `.ai/architecture`：是，更新当前发布事实与人工门禁表述。
- 是否需要新增或更新 `.ai/decisions`：是，新增 `v0.2.0` 未签名 macOS 预览发布 accepted 决策。
- 后续注意事项：本记录在 tag 前只表示发布候选已准备；只有 Release workflow 成功、资产完整且发布页可见后才能称“预览版已发布”，双平台 G3 未补齐前不得称生产就绪。
