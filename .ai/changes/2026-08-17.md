## 2026-08-17-00:15 - 发布 KickSide v0.2.1 未签名 macOS 预览版

- 用户目标：版本提升至 `0.2.1`，推送 GitHub `main`，创建 Release tag 并触发 Actions 构建；用户明确批准本版本继续采用 macOS 未签名、未公证的预览发布例外。
- 涉及目录：`.ai/changes/`、`tasks/`；外部状态为 GitHub `main`、annotated tag `v0.2.1`、Actions 与 Release。
- 修改内容：发布准备提交 `6ea8bd7` 推送 `main` 并通过 8-job CI；创建并推送精确指向该提交的 `v0.2.1` tag；Release workflow 并行构建 Windows x64 与 macOS arm64，上传安装包和 updater 签名，生成跨平台 `latest.json`，最后公开为 Latest Release。
- 修改原因：只有发布候选通过主分支门禁，且 tag 构建的双平台资产、签名和 updater manifest 全部验证成功后，才能将候选提升为可下载预览版。
- 本次已消除的不确定性：main CI `31956793911` 的 8 个 jobs 全绿；Release workflow `31957251104` 的 4 个 jobs 全绿；GitHub Release `v0.2.1` 已公开并为 Latest；正文首行为未签名/未公证警告；8 个资产全部 uploaded、非零且带 SHA-256 digest。
- 本次采用的关键假设：NSIS、MSI、DMG 与 app updater 产物供用户继续双平台 G3；公开 Latest 用于现有 Tauri updater 发现该版本，但不代表 Developer ID、公证、Gatekeeper、Windows Authenticode 或生产就绪。
- 剩余不确定性：Windows NSIS/MSI 新装与升级、SmartScreen、不同 Node 管理器和真实退出视觉，以及 macOS DMG 首开、手动 Gatekeeper 放行、WKWebView/IME/辅助功能仍需目标平台人工验证。
- 影响范围：GitHub 发布渠道与自动更新元数据；发布结果记录不回写已创建的 `v0.2.1` tag。
- 验证方式：本地前端 54 文件/281 项、Rust 287 项、严格 clippy、TypeScript、生产构建和安全门禁；main CI `31956793911` success；Release Actions `31957251104` success；核对 annotated tag target `6ea8bd7`、公开 Latest 身份、警告首行、8 个资产状态/大小/digest；下载解析 `latest.json`，确认 `version=0.2.1`、`windows-x86_64`/`darwin-aarch64`、精确 tag URL 与非空签名。
- 是否更新 README：否；发布准备提交已更新长期发布契约。
- 是否更新 `.ai/architecture`：否；发布结果符合准备阶段记录的当前事实。
- 是否需要新增或更新 `.ai/decisions`：否；沿用已接受的精确 `v0.2.1` 未签名 macOS 预览发布决策。
- 后续注意事项：下一版本仍会 fail closed，必须恢复正式 Apple 签名、公证、stapling，或重新取得精确版本例外；双平台 G3 完成前保持“预览版已发布”表述。

## 2026-08-17-15:32 - 修复 Kimi Code TOC 定位与触发区

- 用户目标：带 Sessions sidebar 时将消息 TOC 放到 sidebar 右边缘约 5px 处，并将展开触发区收窄，避免影响正文选择。
- 涉及目录：`apps/kimi-shell/src-tauri/src/frame_workspace_bridge.js`、`apps/kimi-shell/src/app/linkBridge.test.ts`、`apps/kimi-shell/tests/kimi-web-layout/baselines/`。
- 修改内容：带 sidebar 的原生 TOC 使用相对 `section.con` 左边缘的 5px rail 偏移；TOC 外层和滚动空白区透传指针事件，仅保留短条约 7px 命中区与 `.toc-row` 按钮可点击；TOC 文案设为不可选；更新 4 张带 sidebar 的视觉基线。
- 修改原因：原有内容居中公式在 sidebar 存在时把 TOC 推离 sidebar；`::before` 的 `14px/48px` 隐形扩展和展开后的 `max-content` nav 盒子会扩大 hover/点击命中区并遮挡正文选择。
- 本次已消除的不确定性：无 sidebar 的内容区定位公式保持不变；当前判定只在原生 sidebar 存在时使用 5px 偏移，projection 仍走无 sidebar 路径；sidebar DOM 未被修改。
- 本次采用的关键假设：`section.con` 是原生 TOC 的 positioned ancestor，并随 Sessions sidebar 的 flex 布局移动；“5px”指 sidebar 右边缘的内容区一侧；短条 3px 两侧各增加 2px，命中宽度约 7px。
- 剩余不确定性：真实 WKWebView/WebView2 中的鼠标拖选、触控 focus 与 sidebar 动画仍需本机人工确认；仓库 Chrome 视觉脚本在单图截图时序上不稳定，重复运行会随机报告不同 PNG，不能据此声明视觉 gate 稳定通过。
- 影响范围：仅 Kimi Code iframe 的 all-frame layout enhancement；不影响 Kimi Chat、DSH、generic external pane 或上游 Sessions sidebar。
- 验证方式：`pnpm exec vitest run src/app/linkBridge.test.ts`（32/32）；`pnpm test`（54 个文件、281 项）；`pnpm exec tsc --noEmit`；`pnpm check:kimi-web:visual` 已更新预期基线但重复运行存在单图抖动；`pnpm tauri:build:macos:local` 成功生成 arm64 本机 App。
- 是否更新 README：否；属于局部 UI bugfix。
- 是否更新 `.ai/architecture`：否；未改变 Kimi DOM 契约或模块边界。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：请在本机 App 中重点验证带 sidebar 的 TOC 间距、鼠标靠近短条 1–2px 时才展开、展开后正文拖选，以及无 sidebar 的 800/959/960/1179px pane。

## 2026-08-17-15:49 - 将 Windows 旧版迁移前置到安装器启动

- 用户目标：打开 Windows 安装程序时先检测并卸载历史 Kimi 小助手/Sidekick，再进入 KickSide 正常安装，避免在安装阶段选择卸载后卡住。
- 涉及目录：`apps/kimi-shell/src-tauri/windows/`、`apps/kimi-shell/scripts/`。
- 修改内容：在 NSIS hook 被加载的位置注册 `MUI_CUSTOMFUNCTION_GUIINIT`，GUI 安装在欢迎页前完成旧 NSIS/MSI 精确匹配、用户确认、官方卸载器调用和注册表消失校验；`NSIS_HOOK_PREINSTALL` 仅作为 silent/passive 无 GUI 场景的兜底，并在卸载期间隐藏/恢复安装器窗口、处理执行错误。
- 修改原因：Tauri 默认 `NSIS_HOOK_PREINSTALL` 位于 `Section Install`，发生在安装页之后；旧卸载器与当前安装事务嵌套执行，导致用户看到卸载后安装流程卡住。
- 本次已消除的不确定性：GUI 初始化回调位于 Tauri Welcome page 之前；silent/passive 仍保留迁移路径；卸载返回成功但旧注册项未消失时 fail closed，不继续覆盖安装。
- 本次采用的关键假设：Tauri 2.10 的 `installerHooks` 在 MUI language macros 前被 include，因此可以注册 `MUI_CUSTOMFUNCTION_GUIINIT`，在 `.onInit` 后、首个页面前执行预检。
- 剩余不确定性：当前 macOS 主机无法真实运行 Windows NSIS/MSI 安装器，需在 Windows 上验证每个历史身份、管理员权限、静默参数和真实旧卸载器退出行为。
- 影响范围：Windows NSIS 安装器启动与历史品牌迁移；MSI UpgradeCode、应用数据保留策略和 macOS 安装链路不变。
- 验证方式：`node scripts/check_bundle_resources.mjs` 通过；`git diff --check` 通过；未在本机执行 Windows `makensis` 或 Windows 安装人工 gate。
- 是否更新 README：否；安装身份和数据保留契约未改变，属于安装器生命周期 bugfix。
- 是否更新 `.ai/architecture`：否；未改变现有历史身份 allowlist、MSI UpgradeCode 或模块边界。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：在 Windows 上重点验证“打开 setup → 立即出现旧版检测 → 确认卸载完成 → 显示 KickSide 欢迎页 → 正常安装”，并确认取消/卸载失败时不写入新安装文件。

## 2026-08-17-16:42 - 收口 DSH 控制中心检测与轮询

- 用户目标：优化控制中心反复打开时的 DSH 检测，避免重复 preflight 和重复状态轮询，同时保留控制中心与 DSH pane 的及时状态更新。
- 涉及目录：`apps/kimi-shell/src/app/`、`apps/kimi-shell/src/features/control-center/`、`apps/kimi-shell/src/features/workspace-grid/`、`apps/kimi-shell/src/App.tsx`。
- 修改内容：将 DSH 设置、preflight、状态和启停/安装动作统一收口到 `useDshController`；增加 60 秒 preflight TTL 与并发请求复用；控制中心打开、展开 DSH 详情使用缓存检测，手动“重新检测”强制刷新；启用运行态在控制中心/可见 DSH pane 前台按 1 秒轮询，普通工作区按 5 秒轮询，窗口隐藏时跳过请求；面板仅消费共享控制器并保留安装日志/进度展示。
- 修改原因：原实现由根 App 和控制中心面板各自读取 DSH 状态并各自创建 1 秒定时器，面板每次挂载还会执行一次完整 preflight，导致打开控制中心时重复调用并可能相互覆盖状态。
- 本次已消除的不确定性：控制中心面板不再直接调用 DSH 设置、preflight、状态或启停服务；关闭/重新打开控制中心不会重建 DSH 控制器；disabled 状态不会在 App 初始加载时主动 preflight；手动检测仍可强制获取最新结果。
- 本次采用的关键假设：`workspace-grid` 中当前 slot 或 maximized pane 代表用户可见的 DSH pane；Rust 后台自身已有 5 秒健康监测，因此前端后台 5 秒状态读取足够，前台 1 秒仅用于及时投影 UI。
- 剩余不确定性：当前主机未执行 Windows NSIS 安装器人工验证；本机 App 的 DSH 真实 Node/npm 环境、控制中心开关和多 pane 状态仍需手工点击确认。
- 影响范围：仅 DSH 前端状态生命周期、控制中心检测和状态刷新频率；DSH IPC 契约、Rust 健康监测、Kimi Code/Chat 与已有 Windows 安装迁移逻辑不变。
- 验证方式：`pnpm exec tsc --noEmit`；DSH 相关 Vitest 9/9；全量 Vitest 54 文件、282 项；`pnpm tauri:build:macos:local` 成功生成本机 App；`git diff --check`；`node scripts/check_bundle_resources.mjs`。
- 是否更新 README：否；未改变 DSH IPC、目录职责或使用方式，仅修复内部状态生命周期。
- 是否更新 `.ai/architecture`：否；未改变模块边界和后端事实。
- 是否需要新增或更新 `.ai/decisions`：否；轮询与缓存属于实现层优化。
- 后续注意事项：使用本机 App 验证控制中心连续开关、展开 DSH 详情、手动重新检测、可见/隐藏 DSH pane 以及安装失败日志是否仍正常展示；Windows 侧继续按上一条记录验证安装器启动前迁移。

## 2026-08-17-17:00 - 修复 KimiCode 登录与套餐升级外部跳转

- 用户目标：KimiCode 内点击登录和升级套餐时打开系统默认浏览器，同时不影响普通应用内导航。
- 涉及目录：`apps/kimi-shell/src-tauri/src/`、`apps/kimi-shell/src/app/linkBridge.test.ts`。
- 修改内容：统一 Tauri 全帧桥接与本地代理注入的外链解析；保留真正跨域 `http(s)` 外置；将带 bridge nonce 的本地 KimiCode `/login`、`/auth`、`/oauth`、`/membership`、`/subscription` 路径映射到 `https://www.kimi.com`；补齐 `<a>`、`window.open`、SPA `history.pushState/replaceState`、`location.assign/replace` 与可用 Navigation API 的处理；代理脚本忽略已被另一层桥接处理的 click，避免重复打开。
- 修改原因：原桥接只处理外部 `<a>` 和 `window.open`，升级套餐常走同源相对 SPA 路由，登录流程也可能走 history/location 导航，因此被当作 iframe 内部路由或未被捕获。
- 本次已消除的不确定性：普通同源会话/内容路由仍不会被外置；本地 KimiCode 账号路径会保留 path、query、fragment 并转换为可由系统浏览器访问的 Kimi Web 地址；仍使用既有 workspace iframe source 与 bridge nonce 校验。
- 本次采用的关键假设：本地 KimiCode iframe 使用 loopback `http` origin 且带 workspace bridge nonce；账号与会员页面的浏览器入口使用 `https://www.kimi.com` 同一路径。
- 剩余不确定性：本机无法直接验证真实 KimiCode 登录弹窗和套餐按钮的供应商运行时实现，需在构建 App 中点击登录、升级套餐和普通会话链接各一次；Windows 外部浏览器行为仍需 Windows 人工 gate。
- 影响范围：KimiCode iframe 的账号/会员跳转和跨域链接桥接；KimiChat 与普通同源应用内导航不变。
- 验证方式：`pnpm exec tsc --noEmit`；Vitest 54 个文件、283 项通过；Rust `cargo test --lib` 290 项通过；`git diff --check`；`node scripts/check_bundle_resources.mjs`；`pnpm tauri:build:macos:local` 成功生成 arm64 本机 App。
- 是否更新 README：否；属于外链桥接 bugfix，未改变目录职责或公共契约。
- 是否更新 `.ai/architecture`：否；沿用既有外链桥接和系统浏览器契约。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：使用下方本机 App 测试登录、升级套餐、普通内部路由与真正跨域链接，确认前两者只打开一次系统浏览器且 iframe 不再跳走。

## 2026-08-17-17:18 - 将新标签页外链交给 Tauri 原生浏览器处理

- 用户目标：修复 KimiCode 登录弹窗中“在浏览器中登录”仍无法打开系统浏览器的问题，并减少对外链的前端拦截依赖。
- 涉及目录：`apps/kimi-shell/src-tauri/src/window_manager.rs`、`apps/kimi-shell/src-tauri/src/frame_workspace_bridge.js`、`apps/kimi-shell/src-tauri/src/backend_manager/workspace_injection.rs`、`apps/kimi-shell/src/app/linkBridge.test.ts`。
- 修改内容：主 WebView 增加原生 `on_new_window` handler；所有 `http(s)` 的 `window.open`/`target="_blank"` 请求直接调用系统默认浏览器并拒绝在 KickSide 内创建新窗口；移除两层 JS 对 `window.open` 的包装；带 target 的链接交由 WebView 原生处理，保留普通同框账号路由和跨域链接的既有 fallback。
- 修改原因：登录按钮的授权页可能来自嵌套页面，前端 `postMessage` 到主窗口后会因 iframe source/nonce 校验丢失；原生新窗口事件不依赖嵌套层级、nonce 或前端消息转发。
- 本次已消除的不确定性：系统浏览器打开路径不再依赖 KimiCode iframe 的 bridge message；普通同源会话路由不会被原生 handler 外置；非 `http(s)` popup 不会被系统浏览器误打开。
- 本次采用的关键假设：截图中的登录按钮使用新标签页或 `window.open` 语义；Tauri 2.10 的 `on_new_window` 能捕获主 WebView 内嵌 iframe 的新窗口请求。
- 剩余不确定性：仍需在本机 App 中点击截图中的真实登录按钮确认 WebView 平台事件实际触发；若供应商改为同框 `location` 导航，则需要继续补充原生导航事件的来源判定。
- 影响范围：主窗口所有 `http(s)` 新窗口请求，以及 KimiCode 两层外链脚本；不会改变 KimiChat 普通内部导航。
- 验证方式：`pnpm exec tsc --noEmit`；Vitest 54 个文件、283 项通过；Rust `cargo test --lib` 290 项通过；`cargo fmt --check`；`git diff --check`；`pnpm tauri:build:macos:local` 成功生成 arm64 本机 App。
- 是否更新 README：否；属于窗口外链处理 bugfix。
- 是否更新 `.ai/architecture`：否；未改变系统浏览器与 workspace 模块边界。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：使用本机 App 点击“在浏览器中登录”和升级套餐，确认系统浏览器被打开且 KickSide 不产生空白 popup；再验证普通 KimiCode 会话链接仍可正常使用。

## 2026-08-17-18:05 - 准备 v0.2.2 Action 发布

- 用户目标：在当前 main 合并完成后，通过 GitHub Actions 构建 Windows/macOS 安装包并发布 Release。
- 涉及目录：`.github/workflows/`、`apps/kimi-shell/docs/`、`apps/kimi-shell/package.json`、`apps/kimi-shell/src-tauri/`。
- 修改内容：将应用版本从 0.2.1 bump 到 0.2.2 并同步 Cargo/Cargo.lock/Tauri 配置；新增 v0.2.2 发布说明；将现行未签名 macOS 预览发布校验从 v0.2.1 收口到 v0.2.2。
- 修改原因：v0.2.1 已经发布且指向当前合并前提交，不能覆盖已发布 tag/Release；必须使用新的 patch 版本触发 tag-based Release workflow。
- 本次已消除的不确定性：确认 v0.2.1 已是非 draft Release；确认 updater signing secrets 已配置；确认 release workflow 需要 tag、版本号、发布说明和签名 secret 全部匹配。
- 本次采用的关键假设：本次发布继续沿用现有未签名/未公证 macOS 预览策略，并在 Release notes 中显式警告；版本按 0.2.1 的 patch 递增为 0.2.2。
- 剩余不确定性：GitHub runner 上的 Windows NSIS/MSI 与 macOS arm64 构建耗时、上游下载稳定性以及 Release asset 上传结果需由 Action 实际验证。
- 影响范围：仅发布版本元数据、发布说明和 tag-based Release workflow 校验；不改变运行时代码。
- 验证方式：发布准备提交后执行版本同步、TypeScript/前端测试与 diff 检查；推送 v0.2.2 后核对 Release workflow、四类安装包、updater signatures 和 latest.json。
- 是否更新 README：否；README 已通过动态 Releases 链接展示最新版本。
- 是否更新 `.ai/architecture`：否；未改变系统架构或运行时契约。
- 是否需要新增或更新 `.ai/decisions`：否；版本递增和既有预览发布策略属于发布操作记录。
- 后续注意事项：v0.2.2 仍是 macOS 未签名/未公证预览包；后续版本发布前应恢复正式 Apple 签名/公证流程，不应继续扩大未签名例外。
