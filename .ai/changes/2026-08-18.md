## 2026-08-18-10 - 回退 LAN Gateway 并切换到 Kimi Native LAN

- 用户目标：回退已开展的 KickSide LAN Gateway 接入，改为直接控制 KickSide-owned Kimi Code 在 loopback 与 `0.0.0.0` 之间受控重启；DSH 不进入局域网 MVP。
- 涉及目录：`apps/kimi-shell`、`.ai/architecture`、`.ai/decisions`、`.ai/plans/kickside-lan-access-2026-08-17`、`.github/workflows`、根 README 与 `tasks`。
- 修改内容：删除未发布的 `apps/kickside-lan-gateway` 及其 CI/验证接线；旧 Gateway ADR/文档降级为 future alternative；新增 Native LAN ADR、非持久 runtime 状态、owned-only 事务式切换、external/owned registry host 分界、私有 IPv4 投影、按需 launch URL/QR 三个 main-only IPC，以及控制中心设置行。
- 修改原因：Kimi Code 已原生提供 wildcard bind、Bearer auth、Host 检查与非 loopback 下默认关闭的 terminal/shutdown/debug；第一阶段仅支持 Kimi 时，Gateway 的代理、配对、Cookie、WS、第二 sidecar 和 DSH 表面不必要。
- 本次已消除的不确定性：普通 external registry/legacy lock 不再接受 wildcard；owned wildcard 只能通过本次 process group、启动时间窗与 loopback health/Bearer 验证；LAN 意图不进入 settings schema；完整 token URL 不进入普通状态接口。
- 本次采用的关键假设：Kimi 0.36.1 的原生 `--host 0.0.0.0` 契约稳定；running session 的官方 session list 投影足以作为 P0 拒绝切换门；接口名过滤只影响展示，不承诺识别所有企业 VPN/虚拟网卡。
- 剩余不确定性：真实 macOS/Windows owned runtime 的完整开关与故障回滚、Windows/macOS 防火墙、iOS/Android 浏览器和公共网络提示仍需 G3；当前不自动修改防火墙。
- 影响范围：新增三个 additive Tauri command 与一个小型跨平台 `get_if_addrs` 运行时依赖；未修改 AppSettings schema、Grid persisted state、locator schema、DSH、installer 或 sidecar 打包。
- 验证方式：Rust fmt/check/clippy 与全量 293 tests；TypeScript noEmit、Vite production build 与全量 55 files / 286 tests；command registry、window ACL、capability、bundle resource、diff check 均通过。
- 是否更新 README：是；更新根安全边界和 Shell lifecycle/UI/依赖入口。
- 是否更新 `.ai/architecture`：是；登记 Native LAN 当前事实与验证边界。
- 是否需要新增或更新 `.ai/decisions`：是；旧 Gateway ADR 标记 Superseded，新增 Accepted Native LAN ADR。
- 后续注意事项：应用每次启动必须默认关闭；不得为 DSH 复用该 wildcard 路径；完整 launch URL不得进入日志、诊断、持久化或外部打开命令行；真实入站和防火墙行为仍需 G3。

## 2026-08-18-11 - 修复 LAN 切换后 KimiCode pane 白屏

- 用户目标：修复手机扫码访问正常、但 App 内 KimiCode pane 在 LAN 开关重启后白屏的问题。
- 涉及目录：`apps/kimi-shell/src/app`、`apps/kimi-shell/src/App.tsx`。
- 修改内容：LAN 切换结束后刷新 Shell 核心运行时投影；workspace iframe identity 纳入 Kimi `startCycleId`，保证同端口、同 URL 的后端重启也会重建 frame；新增成功切换、失败回滚与 frame identity 测试。
- 修改原因：LAN controller 与主 Shell controller 原本相互隔离；Kimi 重启复用了相同端口和 Token URL，旧 iframe 在短暂断连后没有新的 key，WebKit 保留白屏文档。
- 本次已消除的不确定性：现场进程确认 Kimi 正常监听 wildcard且手机可访问；本次先修复了确实存在的 AppStatus/iframe restart identity 缺口，但用户复验新 pane 仍白屏，证明它不是最终根因。
- 本次采用的关键假设：`startCycleId` 是每次受管 Kimi 启动的单调 identity；已有 Shell `refreshCoreState` 是刷新 AppStatus 与 workspace URL 的权威入口。
- 剩余不确定性：修复后的真实 WKWebView 开关回归需用新包复验。
- 影响范围：仅前端运行时同步和 iframe remount；不改变 Kimi 参数、LAN 安全边界、Token 或持久化 schema。
- 验证方式：定向 7 tests、TypeScript、全量 57 files / 290 tests、安全/ACL gate 与新本机 App build。
- 是否更新 README：否；公共职责和使用方式未变化。
- 是否更新 `.ai/architecture`：否；属于既有生命周期投影的 bugfix。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：任何会重启 owned Kimi 的新动作都必须刷新 AppStatus，并通过新的 `startCycleId` 触发 frame remount。

## 2026-08-18-12 - 为 Native LAN 增加 loopback iframe CSP adapter

- 用户目标：修复重建和新建 KimiCode pane 在 LAN 模式下仍持续白屏。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager`、Native LAN ADR/spec、Kimi Web 架构事实。
- 修改内容：仅在 owned LAN 模式启动同 generation、随机 `127.0.0.1` workspace adapter；App workspace URL 指向 adapter，手机 URL与 Rust runtime origin 保持真实 Kimi 端口；HTML CSP 仅改写 `frame-ancestors` 允许 Tauri origins，REST/WS 透明转发；敏感请求头日志统一脱敏。
- 修改原因：真实 Kimi 0.36.1 wildcard 响应头包含 `frame-ancestors 'self'`，Tauri origin 不是 Kimi origin，WKWebView 正确拒绝 iframe；顶层手机页面不受该规则影响。
- 本次已消除的不确定性：新 pane 同样失败排除 stale frame；真实响应头直接证明 CSP ancestor 阻断；Kimi CLI 没有允许嵌入的参数。
- 本次采用的关键假设：WebKit 对 CSP `tauri:` scheme-source 的实现符合 CSP scheme-source；生产主窗口 origin 仍由 Tauri 固定为自身 scheme/localhost。
- 剩余不确定性：需要新包在真实 WKWebView 验证 HTML、REST、WebSocket 与开关退出后的 adapter 端口释放。
- 影响范围：LAN 模式 App 内嵌链路新增本机传输 hop；不恢复公开 Gateway、DSH、设备配对或旧 HTML 注入。
- 验证方式：CSP 保留/替换、缺省约束、敏感头脱敏定向测试；Rust fmt/check/clippy/test；新 App 真机复验。
- 是否更新 README：是；登记 LAN-only loopback adapter 例外。
- 是否更新 `.ai/architecture`：是；修正“始终 direct iframe”的旧事实。
- 是否需要新增或更新 `.ai/decisions`：是；在 Accepted Native LAN ADR 增补上游 CSP 后果。
- 后续注意事项：adapter 不得监听 wildcard、不得写 token、不得成为 DSH 或公开 LAN Gateway 的复用入口。

## 2026-08-18-13 - 修复 LAN adapter WebSocket 阻塞 prompt

- 用户目标：修复 App pane 已显示后，`POST /api/v1/sessions/<id>/prompts` 30 秒无响应的问题。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager/workspace_proxy.rs`。
- 修改内容：WebSocket upgrade 改为独立 tunnel worker；普通 HTTP 请求循环不再被长连接占用；增加 32 条并发 WS 硬上限和 RAII 计数回收。
- 修改原因：旧 proxy 是串行 request loop，首条 WS tunnel 在连接生命周期内不返回，之后已 accept 的 prompt POST 只能排队直到前端 AbortError。
- 本次已消除的不确定性：现场 adapter 有大量已接受客户端连接，但只有两条上游连接；与串行 WS tunnel 代码路径精确吻合。
- 本次采用的关键假设：Kimi 0.36.1 的长连接使用标准 WS upgrade；普通 prompt carrier 是有界 HTTP response。
- 剩余不确定性：新包需真实验证 prompt receipt、WS stream、双 pane 并发和停止后的端口释放。
- 影响范围：仅 LAN loopback adapter 调度；真实 Kimi、手机直连、Bearer 与 CSP 边界不变。
- 验证方式：Rust fmt/check/clippy/full test、新本机 App 复验。
- 是否更新 README：否；adapter 的长期职责未变化。
- 是否更新 `.ai/architecture`：否；是已登记 adapter 内部并发修复。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：不得改为无界“每请求一线程”；当前只分离有硬上限的 WS tunnel。

## 2026-08-18-14 - 修复 LAN adapter 流式输出锁饥饿

- 用户目标：修复手机实时刷新、但 App pane 不能实时显示 Kimi 流式输出的问题。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager`。
- 修改内容：workspace adapter 改为直接拥有 loopback TCP 连接；HTTP 每连接有界处理并强制上游 `Connection: close`，WebSocket 使用可克隆的 client/upstream socket 同时复制两个方向；连接总数限制为 64，HTTP headers 限制为 64KiB，并拒绝 chunked 请求体、重复或非法 Content-Length。
- 修改原因：旧 tunnel 把 tiny_http upgrade stream 放在共享 Mutex 中，client→upstream 线程在阻塞 read 时一直持锁，upstream→client 的 assistant delta 永远无法取得锁写入 pane。
- 本次已消除的不确定性：手机直连正常而 adapter pane 停滞与该锁饥饿路径精确对应；新增真实 TCP 双向测试同时发送 client hello 与 stream delta，证明两个方向都能在超时内独立推进。
- 本次采用的关键假设：Kimi Web 0.36.1 的浏览器 HTTP mutation 使用 Content-Length；WebSocket 遵循 HTTP/1.1 Upgrade。chunked 请求体当前显式 501，不作不安全猜测。
- 剩余不确定性：仍需新包在真实 WKWebView 验证长回复、双 pane 与断线重连；文件上传若浏览器改用 chunked request body 会明确失败并需要后续流式 parser。
- 影响范围：仅 LAN 模式的 App loopback adapter；手机直连、真实 Kimi listener、Bearer、CSP 与持久状态不变。
- 验证方式：双向 TCP tunnel 回归测试；Rust fmt/check/strict clippy/full 298 tests；新本机 App build。
- 是否更新 README：否；adapter 职责不变。
- 是否更新 `.ai/architecture`：否；属于既有 adapter 的传输实现修复。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：不得重新用单个锁包装不可拆分的 full-duplex stream；所有未来 tunnel 实现必须有同时双向推进的可执行测试。

## 2026-08-18-15 - 修复 macOS adapter accepted socket 非阻塞继承

- 用户目标：修复新测试包持续报告 `WebSocket error`。
- 涉及目录：`apps/kimi-shell/src-tauri/src/backend_manager/workspace_proxy.rs`。
- 修改内容：非阻塞 accept loop 取得连接后，在交给 HTTP/WS worker 前显式恢复 blocking mode；双向 tunnel 测试改为从 nonblocking listener 接受 client socket，并复现生产配置转换。
- 修改原因：macOS accepted socket 继承 listener 的 `O_NONBLOCK`；client→upstream copy 遇到暂时无数据时立即返回 EAGAIN，随后 shutdown upstream write，Kimi 发出首个 server frame 后因收不到 client hello 而关闭。
- 本次已消除的不确定性：现场每条 WS 都成功 Upgrade、固定收到约 262 bytes 后关闭，且同一时间 adapter 日志持续记录 os error 35；两者与非阻塞继承的失败顺序完全一致。
- 本次采用的关键假设：每连接一个有界 OS thread 的 adapter 需要 blocking socket；listener 自身继续 nonblocking，以便 generation stop 能在 20ms 轮询内退出。
- 剩余不确定性：需要新包在真实 WKWebView 验证连接稳定、stream、双 pane 和重连。
- 影响范围：仅 loopback adapter accepted client sockets；真实 Kimi、LAN 手机连接和安全边界不变。
- 验证方式：nonblocking-listener 双向 TCP tunnel 回归、Rust fmt/check/strict clippy/full tests、新本机 App build。
- 是否更新 README：否。
- 是否更新 `.ai/architecture`：否。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：任何使用 nonblocking listener + blocking worker 的新代码都必须在 accept 后显式设置 socket mode，不依赖平台继承差异。

## 2026-08-18-16 - 修复 macOS Finder 附件拖放

- 用户目标：构建可测试的 macOS App，并让 Finder 文件能拖入 Kimi Code 对话框作为附件。
- 涉及目录：`apps/kimi-shell/src-tauri/src/frame_workspace_bridge.js`、`apps/kimi-shell/src/app/linkBridge.test.ts`、Shell README、Kimi Web DOM contract 与任务记录。
- 修改内容：保留主窗口 `dragDropEnabled=false`；在可信 Kimi surface 内以 capture listener 识别 `types=Files` 且 `items` 暂时为空的 dragenter/dragover，仅调用 `preventDefault()` 允许 WebKit 继续派发 drop；新增可信来源、DSH、普通文本、可见 file item、事件传播与真实 FileList 回归测试。
- 修改原因：Kimi Code 0.36.1 在 dragenter/dragover 只检查 `dataTransfer.items`，而 WKWebView 对 Finder 的受保护阶段可能保持 items 为空；Kimi 已在 drop 阶段正确回退到 `dataTransfer.files`。
- 本次已消除的不确定性：真实 Kimi 0.36.1 生产 bundle 的进入判定和 drop fallback 已核对；无需原生读取文件、传本地路径、构造 File 或依赖 Vue 私有实例。
- 本次采用的关键假设：WKWebView 在最终 drop 阶段继续提供真实 `dataTransfer.files`，与 Kimi 既有 fallback 契约一致；可信 Kimi theme envelope 会在用户开始拖放前初始化。
- 剩余不确定性：Finder→WKWebView→Kimi composer 的完整 G3 必须由新构建实际拖入 txt/png/pdf 与多文件确认；自动化不能模拟 AppKit 的真实拖放生命周期。
- 影响范围：仅 Kimi Code iframe 中 WebKit 隐藏 file items 的阶段；不拦截 drop，不影响 DSH/external/top frame，WebView2 已有 file item 时不介入。
- 验证方式：bridge 36 tests、TypeScript noEmit、全量前端/Rust测试、安全 gate、macOS arm64 production build 与 ad-hoc 签名验证；真实 Finder G3留给本机构建复验。
- 是否更新 README：是；登记 all-frame bridge 的 Finder 兼容职责与不读取文件边界。
- 是否更新 `.ai/architecture`：是；登记 Kimi 0.36.1/WebKit 拖放契约、边界与升级检查。
- 是否需要新增或更新 `.ai/decisions`：否；属于既有 bridge 和原生 WebView 责任边界内的兼容 bugfix。
- 后续注意事项：升级 Kimi 时重新核对其 dragover 判定；若上游开始接受 `types=Files`，应删除本地兼容层，避免重复维护。

## 2026-08-18-17 - 以原生单次授权完成 macOS Finder 附件拖放

- 用户目标：对照 Chrome 的“松开鼠标添加附件”行为，查明 KickSide macOS 无提示且无法添加附件的真实原因，并逐步修复、构建和实机验证。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src/features/workspace-grid`、`apps/kimi-shell/src/app`、Shell README、Kimi Web DOM contract、current state 与任务记录。
- 修改内容：以真实 iframe trace 证明 Finder 事件未进入 DOM；以正确的 Tao `WindowEvent::DragDrop` 证明原生 Enter/Over/Drop 完整；macOS main 平台覆盖启用原生 DnD，新增 `native_file_drop.rs`，为本次 Drop 的普通文件建立 O_NOFOLLOW、30 秒、single-use 的已打开句柄 grant；Shell 只在 Code pane 落点消费 raw bytes，并以 exact origin + nonce 交给 Kimi 自有隐藏 file input。补充 Retina、非 Code pane、错误 nonce、真实 File/change、平台配置与 command registry 回归。
- 修改原因：前一版只修补 WebKit protected-mode `items=[]`，但实机 Kimi iframe 连 `dragenter` 都未收到；Tao NSWindow 已在更上游接管 Finder 拖放，纯 DOM workaround 无法触达。
- 本次已消除的不确定性：真实 debug App 已收到原生文件计数/坐标，且用户把小文件拖入 Kimi Code 后确认出现原生附件条目；无需 swizzle WKWebView、Vue 私有遍历或直接调用 Kimi `/files`。
- 本次采用的关键假设：Kimi 0.36.1 的隐藏多选 file input/change 是当前稳定附件入口；升级 Kimi 时必须重新烟测该契约。Tao 0.34.5 对 Finder 的 legacy filename pasteboard 支持可用于当前 Finder 基线。
- 剩余不确定性：第二 Code pane、多文件、目录/超限、第三方文件管理器现代 file URL、Windows WebView2 与发布构建仍需发布前 G3；macOS native 路径不会显示 Chrome 的 pre-drop hover 提示，只在松开后进入 Kimi 附件列表。
- 影响范围：新增一个 main-only additive Tauri command 和 macOS 原生事件路径；Windows base config 与 HTML5 FileList 路径不变；prefill/import picker/Chat/DSH/external 不读取 grant。
- 验证方式：真实 Finder→debug App→Kimi 附件 G3；Rust fmt/check、TypeScript noEmit、67 个 bridge/Grid 定向测试、安全 capability/resource/command registry gate（188 commands）；使用本地无 updater artifact 配置完成 arm64 `KickSide.app` release 构建，并以 ad-hoc 重签后通过 `codesign --verify --deep --strict`。
- 是否更新 README：是；登记平台分流与安全 grant 边界。
- 是否更新 `.ai/architecture`：是；纠正前一版纯 DOM 结论并记录事实链、契约、升级检查和剩余 G3。
- 是否需要新增或更新 `.ai/decisions`：否；属于既有原生窗口与 all-frame bridge 边界内的兼容修复。
- 后续注意事项：命令不得增加 path 参数；不得把文件名/路径/token写日志；若未来增加 hover UX，必须由 native Enter/Leave 驱动并遵循 DESIGN.md，不能削弱 grant 边界。

## 2026-08-18-13 - 增加 DSH 升级检测与一键升级

- 用户目标：先实现 DeepSeek Harness 的升级检测和一键升级，不处理 macOS 文件拖入。
- 涉及目录：`apps/kimi-shell/src-tauri`、`apps/kimi-shell/src/app`、`apps/kimi-shell/src/services`、`apps/kimi-shell/src/features/control-center`、`.github/workflows`、`.ai/architecture`。
- 修改内容：将 KickSide 推荐 DSH 版本推进到 `0.1.0-rc.7`；新增有 6 小时成功缓存的官方 npm metadata 检测与 semver 状态；控制中心展示本地/推荐/上游版本并提供强制检查和一键升级；升级命令固定包名与推荐版本，校验官方 sha512 integrity 和 staging npm lock；激活后以真实 loopback HTTP 与 `__DSH_BOOT__` 页面身份验证，新版失败时恢复 backup，并在升级前运行中时重启旧实例。
- 修改原因：原安装器只认识编译期 rc.6，无法发现 rc.7；旧事务在新版本真正启动前删除 backup，启动不兼容时无法回滚。
- 本次已消除的不确定性：2026-08-18 官方 npm registry 的 `latest` 与 KickSide 推荐版本均为 `0.1.0-rc.7`，精确版本提供 sha512 integrity；现有私有 staging、固定 npm launcher、entry canonicalization 与进程树收口可复用。
- 本次采用的关键假设：编译期固定 pin 是 KickSide 已验证的推荐版本；npm `latest` 仅用于提示，不自动获得安装资格；npm staging `.package-lock.json` 是本次依赖解析 integrity 的可审计投影。
- 剩余不确定性：Windows/macOS 安装包内的一键升级、真实 rc.6→rc.7 回滚故障注入与 WebView2/WKWebView 交互仍属于 G3；上游未来版本仍需先通过 canary 再推进推荐 pin。
- 影响范围：新增 additive Tauri command/type 和一个直接 `semver` 依赖；改变 DSH 推荐版本与安装激活事务，不改变 DSH URL/PID 持久化、Grid、IM Bridge 或 macOS 拖放。
- 验证方式：Rust DSH 定向测试、`cargo check --locked`、strict clippy、TypeScript noEmit、DSH controller/panel/service 定向测试、安全 command registry gate；完整 gate 与真实 runtime smoke 结果见本次最终交付。
- 是否更新 README：是；登记推荐版本、检测信任边界、integrity 与 readiness rollback。
- 是否更新 `.ai/architecture`：是；更新 DSH 当前事实与验证 pin。
- 是否需要新增或更新 `.ai/decisions`：否；继续沿用编译期固定推荐版本与既有私有安装表面，未引入新的远程 compatibility manifest 或安装来源。
- 后续注意事项：不得把 npm `latest` 直接作为安装目标；推进推荐版本时同步更新 Rust pin、runtime smoke、canary 与 verification gate；更新检测失败不得显示“已是最新”。

## 2026-08-18-15 - 将 DSH 升级提示与运行资格解耦

- 用户目标：把 DSH 是否升级的选择权交给用户；App 只提示可升级并提供升级按钮，不能因发现 rc.7 而阻止已安装 rc.6 启动。
- 涉及目录：`apps/kimi-shell/src-tauri/src/dsh_manager.rs`、DSH 控制中心测试、Shell README、架构事实与验证门禁。
- 修改内容：拆分“运行兼容安装”和“推荐版本安装”两套校验；已验证的 rc.6 与推荐 rc.7 均可运行，新安装与升级 staging 仍严格要求 rc.7；控制中心回归固定“启动实例”与“升级到 rc.7”可同时出现。
- 修改原因：原 `verify_install` 同时承担运行资格和安装激活职责，推荐 pin 从 rc.6 推进到 rc.7 后把可选更新意外变成强制升级。
- 本次已消除的不确定性：本机私有安装确为 rc.6、固定入口完整，历史日志证明其已通过真实 loopback readiness 并多次运行；失败发生在启动前版本门禁。
- 本次采用的关键假设：运行兼容范围采用显式 allowlist，不自动信任任意旧版或上游新版；当前只登记已经验证的 rc.6 与 rc.7。
- 剩余不确定性：Windows 安装包内 rc.6 保留运行资格仍需发布前 G3；未来推进推荐版本时需明确决定是否把前一推荐版本加入兼容 allowlist。
- 影响范围：只改变 DSH 私有安装的版本资格判断和对应提示；不放宽 canonical path、固定入口、npm metadata、sha512 integrity、staging lock、Node 版本或进程所有权边界。
- 验证方式：Rust 定向测试（rc.6 runtime accept、rc.6 recommended-install reject、rc.7 accept、未知版 reject、symlink fail-closed）；`cargo fmt --check`、`cargo check --locked`；DSH React 定向测试 10/10；TypeScript noEmit；Shell 全量 Vitest 57 files / 297 tests；macOS arm64 本地 release App 保留现有 rc.6、不执行升级后自动启动默认工作区，真实日志与进程树确认进入 `DSH 已就绪`。
- 是否更新 README：是；登记兼容旧版可继续运行、升级由用户主动选择。
- 是否更新 `.ai/architecture`：是；登记运行兼容范围与推荐安装版本分离，并补充验证入口。
- 是否需要新增或更新 `.ai/decisions`：否；这是修正同一 DSH 私有安装表面内的门禁职责，不改变安装来源、序列化契约或持久化结构。
- 后续注意事项：不得把 semver 小于推荐版直接等同于兼容；兼容版本必须经过验证后显式登记。

## 2026-08-18-16 - 删除空壳中文 README 兼容页

- 用户目标：删除已经没有实质内容的根目录 `README_zh.md`，并将本轮完成的改动推送到 GitHub `main`。
- 涉及目录：仓库根目录与 `.ai/changes`。
- 修改内容：删除只包含中文主文档迁移跳转的 `README_zh.md`；保留 `README.md` 作为中文主文档、`README_EN.md` 作为英文文档。
- 修改原因：中文内容已经完整迁移到 `README.md`，继续维护第三个空壳入口没有实际价值。
- 本次已消除的不确定性：当前有效 README 和源码均不引用 `README_zh.md`；剩余命中只存在于历史任务、历史发布说明和变更记录中，应保留为历史事实。
- 本次采用的关键假设：旧外部链接失效的成本低于长期保留无实质内容兼容页的维护成本，且用户已明确授权删除。
- 剩余不确定性：外部站点若仍保存旧链接会收到 GitHub 404。
- 影响范围：仅删除根目录历史文档入口，不影响应用、构建、安装或运行时行为。
- 验证方式：全仓库引用检索、Markdown 主入口核对、`git diff --check` 与本轮完整工程 gate。
- 是否更新 README：否；中文和英文主文档内容不变。
- 是否更新 `.ai/architecture`：否；不涉及架构事实。
- 是否需要新增或更新 `.ai/decisions`：否；属于用户明确授权的可逆文档清理。
- 后续注意事项：未来中文文档只更新 `README.md`，英文镜像更新 `README_EN.md`。
## 2026-08-18-18 - 增加 Gitee 发布镜像与可选更新源

- 用户目标：让中国大陆用户可以从 Gitee 检测和下载 KickSide 更新，并在 App 中选择自动、Gitee 或 GitHub 更新源。
- 涉及目录：`.github/workflows`、`apps/kimi-shell`、`.ai/architecture`、`.ai/decisions`、根 README 与 `tasks`。
- 修改内容：保持 GitHub 唯一构建与 canonical Release；新增 Gitee prerelease 暂存/附件上传/匿名回下载 SHA-256 校验/manifest-last/promotion 脚本；生成 GitHub/Gitee 双 manifest；AppSettings schema 14 新增 `appUpdateSource`；Rust 原生 updater 支持明确源与自动并行 SemVer 选择；控制中心增加紧凑更新源选择和实际命中来源。
- 修改原因：GitHub Releases 在中国大陆网络不稳定，而两平台独立构建会造成二进制、平台签名和 updater 签名漂移。
- 本次已消除的不确定性：Gitee 仓库镜像不包含 Release；OpenAPI 支持 Release/附件管理；公开 `browser_download_url` 可匿名下载；`/releases/download/latest/latest.json` 当前可用但需要发布监控；附件保守上限为单文件 100MB、仓库 1GB。
- 本次采用的关键假设：Gitee latest 附件别名保持可用；`GITEE_RELEASE_TOKEN` 对 `endearqb/kickside` 具备最小 Release 写权限；Gitee prerelease 附件可以在 promotion 前完成匿名回验。
- 剩余不确定性：尚未使用真实 `GITEE_RELEASE_TOKEN` 执行首个 staging Release；中国大陆真实网络、Gitee CDN 延迟、双平台旧安装升级和损坏签名仍需 G3；现有 installer-specific Windows ADR 与当前 generic NSIS manifest 的历史事实差异未在本任务扩大处理。
- 影响范围：新增两个 additive Tauri command、一个持久化 enum 字段和一个 GitHub Actions Gitee 镜像 job；不改变 updater 公钥、安装包字节、运行时依赖或 Gitee 构建环境。
- 验证方式：Rust fmt/check/test/clippy；TypeScript noEmit、全量前端测试、安全/command registry gate；manifest Node tests、Gitee 发布脚本语法检查、workflow YAML 解析与 diff check。真实发布验证 blocked 于 `GITEE_RELEASE_TOKEN` 和下一个 release tag。
- 是否更新 README：是；增加 Gitee 下载入口、双源更新行为和单一构建镜像流程。
- 是否更新 `.ai/architecture`：是；登记 schema 14、双源选择、Gitee 镜像和发布门。
- 是否需要新增或更新 `.ai/decisions`：是；新增 Accepted Gitee mirror/update source ADR，并给原 signed updater ADR 增加扩展指针。
- 后续注意事项：不得把 token 放进命令行或日志；不得把 foruda 临时签名 URL 写入 manifest；容量接近保守 1GB 前需制定独立、经确认的旧 Release 保留策略。
- 首次发布补充：增加 `Mirror GitHub Release to Gitee` 手动 workflow，可对与当前 package version 精确一致的既有 stable GitHub tag 做一次无重建回填；它复用同一 manifest/publisher 校验链，并在结束时比对 Gitee latest manifest 字节。
