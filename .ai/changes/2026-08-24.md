## 2026-08-24-00 - 修复 Gitee Windows latest 未指向 v0.2.4

- 用户目标：修复 Windows 从 Gitee 更新源检测不到 0.2.4。
- 涉及目录：Gitee v0.2.4 Release、`.agents/skills/release-kickside`、`.ai/architecture`、`.github/workflows`、`tasks`。
- 修改内容：确认 v0.2.4 的 8 项资产已完整且通过双源字节校验后，关闭 Gitee Release 的 prerelease 标志；移除调用未支持 `remote_mirror/pull` 端点且实测 HTTP 404 的同步 workflow；把发布规程收敛到已验证的 authenticated mirror UI fallback。
- 修改原因：Gitee `/releases/download/latest/latest.json` 只在稳定版 promotion 后更新；残留 prerelease 使 Windows 继续得到旧版 latest 清单。
- 本次已消除的不确定性：Gitee tag/latest API 均返回 `v0.2.4` 与 `prerelease=false`；latest alias 重定向到 v0.2.4 附件 ID `3083094`；清单 `version=0.2.4`、包含 `windows-x86_64`/`darwin-aarch64`，Windows URL 固定到 v0.2.4 且签名非空；公开清单 SHA-256 为 `bd29700b3c9226c5909c3c8dce2e10fefcbe85acdae2cd395a05870d85aae331`。
- 本次采用的关键假设：用户报告该发布缺陷即授权完成既定 v0.2.4 stable promotion；不移动 tag、不替换已校验资产。
- 剩余不确定性：已安装 Windows 客户端可能受本机网络/DNS/代理缓存影响，需要重新点击检查更新；完整安装升级仍属于 Windows WebView2 G3。
- 影响范围：仅修正 Gitee Release 状态和仓库发布规程；GitHub Release、v0.2.4 tag 与资产字节不变。
- 验证方式：Gitee tag/latest API、latest alias redirect、公开 latest.json 版本/平台/URL/签名/SHA-256；8 项附件计数。
- 是否更新 README：否，用户可见功能与运行方式未变化。
- 是否更新 `.ai/architecture`：是，移除失效同步入口并保留公开 ref 验证要求。
- 是否需要新增或更新 `.ai/decisions`：否。
- 后续注意事项：每次 Gitee manifest-last 上传后必须立即 promotion stable，并用应用实际使用的 `/releases/download/latest/latest.json` 做匿名检查。
