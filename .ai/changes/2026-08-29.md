## 2026-08-29-21 - Kimi 官方远程控制一键封装

- 用户目标：把 Kimi Code 官方 Remote Control 包装为本机/局域网/官方远程三态一键功能。
- 涉及目录：`apps/kimi-shell`、根 README、`.ai/architecture`、`tasks`。
- 修改内容：新增三态访问枚举、实验 help capability、官方 Remote argv/env、owned 生命周期切换与回滚、CLI 输出内存状态、按需远程 URL/QR、日志前脱敏、三态控制中心与标题栏持续状态；Remote 登录状态以官方 CLI 启动结果为权威，不用宿主陈旧认证摘要阻断启动；保留既有 LAN IPC commands 并新增 additive commands。
- 修改原因：复用官方 CLI 能力，避免用户理解命令行、环境变量、OAuth 与中继，同时不建设 KickSide 自有远控协议。
- 本次已消除的不确定性：本机实际 Kimi 路径为 `~/.kimi-code/bin/kimi`、版本为 0.39.1；该版本内置 Remote Control，并通过 `KIMI_CODE_EXPERIMENTAL_REMOTE_CONTROL=1` 显示和启用 `--rc, --remote-control`；capability parser 已兼容官方“短参数在前、长参数在后”的 option 行；真实 relay 已连接并确认官方 URL 为带 ANSI 样式的 `code-rc.kimi.com/devices/...?...rc=1&from=kimi_code_cli`，URL parser 已按 host/path/query 精确识别；远程状态机已精确区分 relay connected/connecting/disconnected，禁止成功状态降级回 registering，并在 relay 瞬断期间保留已验证的内存 URL；手机熄屏/浏览器后台导致的 `Remote device disconnected` 不再误报为官方 relay 断开；现有 lifecycle 已具备单 owned process、运行任务检查、external never-kill、整树停止与失败回滚；LAN workspace proxy 只应服务 LAN。
- 本次采用的关键假设：目标 Remote CLI 允许组合通用 `--no-open --port`，并继续产生本地 registry/API ready 信号；官方 CLI 自行校验 Kimi OAuth refresh token，KickSide 不读取内部令牌或注册 API；真实版本若不满足，必须调整公开 CLI 适配，不能调用内部注册 API。
- 剩余不确定性：实际官方 Remote stdout 文案/URL 形态、OAuth 流程、Relay 重连语义和双平台行为仍需完成 G3。
- 影响范围：Kimi owned runtime 启动契约与控制中心；DSH、外部 Kimi、内部注册 API、持久化 schema 不变。
- 验证方式：`cargo fmt --all -- --check`；`cargo check --locked`；`cargo clippy --locked --all-targets -- -D warnings`；`cargo test --locked --no-fail-fast`（314/314）；`pnpm test`（57 files、306/306）；`pnpm build`；`pnpm check:nfr:security`（193 commands）。
- 是否更新 README：是，更新根 README 与 Shell contract README。
- 是否更新 `.ai/architecture`：是，更新 current-state 与 verification-gates。
- 是否需要新增或更新 `.ai/decisions`：否；未删除/改名既有序列化命令，新增契约为 additive，且不引入自建中继或新外部依赖。
- 后续注意事项：使用 Kimi 0.39.1 在 Windows/macOS 补 OAuth、Relay、异地设备、单活、退出/升级整树停止和日志/诊断包脱敏 G3。

## 2026-08-29-23 - v0.2.5 Windows 核查与精确未签名发布准备

- 用户目标：检查并实现 Remote Control 的 Windows 版本，提交推送 GitHub，并发布 0.2.5。
- 涉及目录：`apps/kimi-shell`、`.github/workflows`、`.ai/architecture`、`.ai/decisions`、`tasks`。
- 修改内容：Windows help capability probe 超时/异常改用隐藏的 `taskkill /PID <pid> /T /F` 清理 launcher 与 descendants；新增精确 v0.2.5 unsigned macOS Accepted ADR、warning-first release notes、exact-tag workflow guard、Gitee 手工镜像默认版本与双源发布任务契约。
- 修改原因：共享 Remote Control 主路径无需平台分叉，但 Windows npm/pnpm `.cmd` probe 只终止 leader 可能遗留 Node；当前仓库又缺 Apple Developer ID/公证凭据，必须把用户授权限定为单一 tag 并保持后续版本 fail closed。
- 本次已消除的不确定性：Windows locator、argv/env、状态解析、Tauri IPC 与 owned lifecycle 均走跨平台共享实现；正式 backend 已具备 Windows 无窗口新进程组与 `taskkill /T`；产品所有者于 2026-08-29 明确授权精确 v0.2.5 未签名、未公证 macOS 预览。
- 本次采用的关键假设：GitHub `windows-latest` 的原生 Rust/React/installer build 是本次 G2 权威；未取得 Windows 实机 OAuth/Relay 证据时继续把它列为 G3，而不阻止带明确限制的实验预览发布。
- 剩余不确定性：Windows 11 + Kimi 0.39.1 的 OAuth、Relay、二维码、手机熄屏/重连、NSIS/MSI 0.2.4→0.2.5 更新与退出无残留仍需 G3；最近定时 DSH canary 的 macOS npm install OOM 需独立复核。
- 影响范围：只改变 KickSide 自有探针清理、版本与发布元数据；不改变 Kimi 官方远控协议、内部 API、external process never-kill 或持久化 schema。
- 验证方式：本地 Rust fmt/locked check/strict clippy 与 315 tests；React 57 files/306 tests、production build、10 张视觉基线与 193-command security gate；Go vet/test/race；updater/Gitee publisher 5 tests；Windows x86_64 Bridge sidecar 交叉构建；workflow YAML、版本、release-note 首行与 diff gate；Apple Silicon 0.2.5 App 构建、双 arm64 与 ad-hoc deep/strict codesign。推送后仍需等待同一提交的 Windows/macOS main CI 全绿；tag 后逐项核验 GitHub/Gitee 8 项资产、SHA-256 与 updater manifest。
- 是否更新 README：是，发布版本和 Remote Control 限制需同步。
- 是否更新 `.ai/architecture`：是，记录 v0.2.5 exact-tag 例外与验证边界。
- 是否需要新增或更新 `.ai/decisions`：是，新增 `2026-08-29-v0.2.5-unsigned-macos-release.md`。
- 后续注意事项：v0.2.6 不得继承本例外；必须恢复 Apple Developer ID/notarization/stapling 或重新取得 exact-tag 授权。
